# 牵星（Qianxing）自研量化框架重规划方案 V5.1

> 版本：V5.1 · 日期：2026-09-10  
> 适用范围：自研插件化量化交易、研究、回测、仿真、纸面交易与实盘执行框架  
> 本文是对《自研量化框架完整架构设计方案.md（V4）》的重规划，不覆盖原文；原文保留为历史设计基线。

## 0. 结论先行

### 0.1 新定位

牵星不再把自己定义为“一次性做完所有市场、所有语言、所有插件的全能平台”，而定义为：

> **以确定性事件内核为中心，先做 Bar/L1 研究—事件回测—纸面交易闭环，再逐步扩展到多市场、多账户和实盘的量化交易操作系统。**

第一目标不是连接数量，而是保证以下链路的语义一致：

```text
数据可见性 → 策略信号 → 风控决策 → 订单意图 → 模拟/真实执行 → Fill事实
      ↘ 事件日志、账簿、归因、指标、重放 ↗
```

### 0.2 V5.1 的七项重大调整

1. **参考项目重新聚焦**：采用 7 个真正的量化/交易框架或核心库；DeepSeek Harness 不再作为主参考项目。
2. **从“全量架构”改成“能力分层”**：内核、研究、执行、运营四条能力线分开建设，避免过早引入 WASM、DEX、全量多语言。
3. **MVP 只承诺一个闭环**：单市场/单账户/Bar+L1/事件回测/纸面执行；多交易所、多账户、多策略是后续增量，不再反向污染内核。
4. **统一模型缩小为最小不变量**：事件、时间、身份、订单状态、Fill、账簿、RunManifest 必须稳定；市场规则和连接器全部在适配层。
5. **插件边界收紧**：热路径不做动态插件；启动期装配可替换，运行期只允许观测、数据源故障转移和控制面插件。
6. **质量门前移**：把类型检查、确定性重放、数据质量、前视偏差、对账、故障注入作为交付门，而不是上线前补丁。
7. **吸收 QUANTAXIS 的生态能力**：补上多源数据注册、统一数据容器、因子版本管理、账户快照协议、任务调度与控制面，但全部通过适配层接入，不改变确定性 Kernel。

### 0.3 当前工程基线

当前工作区已经有以下可复用实现：

| 现有模块 | 当前价值 | V5 处理 |
|---|---|---|
| `qx-core` | 时钟、因果队列、事件、定点数、订单/身份、事件摘要 | 继续作为 Kernel，先修复测试编译错误并冻结公共契约 |
| `qx-fenye` | InstrumentSpec、MarketState、符号映射、生命周期 | 归入 Domain/Market，补充版本化与 PIT 生效时间 |
| `qx-guanxing` | Bar、DataView、质量门 | 归入 Research/Data，补齐 raw/normalized/feature 血缘 |
| `qx-xingban` | Bar 撮合、FillModel、FeeModel、Latency、Margin | 归入 Simulation，拆出数据档位与模型兼容性校验 |
| `qx-zhenlu` | RiskGate、OMS、Router | 归入 Execution，后续扩展 AccountCommandQueue 与 reconcile |
| `qx-genglu` | 指标、基础归因、订单对账 | 归入 Operations，拆分指标、审计、对账三个契约 |
| `qx-plugin` | manifest、依赖顺序、冲突检测、Profile/Patch | 保留为启动期装配器，暂不扩展为运行期动态系统 |
| `qx-cli` | 端到端演示与重放校验 | 变成验收样例与 golden test 入口 |

**基线状态**：本版本已完成 `Price/Quantity/Money/Fixed` 类型契约修复、账户隔离账簿和事实重放，`cargo test --workspace`、`cargo clippy --workspace --all-targets -- -D warnings`、release 构建和 CLI 验收入口均已通过。后续工作从“确定性闭环 + 生态边界已具备可执行基础”进入“跨语言与真实适配器补齐”，不得因此降低确定性、重放和对账质量门。

## 1. 八大参考项目对比与取舍

### 1.1 选择标准

参考项目必须至少满足一项：拥有可验证的事件/订单/数据/组合架构；存在真实执行路径；或者在大规模量化研究方面有独特方法。只研究公开接口、模块组织和架构原则，不复制源代码、专有数据或运行时实现。

### 1.2 总览矩阵

| 项目 | 类型 | 最值得借鉴的架构 | 对牵星的落点 | 明确不照搬 |
|---|---|---|---|---|
| **LEAN** | 全生命周期算法交易引擎 | 模块化模型、算法框架的 Alpha→PortfolioConstruction→Risk→Execution 分段、回测/实盘入口 | 把策略输出与组合构建、风控、执行解耦；定义能力矩阵 | 不复制其庞大资产类和数据供应商体系；不把策略 API 当核心内核 |
| **NautilusTrader** | Rust 原生多资产事件引擎 | 统一 Domain Model、Data/Execution Client、BacktestNode/TradingNode、确定性时间与事件顺序 | Kernel 的身份、时间、数据目录、回测/实盘同语义 | 不以宣传吞吐作 SLA；不引入其运行时；不在 MVP 追求纳秒全品种 |
| **RQAlpha** | Python 量化回测/实盘框架 | Mod Hook、系统 Mod、Datasource、配置装配、策略与环境解耦 | `qx-plugin` 的扩展点、Profile、规则包、数据接口 | 不使用其运行时或专有数据；避免全局 Environment 和隐式覆盖 |
| **vn.py** | 国内实盘交易生态 | EventEngine、MainEngine、Gateway、App、回调顺序、网关边界 | 国内柜台适配器、事件总线与恢复流程；将 Gateway 作为外部适配层 | 不复制单进程全局对象习惯；不让 Gateway 类型泄漏进领域模型 |
| **Hummingbot** | 加密实时交易客户端 | Connector、OrderBookTracker、UserStreamTracker、ClientOrderTracker、限频与订单跟踪分离 | VenueAdapter 五层拆分；市场状态、用户状态、订单状态三态分离 | 不把 Python 客户端结构直接放入热路径；不把连接器当 OMS |
| **CCXT / CCXT Pro** | 交易所统一 API | unified API、markets 与 raw id 双向映射、REST/WS、rate limit、exchange-specific fallback | SymbolMapper、RateLimitPolicy、公共/私有 API 分层 | 不用统一 API 代替市场语义；不把字符串 symbol 当 InstrumentId |
| **vectorbt** | 向量化研究/组合模拟库 | 数组广播、策略实例矩阵化、参数扫描、分析可视化 | 研究前筛、参数扫描、候选策略生成；与事件回测共享输入契约 | 不用于真实订单生命周期；不把向量化结果直接当执行真实性 |
| **QUANTAXIS** | 完整的本地量化生态与多市场交易平台 | QAFetch 多源统一入口、QAData 数据容器、QAFactor 因子版本/分析、QIFI 账户协议、QAMarket 账户/订单对象、QASchedule 任务调度、QARSBridge 跨语言桥接 | ProviderRegistry、DataStruct Facade、FactorRegistry、CanonicalAccountSnapshot/QIFI 兼容层、Scheduler 与控制面；为 Rust Kernel 提供 Python/Arrow/JSON 适配 | 不把可变 DataFrame/全局账户对象放进 Kernel；不复制其数据源、运行时或 API 兼容负担；不把“全生态”作为 MVP 前置条件 |

### 1.3 关键结论

#### 结论 A：LEAN 与 Nautilus 决定“主干”

LEAN 的分段模型说明策略、组合构建、风控、执行应有明确责任；NautilusTrader 说明这些责任必须共享稳定的领域类型、时钟和事件语义。牵星采用：

```text
Strategy → Signal → PortfolioConstruction → RiskGate → OrderIntent → OMS → Venue
```

而不是让策略直接创建最终券商订单。

#### 结论 B：RQAlpha 与 vn.py 决定“扩展和接入”

RQAlpha 适合借鉴启动期 Mod 装配，vn.py 适合借鉴国内网关边界和恢复顺序。两者结合后，插件不修改核心，Gateway 不泄漏领域模型，系统可替换性以可重放为上限。

#### 结论 C：Hummingbot 与 CCXT 决定“连接器细节”

市场数据、账户流、订单跟踪必须分离；统一接口只解决“如何调用”，不能决定“该市场的交易、结算、保证金和清算语义”。

#### 结论 D：vectorbt 决定“研究效率”，不决定“实盘真实性”

向量化通道专门解决参数、资产、周期的大规模扫描；任何依赖路径、队列、限价、延迟、部分成交、跨腿对冲的策略，必须进入事件回测复核。

#### 结论 E：QUANTAXIS 决定“生态完整度”，不替代“确定性内核”

QUANTAXIS 的价值不在于某一个撮合算法，而在于把数据获取、数据结构、因子、策略、账户、交易、调度和可视化组织成一个可使用的本地生态。官方仓库当前包含 `QAFetch`、`QAData`、`QAFactor`、`QAMarket`、`QASchedule`、`QARSBridge`、`QIFI`、`QAWebServer` 等模块；其中 QAFetch 统一多数据源与多资产入口，QAData 提供数据容器/变换/查询，QAFactor 提供特征版本和分析，QIFI 提供账户、持仓、订单、成交、转账的跨语言表达。

牵星的吸收方式为：

```text
QUANTAXIS 生态能力       牵星的稳定边界
QAFetch                  DataProvider / ProviderRegistry
QADataStruct             DataStruct Facade → immutable DataView
QAFactor                 Feature/Factor Registry → PIT Candidate
QAMarket / QATrade       VenueAdapter / Account / OMS
QIFI                     Canonical Snapshot + Diff / Export
QASchedule / QAWebServer Control Plane / Scheduler / Query API
QARSBridge               Arrow/JSON/Python bridge，不进入 Kernel 热路径
```

该映射保留 QUANTAXIS 的易用性和生态覆盖，同时把核心约束收紧为：输入可追溯、事件不可变、账户由事实重建、所有外部协议有版本和兼容矩阵。

### 1.4 QUANTAXIS 深度拆解与牵星落地

| QUANTAXIS 能力 | 可复用的产品思想 | 牵星详细设计 | 验收要求 |
|---|---|---|---|
| `QAFetch` 多源数据 | 数据源统一入口、资产和周期覆盖、失败重试/切换 | `ProviderRegistry` 维护 provider、能力、优先级、限频、授权、字段映射、健康度；通过 `DataProvider` 输出 RawRecord，不让 provider 直接写 Normalized | 同一查询可记录 provider、版本、重试链路；切换后 source_hash、质量报告和结果 manifest 可追溯 |
| `QADataStruct` | Bar/Tick/Financial/Feature 的统一容器、筛选、重采样、复权、格式转换 | 外部层提供 Python/Arrow DataStruct Facade；核心只接受带 schema、单位、时间索引和版本的 immutable `DataView` | pandas/JSON/Arrow 转换前后 schema、时区、排序、空值和 hash 一致；复权必须显式声明算法和生效时间 |
| `QAFactor` | feature 定义、FeatureView、版本和二次分析分离 | `FeatureDefinition`、`FeatureArtifact`、`FactorTransform`、`FactorReport` 四个对象；保存 formula、依赖、PIT、覆盖率、缺失处理、中性化/标准化参数 | 因子同名不同版本不得覆盖；候选必须携带因子版本、输入 hash、训练/验证区间和交易成本假设 |
| `QAMarket` / 账户对象 | 行情、订单、持仓、账户状态有可复用模型 | 牵星进一步分离 `MarketState`、`OrderState`、`AccountSnapshot`，通过 `VenueEvent` 和 `LedgerEntry` 更新 | 任何状态可由事件重放得到；快照只做加速，不作为事实来源 |
| `QIFI` | 账户全量快照、增量 Diff、跨语言 JSON/文档化交换 | 定义 `CanonicalAccountSnapshot v1`，支持全量导出、字段级 Diff、版本协商、schema hash、敏感字段剥离；提供 QIFI v2.x 映射层 | Rust/Python/JSON 往返一致；Diff 可重放、幂等；密码、token、数据库地址等连接凭据永不进入快照 |
| `QASchedule` | 定时任务和任务函数集中管理 | `Scheduler` 只负责触发 `JobSpec`，Job 执行必须生成 RunManifest；支持 cron、交易日历、依赖、重试、超时、幂等键和暂停 | 同一 Job 在相同输入下可重跑；任务失败不会隐式重复下单；交易前/后任务有明确窗口 |
| `QAWebServer` / 发布订阅 | 查询、状态推送、控制面与执行面分离 | 只读查询、运维命令、事件订阅分层；写操作全部转为带权限和审计的 `ControlCommand` | UI/API 不能绕过 Risk/OMS；每个控制命令有操作者、原因、审批/权限、结果事件 |

**明确的取舍**：QUANTAXIS 的 Python 生态适合作为研究和控制面参考；牵星不把其模块名、全局状态、数据源实现或兼容 API 直接复制到 Rust Kernel。所有跨语言能力都通过版本化 schema、Arrow/JSON 边界和 RunManifest 连接。

## 2. 目标边界与非目标

### 2.1 V5.1 目标

- Rust 确定性 Kernel，Python 研究/控制面。
- Bar 与 L1 两档数据可回测；撮合模型显式声明假设。
- 同一 `Strategy`、`OrderIntent`、`Fill`、`Ledger` 契约贯通回测、纸面、实盘。
- 支持多源数据注册、数据容器转换、因子版本化和候选策略血缘。
- 单账户、多策略先完成隔离和归因，再扩展多账户。
- 连接器具备重连、限频、幂等、恢复、对账能力。
- 账户状态支持全量快照、增量 Diff、跨语言导出和恢复。
- 任务支持交易日历、依赖、幂等、重试、超时和审计。
- 每次运行可通过 RunManifest 重放、比较和审计。
- 所有核心能力有可自动化验收的质量门。

### 2.2 V5.1 非目标

- 不在第一阶段支持所有资产类别、所有国内柜台和所有 CEX。
- 不在第一阶段实现 DEX、MEV、链上最终性、期权全 Greeks。
- 不在第一阶段实现任意语言插件；先支持 Rust 原生与 Python 控制面。
- 不承诺低延迟 HFT，不用第三方营销吞吐数字作容量目标。
- 不允许策略直接改余额、持仓、订单状态或事件时间。
- 不把微服务数量、消息中间件数量当作架构先进性的指标。

## 3. V5.1 总体架构

```text
┌────────────────────────────────────────────────────────────┐
│ Control Plane：CLI / Python SDK / Config / Scheduler / UI   │
│               Query API / ControlCommand / Auth / Audit     │
├────────────────────────────────────────────────────────────┤
│ Research Plane：ProviderRegistry → Raw/Validated/Normalized │
│  → DataStruct Facade → DataView → Feature/Factor → Vector   │
│  → Candidate → EventBacktest                                │
├────────────────────────────────────────────────────────────┤
│ Trading Plane：Strategy → Signal → Portfolio → Risk → OMS  │
│                → Router → VenueAdapter → Reconcile         │
├────────────────────────────────────────────────────────────┤
│ Simulation Plane：SimVenue / Matching / Fee / Latency       │
│                   Margin / Settlement / RunManifest        │
├────────────────────────────────────────────────────────────┤
│ Kernel：Clock · CausalQueue · EventLog · Identity · Types   │
│         OrderState · Ledger · Snapshot/Diff · Determinism   │
└────────────────────────────────────────────────────────────┘
       ↕                         ↕
  Data/Storage/Protocol Adapters   Venue/Execution Adapters
```

### 3.1 依赖规则

```text
Kernel ← Domain ← Application ← Adapter/Control
```

- Kernel 不依赖网络、文件系统、数据库、Python、交易所 SDK。
- Domain 只依赖 Kernel，保存市场规则与账簿不变量。
- Application 编排策略、回测、执行、路由和对账。
- Adapter 将外部事实转换成标准事实，不将外部对象传入策略。
- Control Plane 可调用所有公开 Application API，但不能绕过 Risk/OMS/Archive。
- Scheduler 只能提交可审计 `JobSpec`，不能直接持有交易所客户端或修改账簿。
- QIFI/JSON/Arrow 只在边界层存在；Kernel 使用强类型结构，禁止以未校验字典作为事实输入。

### 3.2 四种运行模式

| 模式 | 时钟 | 数据 | 执行 | 目标 |
|---|---|---|---|---|
| Research | 索引时间 | Parquet/Arrow、PIT | 向量化或轻量组合模拟 | 参数扫描、因子研究 |
| Backtest | TestClock | 事件回放 | SimulatedVenue | 真实性与回放 |
| Paper | WallClock/EngineClock | 实时行情 | PaperVenue | 连接器、策略、风控联调 |
| Live | WallClock + watermark | 实时行情/账户流 | LiveVenue | 真实执行与对账 |

四种模式只能替换 `DataSource`、`EventSource`、`Venue` 与持久化实现；领域订单、账簿、风控与归因契约不变。

## 4. 核心领域契约

### 4.1 身份三层

```text
CanonicalProduct  = 研究层的可比产品，如 BTC/USDT
InstrumentId       = 执行层的具体合约，如 BTCUSDT-PERP.BINANCE
DataSourceId       = 数据血缘，如 binance.ws.public.v3
```

必须同时保留：`venue_id`、`account_id`、`raw_symbol`、`instrument_type`、`quote_currency`、`settlement_currency`、`valid_from`、`valid_to`、`source_hash`。任何映射变更只能新增版本或退休旧版本，禁止静默覆盖。

### 4.2 时间契约

事件至少包含：

```text
event_time          venue/数据声明的业务时间
receive_time        本地接收时间
engine_time         进入引擎时间
source_seq          外部序号或本地合成序号
engine_seq          引擎全局单调序号
causation_id        因果来源事件
correlation_id      运行/订单/策略链路
```

回测由 `event_time` 驱动，实盘用 watermark 控制迟到事件；原始时间不可被引擎改写。所有同时间事件按固定优先级和 `engine_seq` 排序。

### 4.3 订单与事实

```text
OrderIntent：策略/组合层提出的目标动作，不代表已报单
OrderCommand：OMS 发送给 Venue 的命令，必须幂等
OrderEvent：Venue 或模拟器返回的状态事实
Fill：不可变成交事实
LedgerEntry：由 Fill、Fee、Funding、Settlement 产生的账簿事实
```

订单状态机：

```text
Draft → RiskApproved → PendingSubmit → Submitted → Accepted → Working
                                                      ↓
                 PartiallyFilled → Filled / CancelPending → Cancelled
                                                   ↘ Rejected / Expired / Unknown
```

`Submitted` 只表示本地已发送；`Accepted` 才表示 Venue 确认。`Unknown` 必须进入查询—对账流程，不得自动当作失败补单。

### 4.4 账簿不变量

- 余额只能由合法 `LedgerEntry` 改变。
- 持仓只能由 `Fill`、结算、企业行为或资金管理事件改变。
- 任何策略不能直接写账户余额、持仓、已成交数量。
- 每个 Fill 至少关联 `strategy_id`、`signal_id`、`intent_id`、`order_id`、`account_id`、`instrument_id`、`rule_version`。
- 账簿重放结果必须与实时结果一致；不一致时以事实事件和对账结果为准，不能用快照掩盖差异。

### 4.5 CanonicalAccountSnapshot：吸收 QIFI 的账户交换能力

牵星定义一个不绑定券商和数据库的规范账户交换结构，称为 `CanonicalAccountSnapshot`，兼容 QIFI 的主要语义但不复制连接凭据和实现细节：

```text
SnapshotHeader
  schema_version / snapshot_id / account_id / portfolio_id
  venue_id / trading_day / event_seq / as_of / state_hash

Account
  cash_by_currency / equity / available / margin / frozen
  realized_pnl / unrealized_pnl / fees / funding / settlement

Positions[instrument_id]
  side / quantity / today_quantity / yesterday_quantity
  average_price / mark_price / unrealized_pnl / margin

Orders[order_id]       → 当前订单状态与 client_order_id
Fills[fill_id]         → 不可变成交事实引用
Transfers[transfer_id] → 资金事件引用
ReconcileStatus        → last_reconcile / discrepancy / recovery_state
```

规则如下：

1. 全量快照是重启、查询和跨语言交换的优化结果，真实来源仍是 EventLog、Fill、LedgerEntry 和 Venue 对账事实。
2. `Diff` 只能描述字段新增、修改、删除及其因果事件，必须带 `base_state_hash` 与 `target_state_hash`；应用 Diff 前先校验基线，失败则重新拉取全量快照。
3. 对外导出默认剥离密码、token、数据库地址、私钥和连接器内部状态；安全审计字段只能以引用或摘要形式出现。
4. 股票、期货、期权的账户字段可以扩展，但基础现金、持仓、订单、成交、资金事件和对账状态不能被不同市场重新定义。
5. 每个 schema 版本提供 Rust 类型、Python 类型提示、JSON Schema、示例快照、兼容矩阵和 golden round-trip 测试。

## 5. 研究与数据架构

### 5.1 数据四层

```text
Raw → Validated → Normalized → Feature/Event
```

| 层 | 可变性 | 必须保存 |
|---|---|---|
| Raw | 追加、不可覆盖 | 原始 payload、接收时间、source、hash |
| Validated | 追加版本 | 质量结果、缺失、重复、越界、迟到 |
| Normalized | 版本化 | InstrumentId、单位、时区、精度、映射版本 |
| Feature/Event | 版本化 | 公式、参数、as-of 时间、依赖输入 hash |

### 5.2 ProviderRegistry 与统一数据获取

借鉴 `QAFetch` 的统一入口，但把“查什么”和“从哪里查”拆开。所有数据源实现 `DataProvider`，由 `ProviderRegistry` 负责能力发现和选择：

```text
DataQuery
  asset_class / instrument_set / field_set / frequency
  start / end / as_of / adjustment / quality_policy

ProviderCapability
  provider_id / markets / data_types / frequencies / realtime
  auth_scope / rate_limit / cost / freshness / quality_score

ProviderResult
  raw_records / provider_id / provider_version
  request_id / retry_chain / received_at / source_hash
```

选择顺序固定为：授权可用性 → 能力匹配 → 时间覆盖 → 质量评分 → 成本/限频 → 稳定 tie-breaker。自动切换只允许发生在 Raw 获取层；Normalized 结果必须记录实际 provider，不得把不同来源静默拼接成一个无血缘数据集。

每个 Provider 还必须提供：字段映射、单位映射、交易日历映射、symbol 映射、分页规则、限频规则、重试分类和已知缺陷。所有失败分为可重试、需切换、需人工、永久拒绝四类。

### 5.3 DataStruct Facade 与不可变 DataView

吸收 `QADataStruct` 的易用接口，但核心不暴露可变 DataFrame：

| 外部便捷对象 | 核心对象 | 必须增加的约束 |
|---|---|---|
| BarDataStruct | `BarSeriesView` | schema、时区、排序、单位、calendar_version、数据 hash |
| Tick/L2 Struct | `QuoteView` / `OrderBookView` | source_seq、接收时间、盘口深度、乱序处理策略 |
| Financial Struct | `FinancialView` | report_period、publish_time、effective_time、PIT 版本 |
| Feature Struct | `FeatureView` | formula、parameters、dependency hash、coverage、missing policy |
| Panel/Series | `MultiAssetView` | instrument set 版本、对齐方式、缺失填充规则 |

Facade 可提供 `select_time`、`select_code`、`resample`、`pivot`、`to_arrow`、`to_pandas` 等研究体验；这些方法返回新视图或新物化结果，不修改原始对象。复权、合并、倒序、补齐等转换必须生成 `TransformManifest`，写入输入/输出 hash、算法版本和参数。

### 5.4 因子、特征与候选策略流水线

```text
DataView(PIT)
  → FeatureDefinition
  → FeatureArtifact(versioned)
  → FactorTransform(standardize/neutralize/winsorize)
  → FactorReport(IC/RankIC/coverage/turnover/decay)
  → Universe/PortfolioCandidate
  → EventBacktest
```

`FeatureDefinition` 只描述计算；`FeatureArtifact` 保存结果；`FactorReport` 保存研究结论；`CandidateConfig` 负责把数据、因子、策略、组合、风控、成本和执行模型绑定起来。训练集、验证集、测试集和上线窗口必须显式分开，任何因子报告都要同时报告覆盖率、缺失处理、换手率、容量、交易成本和行业/风格暴露。

### 5.5 DataView 与前视偏差

所有策略读数据都经过：

```text
DataView.as_of(instrument, event_time, source_policy, universe_version)
```

禁止：

1. 使用未来修订后的合约列表或财务数据。
2. 使用同一时间点尚未接收的数据。
3. 用 bar 收盘值在同一 bar 的收盘前成交。
4. 用后验复权因子覆盖当时可见价格。
5. 用模拟账户快照代替事件事实。

### 5.6 质量门

质量门不是日志，而是机器可执行的 `Accept / Quarantine / Reject`：

- 时间单调性、重复、缺失、迟到、乱序。
- OHLC 关系、负价格、数量精度、交易日历。
- Tick/Bar 与 InstrumentSpec 的单位和 lot 对齐。
- 主源/备源差异、断点续传、schema 变化。
- 低粒度数据禁止调用需要深度信息的 FillModel。

### 5.7 研究通道与事件通道的交接

向量化结果输出的不是“已成交订单”，而是候选配置：

```text
CandidateConfig = {
  strategy_version,
  universe_version,
  parameter_set,
  feature_version,
  intended_exposure,
  constraints,
  data_manifest
}
```

候选配置必须进入事件回测；事件回测才生成可审计的 Order/Fill/Ledger。

## 6. 回测与撮合架构

### 6.1 回测主循环

```text
读取下一个市场事件
  → 更新 SimulatedVenue 市场状态
  → 撮合已有订单
  → 分发给 Strategy/Actor
  → 收集 OrderCommand
  → Risk/OMS 校验
  → 入队并按延迟提交
  → 结算同一时间戳的连锁命令
  → 写入 Fill/Ledger/EventLog
```

策略回调之后产生的订单不能回到当前市场事件之前；同一时间戳内的连锁订单必须有上限并可重放。

### 6.2 数据档位 × 模型兼容矩阵

| 数据档位 | 允许的基础模型 | 不允许声称的能力 |
|---|---|---|
| Bar | NextBarOpen、有限路径、显式滑点 | 队列位置、盘口穿越、同 bar 因果细节 |
| L1 Quote | 最优价、价差、概率成交、有限延迟 | 完整深度、真实队列位置 |
| L2 | 深度行走、深度消耗、部分成交 | 市场不会被本单改变这一隐含假设 |
| L3 | 订单级队列、优先级、撤单竞争 | 未记录的外部参与者行为 |

若模型与数据档位不匹配，默认 `Reject`；只有配置显式选择降级，才允许 `Degraded`，并把假设写入结果。

### 6.3 四个模型的最小接口

```text
FillModel   : market_state + order + clock → fills + assumption
FeeModel    : fill + instrument_spec + account → fee_entries
LatencyModel: command + venue_state + seed → delivery_time
MarginRule  : ledger + order/fill + market_state → requirement/violation
```

每个模型必须提供：名称、版本、参数摘要、适用数据档位、随机种子、可重放输入。

### 6.4 RunManifest

至少包含：代码 commit、编译产物 hash、配置、策略版本、数据 source/hash/range、InstrumentSpec 版本、时钟、时区、随机种子、模型版本、运行时版本、输入事件 hash、输出事件/Fill/Ledger/指标 hash。

验收要求：

1. 同一 Manifest 两次执行输出 hash 相同。
2. 只修改费率/滑点/参数时，差异可以定位到受影响事件。
3. 从 EventLog 重建 Ledger 与指标，结果与原运行相同。

### 6.5 双通道回测与账户复核

回测分成两个互补入口：

| 入口 | 适用任务 | 输出 | 必须经过 |
|---|---|---|---|
| VectorResearch | 因子、选股、参数扫描、组合初筛 | Feature/Factor/Portfolio Candidate | PIT、成本近似、覆盖率和研究 Manifest |
| EventBacktest | 订单路径、延迟、部分成交、风控、结算、组合交互 | Order/Fill/Ledger/AccountSnapshot | 完整时钟、事件日志、撮合模型、账簿重放 |

两条通道通过 `CandidateConfig` 连接，而不是共享可变账户对象。事件回测结束后必须生成：收益曲线、基准、成交明细、持仓曲线、费用/资金费、换手率、容量、最大回撤、风险指标、数据质量摘要和假设清单。任何研究报告都必须标注 `VectorOnly`、`EventVerified` 或 `PaperVerified` 状态。

账户复核至少执行三次：事件内账簿重放、回测末尾快照校验、外部 Venue/QIFI 快照对账。三者不一致时，报告差异字段、第一条分叉事件和恢复建议，不允许只显示一个最终净值。

## 7. 执行与连接器架构

### 7.1 VenueAdapter 五层

```text
Protocol：签名、序列化、错误码
Session ：连接、订阅、心跳、重连、限频
Market  ：snapshot/delta、trade、quote、funding、状态
Account ：余额、持仓、用户流、订单回报
Execution：submit/cancel/replace/query/reconcile
```

市场状态、用户账户状态、订单状态必须由不同组件维护；Execution 只产生标准 OrderEvent，不直接修改 OMS。

### 7.2 连接器状态恢复

```text
Disconnected → Connecting → Snapshotting → Live → Degraded
                                      ↘ ReconcileRequired
```

恢复固定为：停止新单 → 保留原始事件 → 重连 → 拉取市场快照 → 拉取账户与未结订单 → 以 client_order_id 查询 → 对账 → 恢复订阅 → 解除熔断。任何补单必须有 `recovery_reason` 与原命令关联。

### 7.3 路由与限频

路由评分必须可解释，至少含有效价、可用深度、费率、预估滑点、延迟、库存、保证金、失败率与结算风险。每个 Venue 独立维护 token bucket/权重/并发限制；资源不足与风控拒绝使用不同错误码。

### 7.4 国内适配路径

第一优先顺序：

1. `PaperVenue`：验证订单状态、延迟、重连和对账。
2. 一个国内现货/期货 Gateway：验证合约、账户、持仓、成交回报顺序。
3. 一个海外 CEX：验证 REST/WS、资金费、限频和多市场。
4. 多账户和跨所路由：在单 Venue 对账稳定后再启用。

### 7.5 Scheduler、JobSpec 与控制面

借鉴 `QASchedule` 的集中任务入口，但 Scheduler 不直接执行交易逻辑，所有任务都编译为 `JobSpec`：

```text
JobSpec {
  job_id / job_version / owner / enabled
  trigger: cron | calendar | event | manual
  window: pre_open | session | post_close | any
  depends_on[] / input_refs[] / output_refs[]
  timeout / retry_policy / concurrency_key / idempotency_key
  permission_scope / dry_run / audit_reason
}
```

最小任务类型包括：数据同步、质量检查、复权/合约更新、因子计算、参数扫描、事件回测、报告生成、账户快照、对账、健康检查和告警。交易相关任务必须有 `dry_run`、审批/权限、幂等键和失败后的人工接管状态。

### 7.6 Query API、事件订阅与安全边界

控制面分为只读和写入两类：

- 只读：运行状态、数据质量、策略心跳、账户快照、订单/成交、对账差异、指标和 EventLog 查询。
- 写入：启动/暂停策略、修改限额、取消订单、切换 Venue、重试任务、确认恢复；统一转换为带 `operator_id`、`reason`、`permission`、`request_id` 的 `ControlCommand`。

事件订阅提供 snapshot + diff 两种模式。客户端丢失序号时必须重新拉取快照；服务端不能用“当前状态推送”替代可追溯事件。API 层永远不能绕过 RiskGate、OMS、Ledger 或审计归档。

## 8. 多策略、多账户与组合治理

### 8.1 多策略

每个策略拥有独立：配置、版本、状态、指标、特征缓存、订单配额、审计主题。策略之间只能通过不可变 `Signal`、组合快照和组合指令通信。

### 8.2 信号到订单

```text
Signal
  → SignalMerger（去重、TTL、优先级）
  → PortfolioConstruction（目标仓位/风险预算）
  → RebalancePlan（目标 delta）
  → RiskGate
  → OMS / Router
```

两策略同标的相反信号必须先净额求解，不能由消息顺序决定谁覆盖谁。

### 8.3 账户边界

V5 先支持一个 `TradingAccount`，但从类型上保留 `account_id`。多账户阶段加入：

- 账户树：现金、持仓、保证金、权限、单账户限制。
- 组合树：净暴露、总杠杆、相关性、风险预算。
- 每账户串行命令队列；组合层使用版本化快照和乐观分配。
- 五类对账：订单、成交、现金、持仓、费用/资金费。

## 9. 插件与技术实现策略

### 9.1 插件分层

| 层 | 可否替换 | 示例 |
|---|---:|---|
| Kernel | 否 | Clock、CausalQueue、Event、OrderState、LedgerInvariant |
| Domain Rule | 启动期 | Fee、Margin、TradingCalendar、RiskRule、FillModel |
| Adapter | 启动期/受控故障转移 | DataSource、Venue、Storage、Metrics |
| Control/Observe | 运行期 | UI、指标 sink、告警、诊断 |

插件 manifest 必须含：id/version、provides/requires、cardinality、priority、replaces、config schema、capabilities、权限、健康检查、关闭策略、签名/哈希。

### 9.2 暂缓 WASM 的原因

WASM 是隔离和多语言的解决方案，不是 MVP 的必需能力。V5 先提供稳定的 Rust trait 与 Python 控制面；只有在以下条件同时满足后再引入 WASM：

- Kernel/Strategy/OrderIntent 契约连续两个版本稳定。
- Python↔Rust 批处理边界和性能基线明确。
- 策略资源配额、fuel、内存、确定性随机接口已有测试。
- 需要第三方策略隔离或跨语言分发。

### 9.3 存储与部署

- 本地开发：Parquet/Arrow + 文件 EventLog + SQLite 元数据。
- 单机纸面：同一进程 Kernel + 独立 Adapter 任务 + 可恢复 EventLog。
- 生产初期：按账户/策略拆分进程，Kernel 仍保持单一事件语义；只有数据、控制、报表等低耦合能力考虑服务化。
- 不为“微服务化”引入跨服务交易事务；跨进程只传不可变事件或命令，并保留幂等键。

运行时配置由 `qx-runtime` 校验，示例位于 `deploy/qianxing.runtime.example.json`；`qx-cli runtime-check` 会检查 API 传输安全、存储后端、worker 角色、账户/交易所绑定和停机超时，`qx-cli serve`、`supervise`、`scheduler-worker`、`strategy-worker` 提供可运行的 API、跨平台进程监督、调度和策略任务进程入口。Scheduler 从版本化 JobSpec 文件恢复状态，按 UTC Cron 触发并写入可恢复 JobQueue，Strategy worker 负责生命周期，并将确定性 Signal 经 Portfolio/RiskGate 转为审计化 SubmitOrder 命令；行情、用户流、Execution、调度、策略和对账 worker 通过同一配置拓扑装配。API 控制命令进入原子持久化控制面和带租约/fencing 的 `control-queue`，不能把凭证写入配置摘要。

## 10. 模块重排与建议目录

```text
crates/
  qx-core/          # Kernel：clock/event/identity/order/ledger invariants
  qx-domain/        # instrument/calendar/position/account/risk contracts
  qx-data/          # raw/quality/normalized/catalog/dataview/feature
  qx-provider/      # ProviderRegistry、多源连接、数据源能力与切换
  qx-research/      # vector candidate、参数扫描、指标输入
  qx-factor/        # Feature/Factor 定义、版本、分析、报告
  qx-sim/           # simulated venue、matching、fee、latency、margin
  qx-execution/     # signal、portfolio、risk、oms、router
  qx-protocol/      # CanonicalAccountSnapshot、Diff、JSON Schema、QIFI 映射
  qx-scheduler/     # JobSpec、交易日历、依赖、重试、幂等
  qx-adapter-api/   # data/venue/storage/metrics trait
  qx-runtime/       # 运行时拓扑、worker 监督、健康状态与停机信号
  qx-bridge/        # Python/Arrow/JSON 边界，不进入 Kernel 热路径
  qx-adapter-paper/ # paper venue
  qx-adapter-ctp/   # 后续，国内柜台
  qx-adapter-cex/   # 后续，REST/WS CEX
  qx-ops/            # audit、reconcile、metrics、run manifest
  qx-plugin/         # startup assembly only
  qx-cli/            # run/backtest/paper/verify/reconcile
  qx-control/        # Query API、ControlCommand、权限、审计
```

当前 `qx-fenye`、`qx-guanxing`、`qx-xingban`、`qx-zhenlu`、`qx-genglu` 可以先保持 crate 名称不变，以模块内部重组为主，避免没有收益的全量改名。

## 11. 分阶段实施路线与验收门

### Phase 0：基线修复与契约冻结（1–2 周）

交付：修复 `Price/Quantity/Money/Fixed` 类型边界；全 workspace 测试绿色；冻结 Event、Order、Fill、Ledger、RunManifest 的序列化和 hash 规则。

验收：`cargo test --workspace` 全绿；同一输入运行两次 hash 一致；公共类型有不变量测试。

### Phase 1：最小事件回测闭环（2–4 周）

交付：Bar DataView、NextBarOpen、基础 Fee、RiskGate、OMS、Ledger、指标、CLI 回测。

验收：SMA/突破策略完成“信号—订单—成交—账簿—报告”；bar t 不得在 t 收盘成交；重放结果一致。

### Phase 2：L1 与纸面交易（3–5 周）

交付：QuoteTick、BestPrice/Probabilistic Fill、PaperVenue、订单恢复、基础对账。

验收：断线重连、重复回报、乱序回报、未知订单、取消超时均有确定结果；paper 与 backtest 共用订单/账簿契约。

### Phase 3：研究通道与候选交接（3–5 周）

交付：ProviderRegistry 最小版、DataStruct Facade、向量化参数扫描、CandidateConfig、事件回测批量运行、分层指标和成本敏感性报告。

验收：至少 100 组参数可复现；同一查询能切换主/备 Provider 并保留血缘；候选配置带完整数据/代码/特征版本；向量化结果不能绕过事件回测直接标记为可实盘。

### Phase 4：因子版本与账户协议（3–5 周）

交付：FeatureDefinition、FeatureArtifact、FactorReport、Universe/PortfolioCandidate、PIT 财务数据接口、CanonicalAccountSnapshot v1、Diff、JSON Schema、Rust/Python round-trip 测试。

验收：因子同名多版本可并存；因子报告能追溯输入和变换；快照脱敏；Diff 基线校验和幂等应用正确；回测账户与快照重建一致。

### Phase 5：规则包、调度和一个真实 Venue（4–6 周）

交付：TradingCalendar、JobSpec/Scheduler、Query API/ControlCommand、InstrumentSpec 版本、一个国内或海外 Venue、限频、恢复、五类对账。

验收：定时同步—质量检查—因子/策略—报告链路可重跑；控制命令可审计；小额/模拟账户完成全流程；异常注入后不重复下单；账簿差异能定位到原始事件。

### Phase 6：多策略与多账户（4–6 周）

交付：SignalMerger、PortfolioConstruction、账户路由、配额、策略/组合/账户三维归因。

验收：相反信号净额正确；账户隔离；资源不足与风控拒绝分码；单账户命令严格串行。

### Phase 7：多 Venue 与生产可靠性（持续迭代）

交付：第二个 Venue、跨 Venue 路由、故障转移、灰度、故障注入、签名插件、审计查询。

验收：至少两个 Venue 能以同一 OrderIntent 运行；路由决策可解释；故障转移全程留痕；上线有回滚方案。

### 暂不排期能力

DEX/链上最终性、期权全模型、WASM 策略市场、分布式撮合、高频纳秒优化。这些能力必须由真实需求和基准结果触发，而不是由架构图触发。

## 12. 工程质量与性能基线

### 12.1 正确性门

- 类型：金额/价格/数量不能隐式混算；溢出、精度、lot/tick 对齐有测试。
- 时间：不可回退；同时间因果顺序固定；迟到事件有策略。
- 数据：质量门可阻断不合格输入；PIT 和 as-of 可验证。
- 交易：状态机只允许合法迁移；命令幂等；Unknown 不自动补单。
- 回放：事件、账簿、指标三重重建一致。
- 安全：凭证只读引用，不进入事件日志；插件权限最小化。

### 12.2 性能基线方法

不使用“每秒多少行/多少订单”的脱离上下文承诺。基准固定数据行宽、事件类型、序列化方式、缓存命中率、策略复杂度、CPU 架构和并发数，分别测：

1. DataView 读取。
2. 事件队列吞吐与尾延迟。
3. 单订单从 Intent 到 Fill 的端到端延迟。
4. 100/1,000/10,000 策略参数扫描。
5. EventLog 写入、重放和账簿重建。

### 12.3 生产指标

订单确认延迟、Fill 延迟、撤单成功率、滑点、拒单率、重连次数、重放 lag、对账差异、风险限额使用、数据质量告警、策略心跳、事件队列深度、资源配额和异常订单数。

## 13. 主要风险与决策

| 风险 | V5 决策 |
|---|---|
| 范围失控 | 以单账户/单 Venue/Bar+L1 闭环为第一生产目标 |
| 事件语义被插件改变 | Kernel 不可替换；规则插件启动期装配 |
| 回测过拟合执行细节 | 数据档位与 FillModel 强制匹配；降级必须审计 |
| 多账户过早复杂化 | 类型预留 account_id，能力后置；先做账簿不变量 |
| 语言边界过多 | 先 Rust trait + Python 控制面，WASM 后置 |
| 连接器不稳定 | 先 PaperVenue 和恢复/对账，再接真实 Venue |
| 依赖许可与数据许可 | 仅借鉴公开架构；运行时、SDK、数据按许可证重新审查 |
| 质量门回退 | 每次契约或适配器变更必须保持 workspace 测试、clippy、release 和 CLI golden 验收全绿 |
| 多源数据静默混合 | Provider 选择只发生在 Raw 层；Normalized 必须带 provider、版本、source_hash |
| 因子研究不可复现 | Feature/Factor 版本、PIT、输入 hash、变换参数写入 CandidateConfig |
| 快照掩盖账簿错误 | Snapshot 只能加速查询；Ledger/EventLog/对账事实优先 |
| 控制面越权交易 | API/调度器只能提交审计 ControlCommand，不得直连账户或绕过 OMS |

## 14. 最终架构原则

1. **先冻结语义，再扩展连接器。**
2. **策略产生意图，组合层决定目标，OMS 决定命令，Venue 返回事实。**
3. **研究速度和执行真实性使用不同引擎，但使用同一领域契约。**
4. **数据来源、市场身份、执行身份三者分离且可追溯。**
5. **撮合模型必须声明不知道什么，禁止从低粒度数据推导不存在的信息。**
6. **可替换性不能破坏确定性；热路径不动态插件化。**
7. **Unknown 是需要对账的状态，不是失败。**
8. **多策略是隔离、净额、配额、优先级和归因，不是简单多线程。**
9. **一项能力只有在有验收门、回滚和故障策略时，才算完成。**

## 15. 官方参考资料

以下资料仅用于核对公开架构和接口概念，未复制第三方代码：

1. [QuantConnect LEAN 官方仓库](https://github.com/QuantConnect/Lean)；[Algorithm Framework 概览](https://www.quantconnect.com/docs/v2/writing-algorithms/algorithm-framework/overview)
2. [NautilusTrader 官方仓库与概览](https://github.com/nautechsystems/nautilus_trader/blob/develop/docs/concepts/overview.md)；[回测执行流程](https://github.com/nautechsystems/nautilus_trader/blob/develop/docs/concepts/backtesting/execution-flow.md)
3. [RQAlpha 官方文档仓库](https://github.com/ricequant/rqalpha/blob/master/docs/source/index.rst)
4. [vn.py 官方仓库](https://github.com/vnpy/vnpy)；[MainEngine/Gateway 实现入口](https://github.com/vnpy/vnpy/blob/master/vnpy/trader/engine.py)
5. [Hummingbot Connector Architecture](https://hummingbot.org/connectors/connectors/architecture/)
6. [CCXT Manual](https://github.com/ccxt/ccxt/wiki/manual)；[CCXT Pro Manual](https://github.com/ccxt/ccxt/wiki/ccxt.pro.manual)
7. [vectorbt 官方文档](https://vectorbt.dev/)；[Portfolio API](https://vectorbt.dev/api/portfolio/base/)
8. [QUANTAXIS 官方仓库](https://github.com/yutiansut/QUANTAXIS)；[QAFetch 数据获取文档](https://github.com/yutiansut/QUANTAXIS/blob/master/doc/user-guide/data-fetching.md)
9. [QUANTAXIS QAData 数据结构](https://github.com/yutiansut/QUANTAXIS/tree/master/QUANTAXIS/QAData)；[QAFactor 因子模块](https://github.com/yutiansut/QUANTAXIS/tree/master/QUANTAXIS/QAFactor)
10. [QIFI 协议规范](https://github.com/yutiansut/QUANTAXIS/blob/master/QUANTAXIS/QARSBridge/QIFI_PROTOCOL.md)；[QASchedule 任务调度模块](https://github.com/yutiansut/QUANTAXIS/tree/master/QUANTAXIS/QASchedule)
11. [Binance Spot REST API 官方文档](https://developers.binance.com/en/docs/products/spot/rest-api)；[Spot 用户数据流 WebSocket API](https://developers.binance.com/en/docs/catalog/core-trading-spot-trading/api/ws-api/user-data-stream)
12. [Binance Spot 账户/订单 WebSocket API](https://developers.binance.com/en/docs/catalog/core-trading-spot-trading/api/ws-api/account)；[Spot executionReport schema](https://developers.binance.com/en/docs/catalog/core-trading-margin-trading/api-ws-streams/~schemas)
13. [Binance Spot Testnet WebSocket Streams](https://developers.binance.com/zh-CN/docs/products/spot/testnet/web-socket-streams)

> 参考项目的许可证、数据授权、实时接口、市场规则和版本行为可能变化。任何生产依赖必须在引入时重新核验，不以本文的架构借鉴等同于运行时授权。

## 16. 本版本实现落地清单

截至当前工作区，本方案已经落地为可编译、可测试的 Rust 实现：

- `qx-core`：定点类型修复、事件时间/血缘字段、订单状态机与 Unknown 对账态、账户隔离追加式 Ledger、显式合约乘数下的现金/权益/已实现盈亏/费用记账、Ledger 事实重放，以及带字段校验、稳定 JSON 和无边界碰撞摘要的 RunManifest；事件日志还会校验序号唯一性与执行排序。
- `qx-guanxing`：QuoteTick、DataCatalog、RawRecord、Bar/L1 质量门、拒绝隐式重排的带不可变元数据和数据哈希且可校验 JSON 恢复的 PIT DataView、按发布/生效时间双截止并可恢复的 FinancialView、参数网格与带血缘 CandidateConfig。
- `qx-xingban`：可复用 `BacktestEngine`、独立账户的批量回测、Bar 因果回测、L1 Tick/L2 订单簿因果回测、订单 Submit/Accepted/Rejected 事实事件、数据档位兼容校验、带版本/参数/假设描述的 Fill/Fee/Latency/Margin 模型、真实命令延迟、显式合约乘数下的初始保证金/权益估值和事件/账簿回放；订单簿回测已支持盘口校验、逐档吃单、部分成交、全局盘口流动性扣减、快照 sequence/ts 单调性、重复 client_id 拒绝和撤单先后语义，以及通过 `TradingInstrumentSpec`/`OrderPolicy` 注入杠杆和衍生品初始保证金 Ledger 语义；报告输出现金基准、持仓、费用、换手、收益率、最大回撤及 bps 风险指标和假设清单，自动生成 RunManifest，并拒绝策略订单越过回测账户/标的边界。
- `qx-strategy`：Rust `Strategy` trait、`StrategyDecision.intents[]` 多订单意图、Native Bar/Tick adapter、版本化 JSONL/`framed_json`/`shared_memory_json`/`shared_memory_columnar` 跨进程策略契约、SPSC mmap ring、C ABI vtable 校验、动态库大小/SHA-256 白名单/可选 detached Ed25519 签名校验与生命周期边界已落地；Python/C++ Worker 已真实接入双向共享 ring，QXCB 已支持 Bar 固定宽度列式传输，Python、C++/Rust 外部策略复用持久 worker 和 `qx-cli strategy-backtest` 入口，策略进程默认使用最小环境白名单并拒绝凭证类环境变量；完整 Arrow 零拷贝行情热路径、端到端性能基准、签名信任根、沙箱和跨平台预编译 SDK 仍未完成。
- `qx-zhenlu`：SignalMerger、OrderIntent→OrderTrace/Fill 归因、StrategyRuntime、AccountRouter、确定性账户分配、账户命令队列、Venue/VenueAdapter、按 L1 容量逐单消耗的 PaperVenue、限频、Snapshotting→Live 断线恢复状态。
- `qx-genglu`：订单/现金/持仓/费用/资金五类对账与策略成交归因；远端快照重复键会被拒绝，不再静默覆盖。
- `qx-plugin`：manifest 配置 schema、稳定 hash、本地摘要签名和 Ed25519 公钥签名验证、能力/权限元数据、JSON 往返、依赖拓扑、冲突检测和五阶段启动计划；私钥托管、密钥轮换和发布系统的信任根仍由部署环境接入。
- `qx-cli`：`all`、`verify`、`paper`、`reconcile`、`runtime-check`、`serve`、`supervise`、`scheduler-worker`、`strategy-worker`、`paper-worker` 和 `paper-submit-order` 端到端验收/运行入口；真实 `reconcile <runtime> <worker>` 会运行一次 Binance Reconciler 并原子保存结构化差异报告，使用账户级账簿与快照事件；`supervise`/PowerShell/Unix 启动入口会校验拓扑、分离 worker 日志并在子进程异常时 fail-fast 停止其余进程。
- `qx-runtime`：运行时 schema、API/行情/用户流/Execution/Scheduler/Strategy/对账 worker 拓扑校验、Strategy 账户/Venue/Instrument 绑定校验、生产环境明文禁用、mTLS Operator 配置要求、worker 启停监督、心跳过期降级、panic/失败转 Failed 和停机 token；`LiveEventPipeline` 已将标准行情、Accepted/Fill/Cancelled、对账要求、账户余额快照归约到按账户/交易所共享的可恢复 EventLog，并在成交后生成 LedgerApplied、恢复 OMS 订单索引和去重索引；支持可选分段 EventLog，完整段不可变、尾段追加并由 manifest 校验事件数/序号/摘要；结算币种账簿/柜台余额差异可计算并进入 Reconciler 降级详情，不自动写入 Ledger；控制面 `SubmitOrder` 的订单载荷校验、控制状态持久化和执行队列入口已提供。`qx-cli runtime-check`、`serve`、`scheduler-worker`、`strategy-worker`、`paper-worker`、本地 `paper-submit-order`、审计 `binance-submit-order` 和 `binance-worker <execution-worker>` 已提供可运行入口，Strategy worker 已将确定性 Signal/Portfolio/RiskGate/OrderIntent 接入 SubmitOrder，Paper Execution 已完成本地 Accepted/Fill/LedgerApplied 闭环，Paper/Strategy/API 运行路径可按配置启用分段 EventLog，Binance market/user/execution/reconcile worker 已通过环境变量或 Secret Manager 投影文件凭证、配置 endpoint 和事件管线提供独立启动入口，新用户流会话/执行订单/对账轮次会重新加载凭据，执行/对账 worker 支持 `--once` 单轮验收，其他供应商 worker 仍按适配器注入。
- `qx-storage`：EventLog、文件控制面和调度状态的原子 JSON 文件存储与重启恢复，带并发追加锁的链式 `AuditFileStore`（幂等追加、前缀同步、按命令/游标查询、篡改检测），支持共享文件系统持久化令牌桶、原子 `ControlPlane` 更新，以及支持幂等入队、共享文件系统 claim 锁、worker 租约、过期接管、fencing token 和严格确认的 `FileJobQueue`/`ControlCommandQueue`；启用 `sqlite` feature 后，控制面状态和 SubmitOrder 队列也切换到 SQLite Immediate transaction；启用 `postgres` feature 后，TLS PostgreSQL 事务后端覆盖控制面、控制命令、JobQueue、审计链、快照和 EventLog，保持相同的幂等、拒绝和 fencing 语义。
- `qx-api`：HTTP/1.1 查询/控制路由、显式 `ServerConfig` 注入的 rustls 服务端 TLS 单请求/持续服务入口、可热替换 TLS 配置、mTLS 客户端证书到 Operator 的可信映射、持续事件推送与优雅关闭、有界实时事件总线，以及可切换本地/共享文件/SQLite 令牌桶的 API 限流和 WebSocket 握手/快照/事件连接入口；控制命令可由上层注入持久化事务提交器与可恢复队列写入器，TLS 与明文共享同一权限和审计处理链。
- `qx-adapter`：可替换传输层上的 REST Provider/Venue、明文 TCP 与 rustls TLS 客户端、HMAC-SHA256 请求签名器边界、TLS WebSocket 用户流会话（握手、认证头、帧解析、分片、ping/pong、客户端掩码）、连接/读写超时、外部成交事实注入和 Snapshot 对账恢复；订单撤改单遵循状态机，异常远端成交数量和 Unknown 订单会进入对账处理。Binance Spot 当前已落地 HMAC 签名请求、LIMIT/MARKET 下单、撤单、按 symbol 的 `allOrders` 历史快照/无 symbol `openOrders` 对账、远端 orderId 恢复绑定、公共 L1 `bookTicker` REST/WSS、主网/Testnet 端点构造、签名用户流订阅会话、可注入重连退避驱动、`executionReport` 成交/撤单事件映射、成交去重、5xx 结果未知保护、环境变量/投影文件凭据和 EventLog 重启后的订单恢复；重连建立的新用户流会话使用最新凭据，执行新订单与对账新轮次也重新加载凭据；仍需在沙盒/真实账号完成网络验收。其他供应商的签名 canonical 规则、证书/私有 CA、认证/订阅 payload、用户事件映射和限频仍按供应商逐家接入。

外部券商、CTP、CEX 的协议实现不能凭空生成，当前已完成统一 `Venue`/`VenueAdapter` 接口与 PaperVenue 参考实现；接入具体交易所时只需在该边界实现协议、会话、行情、账户流和执行五层，不得改变 Kernel、Ledger 和订单事实语义。

### 16.1 V5.1 生态层实现状态

- `qx-provider`：Provider 能力登记、质量/优先级选择、主备故障切换、四类错误分类（可重试/需切换/需人工/永久）、retry_chain 血缘、结果范围/来源/顺序校验、来源哈希校验、DataQuery/ProviderResult JSON 往返和按 `as_of` 截止的确定性 `InMemoryProvider` 已落地；`qx-adapter::RestProvider` 已提供真实 HTTP 请求基线，`TlsHttpTransport` 与 `HmacSha256Signer` 已提供可运行的通用安全边界，具体数据供应商协议仍待逐家接入。
- `qx-factor`：FeatureDefinition、FeatureArtifact、PIT 动量计算、标准化/缩尾变换、要求分组标签的真实组内中性化、按时间横截面实际计算 IC/RankIC/衰减/换手/容量/暴露并强制绑定输入指纹、观测摘要和时间范围的 FactorReport、带训练/验证区间与成本/执行/风险模型绑定的 CandidateBinding 和确定性依赖 DAG 已落地；财务因子库和分布式物化仍待接入。
- `qx-protocol`：`CanonicalAccountSnapshot v1`、确定性 state hash、Diff、基线校验、QIFI Envelope、稳定 raw-integer JSON、serde 无损往返、JSON Schema、可恢复解析和幂等原子文件快照仓储已落地；`qx-storage` 的可选 SQLite/PostgreSQL `SnapshotStore` 已接入同一校验/封存语义。
- `qx-scheduler`：交易时段、可恢复 TradingCalendar、JobSpec、审计原因、依赖图、幂等键、运行状态机、并发键、JSON 恢复校验、五字段 CronSpec 匹配、交易日历/JobWindow 约束、Cron/Event/Manual 触发、失败码/退避约束、超时转人工介入、确定性 `retry_run`、可校验 `RunManifest` 绑定和 `SchedulerWorker` 执行边界已落地；`qx-storage::FileJobQueue`、SQLite JobQueue 和可选 PostgreSQL JobQueue 已提供入队、原子事务 claim、租约/fencing token、过期接管和严格确认语义，`qx-cli scheduler-worker`/`strategy-worker` 已提供可恢复本地进程边界，连接池、消息队列式跨节点后端仍待接入。
- `qx-control`：权限、服务端授予权限校验、审计 ControlCommand、`SubmitOrder`/Cancel/Reconcile 等命令分类、重复请求防护、Accepted→终态状态机、终态后不可重复执行、事件订阅游标、执行结果回写和 JSON 恢复校验已落地；`qx-storage::AuditFileStore`、SQLite `AuditStore` 与可选 PostgreSQL `AuditStore` 已提供文件/事务追加链和审计查询。
- `qx-api`：HTTP 查询/控制、配置化 Operator 权限表、可信身份注入入口、账户 Snapshot + Diff、历史/实时事件游标、丢序基线错误、可切换本地/共享文件/SQLite token bucket 限流、有界 `ApiEventBus`、可替换 `TlsConfigStore` 与 `MtlsIdentityStore`、PEM 证书链/私钥/客户端 CA 和 Operator 证书映射的轮询式安全重载、mTLS `ServerConfig` 构造、证书 DER 到 Operator 映射、WebSocket 握手/快照/事件持续推送/关闭和 `serve_once_tls`/`serve_once_tls_as`/`serve_tls`/`serve_tls_as`/`serve_once_tls_mtls`/`serve_tls_mtls` 已落地；证书签发、秘密管理信任根和集群部署仍待接入。
- `qx-zhenlu`：订单形状校验、单账户/多策略隔离、信号去重与 TTL、限频器、带参考价/乘数的风控门禁、PaperVenue 恢复、账户路由和多 Venue 证据评分已落地；Binance Spot 的 L1 行情、用户事件和重连驱动已由 `qx-adapter` 实现，其他具体 Venue 仍由各自适配器实现，通用 WSS 会话已在 `qx-adapter` 提供。
- `qx-datastruct` + `qx-python` + `python/qianxing_bridge`：Rust 列式 BarFrame facade、固定列顺序、PIT 截止、raw integer JSON、要求输入/输出 hash 的 TransformManifest 校验、Python/Rust round-trip、pandas/pyarrow 可选转换、Arrow C Data Interface 六列零拷贝借用视图、带 release 回调的拥有型 Arrow 列导出，以及 PyO3 原生扩展的 `__arrow_c_array__` capsule 协议已落地；`tools/build_python_wheel.ps1`、`tools/build_python_wheel.sh` 和 `.github/workflows/ci.yml` 已提供本机构建及 Linux/macOS/Windows × Python 3.10/3.13 矩阵，manylinux/musllinux 兼容性与发布签名仍需接入。

这些能力是 QUANTAXIS 参考带来的“生态层补全”，优先级低于 Kernel、账簿、订单事实和 Paper/Live 对账正确性。当前已完成 schema/契约/golden 风格测试、Python/JSON/API/REST 基线、适配器和 API TLS 边界、HMAC 签名篡改/过期拒绝、Ed25519 manifest 签名验证、审计链篡改拒绝、SQLite 控制面/命令队列/审计/快照/限流事务语义、PEM TLS 配置加载与安全重载边界、Arrow C Data Interface 借用/拥有导出与 release 生命周期、PyO3 原生扩展、可构建 Python wheel 和 pyarrow 六列导入、Cron/Calendar/Event/Manual 调度触发、重试和故障恢复测试、Binance 主网/Testnet L1 行情解析、用户流重连驱动、LiveEventPipeline 的事件→Ledger/EventLog/账户快照归约、账户级共享日志并发追加恢复、订单重启恢复、持久化 SubmitOrder 控制面/队列和 Execution worker 执行入口、跨平台 qx-cli/PowerShell/Unix 进程监督入口；剩余生产化工作是 Binance 沙盒/真实账号网络验收、其他供应商的证书/私有 CA 与专用签名协议、Secret Manager/CA 信任根与真实轮换验收、跨节点 PostgreSQL/MQ 后端、远程 CI 矩阵执行、manylinux/musllinux 兼容性与发布签名。

### 16.2 当前实现边界与验收口径

- `qx-api` 当前提供本地 HTTP/1.1 查询与控制边界、`handle_as`/`serve_once_as` 可信身份注入、单进程限流、有界 `ApiEventBus`/`/events/live` 游标接口、调用方注入或通过 `TlsConfigStore` 热替换的 TLS 服务、PEM 加载/轮询重载、mTLS 证书到 Operator 映射、SQLite 事务限流、WebSocket 握手/快照/事件持续推送和关闭；仍未宣称证书签发与秘密管理自动化、跨节点集群部署或外部身份目录联动。
- `qx-adapter` 当前通过可替换 `HttpTransport` 提供 REST Provider/Venue 基线，包含明文 TCP、默认 Mozilla 根证书/主机名校验的 rustls TLS 客户端、HMAC-SHA256 请求签名、带认证头的可注入配置 TLS WebSocket 用户流会话、HTTP 错误分类、请求超时、每 Venue 限频、恢复状态门禁、远端订单/成交字段校验、`as_of` 过滤、外部成交事实幂等注入和远端 Snapshot 对账；Binance Spot 已实现 REST 下单/撤单、按 symbol 的 `allOrders` 历史快照/无 symbol `openOrders`、远端 orderId 恢复绑定、L1 `bookTicker` REST/WSS、主网/Testnet 端点构造、签名用户流订阅、可注入重连驱动和真实端点便捷运行入口、`executionReport` 事件映射，但沙盒/真实账号验收仍未宣称完成。其他具体供应商的签名 canonical 算法、私有 CA、用户流认证/订阅协议、事件映射和限频规则必须逐家实现并做沙盒验收。
- `qx-protocol` 的文件快照和 `qx-storage` 的事件/状态文件均采用临时文件隔离后原子替换；它们解决单机恢复和并发临时文件冲突，不等同于跨进程事务锁或高可用存储。
- `qx-storage` 当前使用原子 JSON 文件保证单机重启恢复，`AuditFileStore` 提供带并发追加锁的追加链、前缀同步、游标/命令查询和篡改检测，`FileTokenBucket` 提供共享文件系统限流，并提供 `FileJobQueue` 的幂等入队、共享文件系统 claim 锁、worker 租约、fencing token 和严格确认；SQLite 和可选 PostgreSQL 已将队列/审计/快照/EventLog 语义搬入事务数据库，但 PostgreSQL 当前仍是单连接控制面实现，不等同于连接池、消息队列式跨节点队列或完整高可用存储。接入消息队列时必须保持事件序列、幂等键、租约 fencing token 和审计哈希语义不变。
- 验收门槛已固化为：`cargo test --offline --workspace`、`cargo clippy --offline --workspace --all-targets -- -D warnings`、`cargo build --offline --workspace --release`、Python 桥接测试、核心确定性校验以及 `qx-cli all/ecosystem` 冒烟全部通过。
