# 自研量化框架 V5 落地审计

更新时间：2026-09-11

本表只把有代码、测试或可运行命令证明的能力记为“已落地”；外部账号、供应商协议和部署基础设施不以接口占位视为完成。

## 已落地并通过验收

| 范围 | 当前实现 | 证据 |
|---|---|---|
| Kernel / Ledger / Event Replay | 定点类型、因果队列、订单状态机、账户账簿、事件重放、RunManifest | `cargo test --offline --workspace`；`python tools/validate_core.py` |
| Backtest / PaperVenue | Bar 因果回测、L1 撮合、费用/延迟/风险门禁、断线恢复和对账 | `cargo run -p qx-cli --release -- all` |
| Provider / Factor / Protocol | Provider failover、PIT、因子报告、CandidateBinding、QIFI Snapshot/Diff/Schema | `cargo run -p qx-cli --release -- ecosystem` |
| Scheduler / Control / Storage | Calendar、Cron/Event/Manual、重试、审计链、FileJobQueue；按配置选择文件或 SQLite 的控制面、SubmitOrder/Job 队列、审计/快照/限流事务；`SubmitOrder` 控制命令权限/幂等校验、原子控制面事务、可恢复命令队列和 CLI/Execution worker 执行入口；Scheduler/Strategy 异步 JobRun 状态推进 | workspace 测试；SQLite feature 测试；SQLite 控制面/队列 fencing 测试；Scheduler/Strategy smoke；CLI dry-run、PauseStrategy 与 Paper SubmitOrder 集成测试 |
| API / Security | HTTP/WebSocket、事件游标、mTLS、证书 DER→Operator、PEM 加载、服务端证书/客户端 CA/Operator 映射轮询重载、SQLite 限流 | `qx-api` 17 项测试与 clippy；API `/health` 实测 200 |
| Binance Spot | REST 签名下单/撤单、按 symbol 的 `allOrders` 历史快照/`openOrders` 对账与远端 orderId 恢复、L1 REST/WSS、主网/Testnet 端点、用户流认证头、错误/过期会话重连和 `executionReport` 映射；支持环境变量/投影文件凭据，用户流新会话、执行新订单、对账新轮次重新加载凭据；适配器回报已接入 LiveEventPipeline/Ledger 集成测试 | `qx-adapter` 27 项测试与 clippy；运行时 credential source 校验；官方协议 payload golden 测试；Adapter→Pipeline→Ledger→恢复测试 |
| Python / Arrow | PyO3 扩展、Arrow C Data Interface、pyarrow 导入、Windows/Unix wheel 构建脚本与跨平台 CI 矩阵 | Python 测试；Windows 本机构建；`.github/workflows/ci.yml` YAML 校验；远程矩阵待执行 |
| Runtime / Deployment | `qx-runtime` 配置 schema、Strategy 账户/Venue/Instrument 绑定校验、可校验 StrategyTargetSnapshot、worker 监督、心跳/过期降级、panic→Failed、停机 token、`runtime-check`、跨平台 `qx-cli supervise`/PowerShell/Unix 启动入口、paper API `serve`、Scheduler/Strategy/Paper Execution 可恢复 worker、Binance market/user/execution/reconcile worker 启动入口与环境变量/投影文件凭证边界；`qx-execution` 统一 Venue 回报归约与 Binance SubmitOrder 执行提交边界；`LiveEventPipeline` 将标准行情、订单回报、成交、取消、对账和账户余额快照写入账户级可恢复 EventLog，并在成交事实后生成 LedgerApplied；策略按账户/Venue 读取持仓，已落终态但未确认的控制队列命令只做安全清理；结算币种账簿/柜台余额差异进入 Reconciler 降级详情，且不自动改账；重启恢复订单跟踪状态供用户流和执行流继续消费 | `cargo test --offline -p qx-runtime`；`cargo test --offline -p qx-adapter`；`cargo test --offline -p qx-execution`；`cargo test --offline -p qx-cli --features sqlite`；Paper `paper-e2e` 统一入口与 9 项 CLI 测试；策略目标快照加载与事件回报幂等回归测试；终态队列崩溃窗口恢复回归测试；执行/对账 worker `--once` 入口编译验证；`serve` + `/health` 实测；监督器角色映射测试；缺凭证 worker 启动前拒绝 |
| Multi-account contract | AccountRouter 白名单、账户配额、确定性 allocation、账户命令队列 | `qx-zhenlu` 单元测试 |

## 尚未宣称完成

| 缺口 | 原因 | 下一验收门 |
| 公共 CCXT 多交易所连接 | `python/qianxing_ccxt` 使用公共 CCXT REST 客户端完成 market/symbol 映射、精度/最小数量/杠杆/维持保证金元数据、OHLCV/ticker/订单/余额/持仓/资金费率/衍生品参数归一化和错误分类；Rust 已接入 CCXT MarketData ticker、Execution SubmitOrder、远端订单 ID 持久化、订单状态/余额/持仓/资金费率 Reconcile、MarketSpec/OHLCV 快照、产品规格驱动回测、Hedge 分仓 Ledger 和多策略 worker/job 隔离；尚未完成 CCXT Pro 用户流、资金账单结算和多交易所 sandbox 闭环 | 以 Binance/OKX/Bybit sandbox 分别完成现货、杠杆、Linear Swap 的真实请求回放与重启对账验收 |
|---|---|---|
| Binance Testnet/Live 真实网络交易 | 需要实际 API key、权限、余额和网络环境；当前只验证协议和注入式故障路径 | 小额/模拟账户完成下单—回报—快照—对账闭环，保留原始事件与回滚记录 |
| Binance worker 真实生产闭环 | 行情、用户回报、成交归约、账户余额快照、订单恢复、持久化 SubmitOrder 队列和 Execution worker 已接入 `LiveEventPipeline`；Reconciler 现在会保存结构化订单/结算币种差异报告；Paper 本地闭环、Scheduler/Strategy JobQueue 和跨平台 fail-fast 监督器闭环已可执行；仍需要真实网络与生产编排环境验收 | 使用沙盒/模拟账户完成“API 接收—队列领取—提交—Accepted—Fill—Ledger—余额快照—重启恢复—对账”闭环，并保留原始事件、结构化对账报告与回滚记录 |
| 其他真实 Venue / 第二个交易所 | 每家 canonical 签名、限频、认证、账户事件和订单规则不同，不能用通用接口替代 | 第二个 Venue 用同一 OrderIntent 完成沙盒闭环与故障转移验收 |
| 证书签发、秘密管理、自动轮换 | TLS 证书仍由部署系统签发和托管；Binance 凭据已支持环境变量或投影文件，文件在新连接/新订单/新对账轮次重新加载 | 接入 Secret Manager/CA，轮换期间旧连接不中断、新连接使用新证书，并完成真实部署验收 |
| PostgreSQL / MQ / 跨节点高可用 | SQLite 保证单机事务语义，不等价于集群一致性 | 保持序列、幂等键、租约 fencing、审计 hash 的跨节点集成测试 |
| 多平台 wheel CI / 发布签名 | 已增加 Unix wheel 构建脚本与 GitHub Actions 的 Linux/macOS/Windows × Python 3.10/3.13 矩阵；当前工作区已验证 Windows 构建路径，远程矩阵尚未执行 | CI 矩阵跑通并保留 six-platform artifacts；manylinux/musllinux 兼容性与签名发布产物仍需发布环境验收 |
| WASM 策略运行时 | V5 明确后置，需先冻结契约、资源配额和确定性随机接口 | 需求触发后单独立项，不作为当前 MVP 完成条件 |

## 标准验收命令

```powershell
cargo fmt --all -- --check
cargo test --offline --workspace
cargo clippy --offline --workspace --all-targets -- -D warnings
cargo build --offline --workspace --release
cargo test -p qx-storage --features sqlite
cargo test -p qx-api --features sqlite
cargo clippy -p qx-cli --features sqlite --all-targets -- -D warnings
cargo build --offline -p qx-cli --features sqlite --release
cargo run --offline -p qx-cli -- runtime-check deploy/qianxing.runtime.sqlite.example.json
cargo run --offline -p qx-cli --features sqlite -- scheduler-worker deploy/qianxing.runtime.sqlite.example.json scheduler-smoke --once
cargo run --offline -p qx-cli --features sqlite -- strategy-worker deploy/qianxing.runtime.sqlite.example.json strategy-smoke --once
cargo run --offline -p qx-cli -- scheduler-worker deploy/qianxing.runtime.paper-strategy.example.json scheduler-paper --once
cargo run --offline -p qx-cli -- strategy-worker deploy/qianxing.runtime.paper-strategy.example.json strategy-paper --once
cargo run --offline -p qx-cli -- paper-worker deploy/qianxing.runtime.paper-strategy.example.json paper-execution --once
cargo run --offline -p qx-cli --release -- all
cargo run --offline -p qx-cli --release -- ecosystem
cargo run --offline -p qx-cli --release -- runtime-check deploy/qianxing.runtime.example.json
cargo run --offline -p qx-cli --features sqlite --release -- paper-submit-order deploy/qianxing.runtime.example.json deploy/qianxing.paper-submit-order.example.json
python -m unittest discover -s python/tests -q
python tools/validate_core.py
```
