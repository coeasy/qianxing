# docs/archive —— 历史审计与逐轮收口记录

这里放**已被取代**的审计方案与逐轮收口记录。它们只回答一件事：**当时是什么状态、当时为什么那样判**。
它们不回答"现在应该怎么用"——那看 [`README.md`](../../README.md)、
[`docs/工业化易用性收口指南-V1.md`](../工业化易用性收口指南-V1.md) 与
[`deploy/README.md`](../../deploy/README.md)；现在**还欠着什么**看
[`docs/自研量化框架审计与重构方案-V13.md`](../自研量化框架审计与重构方案-V13.md)。

## 在册文档

| 文档 | 覆盖轮次 | 归档日期 | 被谁取代 | 今天仍然有用的部分 |
|---|---|---|---|---|
| [自研量化框架重构方案 V11](自研量化框架重构方案-V11.md)（3,734 行 / 384,944 字节，§0–§39） | Q0a–Q72 与 R/S/T 三批的逐轮收口 | 2026-09-26 | §1–§9 的 anatomy 由 V12 §0–§9 逐条改判；方案序列现在由 V13 接续 | 每轮的 `file:line` 取证、变异红绿配对写法、当轮门禁日志名——想复核"这条口径当初是怎么被证明的"时按 §号取 |
| [自研量化框架审计与重构方案 V12](自研量化框架审计与重构方案-V12.md)（1,904 行 / 195,766 字节，§0–§23） | V12 审计 + R1–R4 收口 + §15–§23 的四遍连通性与构建/安装面清点 | 2026-09-26 | V13：R1 各条已落地或被改判，改判结论写在 V13 §9 | §19–§23 的构建/安装面事实（解释器交接、`[0/9]`→`QX_PYTHON` 外传、`*.bat` 行尾与 cmd 解析、`[1/9]` 加进本地门禁）仍是今天 `build.bat`/`build.sh` 的口径来源 |

## 读法（三条纪律）

1. **数字都是快照**。归档文档里的门禁条数、用例数、`#[test]` 数、行数都是那一轮工作树实测出来的值，
   不是现状。现状只有一个来源：根目录 [`README.md`](../../README.md) 的「当前状态」，它只写当轮日志抓到的数。
2. **`logs/` 不随仓库分发**。这些文档引用的 `logs/sXX_*.txt` 是执行那一轮的**本机**目录，
   `/logs/` 在 `.gitignore` 里，所以 clone 下来看不到。日志在不在不影响判据本身——判据都在
   `tools/check_architecture.py` 与 `crates/*/tests` 里，可以当场重跑。
3. **归档不改结论**。移入这里时只做三件事：加"归档状态"那一节、将仓库根相对的路径提及同步到新位置、
   把指向它们的链接改成 `docs/archive/...`（`CHANGELOG.md` 里共 56 + 14 处路径 token 已同步）。
   正文结论一字未改，也不追改——历史里那句"当时是 `[3/8]`"就该留成当时那样。

## 为什么留而不删

更早的方案（V1–V10 各代计划、Barter 对齐稿、可视化终态稿、产品化路线图、差距清单、release note）
在本仓库此前的收口轮里已整体删除，需要回溯时按文件名在 git 历史里取。V11/V12 不照同一办法处理，
理由不是它们结论仍正确，而是**引用面**：`CHANGELOG.md` 有 28 行 / 56 处路径 token 链向 V11、
7 行 / 14 处链向 V12，README 的文档地图也把两者当"实现状态与未完成外部边界"的落点。删掉它们会把这些
逐轮记录的出处打断；移到 `docs/archive/` 只打断路径，而同步路径是一次机械替换、可复核。
