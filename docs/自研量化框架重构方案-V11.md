# 自研量化框架架构审计与重构方案 V11

- 审计基线：commit `3beb23a fix(cli): fund multi-leg spot legs so every declared buy clears the cash check`
  （= 远端 `origin/main` tip，本机 `git ls-remote` 实测一致；V10 全部十阶段已收口并推送）
- 审计时间：2026-09-21
- 取证方式：当前工作树 + 当轮命令输出，结论一律带 `file:line`；**未使用任何外部服务或凭据**，
  因此凡"真实交易所往返 / GitHub 作业绿灯"这类结论一律标注本机不可证明
- 规模事实（当轮实测）：23 个 crate、`crates/*/src/**/*.rs` 合计 **79,938 行**、
  源内 `#[test]` **609 个**（498 个在 `src/`、111 个在 42 个 `crates/*/tests/` 文件里）、
  架构不变量 **107 项**、行数预算登记 **37 条**、能力矩阵 **18 个能力块**

## 0. 本轮决策默认值（用户未答，按保守默认推进；任一条被否决则阶段表相应重排）

| # | 疑问 | 本轮默认取值 | 被否决时的影响 |
|---|---|---|---|
| E1 | paper 成交注入费用后，paper 的 P&L / 手续费常数与已 bless 的产物会变 | **允许受控重 bless**：改动前抓基线、改动后逐条给出"旧值 → 新值 + 为什么"，并做反向验证；期望常数不得先改成实测值 | 若要求"数值一字不动"，Q0a 只能降级为"把不一致写进门禁白名单并如实记录"，纸面收益归零 |
| E2 | `backtest --config` 这类被解析后丢弃的旗标 | **删旗标 + 加不变量**（禁止 clap 旗标落到 `x: _` 的丢弃形状），而不是补一条"看起来能配"的实现 | 若要求接线，Q0b 从 0.5 天变成 2 天，且要重跑全部回测常数 |
| E3 | `ExecutionCostRules` 与其 `deploy/qianxing.costs.example.json`（全仓零生产消费者） | **接入 Bar/深度两条链**，作为"费用口径同源"的载体；接不进就整体删除，不留死配置 | 若判为"保留但暂不接"，则必须给它加显式"未接线"标注，否则门禁不放行 |
| E4 | 本轮是否跑真实外部验收 | **不跑**（无凭据），`sandbox_tested` 继续全量 `false`；本轮只补 `ccxt-submit-order` 之类"想跑也跑不了"的缺口 | 若下发凭据，Q6 提到 Q1 之后立刻做，并把结论写回能力矩阵 |
| E5 | Binance 合约无直连（`fapi` / `dapi` 在 `qx-adapter` 零命中） | **改口径不改代码**：文档与能力矩阵统一成"衍生品只经 CCXT"，并让衍生品直连尝试 fail-closed 且文案点名 | 若要求原生合约适配器，需单独立项（签名、funding、positionSide、测试网矩阵），不在本轮 |

## 1. 项目主要功能梳理（当前代码实际提供什么）

- **内核**（`qx-core` 5,459 行）：定点数 `Money/Quantity/Price/BasisPoints`、订单与成交域模型
  （`order.rs:19` 的 `OrderStatus`、`:156` 的 `Fill`）、因果事件队列与确定性 RNG（`rng.rs:1`，xorshift64\*）、
  事件溯源与重放校验、`fenye.rs:1`（**分野**：合约规格 / 市场状态 / 只增不覆盖的符号映射）、
  `retry.rs:18,60`（`Backoff` + `RetryPolicy` 单点）。
- **数据面**：`qx-guanxing`（质量门 `lib.rs:531`、`as_of()` PIT 视图 `lib.rs:440,192`）、
  `qx-datastruct`（列式 `BarFrame` + Arrow C Data Interface）、`qx-data`（目录 / 摄取 / 指纹 / 公司行为 DTO）、
  `qx-provider`（多源注册表与稳定选择）、`qx-factor`（因子版本与 PIT 工件）。
- **回测与仿真**（`qx-xingban` 9,075 行）：Bar 引擎（`backtest.rs` 3,313 行）、L1 Tick、L2 订单簿、A 股 PIT；
  费用 / 成本 / 延迟 / 保证金 / 强平 / 资金费率的建模原语；快速回测多线程复用同一入口。
- **执行与风控**：`qx-execution`（`PortExecutionService` 网关、`spread_group_barrier` 单点、
  执行事实与端口契约在 `application.rs`）、`qx-zhenlu`（`RiskGate` 门面 + OMS + 组合再平衡 + 路由）、
  `qx-risk`（`RuleSet` 与保守默认规则集版本）。
- **运行时与存储**：`qx-runtime`（配置面、`LiveEventPipeline`、监督与健康）、`qx-orchestrator`
  （worker 启动计划 + 子进程生命周期）、`qx-storage`（文件 / SQLite / Postgres 三态 EventLog、
  控制面、命令与任务队列、outbox，`state_envelope.rs` 统一原子写）、`qx-scheduler`。
- **对外接口**：单一 `qx-cli` binary（19,014 行、41 个顶层命令，clap 派生）+ `qx-api`（HTTP/WS/`/metrics`）
  + `qx-control`（权限与审计命令）+ `qx-protocol`（跨语言账户快照）+ Python / C++ 策略与数据面。
- **审计与对账**：`qx-genglu`（绩效、归因、五类对账的 verdict 单点 `reconcile/order.rs:157`）。

## 2. 架构现状与依赖事实

- **分层是"内核 → 领域 → 基础设施 → 编排 → 门面"，但向上边普遍存在**：
  `qx-adapter`(基础设施) → `qx-zhenlu`(编排)、`qx-execution` → `qx-zhenlu` + `qx-risk`、
  `qx-xingban` → `qx-zhenlu`/`qx-risk`/`qx-strategy`/`qx-data`、`qx-api`(门面) → `qx-storage` + `qx-scheduler`
  （跳过整个编排层）。后果：撮合与仿真的可替换性被编排层类型钉死。
- **dev 依赖环两处**：`qx-risk` ⇄ `qx-zhenlu`（已在 `crates/qx-risk/Cargo.toml:13` 说明理由）、
  `qx-execution` ⇄ `qx-runtime`（**未登记说明**）。
- **`qx-cli` 依赖全部 19 个可依赖 crate，且是唯一 `qx-api` 消费者**：门面层没有充当 CLI 的收口边界。
- **体量集中**：最大 crate `qx-cli` 19,014 行（占 src 约 24%）；最大文件 `qx-xingban/src/backtest.rs` 3,313、
  `qx-api/src/lib.rs` 3,137、`qx-storage/src/sqlite.rs` 2,579、`qx-storage/src/lib.rs` 2,405、
  `qx-adapter/src/binance.rs` 2,291、`qx-zhenlu/src/lib.rs` 2,270、`qx-runtime/src/pipeline.rs` 2,237。
  登记集 37 条只降不升（测试目录是已知例外，`qx-core/tests/ledger.rs` 1,031 行不受棘轮约束）。
- **`qx-zhenlu::lib.rs` 是垃圾抽屉**：风控门、`Signal/SignalMerger`、`RateLimiter:266`、`Venue` trait:326、
  `PaperVenue:1200`、spread 组:338-936、策略运行时:1535 同居一文件，与 `qx-execution` / `qx-adapter` 职责重叠。
- **`qx-orchestrator` 与 `qx-runtime` 边界近乎不存在**：前者只做 worker 计划与子进程看护，
  后者在 `lib.rs:32` 再导出监督类型。
- **V10 之后仍残留的兼容层**：`qx-xingban/src/lib.rs:52` 纯 re-export `qx_core` 费用模型、
  `qx-zhenlu/src/lib.rs:91-100` 的 `legacy_gate_context`（标 deprecated）、
  `qx-execution/src/lib.rs:26,49` 的 `RiskContextPort` / `BorrowedVenuePort`（**全仓零消费者，已 grep 证实**）。

## 3. 主体链路走查（逐条贯通判定，带证据）

### A. 行情 → 信号 → 风控 → 提交 → 成交 → 账本 → 持久化

- Binance 现货：代码路径完整——提交 `venue_runtime/binance_submit.rs:53-89` → 网关
  `qx-execution/src/lib.rs:441-545` → 用户流回报 `venue_runtime/binance_stream_worker.rs:76-120` →
  对账 `binance_reconcile.rs:70-217` → EventLog 落盘；`newClientOrderId=qx-{id}` 幂等
  （`qx-adapter/src/binance.rs:1543,1654`）、租约 + fencing token（`binance_submit.rs:277,358`）、
  重启按 EventLog 重建（`binance_submit.rs:76-78`）。**但从未与真实交易所往返过**（§3E）。
- CCXT 通用 venue：`ccxt_execution.rs:28-259` + 断线重连 `:264-436` + 对账 worker，链完整；
  精度 / 沙盒矩阵是已知空白。
- Paper：队列 `paper_worker.rs:73-270`，无市场事实即拒成交（`:169-176`）。
- Binance 合约：**无直连实现**（`qx-adapter/src/binance.rs` 只有 `/api/v3`，`fapi|dapi` 零命中）；
  合约唯一通路是 CCXT 端点带 swap 参数（`ccxt.rs:569-579`），示例配置指向 OKX。
- 恢复语义的真实缺口：`qx-orchestrator/src/lib.rs:290-299` 的 supervise 是"任一子进程退出即杀掉全部"，
  **没有重启策略、没有重启预算、没有退避**；REST 歧义响应只转 `ReconcileRequired`、不自动重投
  （`binance.rs:1434-1457` + 网关硬拦 `execution/src/lib.rs:471-486`，这是正确的保守，但意味着
  "崩溃后远端未知态"完全依赖人工对账）。
- 文件后端 EventLog 与 outbox **不在同一事务**（`pipeline.rs:407-413`：先追加再投 outbox）。

### B. 回测链（可达面比实现面小得多）

- Bar 链 CLI 实际能装配到的组合是**一组硬编码默认**：`NextBarOpenFill` + `MakerTaker{2,5}bp`
  （或 A 股费用）+ `ZeroLatency` + `NoMargin`（除非 market spec 带 `leverage_tiers`）
  （`qx-cli/src/backtests/mod.rs:42-59`，并被 `src/tests/backtest_entries.rs:17` 钉住）。
  （Q0c 后修正，见 §12.1：费率与延迟这两项已改由 `ExecutionCostBinding` 提供，
  可被 `strategy.cost_rules_path` 指向的文件覆盖，"硬编码"只剩 `NextBarOpenFill` / `NoMargin` /
  `DataTier::Bar` 等撮合与档位项 —— 那些仍属 Q1a 的不可达面。）
- **实现存在但 CLI 选不到**（引擎测试之外无生产调用点）：`BestPrice` / `OneTickSlippage` /
  `Probabilistic` / `VolumeSensitive` 撮合（`qx-xingban/src/fill.rs:114-240`）、`StaticLatency`、
  `FixedRateMargin` / `LeverageMargin`、`VirtualTradingConfig` 全部字段——资金费率、利息、交割、
  强平、汇率、抵押品（`backtest.rs:128-160`；`single_strategy.rs:29` 只设了 `ashare_rules`）、
  订单簿执行模型参数（`orderbook.rs:146-151` 的 `with_execution_model` 无调用者）。
  结论：**"撮合模型 × 数据档位"矩阵在代码里是矩阵，在命令面上是一行**。
- 深度链：`backtest book --fill-tier l1|2` 只吃扁平 `fee_bps`（`backtests/depth.rs:76-101`），
  且现货资金充足检查只在 `if let Some(spec)` 分支内（`orderbook_backtest.rs:480-553`）——
  无 spec 的现货深度回测**没有现金门**，而 Bar 链有（`backtest.rs:661-714`）。
- `deploy/qianxing.costs.example.json` + `ExecutionCostRules::load`（`cost_rules.rs:44`）
  零生产消费者、CI 与门禁也不引用该模板 —— 见 §0 E3。（✅ 本轮 Q0c 已关闭，见 §12：
  成本规则文件由 `ExecutionCostBinding` 唯一读取并接进 Bar/多腿/深度/Paper 四类构造点。）

### C. 多腿 / spread

- 屏障与执行在网关单点（`qx-execution/src/lib.rs:322`，五个提交入口强制传组存储），
  实盘侧形状已被 V10 P1b 收口。
- 回测侧的真实薄弱点：四条套利 kind 共用一条实现（`backtests/multi_builtin.rs:20-29`），
  **只有 `pairs_arbitrage` 有端到端用例**（`tests/multi_leg_attribution.rs:47`）；意图被"乐观记账"——
  `context.positions` 直接按 intents 更新而不看是否成交（`multi_builtin.rs:121-136`），
  每腿费用恒为 `MakerTaker{2,5}` 而不随 venue spec 切换（`:175-181`），
  **一腿被拒时另一腿仍保留仓位**（无跨腿否决）。

### D. 数据面与可复现性

- PIT 有真守卫：`backtest.rs:520` 用 `view.as_of(bars[i-1].ts)`、`:556-560` 记 last-visible mark，
  回归用例在 `qx-xingban/tests/no_lookahead.rs:42` 与 `ashare_pit_asof.rs:141,156,194`。
- 但可复现性有三处实质漏洞：(1) `BacktestEngine::run()` 接受任意 `&[Bar]` 并把来源写成 `"backtest"`
  （`backtest.rs:395`），运行时不绑定数据集指纹；(2) `fingerprint_bars` 只覆盖 instrument + ts + 5 个 raw 字段
  （`qx-data/src/fingerprint.rs:36-47`），**不含日历版本、公司行为、schema 来源**；
  (3) 数据集按可变 `data_root` + `{dataset_id}.bars.json` 寻址（`storage.rs:84-93`），
  `upsert_bars` 原地覆盖（`:33-37`），加载时不复校验已登记指纹（`:127-141`），
  `RunManifest` 的交叉校验只在可选 bundle 路径配置时才跑（`data_binding.rs:45-55`）。
- Bar 有四种表示（`qx-guanxing::Bar` 行式裸 `i128`、`qx-data::Bar` 行式且**从不与前者转换**
  （`provider.rs:257-277` 与 guanxing 平行产出）、`qx-datastruct::BarFrame` 列式、引擎即 guanxing 型）；
  `price_scale`/`volume_scale` 只是元数据声明（`guanxing lib.rs:241-242`）无人校验，
  `to_display` 硬编码 `1e9`（`:610`），手写缩放散在 `backtest.rs:403`、`venue.rs:205-212`、
  `qx-factor/src/lib.rs:155-159`，测试里的 volume 约定本身就不一致（`venue.rs:306` 未缩放 vs
  `backtest.rs:2611` 缩放）。
- 质量门两套：`qx-guanxing`（重复时间戳走隔离）与 `qx-data/src/validation.rs:30-65`（同类输入报错），
  后者只在 qx-data 内部使用。
- 零消费者公开 API（已 grep 证实）：`FinancialView`/`DataCatalog`/`VectorScanner`/`ParameterGrid`/
  `NumericExt`（guanxing）、`DataCache`/`validate_timestamps`/`load_bar_batch`/`TradingCalendar`
  （**全仓零实现**，`qx-data/src/calendar.rs:10`）、`FactorMaterializer`。
  真正的交易日历实现是 A 股那套（`ashare.rs:718-831`），另有第三套 `qx-scheduler::TradingCalendar:246`。

### E. 外部验收与能力自述

- `maturity/capabilities.yaml` 18 个能力块的 `sandbox_tested` **全量 `false`**（门禁项守着）；
  `maturity/evidence/` 下唯一一包 outcome = `skipped`、`credentials_present: false`。
- CI 的 `venue-acceptance` 作业在无 secret 时**自我跳过**（`ci.yml:315` 的 `if:`），
  所以 fork 全绿不等于外部通路验证过；`service-backends` 只需 docker 服务不需 secret。
- `to_wire_json` / `from_wire_json` 这对版本化线格式**只在测试里被调用**（定义在
  `qx-protocol/src/lib.rs:476` 与 `:481`，生产走 `to_json`）；`AccountSnapshot::from_json:412`
  现在按 `ACCOUNT_SNAPSHOT_SCHEMA_VERSION` 拒绝其他版本，契约就是 `schemas/account-snapshot-v1.json:9`
  那格 `const 1` —— 而 `GET /schema/account-snapshot-v1` 发出的常量与那份文件是同一段字节
  （`include_str!`，V11 T3）。此前它只核对协议字符串与两处版本号是否自相矛盾，一份按别的版本
  自洽封存的文档会被当成 v1 收下，而对面 Python 的 `load_account_snapshot` 对同一份产物直接抛错。
- Python↔CCXT worker 无协议版本字段（`python/qianxing_ccxt/worker.py:56-87` 出裸 CCXT JSON，
  `qx-cli/src/ccxt_facts.rs:6-79` 不检查）；`qx-plugin` 的注册表只在 `selfcheck.rs:83-133` 被
  两个硬编码 manifest 走一遍，磁盘上没有 manifest 加载路径，真正的动态加载走
  `qx-strategy/src/c_api.rs:655-737` 且不问 `Registry`。
- 另有已提交的二进制产物 `python/qianxing_bridge/_qianxing_native.pyd`。

## 4. 不合理清单（按影响分级）

### P0 —— 会让收益结论失真或让人误判能力

1. **Paper 成交零手续费**（✅ 本轮 Q0a 已关闭，见 §10；下列行号为修复前事实）：
   `PaperVenue::new` 默认 `ZeroFeeModel`（`qx-zhenlu/src/lib.rs:1223`），
   `with_fee_model:1230` 的**唯一调用点在同文件 `mod tests`（起于 1618）内**；而 `:1227` 的注释正写着
   "Paper 的 `Fill.fee` 必须与回测、实盘同一口径"。同一策略在 Bar 回测里付 5bp、在 paper 里付 0，
   paper 的 P&L 与 `paper-e2e` 通过判定因此系统性偏乐观，且现有 3 条 paper 用例**无一断言 fee>0**。
2. **`backtest --config` 被解析后丢弃**（✅ 本轮 Q0b 已关闭，见 §11；下列行号为修复前事实）
   （`cli.rs:323` 的 `config: _`）：帮助与旗标语义宣称可给配置，
   使用者以为换了风控/费用口径，实际什么都没生效。同类形状还有 `book` 与 `builtin` 的 fee 默认不统一
   （`mod.rs:32-35` 的 2/5bp vs `cli.rs:423` 的 `--fee-bps` 默认 5 vs `cli.rs:373` 的 `funding_bps` 默认 0）。
3. **撮合能力"实现了但接不到"**（§3B）：`VirtualTradingConfig` 全部字段、四种替代撮合、
   静态延迟、多档保证金在命令面上不可达（能力面归 Q1a）；同时 `paper` 内核同一性的表述不成立
   （`orderbook.rs:3` 称 Paper 共用簿内核，实际 paper 走 L1 touch `zhenlu/lib.rs:1335-1412`）
   —— ✅ 本轮 Q0d 已把表述改如实并钉成 6 条门禁判据（§13），**行为面未动**：paper 仍走首档 touch，
   改接簿内核是 §9 登记的 Q1d。
4. **`builtin-strategies` 帮助夸大**（✅ 本轮 Q0b 已关闭，见 §11；下列行号为修复前事实）（`cli_help.rs:57-58`）："17 个内置策略可直接用于回测/Paper/策略接入"，
   但 4 个套利 kind 被 `backtest book` 明确拒绝（`depth.rs:30-38`）、只有 4 个被 `multi-builtin` 接受
   （`multi_builtin.rs:21-29`）；`paper-check` 帮助不披露合成报价注入（`cli_help.rs:109`，
   与它同一实现的 `paper-e2e:107` 却披露了）。
5. **多腿回测一腿被拒不收口另一腿**，且意图按"乐观成交"记入仓位（§3C）——多腿归因数字可以是
   现实中拿不到的组合。

### P1 —— 架构与边界

6. **向上依赖边**与**未登记的 dev 环**（§2）：撮合层依赖编排层类型，端口化目标被反向钉死。
7. **对账 verdict 三套同义枚举**：`AdapterReconcileIssue`（`qx-adapter/src/lib.rs:875`）与
   `VenueReconcileIssue`（`qx-zhenlu/src/lib.rs:306`）变体逐字相同，第三种形状
   `ReconcileReportSnapshot`（`qx-api/src/lib.rs:987`）；`VenueOrderSnapshot` 亦在 zhenlu 定义、adapter 消费。
8. **风控决策三套枚举**：`qx-risk/src/lib.rs:48`、`qx-execution/src/application.rs:70`、
   以及 `qx-risk` 内的 `OrderRiskDecision`，`qx-zhenlu` 的 `RiskContext`/`RiskGate` 只是转发门面。
9. **两套数值类型系统并存**：`Money/Quantity/Price` 包装 vs 裸 `i128`
   （`PortfolioState.cash/positions` `zhenlu/src/portfolio/rebalance.rs:12-14`、`RiskSnapshot`
   `qx-risk/src/lib.rs:17-22`、`FillSnapshot` `qx-protocol/src/wire.rs:61` 手写镜像字段）。
10. **名义价值公式三处分支、SCALE 约定各异**：`qx-core/src/fee.rs:16`、`fenye.rs:90`、`trading.rs:109`
    （`qx-xingban/src/backtest.rs:1049` 是仲裁点）；保证金分档两套不互认
    （`trading.rs:31,281` vs `qx-xingban/src/cost.rs:156`）。
11. **`qx-execution` 两条并行提交路径**（`submit_order*` `lib.rs:1229,1251` vs
    `submit_order_via_gateway*` `:1192,1210`）+ 只被测试与 CLI 使用的 `ExecutionGateway` 类型别名（`:369`）。
12. **死端口与兼容层**（§2 末）：`RiskContextPort` / `BorrowedVenuePort` 零消费者、
    `legacy_gate_context` 已 deprecated 未删、`qx-xingban/src/lib.rs:52` 纯 re-export。
13. **数据面"库"与"链"脱节**：`DataCatalog`/`FinancialView`/`TradingCalendar` 等零消费者
    （§3D），日历与公司行为在 qx-data 里是 DTO、在 A 股引擎里才是实现，第二套质量门在 qx-data。

### P2 —— 工程化与诚实性

14. **同义反复用例**：`qx-protocol/src/lib.rs:830` 断言本文件常量含本文件字面量（`:34`）、
    `qx-guanxing/src/lib.rs:644` 断言 `schema_version==1` 对自己默认值、`qx-api/src/lib.rs:2942`
    断言响应含 `"schema_version":1`、`c_api.rs:1012` const-vs-const、
    `qx-storage/tests/p1c_single_write_path_invariant.rs:70-89` 是源码文本 `contains` 而非行为。
15. **覆盖缺口的形状**：genglu 对账 12 条全是单元、无集成；替代撮合与虚拟交易只有引擎单测；
    跨语言动态 `.dll/.so` 加载与 Ed25519 签名 beyond 单测；`tools/event_consumer_example.py` 只被一份文档引用。
16. **`qx-cli` 仍有 999 行的 `config_commands.rs`**（init/doctor/run 派发/report/status 混装）
    与 540 行单一 `match` 的 `run()`（`cli.rs:133-674`）；CLI 深取他 crate 内部
    （`qx_zhenlu::portfolio::{...}` 且写死 `max_turnover_bps: 10_000`，`strategy_contract.rs:572-588`；
    `SqliteEventLogStore::new` 直连 `runtime_wiring.rs:219`；`StorageError::LeaseHeld` 匹配在
    `binance_submit.rs:279`）。
17. **平台差异仍靠文档约定**（V10 §9 明确未排期，本轮维持）；`QX_PYTHON` 未设时 2 条用例必红是本机事实。
18. **随仓库发布的 Bar 回测示例在结构上不可能成交**（Q0c 轮实测并定位，两个独立原因）：
    - 加密示例：`qianxing.bar-frame.example.json` 与 `.okx.example.json` 各只有 **5 根 Bar**，
      而 `builtin-strategy` / `builtin-strategy.okx` 示例声明的是 `fast 5 / slow 20`
      （macd / ema_cross）—— 慢窗永远填不满，策略一次都不发信号。
    - A 股示例：`qianxing.ashare.bar-frame.example.json` 的时间戳是 1000…6000ms 的合成值，
      而 `qianxing.ashare.calendar.example.json` 声明交易日为 2024-06-03/04；`is_trading()`
      （`qx-xingban/src/ashare/trading.rs:9-22`）据此判定每根 bar 都"不在可交易日"，
      于是 ema_cross(2/3, threshold 0) 明明发了信号却笔笔被拒。这不是配置疏漏而是夹具自相矛盾：
      日历最早可表达的交易日是 1970-01-02（`ashare/timecal.rs:144-148` 会减掉 8 小时偏移，
      1970-01-01 直接判"日期必须不早于 Unix epoch"），所以 ts≈0 的合成帧永远配不上任何合法日历。
    - **为什么这算问题而不只是"示例不好看"**：`fills: 0 / turnover_raw: 0 / return_bps: 0`
      让 `strategy backtest`、`backtest builtin` 两条链的 e2e 断言全部走在"没有任何成交"的路径上，
      撮合、费用、账本任一环节坏了这些用例照样绿；而**拒单原因不落产物**
      （`append_rejection` 只写进内存 EventLog，`backtest.rs:1901`，摘要与 fills.csv 都没有它），
      使用者看到 `fills=0` 无从区分"没信号"与"全被拒"。跨语言示例另有第三种空转：
      `python/examples/backtest_momentum.py` 的 `target_qty: 1` 是 raw 口径（1e-9 单位），
      名义额 ≈1e-7 USDT，5bp 费用在整数记账里归零。
      归口 Q1a（示例必须真成交一次以上）+ Q0e/Q1b（产物落拒单事实）。
      **Q0e 轮的进度**：多腿链（`backtest multi-builtin`）已经落了拒单与"计划 vs 成交"差距的
      产物事实（每腿 `unfilled_planned_qty_raw` / `rejected_orders` / `rejection_reasons` 与
      `pending_reconcile`，§14.1），单腿的 `strategy backtest` / `backtest builtin` 两条链仍只有
      内存 EventLog 里的 `append_rejection`，`fills=0` 依旧无法与"全被拒"区分 —— 那两段的接线
      随 Q1a 的示例修复一起做（示例必须真成交，成交不了就必须看得见为什么）。

## 5. 核心功能是否全部实现 / 主体链路是否全部贯通

| 能力 | 实现 | 被测 | CLI 可达 | 真实外部验证 |
|---|---|---|---|---|
| 内核 / 账本 / 规则集风控 | 是 | 是 | 间接 | 不适用 |
| 数据帧 / 数据集指纹 | 是 | 部分 | 是 | 不适用 |
| Bar 回测（默认口径） | 是 | 是 | 是 | 不适用 |
| 替代撮合 / 资金费率 / 强平 / 多档保证金 | 是 | 仅引擎单测 | **否** | 不适用 |
| L1/L2 深度回测 | 是（队列位置与冲击为简化模型） | 是 | 是（仅扁平 fee_bps） | 不适用 |
| 多腿归因 | 是 | **只有 pairs** | 是 | 否 |
| 跨语言策略（Python / C++） | 是 | 是（cpp 在 CI 编译并 smoke） | 是 | macOS wheel 本机不可证明 |
| Paper 全链 | 是（**撮合是首档 touch，不是回测的簿内核**，§13） | 是 | 是 | 注入合成报价；费用已与回测同源（§10）且口径来自成本文件（§12） |
| 实盘 Binance 现货 | 是 | 仅进程内 | 是 | **从未** |
| 实盘 Binance 合约（直连） | **无实现** | — | — | — |
| 实盘 CCXT | 是 | 进程内 + 契约 | 是 | **从未** |
| SQLite / Postgres / NATS | 是 | 契约用例存在 | 是（feature 门） | CI 绿灯本机不可证明 |
| 崩溃恢复 / 远端未知态对账 | 部分（**无重启策略**，文件后端 outbox 非原子） | 进程内 | 部分 | **从未** |

**判定**：

- **主体链路（行情 → 信号 → 风控 → 提交 → 成交 → 账本 → 持久化 → 对账）在现货 / paper / CCXT 三条上
  结构贯通**，并有租约、幂等、EventLog 重建与审计链支撑。
- **"核心功能全部实现"要打折**：撮合与虚拟交易的高保真能力只存在于引擎，命令面接不到（§3B、§4.3），
  因此实际"可用功能集"远小于"已实现功能集"；Binance 合约无直连适配器。
- **示例配置在空转**（Q0c 轮实测并定位根因，见 §4.18）：随仓库发布的三条 Bar 策略回测示例
  产物全部 `fills: 0 / turnover_raw: 0`，另一条只有 1 笔且名义额小到费用归零。
  链路"可达"不等于链路"跑得出结果"，e2e 断言目前走在零成交路径上。
- **两处正确性缺陷会直接污染结论**：~~paper 零手续费（§4.1）~~ 已由 Q0a 关闭、成本配置悬空
  （§3B 末条）已由 Q0c 关闭（§12）；**仍未处理的是多腿乐观记账 / 无跨腿否决（§4.5）**。
- **可复现性不足以支撑"研究结论"**：运行时不绑定数据集指纹、指纹不覆盖日历与公司行为、
  数据根目录可变（§3D）。
- 结论口径：**链路贯通、核心有缺口、数值面两处偏乐观、外部未证**。

## 6. 重构方案 V11（按依赖排序，每阶段可独立收口）

| 阶段 | 目标 | 主要改动 | 验收证据（含反向验证） |
|---|---|---|---|
| **Q0a Paper 费用同源** ✅ 已收口（§10） | 消灭"paper 成交不付费" | `PaperVenue` 的 `fee_model` 改为**无默认**构造参数（`new` 必须收 `Box<dyn FeeModel>`），编译期强制 29 处调用点回答它；paper 三处站点从 runtime 配置取 maker/taker bp，与 Bar 链同源（`backtests/mod.rs:32-35`） | 新用例：paper 成交 `Fill.fee.raw() > 0` 且等于按 bp 推导值；反向验证：把注入退回 `ZeroFeeModel` → 该用例必须红；旧 / 新 paper 常数成对写进收口记录（E1） |
| **Q0b 旗标诚实性** ✅ 已收口（§11） | 旗标不再被吞 | `Command::Backtest` 的 `config: _` 按 E2 处理；统一 `fee-bps` / `funding-bps` 默认口径到装配单点；`builtin-strategies` 与 `paper-check` 的帮助文本按真实可达面改写（合成输入必须显式写明） | 新不变量：`cli.rs` 分支内不得出现 `ident: _` 丢弃形状（按 clap 字段清单逐个核对，命令面 41 项）；用例断言 `builtin-strategies` 印出的策略集合与"被某个回测入口接受"的集合做差为空 |
| **Q0c 死配置面收口** ✅ 已收口（§12） | 配置不再悬空 | 按 E3：`ExecutionCostRules::load` 接进 Bar 与深度两条装配（费用与延迟都从它来），`deploy/qianxing.costs.example.json` 进 CI 模板校验；接不进则删净（含 `DEFAULT_*_BP` 双处定义） | 用例：改配置里的 `taker_bp` → 回测手续费产物随之变化；反向验证：断开会话里的接线，该用例红；`config validate` 覆盖该模板（当前 18 份模板全走一遍） |
| **Q0d 内核表述与 paper 撮合一致性** ✅ 已收口（§13） | 不再声称未实现的"同一内核" | `orderbook.rs:3` 与相关文档改成如实表述；决策：paper 是否改走 `OrderBook`/`Tick` 内核（本轮默认**只改表述 + 建 Q1d 排期**，不改行为，避免与 Q0a 叠成两个变量） | 门禁：任何"共用内核 / 同一口径"字样所在文件必须能引用到真实共享符号；Q1d 若落地则做 paper vs book 成对数字 |
| **Q0e 多腿成交诚实化** ✅ 已收口（§14） | 归因不再乐观记账 | 意图产生的仓位改为**由实际成交回填**（成组只看 `filled_qty_raw`）；一腿被拒时对另一腿执行显式动作（本轮选**标记 `PendingReconcile`**，不做自动收口）；每腿费用按各自 venue spec —— **未落地**，`TradingInstrumentSpec` 无 maker/taker 字段，改为在产物 assumptions 里显式声明"两腿共用一份成本绑定"（§14.2 第 1 条）；basis / cross-venue / spot-futures 各补一条端到端用例 | 四条 kind 各一条用例（原仅 `pairs_arbitrage`）；反向验证：删掉跨腿否决 → 新增用例红（R2）；删掉成交回填 → 仓位与成交不一致用例红（R1）；两条都实测红且还原后绿（§14.3） |
| **Q1a 高保真能力接线** 🟡 第一批已收口（§15：深度链参数 + 现金门 + 信号层两缺陷 + 三份夹具）；第二批已收口撮合口径那一半（§16：`strategy.fill_model` 经单点解析进三条 Bar 链、档位不可达者 fail-closed）；`--virtual-trading` 仍未做，前置条件见 §15.4 第 1、2 条与 §16.4 第 1 条 | 已实现的引擎能力可达 | 给 `backtest` / `book` 增加最小装配面（`--fill-model`、`--latency`、`--virtual-trading <json>` 或直接吃 §4 的 runtime 段），把 `VolumeSensitive` / `StaticLatency` / `TieredMargin` / funding / 强平暴露出来；深度链的现金门与 Bar 同口径（补 `orderbook_backtest.rs:480-553` 的 specless 分支）；**顺带修 §4.18 的三份示例夹具**（帧长对上窗口、A 股时间戳对上日历、跨语言示例给出可用数量） | 每种新可达组合一条"结果确实变化"的用例（同输入换 fill 模型 → 手续费 / 成交额 / 终值哈希三项至少一项变）；`--snapshot` 前的基线全绿；示例产物 `fills > 0` 且 `turnover_raw` 大到费用非零，否则该示例的 e2e 断言不算覆盖成交路径 |
| **Q1b 可复现性绑定** | 一次运行可被重新执行 | `BacktestEngine::run()` 强制携带数据集指纹（拒绝裸 `&[Bar]`，或要求 `input_fingerprint` 显式标注 synthetic）；`fingerprint_bars` 扩覆盖日历版本 + 公司行为 + schema 来源；`data_root` 可变性收为内容寻址（bundle 内含摘要），加载时复核指纹 | 用例：篡改数据集后指纹不匹配即拒跑；用例：`upsert_bars` 覆盖后旧 run 的 manifest 可检出不一致；反向验证：关掉复核 → 用例红 |
| **Q2a 概念单点（第二轮）** | §4.7–§4.10 收口 | 对账 issue 三套 → `qx-genglu` 一套 + 转换；风控决策三套 → `qx-risk` 一套；`FillSnapshot` 手写镜像 → 由域型推导；`MarginTier` 与 `MarginMode` 归一；`notional` 三处分歧显式命名（现货 / 乘数 / 反向合约）并单点导出 | 架构不变量按符号定义点计数（每类只允许一处定义）；对每类注入"第二处定义"证明门禁咬得住 |
| **Q2b 分层与边界** | 消除向上边 | `Venue` trait / `VenueOrderSnapshot` / paper 撮合从 `qx-zhenlu` 下沉或外移，使 `qx-adapter`、`qx-xingban` 不再依赖编排层；`qx-execution` ⇄ `qx-runtime` 环按"测试夹具下沉"解开并登记；`qx-execution` 两条提交路径合一 | `cargo tree` 方向审计 + 新不变量："基础设施 crate 的依赖不得含 zhenlu/runtime/cli"；拆分等价性用符号 token 多重集比对（V9/V10 既有做法） |
| **Q2c `qx-zhenlu` 拆分** | 结束垃圾抽屉 | 2,270 行按职责拆目录模块：risk 门面 / signal / spread 组 / rate limit / strategy runtime（`PaperVenue` 归 Q2b 处置） | 逐文件行数只降不升；登记集 37 → ≤35；挂载检查 `module_mount_check` 过 |
| **Q3a 数据面收口** | 库与链对齐 | §4.13 的零消费者 API 逐个"接线或删除"（`DataCatalog` / `FinancialView` / `VectorScanner` / `DataCache` / `batch` / `calendar` 接口）；第二套质量门并入 guanxing 口径；Bar 四种表示收敛为"行式 + 列式"两种且转换单点；`price_scale` 声明有人校验、禁手写 `* SCALE` | 新不变量：`pub` 项零消费者即报（允许显式白名单：`qx-python`、`qx-c-api` 等跨语言入口）；缩放校验用例（喂错 scale 的数据即拒） |
| **Q3b 编排与恢复** | 崩溃语义有策略 | supervise 增加重启策略（预算 + 退避，走 `qx-core::retry`）并把"为何不重启"写进事实；`venue_id contains "binance"` 字符串路由换成登记表；文件后端 EventLog→outbox 的原子性要么做成同事务要么显式声明补偿路径 | 用例：子进程异常退出时按预算重启、预算耗尽后进入"降级 + 需人工"状态并留审计；契约用例覆盖文件后端补偿 |
| **Q4 测试诚实性** | 断言真的钉行为 | §4.14 的同义反复用例逐条改写为行为断言，或改名归入"结构门禁"类别并由 `check_architecture.py` 承载；genglu 对账补集成用例；paper 补 fee>0 断言（Q0a 的顺带产物） | 每条改写都要先证明"摘掉被测行为会变红"；门禁脚本按既有约定成对记红/绿 |
| **Q5 外部验收（需凭据，E4 默认不做）** | 把"未证"变"已证" | 沿用 V10 §6 P3 三段程序；补 `ccxt-submit-order` 入口以便第二交易所第三段可跑；`sandbox_tested` 只能由真实结果包翻转 | 带时间戳结果包写回 `capabilities.yaml`；无凭据则维持 `false` 并如实记录 |

**执行顺序与理由**：Q0* 是"数字与能力自述会不会骗人"，必须先于任何结构性重构（否则 Q2/Q3 的验收建立在错误基准上）；
Q1* 扩大可达面与可复现性，会改变产物集合，放在 Q0 之后一次做完再重 bless；
Q2* / Q3* 是纯结构工作，可并行但**必须与门禁整跑串行**（V10 实测：并发拆库会让变异段的红全部失真）；
Q4 贯穿每一轮（新增断言即需反向验证）；Q5 独立且受凭据约束。

## 7. 门禁与验收设计（本轮新增项）

1. 沿用 V10 §7 五条（一轮一脚本一新日志、`--snapshot` 在变异段前、反向验证成对红/绿、数字纪律、
   `sandbox_tested` 只由结果包翻转、棘轮例外显式声明）。
2. 新增不变量（每项都要用注入违规证明会红）：
   - clap 旗标不得在 `cli.rs` 分支中被 `_` 丢弃；
   - "共用内核 / 同一口径"类表述必须引用真实共享符号（✅ Q0d 落地：费用与风控同源由
     `paper_fee_same_source_check()` 承载，撮合内核由 `kernel_claim_check()` 的 6 条判据承载，见 §13.1）；
   - `pub` 项零消费者须登记白名单（跨语言入口与构建目标除外）；
   - 基础设施层 crate 不得依赖 `qx-zhenlu` / `qx-runtime` / `qx-cli`；
   - 同一概念（对账 issue、风控决策、margin 分档、notional）定义点唯一；
   - paper 成交的 `Fill.fee` 不得恒为 0（Q0a 之后）；
   - 成本规则文件的读者全仓唯一，且 Bar 装配的费用与延迟成对取自同一份绑定、深度档缺省费率
     取自该绑定（Q0c 之后，§12.3）；
   - 多腿归因只承认实际成交：成组配对不得引用信号计划量；一腿成交、对手腿落空时必须登记
     `PendingReconcile`；单腿定资算不出来必须报错（可表示上限只允许出现在诊断文案里）
     （✅ Q0e 落地：`multi_leg_honesty_check()` 的 7 条判据，见 §14.1）；
   - 基线跑必须覆盖 `crates/*/tests/`，`--bin qx-cli` 型基线不得被写成"全量"。
3. 每条被改动的 blessed 常数必须在收口记录里成对给出"旧值 → 新值 + 因果"，禁止先改期望再找理由。
4. 门禁脚本自身的判据也要能被证伪（Q0c 轮暴露的一条）：变异段的"残留扫描"目前只 grep 替换文本的
   **首行**，当替换首行恰好等于锚点原行时会误报 `MUT_RESIDUE=yes`（M2 即如此，见 §12.3）。
   待改为整段比对；在此之前，凡 residue 报 yes 必须同时给出 `RESTORE[*]=identical` 与
   还原后的架构/用例复跑结论，两者共同才算闭合。

## 8. 需要用户输入的事项

1. **E1 是否批准受控重 bless**（paper 费用接入后，paper 侧产物与常数会变）。→ Q0a 按默认"批准 + 成对记账"执行，
   五条 paper 分录断言的旧 → 新已写进 `CHANGELOG.md`（§10.3）。
2. **E2 的 `--config` 取舍**：删旗标（默认）还是接线。→ Q0b 按默认**删旗标**执行（§11.1）；这是使用者可见的
   破坏性 CLI 变更，已记进 `CHANGELOG.md` 的 Changed 一节。
3. **E3 的死配置面**：接入（默认）还是删除。→ Q0c 按默认**接入**落地（§12.1），未删配置；
   "接不进则删净"那一支只在深度档的延迟上部分适用（该链路无处落地，改为显式拒绝，见 §12.2 第 3 条）。
4. **E4 / Q5 凭据**：是否下发 Binance testnet 与第二交易所沙盒凭据；若下发，`ccxt-submit-order` 的补口优先级需前移。
5. **E5 合约方向**：维持"衍生品只走 CCXT"的口径（默认），还是立项做 Binance 合约直连。
6. **本轮体量**：Q0a–Q0e 五项（正确性与诚实性）是默认最小交付；Q1 起按序推进，是否需要我一次做完全部？

## 9. 明确未排期（避免"清单即计划"）

- **`qx-guanxing::Bar` 裸 `i128` → `Money` 全量替换**：会触及全部回测结果哈希，需要独立的"数值口径 +
  基线重 bless"轮；本轮只做 Q3a 里的"scale 声明有校验 + 禁手写缩放"。
- **状态机按主体分置**（订单 / 组 / 命令 / 任务四套）：V10 §9 的结论维持——跨层判定已单点化，合并会压平事实。
- **paper 改走订单簿内核**（Q1d）：只登记，不在本轮与 Q0a 同期做，避免两个变量叠加。
- **平台差异系统排查**：延续 V10 §9。
- **`qx-plugin` 的运行时装配**：当前它只是清单注册库（§3E），要接成真实加载路径是独立产品决策。

## 10. Q0a 收口记录（2026-09-21，日志 `/tmp/qx_q0a_gate_084708.log`）

### 10.1 落地内容

§4 P0 第 1 项（Paper 成交零手续费）已关闭，做法是**编译期强制**而非加审计：

- `PaperVenue::new(id, fee_model: Box<dyn FeeModel + Send>)` —— 零费默认值与 `with_fee_model`
  一起删除，"默认不付费"这个形状在类型面上不存在；不计费的用例显式写 `Box::new(ZeroFeeModel)`。
- 费用模型沿 `execute_paper_submit_effect` 的显式形参上溯到调用方（放在 `market_quote` 之后、
  `spread_store` 之前，保持 V10 P1b 的"spread_store 末位"约定）。
- qx-cli 侧新增唯一构造点 `runtime_wiring::execution_fee_model()`；4 处生产点全部经它：
  `venue_runtime/paper_submit.rs:115`、`venue_runtime/paper_worker.rs:188`（两处提交编排）、
  `spread.rs:293`、`ecosystem_smoke.rs:368`（两处直接构造），Bar 回测装配 `backtests/mod.rs:32`
  的 `fee: execution_fee_model(),` 与它们同源。
- 默认费率常数的定义点收进内核：`qx_core::fee::{DEFAULT_MAKER_BP, DEFAULT_TAKER_BP}` +
  `MakerTakerFeeModel::default_maker_taker()`；`qx_xingban::cost_rules` 改为再导出（原第二处定义删除）。

全量调用点为 12 处 `PaperVenue::new` + 12 处 `execute_paper_submit_effect`（其中 3 处生产提交/构造
如上门列，`qx-execution/src/lib.rs` 内 1 处 gateway 构造，其余 20 处是用例现场）。

### 10.2 与 §6 计划的偏离（如实记录）

计划写的是"paper 三处站点**从 runtime 配置**取 maker/taker bp"。实际本轮只做到**同源**：
四处生产点与 Bar 装配取到同一个 `execution_fee_model()`，而它返回的仍是内核常数。
"从配置读"必须等 `ExecutionCostRules` 真的接进装配（§4 P0 第 2 项与 Q0c），否则又是一次
"宣称可达而实现接不到"。因此 Q0c 的改动面已经被压缩成**只有 `execution_fee_model()` 一个函数体**要换。

### 10.3 验收数字（本轮日志实测）

- 前置：`CARGO_CHECK_EXIT=0` / `FMT_EXIT=0` / `CLIPPY_DENY_EXIT=0`、`CLIPPY_WARNING_LINES=0`。
- 基线与收口复跑同为 51 个测试目标、`602 通过 / 0 失败`（覆盖 `crates/*/tests/`），
  证明没有靠删用例换绿；用例数从 602 起算已含本轮新增的 paper 费用行为用例。
- 架构不变量 107 → 114（`paper_fee_same_source_check()` 七项，见 CHANGELOG 同轮记录）。
- 行数棘轮唯一增长项：`crates/qx-execution/src/lib.rs` 1494 → 1496（+2 行形参与其文档），
  同轮 `crates/qx-zhenlu/src/lib.rs` 2270 → 2269。例外已按 §7 第 1 条显式申报。
- 反向验证七条全部成对：M1（注入退回零费）、M5（Ledger 不写 `Fee` 分录）为行为面
  `EXIT=101 → 还原后 0`；M2（Bar 装配退回内联字面量）、M3（生产构造点退回零费，两项同红）、
  M4（重定义常数，两项同红）、M6（删费用例证据）、M7（重复定义 `execution_fee_model`）
  为静态面 `ARCH_MUT_*_EXIT=1 → 还原后 0`。七个 `RESTORE[*]=identical`、`MUT_RESIDUE=no`。
- E1 的受控重 bless：5 条"Ledger 分录条数"断言各 +1 条 `Fee` 分录，旧 → 新成对写在
  `CHANGELOG.md` 的 V11 Q0a 一节。

## 11. Q0b 收口记录（2026-09-21，日志 `/tmp/qx_q0b_gate_091927.log`）

### 11.1 落地内容

关闭 §4 P0 第 2 项（旗标被吞）与第 4 项（能力自述夸大），做法同样是**让骗人的形状不存在**，而不是加警告：

- **删旗标（E2 默认）**：`Command::Backtest` 的 `config: Option<PathBuf>` 声明与分派处的 `config: _`
  一起删除。`qx-cli backtest --config <path>` 现在是 clap 未知参数 → 用法错误退出 2 且 stderr 点名
  `--config`；真正吃配置的四个子入口保留各自 `--config`（V10 P0b 已让它们确实读取）。
  这是使用者可见的**破坏性 CLI 变更**，按 §8 第 2 条的取舍执行并写进 `CHANGELOG.md` 的 Changed 一节。
- **缺省费率指向内核常数**：`cli.rs` 深度档改为 `fee_bps.unwrap_or(qx_core::DEFAULT_TAKER_BP)`，
  与 Q0a 收进内核的 maker/taker 常数同源；`book` 的帮助文本同步写明该来源与当前值。
- **回测准入分区单点**：`cli_help::MULTI_LEG_KINDS`（4 个套利 kind）+ `backtest_entry_of()` 是
  "哪些 kind 需要两条对齐 BarFrame、能被哪个入口接受"的唯一事实来源。消费方为
  `backtests/depth.rs:30`（单标的入口拒绝多腿）、`backtests/multi_builtin.rs:21`（多腿入口只接受这 4 个）、
  `cli.rs:288` 与 `:426` 共用的 `print_builtin_strategies()`（`strategy list` / `builtin-strategies`
  的第三列）、以及帮助里的 13/4 计数 —— 此前是两处各写 4 个 kind 的字面量判断加一句笼统宣称。
- **帮助如实化**：`builtin-strategies` 从"17 个内置策略可直接用于回测/Paper/策略接入"改为写明
  13 单标的 / 4 套利与各自入口；`paper-check` 补上"与 `paper-e2e` 一样注入一条固定合成 L1 报价
  （99/100），不接真实 feed"。

### 11.2 与 §6 计划的偏离（如实记录）

1. §6 的 Q0b 验收写"按 clap 字段清单逐个核对，命令面 41 项"。实际落地的判据是**旗标面**：
   `cli.rs` 分派正文不得出现任何 `ident: _` 丢弃绑定，且 `cli_args.rs` 声明的 **12 个长旗标字段**
   逐个必须在 `cli.rs` 被点名（数字由检查自己从源码算出，写进 PASS 文案）。"命令面多少项"这件事
   属 V10 P0c 的 `cli_help_surface_check()`（help 印出的入口 ≡ clap 命令表 ≡ `cli.rs` 显式派发分支，
   本轮该项仍绿），Q0b 不重复计数、也不把 41 当作实测值搬运。
2. §6 还写"统一 `fee-bps` / `funding-bps` 默认口径到装配单点"。**`funding-bps` 的缺省 0 未动**：
   内核里没有也不该有"默认资金费率"这个常数（资金费率是交易所给的实际值，缺省"不计资金费"是使用语义
   而非口径分叉），且多腿产物已把这个缺省打进事实里 —— `backtests/multi_builtin.rs:283` 在
   `funding_bps == 0` 时打印 `funding_model=disabled`，否则打印 `8h-pro-rata-by-holding-time`。
   因此该项记为"判定为不做"，而不是"已完成"。

### 11.3 验收数字（本轮日志实测）

- 前置门槛：`CARGO_CHECK_EXIT=0`、`FMT_EXIT=0`、`CLIPPY_DENY_EXIT=0` 且 `CLIPPY_WARNING_LINES=0`。
- 本轮基线是"Q0b 改动已落地、变异前"的状态：51 个测试目标、`604 通过 / 0 失败`（覆盖 `crates/*/tests/`）；
  与 Q0a 轮基线 `602` 的差 = 本轮新增 2 条行为用例。`qx-cli` 单 binary 用例数 61 → 63。
- 架构不变量 **114 → 117**：`cli_flag_honesty_check()` 两项 + `paper_fee_same_source_check()` 追加一项
  （深度档缺省费率引用内核常数）。快照前后各跑一次均 `EXIT=0`。
- 行数棘轮：`--snapshot` 后唯一差异是 `crates/qx-cli/src/cli.rs` 登记预算 **674 → 668（下降）**，
  本轮**无增长项、无需例外申报**（Q0a 申报的 `qx-execution` +2 与 `qx-zhenlu` −1 仍在同一份未提交变更里）。
  涉及文件实测行数：cli.rs 668 / cli_args.rs 475 / cli_help.rs 167 / tests/cli_surface.rs 368 /
  tests/backtest_entries.rs 417 / tests/mod.rs 206（用例文件全部在 500 行阈值之下）。
- 反向验证六组全部成对（N1/N2/N3/N5 静态面、N4/N5/N6 行为面）：
  N1 写回 `config: _,` → `ARCH_MUT_N1_EXIT=1`（丢弃形状 `['config']`，另因多一行触发行数棘轮）→ 还原 0；
  N2 新增没人读的 `--experimental-never-read` → `ARCH_MUT_N2_EXIT=1`（"13 个长旗标全部被读到"）→ 还原 0；
  N3 缺省费率退回 `unwrap_or(5)` → `ARCH_MUT_N3_EXIT=1` → 还原 0；
  N4 分区里把 `PairsArbitrage` 换成 `VolatilityBreakout` → `TEST_N4_RED_EXIT=101`
  （逐 kind 用例在 `backtest_entries.rs:348` FAILED）→ `TEST_N4_GREEN_EXIT=0`；
  N5 两个文件同时复活"父命令声明 + 分派丢弃"→ `ARCH_MUT_N5_EXIT=1` **且**
  `TEST_N5_RED_EXIT=101`（子进程用例在 `cli_surface.rs:351` FAILED）→ 两项还原后均 0，
  并用字段总数守卫确认 `cli_args.rs` 的 `config: Option<PathBuf>,` 回到 4 处（四个子入口各自合法声明）；
  N6 帮助退回旧的笼统宣称 → `TEST_N6_RED_EXIT=101`（帮助口径断言在 `backtest_entries.rs:395` FAILED）
  → 还原 0。七个 `RESTORE[*]=identical`、`MUTATION_STILL_PRESENT[*]=no`、`MUT_RESIDUE=no`。
- 收口复跑：`ARCH_FINAL_EXIT=0`（117 项）、`FINAL_EXIT=0`、51 目标 / `604 通过 / 0 失败`、
  `FMT_FINAL_EXIT=0` —— 与基线逐项一致。
- 日志之后追加的一处改动是 `cli_help.rs` 内 `MULTI_LEG_KINDS` 文档注释的文件指向
  （`cli_surface.rs` → `backtest_entries.rs`），无执行语义；增量复跑记在
  `/tmp/qx_q0b_postcomment_092425.log`：`FMT_EXIT=0`、117 项通过、51 目标 / 604 通过 / 0 失败。

### 11.4 交给 Q0c 的事实

`--config` 删除后，"配置面悬空"的问题没有因此解决：`deploy/qianxing.costs.example.json` 与
`ExecutionCostRules::load` 依旧没有生产消费者。Q0c 的改动面已被 Q0a 压缩为**唯一函数体**
`runtime_wiring::execution_fee_model()`（Bar 装配与四处 Paper 构造点共用），
本轮另把这颗缺省值在深度档的对应点（`DEFAULT_TAKER_BP`）钉进了门禁，接线时两侧同时受检。

## 12. Q0c 收口记录（2026-09-21，日志 `/tmp/qx_q0c_gate_194740.log`）

### 12.1 落地内容

§3B 末条（`ExecutionCostRules` 与它的示例模板零生产消费者）关闭。做法与 Q0a/Q0b 一致：
**让"配了却不生效"这个形状不存在**，而不是给它补一句警告。

- **唯一读者**：`ExecutionCostBinding`（`crates/qx-cli/src/runtime_wiring.rs:156`）是成本规则文件
  在全仓唯一的读取点（`execution_cost_binding_from_config` 在 `:200` 调 `ExecutionCostRules::load`）。
  它成对提供 `fee_model()` 与 `latency_model()`，消费方是 Bar 两条链
  （`backtests/single_strategy.rs:94` 策略链、`:278` 内置策略链）、多腿链
  （`backtests/multi_builtin.rs:155`，两条腿共用同一份绑定，否则净成本差没有可比性）、
  深度档（`backtests/depth.rs:69`）与三处 Paper 构造点
  （`venue_runtime/paper_worker.rs:48/191/244`、`paper_submit.rs:121`）。
- **配置面**：`strategy.cost_rules_path: Option<String>`
  （`qx-runtime/src/runtime_config/strategy_schema.rs:139`）带
  `#[serde(default, skip_serializing_if = "Option::is_none")]` —— 没配置时连键都不出现，
  所以已 bless 的 `config_fingerprint` 与全部 RunManifest 逐字节不变，本轮零重 bless。
  相对路径按运行时配置文件所在目录解析（`runtime_wiring.rs:194-199` 是该字段唯一解析点，
  策略链的绝对化预处理刻意不含它，避免同一字段被两处按不同基准落地）。
- **产物可归因**：摘要写 `execution_costs.source`（`backtests/artifacts.rs:81`），
  `cost-rules-file:<路径>` / `runtime-config-default` / `builtin-default` 三态可分；
  A 股规则链自带佣金模型时写成 `ashare-rules:<path>+cost-rules-file:<path>`
  （`backtests/single_strategy.rs:105-118`），费率与延迟两个来源各自记账，不把延迟算到 A 股规则头上。
- **深度档三层优先级**：`--fee-bps` > 成本文件 `taker_bp` > 内核默认
  （`fee_bps.unwrap_or(costs.rules.taker_bp)`）。深度内核只有单一吃单费率，成本规则里的延迟与
  maker 费率在这条链上没有落点，因此**延迟非零即报错并点名来源文件**（`backtests/depth.rs:69` 之后），
  不接受"配置写着 2ms、产物是零延迟"。
- **校验与装配同一份结论**：`config validate` / `runtime-check` 走 `cost_rules_problem()`
  （`runtime_check.rs:306-312`），与装配调用同一个 `ExecutionCostRules::load`；
  缺文件、坏 JSON、bp 越界一律带解析后的路径失败。
- **上游 `77eb160` 一线（7 个提交）在本轮断点合并**，合并提交 `ef461b4`。口径沿用 V10 先例
  （`59f62b9`）：我方结构胜出、对方语义修复逐 hunk 移植。保留 `qx-cli` 目录模块架构、
  `ExecutionCostBinding`、测试主题文件；移植共享成交归约 seam（`qx-core::fill_apply`）、
  账本结算币种与账户事件日志身份三处修复、`fill_tag` 幂等键补 fee 与 venue_order_id，
  以及把 `QX_GIT_COMMIT` 烧进 RunManifest 的 `build.rs`（后果见 §12.4 第 1 条）。

### 12.2 与 §6 计划的偏离（如实记录）

1. §6 的 Q0c 写"接不进则删净（含 `DEFAULT_*_BP` 双处定义）"。实际按 **E3 接线**落地，没有删配置；
   "双处定义"这半句单独解决：默认费率常数的定义点唯一在 `qx-core/src/fee.rs`，
   `qx-xingban/src/cost_rules.rs` 改为再导出，门禁按 `pub const` 形状区分定义与再导出（注入第二处定义即红）。
2. §6 写"`deploy/qianxing.costs.example.json` 进 CI 模板校验"与"`config validate` 覆盖该模板
   （当前 18 份模板全走一遍）"。**没有新写 Rust 侧的模板枚举用例**：18 份模板的整跑已在
   `.github/workflows/ci.yml:204-213` 的 for-loop（`runtime-check` 逐个走），而新校验挂在
   `runtime_check.rs` 的 strategy 循环里，因此那 18 份会自动经过成本文件检查 ——
   再加一份 Rust 侧枚举就是第二份口径。本轮实测记在 §12.3 末条。
3. §6 未预见的一处：**深度档拒绝延迟**。计划只写"费用与延迟都从它来"，但深度内核的接口只有
   `fee_bps`，接线后"配了延迟、深度链静默忽略"会变成新的不诚实面，因此选择报错而非忽略。
   代价是深度链不吃延迟配置这件事被显式化（该链路 `latency_*` 必须为 0），如实记录。
4. 断言层面踩到检出形态问题：`core.autocrlf=true` 下工作树是 CRLF、索引是 LF
   （`git ls-files --eol`：224 个 `.rs` 中 201 个 `i/lf w/crlf`），一条跨行核对源码文本的用例
   （`tests/backtest_entries.rs` 的帮助文本断言）因此变红。修在**测试侧**
   （`backtest_entries.rs:435` 先 `.replace('\r', "")` 再匹配），没有为了让用例绿去改帮助文本或 git 配置。

### 12.3 验收数字（本轮日志实测）

- 前置门槛（`GIT_HEAD=ef461b4`）：`CARGO_CHECK_EXIT=0`、`FMT_CHECK_PREFLIGHT_EXIT=0`、
  `CLIPPY_DENY_EXIT=0` 且 `CLIPPY_WARNING_LINES=0`、`ARCH_BASELINE_EXIT=0 ARCH_PASS_LINES=124`。
- 基线与收口一致（`cargo test --workspace --all-targets --no-fail-fast`，覆盖 `crates/*/tests/`）：
  `SUITE_BASELINE_EXIT=0 TARGETS=53 PASSED=651 FAILED=0` → `SUITE_FINAL_EXIT=0 TARGETS=53 PASSED=651 FAILED=0`；
  收口另加 `FINAL_CHECK_EXIT=0`、`FMT_CHECK_EXIT=0`、`ARCH_FINAL_EXIT=0 ARCH_PASS_LINES=124`。
  与 Q0b 轮（51 目标 / 604 例）的差里本轮自身贡献是 `backtest_cost_provenance.rs` 的 5 条，
  其余来自上游 7 个提交带入的用例文件。
- 架构不变量 **117 → 124**：`paper_fee_same_source_check()` 从 8 项扩到 15 项（+7 新增：读者唯一、
  模板随仓库发布、Bar 装配不得写死零延迟、深度缺省费率取自绑定、schema 声明该字段、
  `config validate` 覆盖该字段、存在成本驱动行为用例；另把两条旧判据改写为"不再由分派层写死"与
  "费用与延迟成对来自同一份绑定"）。新增共享谓词 `is_cli_case_file()`，三处调用点合并。
- 行数棘轮：`SNAPSHOT_EXIT=0`、`BUDGET_DRIFT_LINES=0`、`BUDGET_RESTORED=identical`。
  合并时按 `ec57a62` 先例重基线一次：`crates/qx-execution/src/lib.rs` 登记 **1496 → 1498**
  （上游 `fill_tag` 那处 +2），同批收下 `config_commands.rs` 971 → 965、`worker_entry.rs` 586 → 578 两处下行。
- 反向验证八组全部成对（`RESTORE[*]=identical`、`ANCHOR_BACK[*]=yes`）：
  M1 读完文件仍返回 `ExecutionCostRules::default()` → `M1_TEST_EXIT=101` → `restore_M1_TEST_EXIT=0`；
  M2 造第二个 `load` 读者 → `ARCH_MUT_M2_EXIT=1`（`ARCH_PASS_LINES=123`）→ `ARCH_RESTORE_M2_EXIT=0`（124）；
  M3 装配退回写死 `ZeroLatency` → `ARCH_MUT_M3_EXIT=1`（`122`，成对性与零延迟两条同时红）
  + `M3_CHECK_EXIT=0` + `M3_TEST_EXIT=101` → 还原后 `ARCH_RESTORE_M3_EXIT=0`（124）+ `restore_M3_TEST_EXIT=0`；
  M4 深度缺省费率退回 `qx_core::DEFAULT_TAKER_BP` → `ARCH_MUT_M4_EXIT=1`（123）+ `M4_TEST_EXIT=101` → 两项均 0（124）；
  M5 `config validate` 不再报告成本文件问题 → `ARCH_MUT_M5_EXIT=1` → `ARCH_RESTORE_M5_EXIT=0`（124）；
  M6 成本驱动用例改名 → `ARCH_MUT_M6_EXIT=1`（123）→ 0（124）；
  M7 多腿产物 `execution_costs` 键挪走 → `M7_TEST_EXIT=101` → `restore_M7_TEST_EXIT=0`；
  M8 关掉深度档延迟拒绝 → `M8_TEST_EXIT=101` → `restore_M8_TEST_EXIT=0`。
- 一处判据假象如实记：`MUT_RESIDUE[M2]=yes`。残留检查只 grep 替换文本首行，而 M2 的替换首行恰好
  等于锚点原行，于是误报；同轮 `RESTORE[M2]=identical`、`ARCH_RESTORE_M2_EXIT=0`（124），
  且收口前的全仓扫描在 `RESIDUE_SCAN_DONE` 之前没有输出任何文件，可判无残留。
  该判据的盲点属 §7 门禁设计待办（首行 grep 换成整段比对），不影响本轮结论。
- 模板整跑（本轮实测，不在门禁日志内）：18 份 `deploy/qianxing.runtime*.json` 逐项
  `qx-cli config validate` → `TEMPLATE_SWEEP_OK=17 FAIL=1`；唯一红项是 production 模板的
  `strategy.research_snapshot_path` / `dataset_bundle_path` 两条 `/var/lib/qianxing/research/*`
  绝对路径（部署机路径，与成本面无关，自 V10 起即为记录性条目）。

### 12.4 交给 Q0d 的事实

1. **`QX_GIT_COMMIT` 进了 RunManifest 之后，跑一次全量用例就在仓库里多一批未跟踪产物**：
   本轮收口时实测 `deploy/data/*/runs/` 下新增 24 个未跟踪文件，而仓库里已 bless 的 runs 文件 66 个。
   这是"产物可追溯到 commit"与"测试不污染工作树"的正面冲突，Q1b（可复现性绑定）必须一并裁决
   文件名是否含 commit、或 runs 目录整体 `.gitignore` 只白名单摘要。
2. 成本口径已经"配了生效"，但**能配的还是太少**：Bar 链只有费率与延迟进得来，
   `BestPrice` / `VolumeSensitive` / `TieredMargin` / `VirtualTradingConfig` 仍不可达（§3B），
   这是 Q1a 的范围。同时本轮实测确认：**随仓库发布的 Bar 策略回测示例根本不成交** ——
   `deploy/data/*/runs/` 下 6 份本轮新产出的摘要全部 `fills: 0 / turnover_raw: 0 / return_bps: 0`
   （A 股、OKX、BINANCE 三对，且已 bless 的同名产物也是 `fills: 0`，说明这是既有状态而非本轮回归）；
   另一条 `qianxing-strategy-backtest` 示例只有 1 笔、`turnover_raw=102`（raw 口径下名义额≈1e-10 USDT），
   5bp 费用在整数记账里直接归零。因此"费率驱动费用"的行为用例走的是多腿归因产物
   （两条腿各有真实成交），单标的 Bar 链的用例只能钉"来源字符串"这一层。
   根因与影响面已单独记进 §4.18（加密帧太短 / A 股合成时间戳配不上任何合法日历 / 拒单不落产物），
   修示例归 Q1a，把"为什么没成交"写进产物归 Q0e–Q1b。
3. `orderbook.rs:3` 的"Paper 共用簿内核"表述仍未处理，且 §12.1 的深度链延迟拒绝说明
   **深度档与 Bar 档的能力差是真实的**：Q0d 改表述时要把这个差写清，而不是继续用"同一内核"糊过去。
4. 检出形态（CRLF）会让任何"读源码文本再匹配"的门禁与用例脆断。本轮只修了踩到的那一处；
   Q4（测试诚实性）应统一给这类断言定口径：比对内容前先归一换行，或改成语法级检查。


## 13. Q0d 收口记录（2026-09-21，日志 `/tmp/qx_q0d_round_main.log` + `/tmp/qx_q0d_gate_203911.*.log`）

本轮范围按 §6 的编号默认值执行：**只改表述 + 建 Q1d 排期，不改行为**（paper 仍走首档 touch）。
做法是把"哪句表述是真的、哪句是假的"钉进门禁，而不是只在文档里换一句话——换掉的表述如果没有
判据守着，下一轮改动就会长回来。

### 13.1 落地内容

1. **新增 `kernel_claim_check()`（`tools/check_architecture.py`，挂在 `paper_fee_same_source_check()`
   之后）**，六条判据，架构不变量 124 → 130 项：
   - paper 侧（`crates/qx-zhenlu/src/**`，整文件、剥注释后）不得出现 `OrderBook` / `BookLevel`
     符号——这条钉的是**代码耦合**这一半事实，M3 变异（在 `oms.rs` 里引用 `OrderBookSnapshot`）实测红；
   - 文档指向的真实成交入口必须存在：`qx-zhenlu/src/lib.rs` 仍定义 `impl PaperVenue` 与
     `pub fn on_quote(`（实测在 `:1330`），改名即红（M5）；
   - Tick 链必须确实复用 L2 引擎：`tick_backtest.rs:12-16` 的 `use crate::{… OrderBookBacktestEngine …}`
     被删掉即红（M4）——这条钉的是**正向**表述，避免"为了诚实只写否定句"；
   - `orderbook.rs` 模块文档必须点名两处真实消费者（`orderbook_backtest` / `tick_backtest` + `逐档`），
     且必须写明 `PaperVenue::on_quote`、"首档"与 Q1d 指向；把文档退回 Q0d 之前的四行版本，
     这两条同时红（M1，实测 `ARCH_PASS_LINES=128`）；
   - 全局连坐：`crates/*/src/**`（跳过用例文件）与 `README.md`、`deploy/README.md` 里，
     任何把 Paper 与"共用 / 共享 / 同一 / 都走 / 复用 + 内核 / 撮合 / 订单簿"写进**同一句**、
     又不含真实共享符号（`FeeModel` / `apply_fill_to_books` / `apply_ledger_fill` / `Ledger` / `Oms`）
     的表述一律红（M2 在 README 注入一条即红）。否定语（不成立 / 不得 / 没有 / 不走 / 谎称 …）豁免，
     否则诚实记录错误说法的文字本身过不了门禁。
2. **`crates/qx-xingban/src/orderbook.rs` 模块文档重写**（原第 3 行"可被历史 Tick 回放、Paper 模拟和
   性能基准共同使用"是不成立的说法）。新表述给出：真实消费者两处、Tick 与 L2 之间没有第二套订单语义、
   **Paper 不走这里**（首档一次性 touch、无逐档队列、无排队中的部分成交）、paper 与回测目前真正共享的
   只有执行平面下游三件（`FeeModel`、`qx_core::apply_fill_to_books`、`Ledger`），以及"改接本内核 = Q1d"。
   共享三件的事实核对：`apply_fill_to_books` 的调用点确实是
   `qx-runtime/src/pipeline.rs:1448,1462`（paper 侧）、`qx-xingban/src/backtest.rs:848`（Bar 链）、
   `qx-xingban/src/orderbook_backtest.rs:405`（L2/L3 链）。
3. **`crates/qx-cli/src/backtests/kernels.rs` 补第四套撮合的口径**：该文件是产物清单里
   `matching_kernel` 三个名字的家，只列三条回测链，读者会误以为"内核只有三个"。模块文档补明
   Paper 的撮合不在这张清单里、因为它不产出回测产物，并指向 Q1d。
4. **`docs/牵星完整架构方案-V1.md` §2.3 加现状对照**（该文档已随 §30 的文档收口删除，原文与当时读到的
   那段结论可从 git 历史取回；下面保留的是核对结果，不是链接承诺）。核对后确认该节本身没有说谎：它写的是
   "同一套领域事件、状态归约、订单生命周期、风险检查和审计模型，仅替换…撮合/执行适配器"，
   撮合本来就在"可替换"那一侧。缺的是现状标注，因此补一段说明：事件 / 归约 / `Oms` / `Ledger` /
   `FeeModel` 四模式同源已成立且有门禁，**撮合尚未同源**（Bar / L1 / L2 各用各的引擎，Paper 首档 touch），
   并留一句"读到'同一内核'时不要把'同一撮合'一起读进去"。
   `README.md:20` 的"回测与实盘共享同一规则内核"经核算是**规则口径**（风控与费用同源，Q0a/Q0b/Q0c
   已把它接成配置驱动），不是撮合口径，故保留原文、不纳入本轮改写。

### 13.2 与计划的偏离与本轮踩到的坑

1. §6 的验收写的是"字样所在**文件**必须能引用到真实共享符号"（文件粒度）。落地收紧为
   **同句**粒度：文件粒度会被文件里任意一处 `Ledger` 字样蒙过，等于不设防。连坐判据的扫描范围
   排除 `docs/`——本文件 §4.3 与 §13 自己要记录错误说法，纳入扫描会自相矛盾。
2. **门禁自己错过一次**：判据 1 最初照惯例套了 `non_test_source()`，而 `qx-zhenlu/src/lib.rs:16`
   就挂着一行 `#[cfg(test)] use`，`non_test_source` 按"首个 `#[cfg(test)]` 之前"截断后只剩 15 行，
   `PaperVenue` 本体（`:1200+`）根本没进扫描——M3 变异第一次跑没红就是这个原因。
   改为整文件扫描 + `re.sub(r"//.*", "")` 剥注释（注释里出现 `OrderBook` 是合法的如实表述，
   文字表述归连坐判据管）。修完 M3 红、M3n 阴性对照绿。
3. **阴性对照第一次也假红**：msys 的参数转换会把以 `//` 开头的实参折成单个 `/`，注入进文件的不是
   注释而是 `/ 与逐档…` 的残缺文本，于是被判据 1 命中。把实参改成以换行起头后对照转绿。
   这条记给 Q4：注入脚本层面的坑，不是判据缺陷。
4. **行数棘轮例外**（§7.1 第 1 条要求显式声明）：`orderbook.rs` 664 → 671 行（+7，全部在模块文档），
   按快照头部"确需增长时重新生成并审阅 diff"重 bless，`--snapshot` 的 diff 实测只有这一行
   （`BUDGET_DRIFT_LINES=0` 是收口后的复跑口径：先落快照再跑，故漂移为零）。
5. §6 验收的后半句"Q1d 若落地则做 paper vs book 成对数字"**本轮不适用**（行为未改），
   连同 Q1d 落地时必须同步改掉判据 1/判据 4 的成对要求一起移交 §13.4。

### 13.3 验收数字（本轮日志实测）

- 前置门槛（`GIT_HEAD=b311f90`，`GIT_STATUS_LINES=33`）：`CARGO_CHECK_EXIT=0`、
  `FMT_CHECK_PREFLIGHT_EXIT=0`、`CLIPPY_DENY_EXIT=0` 且 `CLIPPY_WARNING_LINES=0`、
  `ARCH_BASELINE_EXIT=0 ARCH_PASS_LINES=130`。
- 产物污染复现（§12.4 第 1 条的实测追加）：收口后 `git status --porcelain` 里
  `deploy/data/**/runs/` 下的未跟踪文件单独数得 **40 条**，而本轮只改了文档与门禁脚本、
  没跑任何 CLI 命令——增长全部来自一轮门禁里的两次全量用例。"每轮门禁必产一批 commit-pinned 产物"
  这件事已经把 Q1b 的裁决变成阻断项，见 §13.4 第 3 条。
- 用例基线与收口一致（无行为改动，故本轮不新增 Rust 用例）：
  `SUITE_BASELINE_EXIT=0 TARGETS=53 PASSED=651 FAILED=0` →
  `SUITE_FINAL_EXIT=0 TARGETS=53 PASSED=651 FAILED=0`；`qx-cli` 单测仍 95 条全绿。
- 变异段（六次注入，全部 `INJECT_OK` 且 anchor 唯一）：
  M1（文档退回旧版）→ `ARCH_MUT_EXIT=1`，两条文档判据同时红，`ARCH_PASS_LINES=128`；
  M2（README 写入"Paper 与回测共用撮合内核"）→ 红，连坐判据点名该句；
  M3（`oms.rs` 引用 `OrderBookSnapshot`）→ 红，点名 `crates/qx-zhenlu/src/oms.rs`；
  M3n（**阴性对照**，同位置只加一行注释）→ `CONTROL_STAYS_GREEN=yes`，`ARCH_PASS_LINES=130`；
  M4（Tick 不再复用 L2 引擎）→ 红；M5（`on_quote` 改名）→ 红。
  六次还原全部 `RESTORE[*]=identical` + `ANCHOR_BACK[*]=yes`，且每次还原后
  `ARCH_RESTORE_*_EXIT=0 ARCH_PASS_LINES=130`（六次，逐个复跑）。
  `MUT_RESIDUE[M2]=yes` 与 `[M3n]=yes` 是 §7.4 已登记的判据假象：M2 的 replacement 首行就是 anchor 首行，
  M3n 的 replacement 以换行起头使"首行"为空串（空串恒被 `grep -qF` 命中）。二者都有
  `RESTORE=identical` + 还原后复跑绿成对佐证，不是残留。
- 收口复跑：`FINAL_CHECK_EXIT=0`、`FMT_CHECK_EXIT=0`（无新增未格式化文件）、
  `ARCH_FINAL_EXIT=0 ARCH_PASS_LINES=130`、`SUITE_FINAL_*` 同上。
- 诚实性边界：`maturity/capabilities.yaml` 的 `sandbox_tested` 仍 18 条全为 `false`（本轮无外部动作），
  未使用任何网络、凭据或外部服务。

### 13.4 交给 Q0e / Q1a / Q1d 的事实

1. **Q1d 落地时判据是成对的**：paper 一旦改接簿内核，判据 1（zhenlu 不得引用 `OrderBook*`）与
   判据 4（文档必须写"首档 touch"）必须与行为改动**同一次**改掉，否则门禁会把旧口径钉成化石；
   同一次里还应把 `kernels.rs` 的"第四套不产回测产物"说法一并改掉。§6 的验收后半句（paper vs book
   成对数字）在那一轮才生效。
2. **成对数字目前没有可复用的夹具**：`deploy/` 下 paper 侧是合成报价注入
   （`qianxing.runtime.paper-strategy.example.json`、`qianxing.paper-submit-order.example.json`），
   回测侧是 `qianxing.bar-frame*.example.json` 与 `qianxing.depth-frame*.example.json`，
   两边没有共享过同一份盘口输入。Q1d 需要新造一份"同一 L1 输入两跑"的最小夹具，
   否则"成对"只能停留在口头。
3. **runs 产物增长已经到必须裁决的程度**：本轮没跑任何 CLI，仅两次全量用例就把
   `deploy/data/**/runs/` 的未跟踪文件推到 40 条（§13.3）。Q1b 除了"文件名是否含 commit /
   runs 目录整体 `.gitignore` 只白名单摘要"之外，还要顺手回答"门禁轮里跑出来的产物算不算
   需要复核的产物"，否则每轮收口都要在 `git status` 里人工排除一批噪声。
4. 本轮把表述与代码事实对齐后，**paper 与回测的差异只剩撮合这一件**这一结论是可检的；
   Q0e（多腿成交回填 + 把拒单写进产物）与 Q1a（示例夹具修 + 高保真能力可达）可以按 §4.18 的
   验收追加口径直接推进，不必再回到"口径是否同源"的争论上。

## 14. Q0e 收口记录（2026-09-21，日志 `/tmp/qx_q0e_gate_mut_214803.log` + `/tmp/qx_q0e_rust_mut_215003.log` + `/tmp/qx_q0e_final_215028.log`）

本轮范围按 §6 执行，只有一处按编号默认值收敛：一腿被挡时对另一腿**只标记 `PendingReconcile`、
不自动收口**。理由是"收口"要真的下一张反向单，回测里等于再造一条执行链，会把 Q0e 与 Q1a
叠成两个变量；而"看得见裸腿"才是这一阶段的危害点，先把事实导出。

### 14.1 落地内容

1. **归因配对口径改成只看实际成交**（`crates/qx-cli/src/multi_leg.rs:381-387`）。
   改之前只要某条腿在**信号计划**里出现过量，即使那条腿一笔都没成交（现金不足、名义额上限、
   reduce-only 被挡），组里照样挂上它的计划数量——净敞口、保证金峰值与归因费用于是可以描述
   一个现实中拿不到的组合。改后成组与否只看 `filled_qty_raw`，为零即 `paired = false`。
2. **裸腿必须显式存在**（`MultiLegPendingReconcile` 定义在 `:275`，写入点 `:410-435`）：
   组没成时该腿的成本与成交量进 `residual_*`（不再静默丢弃），有成交的腿逐条登记成
   `{leg, counterpart, ts, filled_qty_raw, turnover_raw, fees_raw, counterpart_filled_qty_raw}`，
   对手腿成交量一并写出，读产物的人立刻能看出"谁被挡了"。策略选择写进产物的
   `policy = mark-pending-reconcile-no-auto-close`。
3. **每腿"计划 vs 成交"事实**（`MultiLegLegIntegrity` `:287` + `multi_leg_leg_integrity()` `:311`）：
   `planned_qty_raw / filled_qty_raw / unfilled_planned_qty_raw / vetoed_signal_ts /
   rejected_orders / rejection_reasons`。拒单原因是从该腿事件日志里 `EventKind::Rejected`
   直接归并（`count_by_reason`，按次数降序），**没有新增事件、没有改 `result_hash`**，
   所以 blessed 产物哈希不受本轮影响。
4. **单腿定资拆成独立模块** `crates/qx-cli/src/backtests/leg_funding.rs`（新增 60 行），
   三条口径写在明面上：① 用**本腿自己的**全帧最高价（旧口径取两腿全局最大值，一条 1e8 倍的
   参考价腿会把主腿账户撑爆，便宜腿也跟着背不该背的名义额）；② 手续费余量按**当前生效的
   成本绑定** `costs.rules.taker_bp` 折算（旧口径是拍脑袋的 ×2）；③ 算不出来（checked 运算溢出
   或超出 `i64` 可表示范围）**报错**，不再 `.min(i64::MAX as i128)` 静默截断——截断等于把
   "这条腿根本买不起"伪装成"回测跑通了、零成交"，是 §4.18 的同一类失真。
   拆出去的另一原因是 `backtests/mod.rs` 的顶层条目上限（`CLI_BACKTESTS_MOUNT_CEILING = 8`，
   实现不得长回挂载文件）：放回去就是 11 条，红。
5. **产物升 `schema_version: 2`** 并新增 `accounts`（两腿初始现金 + 定资规则原文）、`legs`、
   `pending_reconcile` 三块；stdout 多两行 `[Multi-leg · Integrity]` 与 `[Multi-leg · Reconcile]`
   （token 约定：一个 `key=value` 一件事实，拒单原因里的空格换 `_`）。
   归因摘要里追加 `residual_filled_qty_raw` / `residual_fees_raw`，并加两道**闭合守卫**
   （`multi_builtin.rs:285`、`:301`）：裸腿成交之和必须等于 `residual_filled_qty_raw`、
   每腿归因成交量必须等于撮合 fills 之和，不闭合就直接失败，防止"导出面漂亮但内部对不上"。
6. **端到端用例 3 → 6 条**（`crates/qx-cli/tests/multi_leg_attribution.rs`，256 → 507 行）：
   `every_multi_leg_kind_reports_fully_filled_legs`（四条 kind 全跑，过去只有
   `pairs_arbitrage` 有用例，另外三条的腿序/规格/归因可以整体坏掉而无人报警）、
   `vetoed_leg_never_pairs_against_a_filled_counterpart`（用运行时配置的
   `max_notional_raw` 把主腿全部挡下，造出真实裸腿场景）、
   `multi_leg_funding_bound_fails_loudly_instead_of_capping_cash`（quantity 顶到定资越界，
   断言退出码非零、stderr 点名上限与 `quantity`、且**不得**产出归因摘要）。
   原有三条用例的数值断言一字未改就通过——这轮改动在"全都成交"的夹具上是等价的，
   所以必须新造被挡场景才算observable。
7. **新增门禁 `multi_leg_honesty_check()`**（挂在 `kernel_claim_check()` 之后），7 条判据：
   配对函数体内不得再出现 `planned_qty_raw`；`MultiLegPendingReconcile` 的定义点与写入点都在；
   定资函数体内 `i64::MAX` 只允许出现 1 次（诊断文案）；两个定资调用必须各自传本腿行情帧与
   生效费率；诚实性事实同时进 stdout 与产物且有闭合守卫；四条 kind + 两条反向用例齐备；
   产物必须声明两腿共用一份成本绑定。架构不变量 130 → 137 项。

### 14.2 与计划的偏离与本轮踩到的坑

1. **§6 的"每腿费用按各自 venue spec"没做**，改成立即可查的偏离声明。原因：
   `TradingInstrumentSpec` 没有任何费率字段，唯一生效的费率来源 `ExecutionCostRules` 只有
   全局 `maker_bp` / `taker_bp`（Q0c 接的就是它），所以"按 venue 分档"无处落地。
   仓库里确实有一套分档费率——`crates/qx-core/src/fenye.rs`（464 行，带 `maker_bp` / `taker_bp`），
   但 `fenye::` 在其自身文件之外**零消费者**（全仓只有 `qx-core/src/lib.rs:16` 的挂载声明），
   是 V10 P2a 合并留下的死码。与其把它"接上"冒充真实口径，不如把偏离写进产物 assumptions
   （`fees=one explicit cost binding shared by both legs; market spec carries no maker/taker field,
   so per-venue fee tiers are not applied`）与 `capabilities.yaml` 的
   `backtest_multi_leg_fees_share_one_cost_binding_because_market_spec_has_no_tier_fields`。
2. **门禁判据假绿过一次**：G3 最初写的是"函数体内有 `i64::try_from(required)`"，
   而 `i64::try_from(required).unwrap_or(i64::MAX)` 照样命中这个子串——变异注入后门禁仍绿。
   改成"`i64::MAX` 在函数体内只出现 1 次"后，任何写法（`.min` / `.unwrap_or` / `.or`）的
   静默截断都会多出一处字面量而变红。
3. **行为变异假红过两次**（同一个坑的镜像）：R3 的第一版 `.unwrap_or(i64::MAX)` 与第二版
   `.or(Ok(i64::MAX))` 都**编译不过**（E0308 / E0283），日志里的"红"来自编译器而不是用例。
   换成可编译的 `i64::try_from(required).or(Ok::<i64, std::num::TryFromIntError>(i64::MAX))`
   之后才是真红（`multi_leg_funding_bound_fails_loudly_instead_of_capping_cash ... FAILED`）。
   记给 Q4：**变异段必须区分"编译失败"与"用例失败"**，前者只能算注入未生效。
4. **锚点与行尾**：源码是 CRLF（`core.autocrlf=true`），多行 python 锚点在字节层面找不到，
   全部改单行锚点；门禁读文件走 `read_text`（CRLF 归一为 `\n`），所以门禁里的匹配串要写
   `"\n}"`，写成真实换行会把 python 字符串截断成语法错误（本轮就踩了一次）。
5. **破坏性 splice 误删过函数**：改 `multi_leg.rs` 时锚点取的是"文档注释前缀 + 换行"，
   边界写错导致 `multi_leg_bar_close_raw` 整块被删。发现方式不是编译错误而是
   `git diff | grep "^-"`；按备份逐字还原。规则：删除型改动必须看 diff 的减行，不能只跑编译。
6. **行数棘轮的盲区**（本轮暴露，交给 §14.4）：`source_line_counts()` 只扫
   `crates/*/src/**/*.rs`，所以 `crates/qx-cli/tests/multi_leg_attribution.rs` 从 256 行写到
   **507 行**（已越过 `OVERSIZED = 500`）而不触棘轮、也不需要 `--snapshot`。
   本轮没有把它塞进 `src`（那会立刻变红），但"用例文件不受棘轮管"这个事实要么在 Q4 扩范围，
   要么显式承认——不能既享受豁免又声称棘轮覆盖全仓。

### 14.3 验收数字（本轮日志实测）

- 门禁段（`/tmp/qx_q0e_gate_mut_214803.log`）：`BASELINE_GATE_EXIT=0`（137 项）→
  G1/G2/G3/G4/G5/G6/G7 七次注入**全部 RED**且各自点名对应判据 →
  七次 `RESTORE[*]=identical` → `FINAL_GATE_EXIT=0`（137 项）。
- 行为变异段（`/tmp/qx_q0e_rust_mut_215003.log`）：`BASELINE_TEST_EXIT=0 6 passed` →
  R1（抽掉"只看成交才成组"）→ `vetoed_leg_never_pairs_against_a_filled_counterpart FAILED`（5 passed; 1 failed）→
  R2（抽掉裸腿登记）→ 同一条 FAILED → R3（定资退回静默截断）→
  `multi_leg_funding_bound_fails_loudly_instead_of_capping_cash FAILED` →
  三次 `RESTORE[*]=identical` → `FINAL_TEST_EXIT=0 6 passed`。
- 裸腿场景实测（同一份 `pairs_arbitrage` 夹具，运行时配置写
  `risk_rules.max_notional_raw = 10_000_000_000_000`，落在 ETH 腿 6e12 与 BTC 腿 1.2e14 之间）：
  主腿 `fills=0`、`planned_qty_raw=6000000000`、`unfilled_planned_qty_raw=6000000000`、
  `vetoed_signals=3`（`ts = 82800000 / 147600000 / 158400000`）、`rejected_orders=34`，
  原因全部是 `[BUSINESS_VIOLATION] MaxNotional: 投影持仓名义额超过账户限额`；
  对冲腿 `fills=3`、`filled_qty_raw=6000000000`；
  归因 `groups=0 turnover_raw=0`、`residual_filled_qty_raw=6000000000`、
  `residual_fees_raw=9000000000`；`pending=3 naked_filled_qty_raw=6000000000`；
  账户 `primary_initial_cash=252127000000000`、`reference_initial_cash=100000000000000`。
  改前这条场景会照样把主腿按计划量配进组：R1 变异（抽掉"只看成交才成组"）实测
  `assertion left == right failed: 单腿成交绝不能配成套利组，left: 3, right: 0`——
  净敞口与保证金峰值于是都描述了拿不到的组合。
- 全量收口（`/tmp/qx_q0e_final_215028.log`）：`SUITE_EXIT=0`，`Running` 52 段 + `Doc-tests` 21 段
  = 73 个 `test result:` 段，`passed=654 failed=0`；对照 §13.3 基线的 651 → **654**，
  多出的 3 条全部来自 `multi_leg_attribution`（3 → 6）；`qx-cli` 单测仍 95 条全绿。
- 前置门槛：`CLIPPY_DENY_EXIT=0` 且 warning 0 行（`/tmp/qx_q0e_clippy_final.log`）、
  `cargo fmt --all --check` 退出 0、`maturity/line_budgets.yaml` 未改动（本轮四个 src 文件都在
  500 行内：`multi_leg.rs` 471、`multi_builtin.rs` 467、`backtests/mod.rs` 193、`leg_funding.rs` 60）。
- 诚实性边界：本轮未使用任何网络、凭据或外部服务，`maturity/capabilities.yaml` 的
  `sandbox_tested` 仍 18 条全为 `false`；`multi_leg_execution` 只补了两条**本地**证据路径
  （新用例文件与 `leg_funding.rs`）和一条费率偏离，四档状态未翻转。

### 14.4 交给 Q1a / Q1b / Q4 的事实

1. **策略侧仍是"意图仓位"口径**。本轮改的是归因与产物，`StrategyContext::positions`
   （`crates/qx-xingban/src/lib.rs:172-187`）是策略自持的 `BTreeMap`，引擎只往里读、
   成交回报不回填。要消灭"策略以为建了仓、账上其实没有"，得让内置策略看到实际成交——
   那需要动 `qx-xingban` 的策略上下文契约，与 Q1b 的输入指纹改动会撞同一批回测哈希，
   所以按 §6 的依赖序留在 Q1a 之后一次做。本轮已经把入口留在产物里：
   `legs[].vetoed_signal_ts` 就是"策略以为有仓"的时点清单。
2. **单腿两条链还没有拒单事实**：`strategy backtest` / `backtest builtin` 的
   `append_rejection` 仍只写内存 EventLog（§4.18）。Q1a 修示例夹具时必须顺手把
   `[Multi-leg · Integrity]` 的 token 约定推过去，否则"示例零成交"仍然分不清是没信号还是全被拒。
   ——**Q1a 第一批已结**，见 §15.1 第 3 条。
3. **每腿 venue 费率的前置条件是字段承载**：给 `TradingInstrumentSpec` 加 maker/taker，
   或把 `qx-core/src/fenye.rs` 真正接线并删掉重复的全局 `ExecutionCostRules` 语义。
   在那之前任何"按 venue 分档计费"的说法都只能写成偏离声明（§14.2 第 1 条）。
4. **门禁与被检物的两处结构性缺口**，建议 Q4 一并裁决：① `source_line_counts()` 不覆盖
   `crates/*/tests/`（§14.2 第 6 条）；② 变异段要能把"编译失败"判为注入未生效而不是红（§14.2 第 3 条）。
   另加 §13.4 第 3 条仍未裁决的 `deploy/data/**/runs/` 产物增长。

## 15. Q1a 第一批收口记录（2026-09-22，门禁 `/tmp/qx_gateQ1a2_v3.log` + 证据 `/tmp/qx_q1a_evidence_000305.log` + 成对数字表 `/tmp/qx_q1a_ev_table.txt`）

本轮结掉 §6 Q1a 行的两件半：信号正确性的前置修复、深度链的现金门与撮合参数、三份 §4.18 示例夹具。
Bar 链的 `--fill-model` 与 `--virtual-trading` **没做**——两者都不是"接上线就有证据"的形状，
理由写在 §15.2 第 1 条，待办与前置条件写在 §15.4 第 1、2 条。

### 15.1 落地内容

1. **先修信号，再接能力**（`crates/qx-strategy/src/builtin.rs:777`）。内置 `ema()` 的旧值权重写成
   `window`，而 α = 2/(window+1) 要求它是 `window-1`：两个权重之和于是是 `(window+2)/(window+1)`，
   直流增益不为 1，常数价格列会收敛到 2× 该常数，快慢线的相对位置随窗口大小漂移。`ema_cross` /
   `macd` / `keltner_trend` 共用这个函数，交叉判定在修之前无从谈起——这是 §4.18"示例全零成交"
   诊断里两个真正的代码缺陷中的第一个，其余成因是夹具与口径问题。新用例
   `ema_keeps_unit_dc_gain_and_lags_on_the_trend_side`（`builtin.rs:904`）钉两条：任意窗口下常数
   序列的 EMA 必须等于该常数（增益为 1），且上升段快线必须在上、下降段快线必须在下。
   同一层还有第二个缺陷：**滚动历史上限低于信号门槛**。`max_history()` 原来只按
   `slow_window`/`period` 取上限（默认 5/20/14 算出 32 根），而 MACD 的第一个信号要 35 根，
   于是"示例帧再长也永远不信号"。修法是把门槛与上限收进同一份清单
   （新增 `required_bars()` `builtin.rs:233`，`max_history()` `:225` 取两者最大值，
   信号门槛 `:349` 读同一个函数），用例
   `macd_signals_within_the_rolling_history_cap` 先断言 `max_history() >= required_bars()`，
   再喂 70 根 V 形价格要求真的发出过意图。
2. **三份 §4.18 夹具改到"能成交"而不是"能跑"**：
   - `deploy/qianxing.bar-frame.example.json` 5 根 → **70 根**（`ts` 1000…70000）。旧夹具比任何
     一条内置策略的预热窗口都短（`builtin.rs:235-252`：最小的是 `period+1`，MACD 要 35 根），
     "示例跑通、零成交"是必然结果，与策略好坏无关。
   - `deploy/qianxing.ashare.bar-frame.example.json` 的时间戳从 `1000…6000` 换成真实交易时段的
     epoch 毫秒（`1717378200000…1717466400000`），价格改成先跌后涨，让 A 股 PIT 与日历约束第一次
     有东西可交易：本轮实测摘要 `turnover_raw=1045000000000`、`fees_raw=5104500000`、
     `final_equity_raw=100019895500000`、`rejected_orders=0`（digest `c62ffd9a62646398`）。
     同目录另外三份**旧 blessed 产物**的 `turnover_raw` 与 `fees_raw` 仍是 0——夹具变了它们没变，
     属 §15.4 第 4 条的 rebless 范围。
   - `python/examples/backtest_momentum.py` 的目标仓位从 `1` 换成 `ONE_UNIT = 1_000_000_000`：
     跨语言契约里的数量是定点裸值，写 `1` 就是 1e-9 个标的，名义额小到 5bp 手续费在整数记账里
     归零，这个示例等于从没走过费用与账本路径。同一口径错误在四份 runtime 模板里共 5 处同时修掉
     （`builtin_quantity: 1 → 1000000000`：`ccxt` 的 125/148、`multi-venue-arbitrage` 的 128、
     `builtin-strategy` 与 `.okx` 的 47），并在 `strategy_schema.rs` 的字段文档里写明
     "runtime 是 raw 口径、命令行位置参数 `[QUANTITY]` 是整数单位口径，两者差 1e9 倍"。
     三份夹具由一条新用例统一看住（`crates/qx-cli/tests/fast_backtest_manifest.rs` 的
     `shipped_examples_fill_positions_and_pay_nonzero_fees`）：binance / okx / ashare 各跑一次
     `strategy backtest`，要求退出 0、`fills > 0`，并且摘要里 `turnover_raw` 与 `fees_raw`
     **都非零**——这正是 §6 Q1a 行末那句"否则该示例的 e2e 断言不算覆盖成交路径"的落地。
3. **拒单事实从多腿局部提升为三条链共用**（`crates/qx-cli/src/backtests/artifacts.rs:43/63/76`
   的 `rejection_facts()` / `rejection_facts_line()` / `rejection_count()`，Q0e 长在 `multi_leg.rs`
   里的私有版本删掉，§14.4 第 2 条就此结掉）。摘要产物新增 `rejected_orders` 与 `rejection_reasons`
   （`artifacts.rs:103-108`），stdout 新增 `[Builtin · Integrity]` 与 `[Strategy · Integrity]` 两行，
   token 语法与 Q0e 的 `[Multi-leg · Integrity]` 完全一致（`次数x原因`、空格换 `_`、无拒单写 `none`）。
   没有这一条，`fills=0` 分不清"策略没发信号"和"信号全被挡"，而这两件事对使用者的含义相反。
   新用例 `blocked_signals_are_distinguishable_from_silent_strategies`
   （`crates/qx-cli/tests/fast_backtest_manifest.rs:280`）。
4. **深度链补上无 market spec 时的现货买入现金门**（`crates/qx-xingban/src/orderbook_backtest.rs:573-586`），
   与 Bar 链及同文件 `:535-556` 的有规格分支同一口径：买入所需现金 = 名义额 + 名义额 × `fee_bps`/10000，
   超过可用现金即写 `Rejected` 事件并跳过这笔，而不是让账本记成负现金。用例
   `specless_spot_buy_beyond_available_cash_is_rejected_not_overdrawn`（`:924`）断言 `turnover_raw`
   为 0 且事件里点名原因（`:963`）。§6 点名的"补 specless 分支"即此。
5. **两个撮合参数接到命令面**：`--latency-snapshots` / `--market-impact-bps`（`cli_args.rs:477/479`，
   越界在 clap 层由 `parse_bps()` `:34` 挡住，不等内核报错）→ `DepthExecutionModel`
   （`backtests/depth.rs:13`，`to_kernel()` `:21`）→ 内核 `OrderBookExecutionModel`，L1 与 L2 两条
   装配各自 `with_execution_model()`（`depth.rs:118` 起）。三处同时留痕，缺一处即"能配但看不见"：
   `[Depth · Execution]` 行（`:215`）、产物 `model_descriptors` 的四参数描述子
   （`orderbook_backtest.rs:361`）、`depth_run_config_hash()`（`depth.rs:258-259`）。最后一处不是
   可选项——产物按内容寻址落盘，参数不进 config hash 就会让两个不同结果抢同一个 digest 路径。
   缺省口径与改动前逐位一致：那条把 `final_equity_raw` 钉成 `100_265_589_000_000` 的用例一字未改
   仍通过，本轮 baseline 实测同为 100265589000000（§15.3 表第一行），但描述子会把两个 0 写出来。
6. **`queue_position_bps` 故意不做成旗标，并且用例钉住它不存在**。判据：内核只在
   `orderbook.rs:332` 的 `order.limit.is_some()` 分支用到它，而内置策略构造 intent 时恒
   `limit: None`（`builtin.rs:721`），17 条策略全部发市价单——非零值换不动任何一笔成交。
   声明一个换不动结果的参数就是 Q0b 判掉的假旗标形状。于是
   `depth_execution_model_flags_change_results_and_are_recorded`（`tests/depth_backtest.rs:226`）
   除了比对两个真参数，还断言 `--queue-position-bps 5000` 退出码非 0 且 stderr 点名该旗标。
7. **Q0c 的延迟冲突文案改成可执行指令**（`depth.rs:99`）：不再只说"不接受"，而是点名
   "请把 `latency_base_ns`/`latency_insert_ns` 置 0，或用 `--latency-snapshots` 表达深度延迟"。

### 15.2 与计划的偏离与本轮踩到的坑

1. **§6 点名的 `--fill-model` 与 `--virtual-trading` 没做，因为两者都还缺前置条件**：
   - FillModel 只被 Bar 引擎消费，而 `backtest.rs:387` 的 `data_tier.supports(fill.tier())` 使
     Bar 数据只接受 Bar 档模型。`Probabilistic`（L1）与 `VolumeSensitive`（L2L3）在 CLI 上
     **没有任何落点**——做成旗标只会让使用者每跑一次都撞一次档位错误。要真暴露它们，得先让
     Bar 链能吃 L1/L2 帧，那是独立决定。本轮 Bar 生产选择点仍是写死的
     `backtests/mod.rs:62` `fill: Box::new(NextBarOpenFillModel)`。
   - `VirtualTradingConfig`（资金费、利率、交割、强平、汇率、抵押品）没有 serde，引擎用例之外
     没有任何构造点，接进来要先手写解析器；更要紧的是 Bar 帧里没有标记价，资金费与强平在现有
     输入下算不出来。把"配了但永远不会触发"的字段做成旗标，与 §15.1 第 6 条是同一件事的反面。
2. **陈旧二进制冒充过证据**（本轮实际踩到，差点写进文档）：门禁末尾 `cp -p` 还原把源码 mtime
   带回变异之前，于是独立使用的 `target/debug/qx-cli.exe`（23:51:40.74）**是变异期的构建产物**
   （`orderbook_backtest.rs` 当前 mtime 23:51:41.40）。拿它跑出的表里描述子被摘成
   `orderbook-matching@v1 fee_bps=5`（只剩费率），六个 case 出现两个相同的 `model_fingerprint`，
   看起来像"参数进了结果却没进指纹"的真缺陷。`cargo build -p qx-cli --bin qx-cli` 重链后同一份表
   自洽。规则：**证据日志的第一段必须先打印可执行文件与相关源码的 mtime**（本轮日志已按这条加，
   见其 § 0 段），否则"红绿成对"与"证据"都可能来自上一版产物。
3. **MSYS `/tmp` 与原生解释器的 `/tmp` 不是同一个目录**（本轮踩两次，同类坑已记过）：
   `python - <<EOF` 里写 `/tmp/xxx.sh` 会改到 `C:\tmp` 的同名文件且毫无报错（表现为"补丁生效了但
   脚本没变"），`git show … > /tmp/x.json` 之后原生 python 读不到（表现为 `FileNotFoundError`，
   退出码却像正常）。一律 `cygpath -w` 转换后传参。
4. **`cargo test -- <filter>` 的假绿**：过滤器匹配的是**函数名**，写成文件名或带模块前缀会得到
   `0 passed; 0 failed; N filtered out` 且退出码 0——"摘闸后仍绿"于是是假的。变异段现在每次跑完
   把 `test result:` 原文打进日志（`/tmp/qx_gateQ1a2_v3.log` 的 `RESULT=` 行），0 命中一眼可见。
5. **L1 与 L2 的 `result_hash` 会逐位相同**（`/tmp/qx_q1a_ev_table.txt`）：
   `qianxing.depth-frame.l1.example.json` 是 `qianxing.depth-frame.example.json` 的一档投影
   （各 80 个快照、top-of-book 逐快照一致，实测 `same=True`；后者每档 3×3），而市价单只吃最好一档，
   于是同一撮合参数下两档的 `result_hash`、`fees_raw`、`final_equity_raw` 全等。也就是说
   **档位接错不在 result_hash 上留痕**，可区分性只有 `data_tier`、`model_fingerprint`、
   `input_event_hash` 三处（本轮实测六行的 `model_fingerprint` 两两不同，`input_event_hash`
   只在同档内相同）。记给 Q1b 的输入指纹绑定与 §15.4 第 3 条。
6. **`cp -p` 还原吞掉本轮新增用例**（§14.2 第 5 条同一类坑复现）：M2 的备份抓在"追加引擎描述子
   断言"之前，还原后新断言整块消失且不报编译错。发现方式仍是 `grep` 锚点而不是编译。
   规则重申：每次追加用例后**立刻**刷新该文件的变异备份。
7. **反向验证第一次没咬住**：M2（描述子退回只写 `fee_bps`）起初只有 CLI 侧红，引擎侧 76 passed
   全绿——"参数写进产物"这件事引擎层原来没人管。补上 `orderbook_backtest.rs:1173/1184` 两条
   描述子断言后，M2 变成引擎与 CLI 双红（§15.3）。**只有消费者侧断言的门禁不算门禁**。

### 15.3 验收数字（本轮日志实测）

- 门禁（`/tmp/qx_gateQ1a2_v3.log`）：`cargo fmt --all --check` 与 `cargo clippy --workspace
  --all-targets` 退出 0 且 warning 0 行；`cargo test --workspace --all-targets --no-fail-fast`
  `TEST_TARGETS=53`、**`TEST_PASSED=660 TEST_FAILED=0`**。对照 §14.3 基线 654 是净增 6 条，
  且逐条对得上（本轮 `git diff` 里新增的 `#[test]` 函数恰好这六个，删掉的老用例 0 条）：
  `ema_keeps_unit_dc_gain_and_lags_on_the_trend_side`、`macd_signals_within_the_rolling_history_cap`
  （qx-strategy）、`specless_spot_buy_beyond_available_cash_is_rejected_not_overdrawn`（qx-xingban）、
  `depth_execution_model_flags_change_results_and_are_recorded`、
  `shipped_examples_fill_positions_and_pay_nonzero_fees`、
  `blocked_signals_are_distinguishable_from_silent_strategies`（qx-cli 集成用例）；
  本轮改过的两份模板 `config validate` 退出 0；`ARCH_EXIT=0`
  且 `架构不变量自检全部通过 ✓（137 项）`；`MUTATION_RESIDUE=none`。
- 反向验证成对红/绿（同一日志）：M1（摘掉 `[Depth · Execution]` 行）
  `MUT_M1_EXIT=101`，`depth_execution_model_flags_change_results_and_are_recorded FAILED`
  （`tests/depth_backtest.rs:266`）→ `RESTORE[M1]=identical` → `MUT_M1R_EXIT=0 1 passed`；
  M2（四参数描述子退回只写 `fee_bps`）双红——引擎侧 `MUT_M2_EXIT=101`
  （`order_book_report_is_deterministic_and_carries_unified_metrics FAILED`，
  `orderbook_backtest.rs:1170`）与 CLI 侧 `MUT_M2T_EXIT=101`（`tests/depth_backtest.rs:250`）
  → `RESTORE[M2]=identical` → `MUT_M2R_EXIT=0 1 passed`。
- 撮合参数六 case（`/tmp/qx_q1a_evidence_000305.log` + `/tmp/qx_q1a_ev_table.txt`；同一
  `sma_cross`、同一帧、`fee_bps=5`、`snapshots=80`、`fills=1`）：

| case | result_hash | model_fingerprint | config_hash | fees_raw | final_equity_raw | return_bps |
|---|---|---|---|---|---|---|
| L2 缺省 | `32d8ea011a3ec946` | `31f6292c7d0acf38` | `1cf674577f422c11` | 32411000000 | 100265589000000 | 26 |
| L2 冲击 50bp | `ccc1ea04991c300b` | `230e5a68855baf50` | `caa426169c55e587` | 32573055000 | 99941316945000 | -5 |
| L2 延迟 2 快照 | `71d8e01d91418b7e` | `509ea904c41e2caa` | `77642feff6adfaf7` | 32436000000 | 100215564000000 | 21 |
| L1 缺省 | `32d8ea011a3ec946` | `1f6ef283165e7476` | `6fb000110f8ff645` | 32411000000 | 100265589000000 | 26 |
| L1 冲击 50bp | `ccc1ea04991c300b` | `920aa1e71f2cc44a` | `0c9e21b061cde93b` | 32573055000 | 99941316945000 | -5 |
| L1 延迟 2 快照 | `71d8e01d91418b7e` | `868e9e5f10cf32c4` | `d76c7f9eb2e28c0b` | 32436000000 | 100215564000000 | 21 |

  六行 `model_fingerprint` 两两不同；`input_event_hash` 只在同档内相同（L2 `5e8274a9b7df1398`
  / L1 `35c002e1f9a9d8bc`）。被摘掉的第三个参数实测
  `QUEUE_FLAG_EXIT=2`，stderr 原文 `未知命令或未知参数: error: unexpected argument '--queue-position-bps' found`。
- 13 条内置策略在同一份 70 根 Bar 帧上的成交面（`backtest builtin --config
  deploy/qianxing.runtime.builtin-strategy.example.json`，13 次全部退出 0）：
  `fills=1` 六条（sma_cross、ema_cross、macd、donchian_breakout、momentum、keltner_trend，
  `return_bps=1`；后四条另带 36/40/36 次 `NoShort` 拒单）、`fills=2` 四条（rsi、mean_reversion、
  grid、vwap_reversion，`return_bps=-1`，拒单 12/16/9/16 次）、`fills=0` 三条（bollinger 有 3 次
  `NoShort` 拒单——原因看得见；atr_trend 与 volatility_breakout `rejected_orders=0`，是"没信号"）。
  后两条的零成交可证明是夹具性质而非接线问题：70 根里 `|Δclose| > ATR14` 的根数为 **0/56**
  （最大单根变动 1.0e9，最小 ATR14 为 1.5e9），波动率突破要"当日振幅 > 2×前 14 根均幅"，
  实测最大振幅比 **1.333**。本轮不改它——"零成交但说得清为什么"正是 §14.4 第 2 条要的形状；
  要造成交得改帧形状，那属 Q1b 的数据面。
- 行数棘轮 re-baseline（`maturity/line_budgets.yaml`，本轮唯一增长项及原因）：
  `crates/qx-cli/src/cli.rs` 668 → **675**（两个新旗标的绑定）、
  `crates/qx-strategy/src/builtin.rs` 1009 → **1097**（`ema` 修复 + 增益用例）、
  `crates/qx-xingban/src/orderbook_backtest.rs` 1112 → **1247**（现金门 + 四参数描述子 + 两条用例）。
  快照后 `tools/check_architecture.py` 复跑 137 项全过（收口时再跑一次，仍 `[PASS] 单文件行数预算
  只降不升、无未登记的超大文件`）。
- 产物卫生（本轮实测）：`git ls-files 'deploy/data/**/runs/*'` = **66** 个跟踪产物，其中
  `*.summary.json` 16 份，**含 `rejected_orders` 的 0 份**、含 `latency_snapshots=` 描述子的 **0 份**
  ——全部 blessed 产物都早于本轮的两次 schema 变更；本轮测试另产生 12 个未跟踪 run 文件。
- 诚实性边界：本轮未使用网络、凭据或外部服务，`maturity/capabilities.yaml` 的
  `sandbox_tested` 仍 18 条全为 `false`（实测 `SANDBOX_TESTED_TRUE=0 / SANDBOX_TESTED_TOTAL=18`）。

### 15.4 交给 Q1a 第二批 / Q1b 的事实

1. **Bar 链的高保真面还差两个入口，且各自有前置**：`--fill-model` 只有先让 Bar 链能吃 L1/L2 帧
   才能暴露 `Probabilistic` / `VolumeSensitive`（否则只在 Bar 档模型之间选，等于给两个选项做旗标）；
   `--virtual-trading` 需要 `VirtualTradingConfig` 的 serde 或手写解析器，且 Bar 帧缺标记价，
   资金费与强平在现有输入下无法触发——先补输入面，再谈旗标。
2. **`queue_position_bps` 的前置条件是限价单意图**：内置策略 intent 恒 `limit: None`
   （`builtin.rs:721`）。不给策略侧加限价意图，这个参数永远是装饰；加了之后 §15.1 第 6 条那条
   "断言旗标不存在"的用例必须同时反转，别留下自相矛盾的钉法。
3. **档位可区分性不能靠 `result_hash`**（§15.2 第 5 条）：Q1b 的输入指纹绑定应把 `data_tier`
   纳入"同一份输入只能有一个结果"的判据，并把本轮六 case 的 `model_fingerprint` 表当基线。
4. **16 份 blessed 摘要全部过期**（无 `rejected_orders`、无四参数描述子），A 股目录里三份旧产物
   的 `turnover_raw`/`fees_raw` 还是 0。Q1b 重 bless 时连带 §13.4 第 3 条与 §14.2 第 6 条
   （`deploy/data/**/runs/` 增长、测试文件不受棘轮管）一起裁决，别把"测试跑出来的新产物"
   和"仓库里的示例产物"混成一次提交。

## 16. Q1a 第二批收口记录（2026-09-22，门禁 `/tmp/qx_q1a2b_gate.log` + 命令行证据 `/tmp/qx_q1a2_ev.log`）

§6 的 Q1a 行把第二批写成"Bar 链 `--fill-model` / `--virtual-trading`"。本轮做完 **撮合口径**那一半，
并把它从"旗标"改成"运行时配置字段"（理由见 §16.2 第 1 条）；`--virtual-trading` 仍未做，前置条件没变。

### 16.1 落地内容

1. **单点解析模块 `crates/qx-cli/src/backtests/fill_model.rs`（167 行，新）**：内核 `FillModel`
   家族有五个成员，`BarFillModel` 只承认 Bar 输入撑得起的三个——`next_bar_open`（缺省，下一根
   开盘成交）、`best_price`（当根收盘成交）、`one_tick_slippage`（当根收盘加一档）。
   `bar_fill_model(configured, instrument_spec)` 是四条 Bar 链唯一的取口径入口，返回
   `BarFillModelBinding { fill, name, source }`；`name`/`source` 一起返回是为了让产物能分清
   "没人提这一项"与"配置里声明了同一个模型"（与 `ExecutionCostBinding::source` 同一条理由）。
2. **配置面 `strategy.fill_model`**（`runtime_config/strategy_schema.rs:153`）：
   `#[serde(default, skip_serializing_if = "Option::is_none")]`，与 `cost_rules_path` 同口径——
   省略这一项时序列化字节必须逐字节不变，否则"新增一个配置面"会静默改掉所有历史结果的工件路径。
   三条 Bar 装配链（策略 / 内置 / 多腿的每一条腿）全部读它；深度链不读，因为它不经过 `FillModel`。
3. **一档滑点只认 market spec 的 `price_tick`**（`fill_model.rs:124`）：缺 spec 或 spec 里那一档
   不是正数都拒绝，不猜默认值——`ccxt_market_to_spec` 在字段缺失时会兜成 `1`，让滑点退回那个 `1`
   等于把"没有规格"伪装成"一档就是 1"。
4. **不可达的两个成员按档位拒绝而不是按拼写拒绝**：`probabilistic` → "需要 L1 一档盘口"、
   `volume_sensitive` → "需要 L2/L3 深度盘口"，并说明"Bar 输入只有 OHLCV，装上它等于声称还原了
   并不存在的盘口"，同时点名深度链走 Tick/OrderBook 内核、不经过 `FillModel`。
5. **产物留痕**：策略链摘要新增 `fill_model: {name, source}`（`backtests/artifacts.rs:137`）；
   多腿链不写摘要，改在归因产物里写同一份 JSON（`multi_builtin.rs`）。撮合模型本身另有
   `model_descriptors[0]`（内核把顺序写死为 `[fill, fee, latency, margin]`），本轮把它从"永远
   `NextBarOpen@v1`"变成真话。
6. **多腿口径守卫**（`multi_builtin.rs:214-223`）：逐腿记录 `(name, source)`，两腿不一致即报错，
   一条都没记录也报错——归因里的净成本差绝不能混进两套撮合假设。一档滑点的**大小**允许随各腿
   自己的 `price_tick` 变化，那是标的规格不是口径分叉。
7. **校验与装配同源**：`fill_model_problem()` 是判据，`bar_fill_model()` 用同一张表，
   `config validate` 经 `fill_model_failure()` 只多套一层字段名前缀。因此"validate 放行过的名字，
   装配要么跑得动要么只缺 market spec"。
8. **门禁新增 5 项**（`tools/check_architecture.py` `backtest_assembly_check()`，137 → 142 项）：
   撮合模型只在 `fill_model.rs` 构造且装配字段取自 `self.fill`、三条链的口径全部经
   `bar_fill_model` 解析（计数 ≥3 且 `configured_fill_model` 在位）、schema 声明该字段、
   `config validate` 调用同一判据、模型驱动用例在位。登记表一侧同时受既有的
   "回测主题模块与登记表一致"与 `module_mount_check()` 双向约束。

### 16.2 与计划的偏离与本轮踩到的坑

1. **不做 `--fill-model` 旗标，改做 `strategy.fill_model` 配置字段**。§6 原文是旗标。理由是
   §15.1 第 5 条刚判过的同一类缺陷：命令行旗标不进 `config.fingerprint()`，于是同一份配置 +
   同一份输入会因为敲不敲旗标得到两个结果，而"同一份输入只能有一个结果"的判据看不见这个变量。
   配置字段同时被四条链读到（旗标只能挂在一个入口上），读法也与 `risk_rules`/`cost_rules_path`
   一致。代价：命令行面上没有"这一轮用了什么口径"的显式提示，本轮用 stdout 的
   `[Builtin · Execution]` / `[Multi-leg · Execution]` 两行与产物里的 `fill_model` 补上。
2. **`probabilistic` / `volume_sensitive` 依然不可达，且本轮故意不做成选项**（照 §15.4 第 1 条）。
   现在的路径是让 Bar 链能吃 L1/L2 帧，那是输入面的改动，不是加两个枚举值；本轮把它们做成
   "被拒绝并说明档位"的显式清单，比做成"选了就报错"的旗标值更诚实，因为错误文本本身就是文档。
3. **仓库里没有现货的 CCXT 形状 market spec**。回测入口的 spec 走 `ccxt_market_to_spec`
   （吃 `base`/`price_tick_raw`），而 `deploy/qianxing.binance.spot.spec.json` 是
   `TradingInstrumentSpec` 形状（吃 `instrument`/`price_tick`），传给回测入口退 2 并报
   `CCXT market 缺少 base`。本轮命令行证据只能用 `qianxing.market.binance.btc-swap.spec.json`
   （`market_type=swap`，`price_tick_raw=1000000`）配现货帧——它证明的是"档位取自 spec"，
   不是"这份 spec 适合该标的"。用例侧则自造一份 `price_tick_raw=3_000_000` 的 CCXT 形状夹具，
   刻意避开兜底值 `1` 与仓库里的 `1_000_000`，否则"取自 spec"无从证明。
4. **`BarFillModelBinding` 没有 `Debug`**（装着 `Box<dyn FillModel>`），所以用例里取错误要过一层
   `map(|binding| binding.name).unwrap_err()`。这是刻意保留的形状：与其给一个装 trait object 的
   结构硬凑 `Debug`，不如让用例顺手钉住"错误里带模型名"。
5. **依赖 crate 的测试字面量不在 `cargo check -p qx-cli --all-targets` 的覆盖里**：给
   `StrategyRuntimeConfig` 加一个必填字段后，只有 `cargo test --workspace --all-targets` 才会
   编译 `qx-runtime/src/runtime_config/strategy_tests.rs:259` 的那份完整字面量并报
   `E0063 missing field fill_model`。收口轮跑 `--all-targets` 不是仪式。
6. **rustfmt 与 `<500` 硬门槛的冲突**：`runtime_check.rs`（Phase 4p 拆出的兄弟模块，门槛是严格
   `< 500`，不是 `≤ 500`）原本写成 `failures.extend(fill_model_failure(a, b));`，rustfmt 按
   `fn_call_width=60` 把嵌套调用折成 4 行 → 501 行 → 门禁红。最终形状是先 `let` 再 `extend`
   （2 行），并把"为什么这里不判档位前置"的理由留在 `fill_model_problem()` 的文档注释里而不是
   调用点。同理，`tests/backtest_entries.rs` 里那 5 行断言被搬进新用例文件，才让它停在 499。
7. **上一轮记下的"快照总量"数字本轮不复用**：本轮 `--snapshot` 的实际唯一变化是把
   `crates/qx-xingban/src/orderbook_backtest.rs` 从登记的 1,247 收紧到实测 1,245（该文件本轮
   未被改动，属上一轮遗留的偏高条目）。

### 16.3 验收数字（本轮日志实测）

- 门禁 `/tmp/qx_q1a2b_gate.log`：13 个变异锚点预检全部 `ANCHOR[…]=1`
  （`ANCHOR_PRECHECK_BAD=0`）；`CARGO_CHECK_WORKSPACE_EXIT=0`、`CHECK_ERROR_LINES=0`；
  `FMT_CHECK_EXIT=0`；`CLIPPY_EXIT=0` 且 `CLIPPY_WARNING_LINES=0`；
  `ARCH_BASELINE_EXIT=0` 且 `架构不变量自检全部通过 ✓（142 项）`。
- 全量用例两种口径：`cargo test --workspace --all-targets --no-fail-fast` 不设 `QX_PYTHON` 时
  `FULLTEST_NOENV_EXIT=101`、53 个目标、`NOENV_PASSED=663`，失败的两条是既知的本机项
  `tests::e2e_and_python_contract::rust_invokes_python_{multi_intent_strategy_contract,strategy_jsonl_worker_through_versioned_contract}`
  （PATH `python` 是 WindowsApps 占位桩）；设 `QX_PYTHON` 指向 `python/.venv` 后
  `FULLTEST_QXPYTHON_EXIT=0`、53 个目标、`QXPYTHON_PASSED=665 QXPYTHON_FAILED=0`。**665 对
  §15.3 的 660 是净增 5 条，恰好等于本轮新增用例数，删掉的老用例 0 条**。
- 静态文本反向验证 S1–S7（摘闸门 → `check_architecture.py` 必须变红）：
  `MUTATED_S{1..7}_ARCH_EXIT=1` 逐条成立，`RESTORE[S*]=identical`，
  `RESTORED_S{1..7}_ARCH_EXIT=0`。摘除点分别是：`config validate` 的判据调用改名、schema 字段改名、
  装配字段退回硬编码 `Box::new(NextBarOpenFillModel)`、策略链解析点改名、多腿链解析点改名、
  登记表里删掉 `fill_model` 条目、模型驱动用例改名。
- 行为层反向验证 B1–B6（`cargo test -p qx-cli --bin qx-cli -- backtest_fill_model`）：
  `MUTATED_B{1..6}_EXIT=101` 且每例都是 `4 passed; 1 failed`（不是编译失败），
  `RESTORE[B*]=identical` → `RESTORED_B{1..6}_EXIT=0` `5 passed`。摘除点：`best_price` 退回
  下一根开盘撮合、摘要键改名、`probabilistic` 的档位判据变成"未知名字"、`price_tick` 缺省兜底
  成 1、多腿不再记录腿级口径、内置链无视配置声明。收口时 `RESIDUE[*]=clean` ×13、
  `FINAL_ARCH_EXIT=0`、`FINAL_TARGETED_EXIT=0`（17 passed）。
- 命令行结果可区分性（`/tmp/qx_q1a2_ev.log`，同一份 `qianxing.bar-frame.example.json`、同一份
  `sma_cross` 运行时配置，只改 `strategy.fill_model`）：

| case | `model_descriptors[0]`（截到 params） | result_hash | turnover_raw | fees_raw | final_equity_raw | 摘要 `fill_model` |
|---|---|---|---|---|---|---|
| 未声明该键 | `NextBarOpen@v1[tier=Bar;params=` | `4b128bdee0ce75dd` | 86500000000 | 43250000 | 100018956750000 | `next_bar_open` / `builtin-default` |
| 声明 `next_bar_open` | `NextBarOpen@v1[tier=Bar;params=` | `4b128bdee0ce75dd` | 86500000000 | 43250000 | 100018956750000 | `next_bar_open` / `runtime-config` |
| 声明 `best_price` | `BestPrice@v1[tier=Bar;params=` | `629219d09096e384` | 87500000000 | 43750000 | 100017956250000 | `best_price` / `runtime-config` |
| 声明 `one_tick_slippage` + spec | `OneTickSlippage@v1[tier=Bar;params=tick=1000000` | `96de2663bbc67d3c` | 87501000000 | 43750500 | 100017955249500 | `one_tick_slippage` / `runtime-config` |

  前两行是本轮最重要的一条：**结果逐位相同而来源不同**，正是"默认值不得冒充选择"要的形状；
  后两行相对基线的 `turnover_raw`/`fees_raw`/`final_equity_raw`/`result_hash` 四项全部变化，
  满足 §6 Q1a 行"三项至少一项变"。
- 四条拒绝路径全部退出 2、stderr 说清理由、且**不留下任何摘要产物**
  （`SUMMARIES_AFTER_FAILURES=0`）：`one_tick_slippage` 缺 spec、名字写成 `next-bar-open`
  （报"未知"并给全清单）、`probabilistic`、`volume_sensitive`（后两条报档位）。
  `config validate` 对 `probabilistic` 退 2 并原样印出 `[FAIL] strategy.fill_model 撮合模型
  probabilistic 需要L1 一档盘口…`，与装配侧文本同源；对 `best_price` 与"未声明"都退 0 且
  不提及该字段；"未声明"那份同样退 0 且 0 次提及（`VALIDATE_MENTIONS=0`、`VALIDATE_ABSENT_EXIT=0`）。
- 配置指纹随口径变化（同一份配置只改这一项）：`absent=d2a4c4e335209221`、
  `next_bar_open=813ede121ff6d041`、`best_price=7f606946e554a65c`、
  `one_tick_slippage=463d9c0e60fc0c58`、`probabilistic=ba153af4bd0baf3c` —— 五个值两两不同，
  所以"同一份输入只能有一个结果"的判据这一轮把撮合口径也算进去了。
  注意"未声明"与"声明 `next_bar_open`"指纹不同而结果相同：指纹跟的是**声明**，不是解析后的模型。
- 内置链与多腿链的命令行留痕：`backtest builtin --config … sma_cross` 在缺 spec 时退 2、
  补 spec 后退 0 并打印 `[Builtin · Execution] fill_model=one_tick_slippage source=runtime-config`；
  `backtest multi-builtin --config …` 打印 `[Multi-leg · Execution] fill_model=best_price
  source=runtime-config`，归因产物 `multi-leg-pairs_arbitrage-cc12061d53cc50c2.spread-attribution.json`
  里 `fill_model = {'name': 'best_price', 'source': 'runtime-config'}`；同样配置去掉 spec 退 2。
- 行数棘轮：`SNAPSHOT_EXIT=0`、`BUDGET_DIFF_EXIT=1`，唯一差异是
  `crates/qx-xingban/src/orderbook_backtest.rs: 1247 → 1245`（收紧，非本轮触及；实测 `wc -l` = 1245）。
  **本轮没有增长项**：新文件 `backtests/fill_model.rs` 167 行、用例文件 368 行都在门槛外，
  无需登记。快照 37 条、求和 46,411 行。本轮触及的关键文件实测：
  `runtime_check.rs=499`、`tests/backtest_entries.rs=499`、`backtests/mod.rs=202`、
  `multi_builtin.rs=496`、`single_strategy.rs=375`、`strategy_schema.rs=356`。
- 产物卫生：`git ls-files 'deploy/data/**/runs/*'` = **66**，本轮全量用例跑完后
  `git status --porcelain deploy` 里被改写的跟踪产物 **0 个**（`MODIFIED_DEPLOY=0`）——这就是
  `skip_serializing_if` 那条纪律的全部意义。另有 12 个未跟踪产物是本轮测试新写进
  `deploy/data/*/runs/`（3 个 run 哈希 × 4 文件），仓库里累计 24 个未跟踪；这条脏源留给 Q1b 裁决。
- 诚实性边界：本轮未使用网络、凭据或外部服务，`maturity/capabilities.yaml` 的 `sandbox_tested`
  仍 18 条全为 `false`；`local_backtest` 那条"Bar 链撮合模型与虚拟成交配置都不可达"的限制
  已按事实拆成两条（撮合口径已接线；虚拟成交缺标记价输入，深档模型缺盘口输入）。

### 16.4 交给 Q1a 第三批 / Q1b 的事实

1. **`--virtual-trading` 的阻塞点没动**：`VirtualTradingConfig` 仍无 serde 或手写解析器，Bar 帧
   仍缺标记价，资金费与强平在现有输入下无法触发。要做要先补输入面，别先做旗标。
2. **撮合口径的可达性还差一次输入面改造**：把 `Probabilistic` / `VolumeSensitive` 接进 Bar 链
   需要 Bar 链能吃 L1/L2 帧。届时本轮那张 `BarFillModel::REACHABLE` 表和 `fill_model.rs:56-57`
   两条档位拒绝文案要同时改，§16.1 第 4 条与用例
   `unreachable_or_under_specified_fill_models_fail_closed` 里"断言被拒绝"的那两条必须反转，
   别留下自相矛盾的钉法。
3. **`fill_model` 进的是 `config.fingerprint()`，还没进结果产物的输入指纹绑定**：Q1b 做"数据集指纹
   强制携带"时，应把 `data_tier` 与本轮五值 `config fingerprint` 表一起纳入"同一份输入只能有一个
   结果"的判据（§15.4 第 3 条同源），并考虑摘要里的 `fill_model` 与 `model_descriptors[0]` 谁做
   权威——现在是两处留痕、同源单点产出，Q1b 若只做其中一项的绑定就会漏。
4. **现货回测缺一份 CCXT 形状的 market spec**（§16.2 第 3 条）：要么补一份
   `qianxing.market.binance.btc-spot.spec.json`，要么让回测入口也吃 `TradingInstrumentSpec`
   并明确两形状的优先序。现状是"能跑的示例只有合约规格配现货帧"这种半吊子。
   **→ Q54/Q55 按第三条路关掉**：既不补夹具也不排优先序，而是让两种形状走同一个 loader
   （按 `base_currency` 判定），见 §17.1 第 1 条。
5. **16 份 blessed 摘要全部过期**（无 `rejected_orders`、无四参数描述子，本轮之后还缺
   `fill_model` 键），A 股目录里三份旧产物的 `turnover_raw`/`fees_raw` 仍是 0。Q1b 重 bless 时
   连带 §13.4 第 3 条与 §14.2 第 6 条（`deploy/data/**/runs/` 增长、测试文件不受棘轮管）一起裁决。

## 17. Q54/Q55 收口记录：回测链与 onboarding 的规格同源（2026-09-22，门禁 `/tmp/qx_q55_gate.log`）

本轮**不在 §6 排期内**：它来自"交易链路和回测链路还有哪些潜在问题，全部修复"这条实测指令，
顺带关掉 §16.4 第 4 条（现货回测缺一份 CCXT 形状 market spec）。缺陷不是设计出来的，是照着
`init` 打印的首屏命令一条条真跑出来的——首屏命令自己失败，等于文档里最显眼的那行是假的。

### 17.1 落地内容

1. **产品规格只有一处读法**（新 `crates/qx-cli/src/market_spec.rs`，198 行）：
   `market_spec_from_value` 按 `base_currency` 在场与否判定形状，**不做 try-parse 回落**；
   回测链 `market_spec_with_margin`（`backtests/mod.rs:81`）与 live/paper worker 链共用它。
   原来的缺陷（D1）是回测侧只认 CCXT 归一化形状、worker 侧两种都认，于是 `init` 生成的
   `qianxing.binance.spot.spec.json`（冻结产品规格）交给回测入口必报 `CCXT market 缺少 base`。
2. **缺精度一律拒绝而不是兜底**（D2）：`price_tick_raw` / `qty_step_raw` / `min_qty_raw` 缺失
   即报错并点名缺哪一项、该怎么补；衍生品另要求显式 `max_leverage` 与 `maintenance_margin_bps`。
   这条是 fail-open 的根因：兜底出来的 `1` 与真实声明的 `1` 在产物里长得一模一样，
   且照样通过 `TradingInstrumentSpec::validate()`，事后从产物看不出闸门已经废了。
3. **onboarding 命令面单点化**（新 `crates/qx-cli/src/init_project.rs`，486 行，从
   `config_commands.rs` 逐字拆出）：`init_backtest_step` 只在"文件真的复制进项目 **且** 入口真的
   读得动这份绑定"时才印命令（D3：以前 7 个 profile 印同一行，`base`/`paper` 没绑策略、
   `ccxt`/`multi-venue` 连 BarFrame 都没复制，四种 profile 的首屏命令注定报错）；
   印出来的行情与规格路径一律带上项目目录（D4：裸文件名等于隐含"先 cd 到项目目录"这条
   从未写在屏幕上的前提）。
4. **`strategy init` 生成自包含项目**（D5，本轮实测新发现）：以前把模板里的 `deploy/…` 相对
   路径原样写进 `<输出目录>/runtime.json`，而运行时相对路径**按配置文件所在目录**解析，
   于是刚生成的配置读不到自己的行情夹具，首屏命令也只在仓库根可用。现在与 `init` 共用
   `normalize_init_template_paths` + `copy_init_asset`：键改裸文件名、资产复制进项目、命令带绝对路径。
5. **"哪些 kind 是双腿"只剩一个谓词**：`BuiltinStrategyKind::needs_reference_leg()`（内核），
   消费点为 `qx-strategy/src/builtin.rs` 两处、`runtime_config/strategy_validation.rs` 一处
   （原来各写一遍四元 `matches!` 名单），CLI 侧准入名单 `MULTI_LEG_KINDS` 与它由门禁逐项对齐。
6. **新增门禁 8 项**（148 → 156，见 §17.3 的名单）：规格解析单点、三项精度必须声明、
   两形状按 `base_currency` 判定、回测链/worker 链各自经 loader、两形状同源用例在位、
   双腿 kind 内核谓词≡CLI 名单、init 两个单标的入口按谓词拒绝、拆出模块成对挂载。
7. **新增用例 9 条**：`tests/init_onboarding.rs` 4 条（把 `init` 印出的每条命令原样执行）+
   `tests/market_spec_single_source.rs` 5 条（两形状同源、错标的即拒、缺精度即拒、
   回测链吃冻结形状、回测链吃 CCXT 形状且 `result_hash` 与前者相等）。

### 17.2 与计划的偏离与本轮踩到的坑

1. **单元层直接调 loader 的用例，永远证明不了"调用点没绕过它"**（本轮最重要的一条）。
   首跑的 M1 变异（把 `backtests/mod.rs` 的 loader 调用换回 `from_value::<TradingInstrumentSpec>`）
   让门禁变红，但 `market_spec_single_source` 当时那 4 条**全绿**——仓库那份 binance 夹具本来就
   是冻结形状，绕过 loader 照样读得懂。行为证据当时只是碰巧落在 `init_onboarding` 的 ashare
   profile（它的夹具是 CCXT 形状，报 `missing field instrument`）。修法不是改文案，是补一条
   **把 CCXT 形状写成临时文件喂给真实回测入口**的用例，并要求两种形状 `result_hash` 相等；
   补完 M1 才真的红（日志 `TESTS_M1_EXIT=101`）。
2. **变异必须只摘行为，不能连类型一起摘掉**。M6 首版把那行归一调用注释掉，结果 `copied_assets`
   的唯一类型约束消失，`copy_init_asset(&str)` 把它推成 `BTreeSet<str>`（unsized）→ 变异体
   根本编译不过（`BUILD_M6_EXIT=101`，红的是 rustc 不是断言）。换成等类型的空消费
   `let _: &BTreeSet<String> = &copied_assets;` 后红得干净（`TESTS_M6_EXIT=101`）。
3. **`TaskStop` 杀掉 `bash script > log` 只杀掉外层包装，脚本自己继续跑到 `GATE_DONE`**，
   而它的日志 fd 跟着 `mv` 一起走。于是两份门禁**并发变异同一批文件**：新日志 STAGE 1 报出
   当前源码里根本不存在的 `BTreeSet<str>` 编译错误（假红），旧日志 STAGE 8 又报出对方留下的
   兜底变异。两份日志同时作废。对策已写进脚本，且比单实例锁更强：开跑时给 5 个变异目标拍
   pristine 快照，每次动手前逐个 `cmp`，被第三方动过就 `CONCURRENT_TREE_CHANGE=…` 立刻 `exit 6`
   ——锁不够用，因为锁目录会被手工 `rmdir`（本轮就这么把互斥弄没了）。
4. **备份名不能用 `tr '/' '_'` 反解**：`worker_runtime.rs`、`init_project.rs` 自带下划线，
   残留检查会把"备份文件找不到"当成"无残留"（假绿）。改成 `manifest` 记 `序号<TAB>原路径`。
5. **本轮欠的 fmt 债让第一次整跑白跑**：新写的两个文件有 6 处折行没过 `cargo fmt --check`，
   STAGE 1 就废；`init_project.rs` 格式化后 486 行，距兄弟模块的严格 `< 500` 只剩 13 行余量。
6. **行数棘轮的这一轮是净下行**：`config_commands.rs` 965 → 590、`worker_entry.rs` 拆出
   `market_spec.rs` 后 460 行**退出登记集**（删掉一个登记项）、`builtin.rs` 1097 → 1091。
   登记表对比取的是 **HEAD 提交那份**（本轮 `--snapshot` 在写代码时已执行完，"改动前"只能取
   `git show HEAD:maturity/line_budgets.yaml`），日志里 `HEAD_BUDGET_*` 与 `BUDGET_*` 两组成对。

### 17.3 验收数字（本轮日志实测，`/tmp/qx_q55_gate.log`，2026-09-22）

| 步骤 | 实测 |
| --- | --- |
| STAGE 0 `cargo check --workspace --all-targets` | `WORKSPACE_CHECK_EXIT=0` |
| STAGE 1 `cargo fmt --all --check` / `cargo clippy --workspace --all-targets` | `FMT_EXIT=0`、`CLIPPY_EXIT=0`、`CLIPPY_WARNING_LINES=0` |
| STAGE 2 门禁基线 | `ARCH_baseline_EXIT=0`，`架构不变量自检全部通过 ✓（156 项）`（本轮 +8） |
| STAGE 2 行数棘轮 | `HEAD_BUDGET_ENTRIES=37 / SUM=46411` → `BUDGET_ENTRIES=36 / SUM=45452`（−1 项、−959 行），`BUDGET_DIFF_EXIT=1` 且差异只有下行与删项 |
| STAGE 3 `cargo test -p qx-cli --bin qx-cli` 不设 `QX_PYTHON` | `BIN_TESTS_NO_QX_PYTHON_EXIT=101`，`106 passed; 2 failed`（两条既知的解释器依赖用例） |
| STAGE 3 同一命令设 `QX_PYTHON` | `BIN_TESTS_WITH_QX_PYTHON_EXIT=0`，`108 passed; 0 failed`；`CLI_TEST_FUNCS=112` |
| STAGE 4 反向验证 | 7 个变异逐个红：M1 `ARCH=1`+`TESTS=101`+`TESTS(M1b)=101`、M2 `ARCH=1`+`TESTS=101`、M3 `ARCH=1`、M4/M5/M6 `TESTS=101`、M7 `ARCH=1`；还原侧 `ARCH_*_RESTORED_EXIT=0` / `TESTS_*_RESTORED_EXIT=0` 全绿，`RESTORE[…]=identical` ×7 |
| STAGE 4 并发保护 | `PRISTINE_OK` ×8，无 `CONCURRENT_TREE_CHANGE` |
| STAGE 5 残留 | `BACKUP_COUNT=7`、`MUTATION_RESIDUE=no`、7 条 `RESIDUE_CLEAN` |
| STAGE 6 `QX_PYTHON=… cargo test --workspace --all-targets --no-fail-fast` | `WORKSPACE_TESTS_EXIT=0`、`OK_SUITES=53`、`RUST_PASSED=674`、`RUST_FAILED=0` |
| STAGE 7 跟踪产物 | `MODIFIED_DEPLOY=0`（`DEPLOY_LINES=48` 全是 Q1a 遗留的未跟踪 run 目录） |
| STAGE 8 收口复跑 | `ARCH_final_EXIT=0`、`BUILD_final_EXIT=0`、`FRESHNESS_OK` |

`capabilities.yaml` 的证据路径与 limitations 在门禁整跑**之后**才改（`cli_scenario_init` 的证据
此前指向 `config_commands.rs`，init 代码已被拆走），改完单跑 `check_architecture.py` 三次均
`EXIT=0`、`[FAIL]` 行数为 0。本轮未使用任何外部服务或凭据，`sandbox_tested` 保持 `false`。

### 17.4 交给 Q1b / Q3a / Q4 的事实

1. **"两种形状 `result_hash` 相等"这条断言目前成立，是因为 spec 文件路径没进输入指纹**。
   Q1b 把输入指纹绑进产物时，这里要么改成按内容判等，要么把路径纳入并让该用例显式承认
   "换文件名会换哈希"——现在不改，Q1b 会莫名其妙红一条。
2. **`ccxt` / `multi-venue` profile 仍然不印回测步骤**，因为 `init` 不为它们复制行情帧；
   这条已写进 `cli_scenario_init` 的 limitations（`ccxt_and_multi_venue_profiles_print_no_backtest_step_…`）。
   要让它印，得先有可随仓库发布的行情夹具，属 Q3a 数据面。
3. **"新增第三种 spec 形状"不会被任何现有门禁发现**：本轮的门禁钉的是"解析只在一处 +
   判定按 `base_currency` + 两条链各自委托"，都是文本级不变量。真正扩形状时要新增可达组合
   用例，并按 Q4 的方向把这类静态不变量换成契约用例。
4. **`init_project.rs` 只剩 13 行余量**（486 / 严格 `< 500`）。下一轮往 onboarding 面加东西
   要么先拆第二个兄弟模块，要么按 §16.2 的做法把用例搬进 `crates/*/tests/`（不受棘轮管）。

## 18. Q56 收口记录：提交同步返回的成交纳入同一条精度闸门（2026-09-22，门禁 `/tmp/qx_q56_gate.log`）

同样不在 §6 排期内，是"交易链路还有哪些潜在问题，全部修复"这条实测指令的第二批（第一批 §17 打的是
回测链与首屏命令）。这一批打的是**实盘提交路径**。

### 18.1 缺陷与落地内容

`TradingInstrumentSpec::validate_fill` 此前在全仓只有一个调用点：`ingest_venue_events_with_pipeline`
（用户流 / `sync_order` 回报的归约入口）。但成交进入账本其实有两条入口，另一条是
`PortExecutionService::submit` —— Venue 在**同一次提交回包**里就把成交事实返回来
（Paper 的即时成交、Binance 下单响应里的 `fills`、CCXT `create_order` 后的 `filled_raw`）。
这条入口只做形状校验（`validate_submit_events`：`order_id` 对得上、数量为正、不超剩余量），
不查 tick/step。于是同一条 100.05 的成交（0.1 tick 的产品）：

- 交易所稍后把它推下来 → `ReconcileRequired`，不记账，原因回传 worker；
- 交易所随提交同步返回它 → 直接 append 成 `FillWithSpec` 并入账。

"越界回报只能转待对账"这条纪律只剩半条，且**恰好是资金动得最快的那半条**（同步成交意味着
这一笔已经在远端成立，本地却在没有规格核对的情况下把它写进账本）。

落地：

1. `crates/qx-execution/src/lib.rs:520-538`（新）：提交路径在 `validate_submit_events` 之后、
   append 之前过同一个谓词。规格取用顺序与落库形状一致 —— 回报自带 `FillWithSpec.spec` 优先，
   缺省才回落到服务冻结的 `instrument_spec`（因为下面 `append` 保留的就是回报自带那份，
   校验一个、落库另一个就是假同源）。
2. 越界处置与"未知结果"契约同构：`mark_reconcile(client_id, "submit-fill-out-of-spec")` 后
   直接 `Err` 冒泡，**Accepted 与成交一条都不落**。这是刻意的：提交结果存疑时留下的事实必须
   只有一条待对账，否则下游按"已 Accepted"推进订单状态。
3. `venue_submit_contract.rs`（新用例）：一张 5 形状表 —— 裸 `Fill` 在 tick / 越界、
   `FillWithSpec` 在 tick / 越界、"冻结规格宽（1.0）+ 回报规格严（0.1）"的优先级。
   断言用**回报事实的形状标签序列**（`["accepted","fill-with-spec"]` vs `["reconcile"]`）而不是
   事件数量相等：数量相等可以让"落了两条错的事实"蒙混过关。
4. 门禁 3 项（156 → 159）＋ 用例下限 13 → 14：见 §18.3。原"只有一个谓词、一个调用点"的
   措辞改了：谓词仍然唯一，调用点是**一个恰为两处的集合**（归约入口 + submit），第三处即红。

### 18.2 与计划的偏离与本轮踩到的坑

- **"两处"比"一处"难钉**：`copies` 那张表本来把 `REPORT_FUNNEL_FILE`（就是 `lib.rs`）整个排除在外，
  所以第二次调用长在 `lib.rs` 里是**门禁完全看不见**的。新加的 `funnel.count("validate_fill(") == 2`
  补的正是这个洞，而它自己也得被反向验证 —— G2 往 `mark_reconcile` 前插第三处调用，
  日志点名"3 处（期望 2）"才算咬住。
- **优先级用例的方向容易搞反**：我先写成"冻结宽（1.0）+ 回报严（0.1）+ 100.5 → 期望拒绝"，
  实测**红在期望上**（`embedded-spec-wins-over-lenient-frozen-spec: ["accepted","fill-with-spec"]`
  被断言成拒绝）—— 因为 100.5 落在回报那份 0.1 的 tick 上，**回报自带的严规格才是放行的一方**。
  真正的优先级证明正好是这个"按回报放行"：`embedded.or(frozen)` 与 `frozen.or(embedded)` 只在
  两份规格松紧不同（100.5 对 1.0 越界、对 0.1 在档）时才分岔，所以这一条必须以**放行**为期望，
  把优先级反转成"冻结优先"的 MQ56b 才会红。顺带明确一个产品决定：**回报自带规格优先，
  即使它比冻结规格更宽松**。理由是落库与后续账簿用的就是那一份；"两处都不相容就拒"看着更安全，
  实际是引入第二种口径。
- **`set -u` 下的 `$label_EXIT` 静默炸**：`echo "CARGO_TEST_$label_EXIT=$?"` 被 bash 读成一个
  变量名 `label_EXIT`，整轮在 STAGE 5 首行退出。前四段结果仍写进了日志，但那一份日志不完整，
  修完重跑整轮而不是拼接（同 §17.2 的"日志不可拼接"）。
- **整仓跑"必然红 2 条"要写断言而不是靠人记**：未设 `QX_PYTHON` 时 `cargo test --workspace` 撞
  WindowsApps 占位桩必红 2 条。旧脚本只把这当作已知噪音；本轮改成显式
  `WORKSPACE_FAILED_WITHOUT_QX_PYTHON=2` 且不等 2 就 `exit 5`，再多一条真回归就不可能被判成噪音，
  然后带解释器重跑（qx-cli 单 binary 109 条、含 doc-test 的整仓 675 条）作为绿的真口径。

### 18.3 验收数字（本轮日志实测，`/tmp/qx_q56_gate.log`，2026-09-22）

| 步骤 | 实测 |
| --- | --- |
| STAGE 1 `cargo check --workspace --all-targets` | `STAGE1_CHECK_EXIT=0` |
| STAGE 2 `cargo fmt --all --check` | `STAGE2_FMT_EXIT=0` |
| STAGE 3 `cargo clippy --workspace --all-targets -- -D warnings` | `STAGE3_CLIPPY_EXIT=0` |
| STAGE 4 行数棘轮 | `HEAD_BUDGET_ENTRIES=37 / SUM=46411` → `WORK_BUDGET_ENTRIES=36 / SUM=45471`；`WORKTREE_LIB_RS_LINES=1517`（唯一上调项，已 `--snapshot`） |
| STAGE 5 整仓 `--all-targets`（不设 `QX_PYTHON`） | `CARGO_TEST_workspace_EXIT=101`，`SUM_workspace_passed=167 failed=2`，`WORKSPACE_FAILED_WITHOUT_QX_PYTHON=2` |
| STAGE 5 同一批设 `QX_PYTHON` | `CARGO_TEST_binpy_EXIT=0`、`109 passed; 0 failed`；含 doc-test 的整仓 `CARGO_TEST_WITH_DOC_EXIT=0`、`SUM_with_doc_passed=675 failed=0` |
| STAGE 5 静态门禁 | `STAGE5_ARCH_EXIT=0`、`架构不变量自检全部通过 ✓（159 项）`、`STAGE5_ARCH_FAIL_LINES=0` |
| STAGE 6 cargo 变异 | MQ56a（闸门永不相中）`0 passed; 1 failed` → 还原 `1 passed; 0 failed`；MQ56b（规格优先级反转）同样成对 |
| STAGE 7 静态门禁变异 | G1 删提交侧闸门 → `ARCH_MUTATED_G1_EXIT=1`（同时点亮"提交同步…"与"恰为两处"两项）；G2 插第三处调用 → `=1`（"3 处（期望 2）"）；G3 破坏用例证据 → `=1`；三项 `ARCH_RESTORED_G*_EXIT=0` |
| STAGE 8 残留与收口 | `PRISTINE_OK` ×8、`MUTATION_RESIDUE=none`、`FINAL_ARCH_EXIT=0`、目标用例 `1 passed; 0 failed` |

`MODIFIED_TRACKED_FILES=16` 是本轮与 §17 未提交成果的合计。`capabilities.yaml` 的
`paper_execution` 证据在整跑之后追加，改完单跑 `check_architecture.py` 仍 `EXIT=0` / `159 项`。
本轮未使用任何外部服务或凭据，`sandbox_tested` 全部保持 `false`。

### 18.4 交给 Q2 / Q3a / Q4 的事实

1. **提交侧闸门只在"服务绑了规格"时才生效**：`instrument_spec` 为 `None` 时谓词整段跳过。
   Paper/CCXT/Binance 三条 worker 链都绑（P0a 的 fail-closed 已保证），但任何新入口忘记
   `with_instrument_spec`，这里就又是半条纪律。Q2 实盘化时应把它做成构造期必填而不是可选链。
2. **"成交进入账本只有两条入口"这个前提没有门禁保护**：本轮钉的是"这两处各调一次谓词"，
   新增第三条入口（例如未来的批量回报导入）若不走这两处，就不会被红。真正闭合要靠 Q4
   把它换成"账本写入前必须过一次规格校验"的类型级约束。
3. **`submit` 越界时 Accepted 也不落**：远端事实上订单已被接受且已成交，本地只有一条
   `ReconcileRequired`。这是"未知结果"契约的保守一侧，代价是 worker 必须去远端查单；
   Q2 的对账演练要覆盖这条路径（`mark_reconcile` 的 reason 串 `submit-fill-out-of-spec` 是查询锚点）。
4. **`lib.rs` 已到 1,517 行**（预算同步上调）。它是 V9 §8.2 登记集里最大的执行文件，
   下一次实质性往里加东西前应先拆 `PortExecutionService`（提交/撤单/多腿三段），
   而不是继续吃棘轮上调。

## 19. Q57 收口记录：对冲补偿提交并入同一道闸门，补偿成交带冻结规格落库（2026-09-22，门禁 `/tmp/qx_q57_gate.log`）

"交易链路还有哪些潜在问题，全部修复"这条实测指令的第三批。§18 把提交侧闸门补成"两条入口同纪律"，
本轮顺着同一根线摸到**第三条入口**：多腿对冲补偿的自动提交。

### 19.1 缺陷与落地内容

成交进入账本有三条入口，前两轮各修了一条：

| 入口 | 形状契约 | 精度闸门（`validate_fill`） | 落库规格 |
| --- | --- | --- | --- |
| `ingest_venue_events_with_pipeline`（用户流 / `sync_order`） | 有 | 有（Q5x） | 有 |
| `PortExecutionService::submit`（提交同步回包） | 有 | 有（§18） | 有 |
| `HedgeRecoveryWorker::execute_with_validator`（补偿腿回包） | **无** | **无** | **无** |

补偿腿由恢复扫描自动构造并自己 `router.submit_order(...)`（`build_compensation_order`：
`client_id` 从 group+leg 哈希、`reduce_only=true`、`rule_version=spread-hedge-v1`），拿到回包后
直接 `append_fact`。两个后果：

1. 回包越界或与订单不匹配也照样入账 —— 而且这条路径**没有人复核**，它是故障恢复自己触发的。
2. append 的是裸 `ExecutionEvent::Fill`，下游 `RuntimeExternalEvent::Fill` → `apply_fill` 走
   `FillTerms::LegacyMultiplier(1)`。衍生品补偿腿因此按 `contract_size = 1` 记账：对冲方向是对的，
   名义额、保证金和仓位数量却差一个合约面值。这是本轮里更贵的那半条。

落地（四处）：

1. `crates/qx-execution/src/lib.rs:1075-1133`：把 §18 内联在 `submit` 里的校验抽成共用守卫
   `gate_submit_facts(order, facts, spec)` + `SubmitFactViolation{Shape,Precision}`，
   `reason_tag()` 集中产出 `invalid-submit-response` / `submit-fill-out-of-spec` 两个原因段。
   `submit`（:517）与 worker（:927）各调一次，`lib.rs` 内 `validate_fill(` 调用点仍恰为两处。
2. `HedgeOrderValidator`（:655-666）新增 `instrument_spec(&order) -> Result<Option<Spec>,String>`，
   默认 `Ok(None)`；worker 在 :866-890 于**登记与提交之前**解析规格，`Err` 即拒绝该腿补偿、
   不登记、不提交、不落事实，诊断点名"未解析到产品规格，拒绝自动对冲"。
3. `append_fact`（:984-1009）多了 `spec` 形参：带规格时把 `Fill` 升成 `FillWithSpec` 再入 envelope，
   与 `PortExecutionService::append` 同源。
4. `crates/qx-cli/src/spread.rs:254-299`：补偿 guard 从"只带风控的闭包"升级为 `RecoveryGuard`，
   `instrument_spec` 复用 `load_worker_instrument_spec` 这唯一读法（Q54 的成果）；
   Paper 补偿重放的 ingest（:380-400）据此在带规格时走 `ingest_venue_events_with_spec`。

### 19.2 与计划的偏离与本轮踩到的坑

- **`Ok(None)` 继续、`Err` 拒绝**是刻意的不对称。`None` 只代表"该 worker 未配置规格"，那是现货
  `contract_size=1` 的既有口径（P0a 的 fail-closed 只覆盖三条正经 worker 链，恢复扫描历史上允许
  无规格 worker）；`Err` 代表配置读坏了，此时留一条 `ReconcileRequired` 会把"本地读不到文件"
  伪装成"远端结果未知"，污染对账口径 —— 所以对 `Err` 连登记都不做。
- **闸门位置决定了"未知结果"的形态**：补偿腿的订单必须先生成才能提交，所以越界路径留下的是
  "已登记 + 仅一条 `ReconcileRequired`"，与 §18 的 submit（Accepted 都不落）不同。断言因此看
  事实的**形状标签序列**和 router 调用次数，而不是订单状态。
- **给 trait 加必填方法会打断一条 blanket impl**：`impl<F: Fn(&Order)->Result<(),String>> HedgeOrderValidator for F`
  是给测试用的；新 `instrument_spec` 必须带默认体（`Ok(None)`）才不炸一片。代价是"忘了接规格"
  在类型层完全不可见 —— 所以 G5 变异（把 `RecoveryGuard::instrument_spec` 改成恒 `Ok(None)`）
  必须由门禁点名 `crates/qx-cli/src/spread.rs`，本轮把它加进 `LIVE_REPORT_FILES`。
- **两条 cargo 变异红在同一行不代表只测到一个洞**：MQ57a（worker 传 `None`）与 MQ57b
  （`append_fact` 丢规格）都红在 `recovery_and_replay.rs:402`，容易被误判成重复。前者测"越界没拦"，
  后者测"在 tick 上但落库丢了规格"，只有两条都红才说明闸门与载货是分别咬住的。
- **新用例直接写进 `execution_and_multi_leg.rs` 会被棘轮挡下**：该文件 683 行、未登记，加测试只会
  更红。拆出 `crates/qx-cli/src/tests/paper_hedge_recovery.rs`（含搬迁的原用例）并补 `mod` 挂载；
  顺带把 `EXECUTION_TEST_FLOOR` 从 13 抬到实测 25 —— 旧注释写着"删一条就红"，但 13 早已低于实际
  条目数，那只是一个假装咬得住的地板。

### 19.3 验收数字（本轮日志实测，`/tmp/qx_q57_gate.log`，2026-09-22）

| 步骤 | 实测 |
| --- | --- |
| STAGE 1 `cargo check --workspace --all-targets` | `STAGE1_CHECK_EXIT=0` |
| STAGE 2 `cargo fmt --all --check` | `STAGE2_FMT_EXIT=0` |
| STAGE 3 `cargo clippy --workspace --all-targets -- -D warnings` | `STAGE3_CLIPPY_EXIT=0`、`CLIPPY_WARNING_LINES=0` |
| STAGE 4 行数棘轮 | `HEAD_BUDGET_ENTRIES=37 / SUM=46411` → `WORK_BUDGET_ENTRIES=36 / SUM=45573`；`WORKTREE_LIB_RS_LINES=1619`、`WORKTREE_SPREAD_RS_LINES=486` |
| STAGE 5 整仓 `--all-targets`（不设 `QX_PYTHON`） | `CARGO_TEST_workspace_EXIT=101`，`SUM_workspace_passed=168 failed=2`，`WORKSPACE_FAILED_WITHOUT_QX_PYTHON=2` |
| STAGE 5 同一批设 `QX_PYTHON` | `CARGO_TEST_WORKSPACE_WITH_PYTHON_EXIT=0`、`SUM_workspace_with_python_passed=677 failed=0`、`OK_SUITES=53`；含 doc-test `CARGO_TEST_WITH_DOC_EXIT=0`、`SUM_with_doc_passed=677 failed=0` |
| STAGE 5 静态门禁 | `STAGE5_ARCH_EXIT=0`、`架构不变量自检全部通过 ✓（164 项）`、`STAGE5_ARCH_FAIL_LINES=0` |
| STAGE 6 cargo 变异 | MQ57a（worker 传 `None`）`0 passed; 1 failed` → 还原 `1 passed; 0 failed`；MQ57b（`append_fact` 丢规格）同样成对；MQ57c（`spread.rs` 忽略规格）`1 passed; 1 failed` → 还原 `2 passed; 0 failed` |
| STAGE 7 静态门禁变异 | G1 删 worker 闸门 → `=1`（点亮两项，含"调用点 1 处（期望 2）"）；G2 删 `append_fact` 升级 → `=1`；G3 破坏用例四形状证据 → `=1`；G4 复制精度 reason 段 → `=1`（"精度 2 处（期望 1）"）；G5 `RecoveryGuard::instrument_spec` 恒 `Ok(None)` → `=1`（"未接规格 ['crates/qx-cli/src/spread.rs']"）；五项 `ARCH_RESTORED_G*_EXIT=0` |
| STAGE 8 残留与收口 | `PRISTINE_OK` ×18、`MUTATION_RESIDUE=none`、`FINAL_ARCH_EXIT=0`、`BUILD_final_EXIT=0` |

`MODIFIED_TRACKED_FILES=19` 是本轮与 §17/§18 未提交成果的合计。本轮未使用任何外部服务或凭据，
`sandbox_tested` 全部保持 `false`。

### 19.4 交给后续轮次的事实

1. **"成交入账只有三条入口"现在才是被数过的**：本轮把第三条并入，但入口集合本身仍靠
   `check_architecture.py` 里写死的文件清单（`LIVE_REPORT_FILES` + `lib.rs` 调用点计数）维持。
   Q4 的类型级约束（账本写入前必须持有规格）才是真闭合。
2. **`lib.rs` 连续两轮上调：1,498 → 1,517 → 1,619**。它已是登记集里最大的执行文件，下一轮动它之前
   必须先拆 `PortExecutionService`（提交 / 撤单 / 多腿三段）或把 `gate_submit_facts` 连同
   `SubmitFactViolation` 挪进独立模块，不要再吃第三次上调。
3. **补偿腿的 `instrument_spec` 只有 Paper 链真接了**：`RecoveryGuard` 走 worker 冻结规格；
   实盘恢复扫描若要复用同一 worker，必须同样配置 `instrument_spec_path`，否则衍生品补偿腿仍会
   落到 `Ok(None)` 的乘数 1 口径 —— 这是配置缺口而不是代码缺口，Q2 演练要覆盖。
4. **`Ok(None)` 与 `Err` 的分工要用例锁住**：本轮 4 行场景表覆盖了两条，但"worker 未配规格 +
   衍生品"这种可疑组合在补偿链上仍放行（只过形状闸门）。它应当和 P0a 一样在配置校验期就红。



## 20. Q58/Q59 收口记录：多腿回测的记账前提先被问清，保证金与资金费只向衍生品腿计提（2026-09-22，门禁 `/tmp/qx_q58_gate.log`）

"回测链路还有哪些潜在问题，全部修复"这条实测指令的第一批。清单上 FN1（"衍生品必须给 spec"的守卫是
死代码）与 FN2（缺 spec 时保证金/资金费静默按 0）看着是两条，实际是同一处前提缺失的两半：闸门判不
成立，计费侧就照着"有 spec 即收费、没 spec 即 0"往下走。分两轮修会让闸门与计费口径各漂移一次，
因此合并成一轮，Q59 随本轮一并关闭。

### 20.1 缺陷与落地内容

多腿归因里两笔钱的计提前提：

| 情形 | 本轮之前 | 本轮之后 |
| --- | --- | --- |
| 两条腿都没有 spec、`--funding-bps 25` | 跑通：名义额按现货乘数 1、`funding_raw=0`、`margin_peak_raw=0`，产物仍写 `margin_model=realized-initial-margin-leverage-1`（硬编码） | 撮合前拒，文案点名"哪条腿缺 spec、缺它为什么算不出" |
| 现货腿带了 spec | 按**全额名义额**记保证金，也计提资金费 | 只按衍生品形态计提，现货腿记 0 |
| 两条腿都是现货却要资金费 | 静默记 0 | 拒："资金费只向持仓衍生品合约的一方收取" |
| `--config` 声明 `product=perpetual`、没给主腿 spec | 无人问 | 拒，并把声明的形态回显进文案 |

三处缺陷各自独立成立：

1. **守卫是死代码**：`multi_builtin.rs`（HEAD 版 :133-139）的条件是
   `primary_spec.is_some() && primary_spec_path.is_none()`，而 `primary_spec` 只可能由那个 path
   经 `market_spec_with_margin` 解析出来 —— 永不成立。它同时也在**撮合之后**才被移到产物段附近，
   即使成立也已经太晚。
2. **计费侧不看产品形态**：`multi_leg_leg_margin` 以 `let Some(spec) = leg.spec else` 为入口条件，
   任何带 spec 的腿都按 `spec.notional(standing, price)` 记保证金。Bar 引擎对现货用 `NoMargin`
   （`leverage_tiers` 只存在于衍生品规格），于是归因侧给现货腿又记一笔等于全额现金的保证金 ——
   同一笔现金算两次。`multi_leg_leg_buckets` 的资金费同样只判 `leg.spec` 是否 `Some`。
3. **产物替没发生过的事背书**：stdout 的 Attribution 行把 `margin_model` 写成常量
   `realized-initial-margin-leverage-1`（HEAD :348），读者无法区分"两条腿都是现货"与"衍生品腿
   漏了 spec"这两种 `margin_peak_raw=0`。

落地（五处）：

1. `crates/qx-cli/src/backtests/leg_funding.rs:66-80` `configured_instrument_product`：产品形态的
   第二个事实来源，读 `--config` 的 `strategy.product`，与 `backtest_risk_binding` /
   `configured_fill_model` 同构 —— 给了配置就必须认它声明的口径。
2. `leg_funding.rs:82-130` `multi_leg_spec_guard(legs, funding_bps, declared_product)` 四条判据：
   ①声明衍生品却无主腿 spec → 拒；②`funding_bps != 0` 时任一腿缺 spec → 拒；③两条腿都不是衍生品
   → 拒；④其余放行。①与③是两种互不覆盖的非法组合（前者看声明、后者看规格）。
3. `crates/qx-cli/src/backtests/multi_builtin.rs:88-95`：在两条腿的 spec 解析完、任何
   `run_leg(`（:228/:237 附近）之前调用闸门。
4. `crates/qx-cli/src/multi_leg.rs:191-195`（资金费）与 `:241-244`（保证金）改为
   `leg.spec.filter(|spec| spec.product.is_derivative())`，非衍生品腿记 0；计费侧因此只需要处理
   "形态明确"的腿，"衍生品缺 spec"这一格已由闸门排除。
5. `multi_builtin.rs:153-161` 由两份腿级规格真实推导 `margin_model`（`realized-initial-margin-leverage-1`
   / `none-no-derivative-leg-spec`），:366 打进 stdout，产物 :415-419 新增 `market_specs`（逐腿
   spec 路径，`None` 就是 `null`）与 `margin_model`，`assumptions` 里的资金费/保证金文案同步；
   `cli_help.rs` 的 `multi-builtin` 说明把这条命令面契约写给用户。

### 20.2 与计划的偏离与本轮踩到的坑

- **既有用例把缺陷口径写成了期望值**。六条 `--funding-bps 25` 却完全不给 spec 的回测用例
  （`backtest_cost_provenance` / `backtest_fill_model` / `backtest_risk_provenance`）验的是溯源，
  不是资金费 —— 它们能跑通本身就是 FN1+FN2 的症状，故一律改成 `0` 并在注释里写明为什么不改测资金费。
  判据不是"改口径必然要动用例"，而是每一处改动都要能单独回答"它原来在测什么"。
- **`vetoed_leg_never_pairs_against_a_filled_counterpart` 撞上的是第①条判据**：它的运行时配置
  声明 `product=perpetual`，而 `strategy_validation.rs:336-347` 规定现货只能 Cash/1x/OneWay 且
  `allow_short=true` 直接拒 —— 也就是说这条要卖空的用例**本来就不可能**在合法配置下无规格跑通。
  补两条 swap 规格后其全部硬编码数字（`6000000000`、veto 分档）逐条不变，说明第①条拒的是"没有
  记账前提"而不是"结果变了"。
- **断言写错口径比写错数字更贵**：`totals.margin_peak_raw` 是跨组取**最大值**，逐腿逐行求和是
  **求和**，两者没有相等关系。第一版新用例把 `assert_eq!(margin_peak_raw, {逐腿求和})` 当成"保证金
  只落在衍生品腿"的证据 —— 这条断言在正确实现下也可能不成立。改成三条各自口径明确的判据：现货腿
  两列合计为 0、衍生品腿资金费合计等于产物 `funding_raw`、`margin_model` 与 `market_specs` 来源如实。
- **`{:?}` 打在 `Option<TradingProduct>` 上会得到 `Some(Perpetual)`**：文案与断言都得跟着别扭。
  改成 `if let Some(product) = declared_product.filter(|p| p.is_derivative())` 先绑定再打印，
  文案里就是 `strategy.product=Perpetual`。
- **变异 needle 会被工作树的混合行尾吃掉**（整跑前的 dry-run 抓到，省掉一轮白跑）：Git 触摸过的
  文件是 CRLF，而 Python 以 `newline=""` 重写过的是 LF。`multi_builtin.rs` 是 CRLF，脚本里按 LF
  写的多行 `old` 命中数 0，`swap` 会以退出码 7 中止整轮。修法是 needle 用"该文件自己的行尾"
  `nl.join([...])` 构造。预检方式：把受影响文件 `cp -p` 进 `%TEMP%` 镜像路径逐个跑变异脚本，
  **每跑一个就重新 `cp -p` 还原**（否则前一个脚本的改写会把后一个的锚点吃掉，表现为假的 0 命中）。
- **`cargo test --test <target> <filter>` 过滤的是测试函数名，不是文件名**：脚本初版用
  `multi_leg` 当过滤词，看着合理，但 Q58 四条用例名里没有一条含这个子串 —— 变异段会一个用例都
  跑不到，两侧同绿，是一整段假证据。改成每次跑完整个 `--test multi_leg_attribution`（全 10 条，
  0.3 秒级），红绿对里到底是哪几条红也一并打印出来。
- **MQ58d 的还原行不是绿**：它撞上已知的 Windows 链接锁
  （`error: failed to remove file target\debug\qx-cli.exe`）。该轮的记账方式是"红算 MQ58d，
  绿算同一批 pristine 字节在 MQ58e 还原行与 STAGE 8 的两次 `10 passed`"，而不是把锁错误当回归，
  也不是补一次 `touch` 重跑冒充配对。
- **三处搬家而不是抬一次棘轮**：加完守卫与用例后，红的是 `multi_builtin.rs` 519 > 500、目录模块
  顶层项 9 > 8、两个 ≥500 行未登记项 —— 全是结构线。修法是把 `run_ccxt_builtin_backtest` 搬进
  它自己委派的那条链（`single_strategy.rs`，`BACKTEST_ENTRY_OWNERS` 随之改指并写明理由）、把
  `configured_instrument_product` 搬进同域的 `leg_funding.rs`、把 `backtest_entries.rs` 里三条
  worker 入口用例拆成新主题文件 `src/tests/strategy_worker_entries.rs` 并补 `mod` 挂载。
  行数预算与门槛本轮一个字节都没动（预算合计继续下行，见下表）。

### 20.3 验收数字（本轮日志实测，`/tmp/qx_q58_gate.log`，2026-09-22）

| 步骤 | 实测 |
| --- | --- |
| STAGE 1 `cargo check --workspace --all-targets` | `STAGE1_CHECK_EXIT=0` |
| STAGE 2 `cargo fmt --all --check` | `STAGE2_FMT_EXIT=0` |
| STAGE 3 `cargo clippy --workspace --all-targets -- -D warnings` | `STAGE3_CLIPPY_EXIT=0`、`CLIPPY_WARNING_LINES=0` |
| STAGE 4 行数 | `HEAD_BUDGET_ENTRIES=37 / SUM=46411` → `WORK_BUDGET_ENTRIES=36 / SUM=45573`；`multi_builtin.rs=470`、`single_strategy.rs=428`、`leg_funding.rs=130`、`multi_leg.rs=450`、`backtest_entries.rs=423`、`multi_leg_attribution.rs=717`；`MODIFIED_TRACKED_FILES=28` |
| STAGE 5 整仓 `--all-targets`（不设 `QX_PYTHON`） | `CARGO_TEST_workspace_EXIT=101`、`SUM_workspace_passed=168 failed=2`、`WORKSPACE_FAILED_WITHOUT_QX_PYTHON=2` |
| STAGE 5 同一批设 `QX_PYTHON` | `CARGO_TEST_WORKSPACE_WITH_PYTHON_EXIT=0`、`SUM_workspace_with_python_passed=681 failed=0`、`OK_SUITES=53`；含 doc-test `CARGO_TEST_WITH_DOC_EXIT=0`、`SUM_with_doc_passed=681 failed=0` |
| STAGE 5 静态门禁 | `STAGE5_ARCH_EXIT=0`、`架构不变量自检全部通过 ✓（169 项）`、`STAGE5_ARCH_FAIL_LINES=0`、`STAGE5_ARCH_PASS_LINES=169` |
| STAGE 6 cargo 变异 | MQ58a 红 `:653`（`9 passed; 1 failed`）→ 还原 `10 passed`；MQ58b 红 `:654` → `10 passed`；MQ58c 一次点亮两条（`:577`、`:616`，`8 passed; 2 failed`）→ `10 passed`；MQ58d 红 `:703`（`9 passed; 1 failed`）→ 还原行为链接锁错误，绿由 MQ58e 还原行与 STAGE 8 的 `10 passed` 记账；MQ58e 红在库侧 `src/tests/backtest_entries.rs:141`（`8 passed; 1 failed`）→ 还原 `10 passed` + `9 passed` |
| STAGE 7 静态门禁变异 | G1 改闸门文案 → `=1`（"不再覆盖这两类非法组合"）；G2 删闸门调用 → `=1`（"落到了撮合之后"）；G3 删两处衍生品过滤 → `=1`；G4 让 `margin_model` 不再由规格决定 → `=1`；G5 重命名 Q58 用例 → `=1`；五项 `ARCH_RESTORED_G*_EXIT=0` |
| STAGE 8 残留与收口 | `PRISTINE_OK` ×22、`MUTATION_RESIDUE=none`、`FINAL_ARCH_EXIT=0`（169 项）、集成 `10 passed; 0 failed`、库侧 `9 passed; 0 failed`、`BUILD_final_EXIT=0` |

`MODIFIED_TRACKED_FILES=28` 含 Q54–Q57 尚未提交的成果。本轮未使用任何外部服务或凭据，
`sandbox_tested` 全部保持 `false`。

### 20.4 交给后续轮次的事实

1. **`strategy.product` 只有多腿链认**：`configured_instrument_product` 现在只喂给 `multi-builtin`。
   `backtest builtin` / `ccxt-builtin` 两条链的"声明衍生品却没给 spec"仍未收口，且这两条链还会
   静默丢掉 A 股规则与费用模型 —— 那就是清单上的 Q61，同一族的"给了 `--config` 却不认它"。
2. **两处衍生品回测准入判据还没同源**：单标的链走 `market_spec_with_margin` 里那句
   "衍生品跨语言回测必须提供 market-spec.json"（`single_strategy.rs`），多腿走本轮闸门。
   判据文案、覆盖范围（单标的要求 spec 只为"跨语言"这一条链服务）都不同，Q3a 应合成一份。
3. **`margin_model` 里的 `leverage-1` 是唯一口径**：`multi_leg_leg_margin` 恒按
   `spec.initial_margin(standing, price, 1)` 计提。名字已经把假设写进产物了，但真实杠杆档位
   尚未接线 —— Q2/Q4 接档位时必须同时换掉这个常量串，否则产物会继续替 1x 背书。
4. **`market_specs` 披露的是路径而不是指纹**：本轮把它写成 `Option<PathBuf>` 的
   `display().to_string()`。Q1b 把输入指纹绑进产物后，这里必须换成规格内容的指纹，否则"同一份
   产物"仍然可能出自一个被原地改过的 spec 文件 —— 披露来源不等于披露内容。
5. **"按路径数入口"的门禁继续增加**：本轮 5 项里有 3 项是文本锚点判据（调用点先后、过滤式存在、
   用例函数名齐备）。这类判据对搬家与改名敏感（G5 就是靠改名点红的），Q4 的类型级约束
   （账本写入前必须持有规格）才是真闭合。

## 21. Q60 收口记录：涨跌停的锚换成上一交易日收价，封死的板不再成交（2026-09-22，门禁 `/tmp/qx_q60_gate.log`）

清单上的 FN3（"A 股涨跌停的昨收锚到上一根 Bar，`previous_close_raw` 没有生产者"）。它和 FN1/FN2
一样属于"代码写完了、前提没人守"那一族，但更难看见：缺陷只在**分钟线**上显形，而仓库里所有既有 A 股
用例喂的都是日线 —— 日线上"上一根 Bar"恰好就是昨天，`previous_close` 甚至从来没有被任何一条用例调用过，
于是整批既有用例在全绿状态下把这个口径当了真。

### 21.1 缺陷与落地内容

`AshareRuleConfig::previous_close` 是涨跌停带宽唯一的锚点来源（HEAD 版 `ashare/trading.rs:28-35`：
覆盖表 miss 就取 `bars[index-1].close`），唯一调用点 `backtest.rs:504`。

| 情形 | 本轮之前 | 本轮之后 |
| --- | --- | --- |
| 分钟线，今天 10:00 那根封死在涨停价 11.00（昨收 10.00） | 锚 = 5 分钟前那根的 10.50 ⇒ 板算成 11.55 ⇒ **买入照常成交** | 锚 = 上一交易日收价 10.00 ⇒ 板 11.00 ⇒ 该 Bar 买/卖两侧都被 `set_side_blocks` 挡住，挂单原序退回、下一根再试 |
| 日线 | 上一根即昨日，结果正确 | 同一答案（新锚在日线上退化为一根跨日 Bar） |
| 该标的历史里的第一根 Bar | `checked_sub(1)` 得 `None` ⇒ 不判板 | 找不到上一交易日 ⇒ 同样 `None`，**不拿当天价格当昨收**（那会把板算成 ±0%） |
| 除权除息日 | 只能靠 `previous_close_raw` 覆盖，而它在仓库内**没有生产者**（`deploy/qianxing.ashare.rules.json:17` 就是空的） | 覆盖表仍优先（本轮把它钉进门禁），缺它时明确给的是**不复权**的原始昨收 |

落地三处：

1. **锚的推导**（`ashare/trading.rs:40-51`）：`previous_close_raw.get(&current.ts)` 命中即返回，
   否则 `bars[..index].iter().rev().find(|bar| day_key(bar.ts) != day)` —— 升序行情下这就是上一
   交易日的最后一根。文档里写清了三件事：为什么不判板优于判错板、覆盖表才是除权除息的出口、
   缺它时口径是不复权。
2. **行为用例**（新文件 `crates/qx-xingban/tests/ashare_limit_anchor.rs`，2 条）：正向那条要求
   封板 Bar 零成交且现金额分文不动；反向那条**同一份行情、同一根封板线**，只把锚换成缺陷口径
   （经覆盖表注入 10.50），单就在 11.00 成交。两条一起才排除"靠别的口径兜住"的可能。
   单元侧 `src/ashare/tests.rs:345-377` 逐 Bar 钉锚，并显式验旧口径那条 11.55 的板拦不住同一笔买入。
3. **门禁 4 项**（`check_architecture.py` 的 `ashare_limit_anchor_check`，169 → 173）：锚只有一个
   定义点与一个引擎调用点、覆盖表先于推导且跨日扫描取该日最后一根（`.rev()`）、口径写进代码文档、
   行为用例与单元测试在位。

### 21.2 与计划的偏离与本轮踩到的坑

- **首轮集成用例是红的，红得有道理**：一开始把下单 Bar 放在 10:50 那根、期望它在封板那根被拦，
  结果成交在了 10.50。原因是本引擎 `submit_at(order, bar.ts)` 与 `on_bar(bar, bar.ts)` 在**同一根
  Bar** 上先后调用（`backtest.rs:834/840`）—— 挂单当根就吃本根的开放价，本根的侧向闸门当然也作用于
  它。触发点因此挪到封板那根本身。
- **`Ledger::cash_for` 返回的是 `i128` 而不是 `Money`**：新用例里再 `.raw()` 直接编译不过（`E0599`）。
- **`ashare.rs` 顶在预算上（1202）**：给 `previous_close_raw` 字段补一句说明就越界了。压成一行
  文档而不是抬棘轮 —— 与 Q58 的"搬家而非抬线"同一条路。
- **变异脚本的行尾要跟着被改文件走**：`ashare/trading.rs` 是 LF 而 `backtest.rs` 是 CRLF，
  跨文件拼字面量必须按本文件口径归一（本轮沿用 Q58 的 `nl.join([...])` 写法）。
- **G4 第一版踩了行数棘轮**：想靠"在引擎里加一行注释式的第二次取锚"点亮"调用点唯一"，结果多出的
  一行把 `backtest.rs` 顶到 3312 > 预算 3310，一次点亮两项，红得不干净。改成在原行内重复调用
  （`.or_else(|| rules.previous_close(bars, i))`），行数不变、只点亮那一项。
- **一次探针把改动留在了树里**：预检脚本参数传错（`$0` 当成解释器用），变异应用后没还原，
  靠 `grep` 残留标记 + 按字节删除那一行才清干净。教训是预检也必须"备份 → 应用 → 还原 → `cmp`"
  闭环，不能只靠人工看输出。

### 21.3 验收数字（本轮日志实测，`/tmp/qx_q60_gate.log`，2026-09-22）

| 步骤 | 实测 |
| --- | --- |
| STAGE 1 `cargo check --workspace --all-targets` | `STAGE1_CHECK_EXIT=0` |
| STAGE 2 `cargo fmt --all --check` | `STAGE2_FMT_EXIT=0` |
| STAGE 3 `cargo clippy --workspace --all-targets -- -D warnings` | `STAGE3_CLIPPY_EXIT=0`、`CLIPPY_WARNING_LINES=0` |
| STAGE 4 行数 | `HEAD_BUDGET_ENTRIES=37 / SUM=46411` → `WORK_BUDGET_ENTRIES=36 / SUM=45573`；`trading.rs=168`、`ashare.rs=1202`、`src/ashare/tests.rs=378`、`tests/ashare_limit_anchor.rs=165`、`ashare_pit_asof.rs=201`；`MODIFIED_TRACKED_FILES=31` |
| STAGE 5 `cargo test -p qx-xingban --all-targets` | `CARGO_TEST_xingban_EXIT=0`、`SUM_xingban_passed=87 failed=0`；新集成 `2 passed`、新单元 `1 passed`、邻近 PIT 链 `3 passed` |
| STAGE 5 整仓 `--all-targets`（不设 `QX_PYTHON`） | `CARGO_TEST_workspace_EXIT=101`、`SUM_workspace_passed=168 failed=2`、`WORKSPACE_FAILED_WITHOUT_QX_PYTHON=2` |
| STAGE 5 同一批设 `QX_PYTHON` | `CARGO_TEST_WORKSPACE_WITH_PYTHON_EXIT=0`、`SUM_workspace_with_python_passed=684 failed=0`、`OK_SUITES=54`；含 doc-test `CARGO_TEST_WITH_DOC_EXIT=0`、`SUM_with_doc_passed=684 failed=0` |
| STAGE 5 静态门禁 | `STAGE5_ARCH_EXIT=0`、`架构不变量自检全部通过 ✓（173 项）`、`STAGE5_ARCH_FAIL_LINES=0`、`STAGE5_ARCH_PASS_LINES=173`（Q60 四项逐条 `[PASS]`） |
| STAGE 6 cargo 变异 | MQ60a（锚退回上一根 Bar）红在集成 `:143` + 单元 `:361`；MQ60b（覆盖表失效）红在**另一条**集成 `:163` + 单元 `:377`；MQ60c（去掉 `.rev()`）红在 `:143` + 单元 `:362`；三处还原后均 `2 passed; 0 failed` 与 `1 passed; 0 failed` |
| STAGE 7 静态门禁变异 | G1 删"不复权"文案 → 点亮"锚的口径…写进代码文档"；G2 删 `.rev()`、G3 覆盖表按错 key 取 → 双双点亮"昨收锚按上一交易日推导"；G4 引擎里再抄一份锚 → 点亮"只有一个定义点与一个引擎调用点 — 定义 1 处、引擎调用 2 处"；G5/G6 重命名集成/单元用例 → 点亮"同一根封板线在两种锚下结果不同"。六项 `ARCH_MUTATED_G*_EXIT=1`（各只点亮一项）、`ARCH_RESTORED_G*_EXIT=0` ×6 |
| STAGE 8 残留与收口 | `PRISTINE_OK` ×20、`MUTATION_RESIDUE=none`、`FINAL_ARCH_EXIT=0`（173 项）、集成 `2 passed`、单元 `1 passed`、PIT `3 passed`、`BUILD_final_EXIT=0` |

`MODIFIED_TRACKED_FILES=31` 含 Q54–Q60 尚未提交的成果。本轮未使用任何外部服务或凭据，
`sandbox_tested` 全部保持 `false`。

### 21.4 交给后续轮次的事实

1. **`previous_close_raw` 至今没有生产者**：本轮把"覆盖表优先"钉进了门禁，但仓库内唯一写它的地方
   是 `deploy/qianxing.ashare.rules.json:17` 的空对象。也就是说真实 A 股分钟回复现在锚的是
   **不复权**的原始昨收，除权除息日会算错板。补它需要公司行为数据侧（Q3a 的 PIT 快照链路）把
   调整后昨收写进规则快照，而不是在引擎里现算 —— 分红明细不在 Bar 里。
2. **A 股规则只在 Bar 回测链生效**：`VirtualTradingConfig.ashare_rules` 的消费者只有
   `qx-xingban/src/backtest.rs`；`runtime_check.rs:283` 与 `path_resolution.rs:297` 却会为**任何**
   策略条目检查并解析 `ashare_rules_path`。于是 paper/live 策略配了它 = 静默不生效，与 Q61
   （`backtest builtin` 丢掉同一份规则）是同一族"给了配置却不认它"的口子。
3. **封板不是拒单而是延后**：`set_side_blocks` 让挂单原序退回 `pending`（`venue.rs:117-125`），
   新用例只断言了"这一根不成交、现金不动"。真实 A 股涨停上的限价买单当日不会成交、次日要重挂，
   当前口径把它变成"下一根只要不封死就成交"。这是既有的撮合语义选择，不在本轮范围内，
   但 Q2 的高保真口径清单应显式记一笔。
4. **门禁继续靠文本锚点**：本轮 4 项里 3 项是字面量判据（`.rev()`、`previous_close_raw.get(&current.ts)`、
   用例函数名），G1/G5/G6 就是靠删词与改名点红的。锚的正确性最终要靠"昨收只能由会话边界推导"
   这一层的类型或数据形状（例如规则快照直接带交易日历分桶），Q3a 收。


## 22. Q61 收口记录：A 股段两条 Bar 链必须生效、另两条必须当场拒（2026-09-22，门禁 `/tmp/qx_q61_gate2.log`）

清单上的 FN4（"`backtest builtin` 静默丢掉 A 股规则与费用模型"）。缺陷形状与 Q58/Q59 同族，但触发
方式更日常：`backtest builtin` / `backtest ccxt-builtin` 都收 `--config`，也真读它 —— 唯独
`strategy.ashare_rules_path` / `ashare_actions_path` / `ashare_calendar_path` 三个键没有任何读者。
于是**同一份配置**在 `strategy backtest` 上按 T+1、整手、涨跌停与 A 股佣金跑，换到内置链就变成
"没有交易制度、费率按 maker/taker 兜底"，而两边的 stdout 都长得一样。

### 22.1 落地内容

读法收成一个模块（新文件 `crates/qx-cli/src/backtests/ashare_binding.rs`，118 行），三条规则一次定死：

1. **配对**：只给 actions/calendar 不给 rules —— 直接错。公司行为与交易日历没有规则快照就没有承载对象，
   静默丢掉等于让回测看不见分红与停牌。
2. **fail-closed**：配了快照就必须 `enabled: true`；快照 JSON 非法、`validate()` 不过一律整轮失败，
   绝不退成"按无制度跑"。
3. **两份口径一起换**：`AshareRuleConfig` 与由它构造的 `AShareFeeModel`（佣金/印花税/过户费）同时进装配，
   成本来源改写为 `source=ashare-rules:{路径}`，并新增一行 `[Builtin · A 股规则] t_plus_one=… lot_size=…
   price_tick=… commission_bp=… stamp_duty_bp=… transfer_fee_bp=… path=…` —— 不落摘要文件的入口，
   口径只能靠 stdout 交代。

承不了这个段的两条链**当场拒**（`reject_ashare_rules_config`，文案点名入口与原因并指向
`strategy backtest`）：`backtest book` 的 L1/L2 盘口引擎没有 T+1/整手/涨跌停的挂钩点；
`backtest multi-builtin` 一份 `strategy` 段套不住两条腿各自的交易制度。命令面帮助同步改口：
`backtest builtin` / `ccxt-builtin` 写明 `--config` 读取的是策略口径、`book` / `multi-builtin` 写明
A 股段在此没有落点。

实测（本轮抓取自同一份 `deploy/qianxing.ashare.bar-frame.example.json`，6 根 Bar，`grid`）：

| 数量 | `--config` | fills | rejected_orders | result_hash |
| --- | --- | --- | --- | --- |
| 150 | 不给 | 2 | 0 | `7cdc032c6707ef9b` |
| 150 | 给 | **0** | **2**（`2xA_股买入数量必须是_100_股整手`） | `9904adde193e1237` |
| 100（正好一手） | 不给 | 2 | 0 | `5209a4f61d94d34a`（return_bps=1） |
| 100 | 给 | 2 | 0 | `3f20d31f49abcfd6`（**return_bps=0**） |

150 那两行证明整手闸门真的改变结果；100 那两行把"规则"与"费率"两个变量拆开 —— 成交笔数、
拒单数全同，只有 A 股佣金/印花税/过户费换掉了 maker/taker，收益就从 1bp 归零。缺一行都不算证据。

### 22.2 与计划的偏离与本轮踩到的坑

- **第一轮门禁（`/tmp/qx_q61_gate.log`）有四处变异不咬，全部是真弱点而非脚本噪音**：
  (a) 多行 needle 撞上 CRLF 工作树文件 —— 本仓跟踪文件是 CRLF、新写文件是 LF，字面量对不上就
  `needle 0 次`，`mutate()` 改为按被改文件自身的换行符归一；(b) G5 用"改名"点亮证据项时新名字
  仍含被钉住的子串，换成无关名字；(c) **挂载判定是子串包含**，把 `pub(crate) use ashare_binding::*;`
  注释掉照样绿 —— 已改成整行正则（`mount_pair_present`），Phase 4p/4s 两处同一口径；
  (d) 解析点唯一性的作用域只有 `backtests/` 目录，抄一份到目录外不红 —— 实测全仓 `AshareRuleConfig`
  的反序列化点只有 1 处，故把判据扩到 `crates/*/src/**`（排除用例盘），G1b 才咬得住。
- **顺着 (c) 挖出门禁脚本自己的死代码**：`check_architecture.py` 的存储定义检查 `# (e)`（四个文件
  状态存储各只有一份定义）从 V10 P1c 起就写在 `module_declarations()` 的 `return` 之后 —— 语法合法、
  永不执行。它被搬回 `storage_retry_check` 内立即生效，静态门禁 **182 项 → 186 项**，四项全 PASS，
  说明这半个月里"存储拆分不许多出第二份定义"这条约束实际上无人看守。这是本轮最值钱的副产品：
  门禁数从不变绿本身不能证明任何事，要看每项判据是否真的在路径上。
- `single_strategy.rs` 装下加载器就到 539 行（`OVERSIZED = 500`），因此 A 股读法独立成模块并挂进
  `CLI_BACKTESTS_MODULES` / `BACKTEST_ENTRY_OWNERS` 登记表；`mod.rs` 顶层条目仍在
  `CLI_BACKTESTS_MOUNT_CEILING = 8` 内。
- `strategy backtest` 一侧新加的用例（`strategy_chain_rejects_the_same_broken_pairing`）最初被无关
  错误挡住：该链在走到 A 股绑定之前还要读数据集与产物目录，于是把用例里的 `storage.data_dir` 与
  快照路径一律绝对化（数据集目录改到临时目录，避免用例往 `deploy/data` 写产物）。

### 22.3 验收数字（本轮日志实测，`/tmp/qx_q61_gate2.log`，2026-09-22）

| 步骤 | 实测 |
| --- | --- |
| STAGE1 `cargo check --workspace --all-targets` | `STAGE1_CHECK_EXIT=0` |
| STAGE2 `cargo fmt --all --check` | `STAGE2_FMT_EXIT=0` |
| STAGE3 `cargo clippy -p qx-cli --all-targets` | `CLIPPY_WARNING_LINES=0` |
| STAGE4 `cargo test -p qx-cli --test ashare_builtin_backtest` | `5 passed; 0 failed` |
| STAGE4 `QX_PYTHON=… cargo test -p qx-cli --bins` | `110 passed; 0 failed` |
| STAGE6 静态门禁基线 | `ARCH_FAIL_LINES=0` → `MUT_STATE=clean` |
| STAGE7 整仓设 `QX_PYTHON` | `WORKSPACE_WITH_QX_PYTHON_EXIT=0`、`SUM_with_qxpython_passed=689 failed=0`、`OK_SUITES=75` |
| STAGE7 整仓不设 `QX_PYTHON` | `WORKSPACE_EXIT=101`、`SUM_without_qxpython_passed=168 failed=2`（两条 Python worker 用例，本机必然失败项） |
| STAGE8 收口 | `PRISTINE_OK final`、`FINAL_ARCH_EXIT=0`（186 项）、`BUILD_final_EXIT=0`、`MUTATION_RESIDUE=` 空 |

变异反向验证逐项咬住（行为 6 项 + 静态 9 项，每项各只点亮一处，还原后 `RESTORE_EXACT`）：

| 变异 | 抽掉什么 | 红在 |
| --- | --- | --- |
| MQ61a | 内置链退回不读 A 股段 | 4 条用例失败 |
| MQ61b | 只接规则不接费率（`assembly.fee = binding.fee` 摘掉） | 2 条 |
| MQ61c | 深度档不再拒绝 A 股段 | 2 条 |
| MQ61d | 多腿链不再拒绝 | 2 条 |
| MQ61e | `enabled:false` 不再 fail-closed | 2 条 |
| MQ61f | actions/calendar 配对校验失效（`if false && …`） | 3 条（覆盖两条 Bar 链） |
| G1 / G1b | 解析点复制到同目录另一条链 / 复制到 `dataset_commands.rs`（目录外） | 均点亮"A 股规则快照的 JSON 读法全仓只有一处" |
| G2 / G7 | 内置链不绑定 / 费率件不装配 | 均点亮"两条 Bar 回测链各自绑定 A 股规则与费率" |
| G3 / G4 | 深度档 / 多腿链的拒绝调用消失 | 各点亮对应 `… 承不了 A 股段` |
| G5 / G5b | 任一 Q61 行为用例改名 | 点亮"命令行子进程用例逐条咬住 Q61 口径" |
| G6 | 把 `pub(crate) use ashare_binding::*;` 注释掉 | 点亮"回测主题模块在 backtests/mod.rs 以 mod + pub(crate) use x::* 成对挂载" |
| G8 | （只读探针）存储定义项是否在活路径上 | `STORE_DEFINITION_ITEMS=4`、`G8_STORE_CHECK_LIVE=True`、`G8_DEAD_CODE_IN_HELPER=False` |

本轮未使用任何外部服务或凭据，`sandbox_tested` 全部保持 `false`。

### 22.4 交给后续轮次的事实

1. **Q64：`--config` 的 `strategy.builtin_*` 信号参数在内置链同样没人读**。`run_builtin_backtest`
   走 `BuiltinStrategyConfig::new(...)` 的 5/20/14/100 写死默认，而 `strategy backtest` 走
   `builtin_strategy_config_from_runtime`。同一份 `deploy/qianxing.runtime.ashare.example.json` +
   同一份 6 根 Bar：`strategy backtest` 给 `fills=1 / result_hash=3efef64ed899097a`，
   `backtest builtin ema_cross` 给 `fills=0 / result_hash=216c9e646a571444`。形状与 Q61 完全同族，
   本轮**只修 A 股段**，没有顺手把信号口径一起改 —— 一次性改两类口径会让"哪一处让结果变了"重新变模糊。
2. **Q65：A 股制度在交易链路（paper/live）仍然没有落点**。§21.4 第 2 条的推断本轮坐实为静态事实：
   `AshareRuleConfig` 的反序列化点全仓 1 处（`ashare_binding.rs:38`）、`validate_order` 的调用点
   只有 `qx-xingban/src/backtest.rs`，而 `venue_runtime/*.rs`、`runtime_wiring.rs`、`qx-execution/src/*.rs`
   里 `ashare` / `A 股` 零命中。即 `environment: paper` 的策略段配了 A 股规则 = 静默不生效。
   要么把闸门接进 paper 提交路径（需要 T+1 结算状态），要么按本轮同一口径当场拒 —— 不能再停在"配了不认"。
3. **产物仍未被指纹看住（Q1b）**：`strategy backtest` 这次实跑又往 `deploy/data/qianxing-ashare/runs/`
   落了新文件，工作树里该类未跟踪产物已累计多组；`run_manifest` 的输入指纹绑定仍是 Q1b 的活。
4. **门禁的文本锚点继续扩张**：本轮 6 项新判据里 4 项靠字面量（`binding.fee`、`assembly.fee = `、
   解析式正则、用例函数名）。真正的形状约束是"规则与费率成对进入装配"这一件事，它应该由类型系统表达
   （一个 `AshareBacktestBinding` 只能整体 move、不允许只 move 一半），Q3a/Q11 收。

## 23. Q64 收口记录：四个信号参数五链同源，且复检必须排在印口径之前（2026-09-22，门禁 `/tmp/qx_q64_gate3.log`）

清单上的 FN7（"`backtest builtin` 忽略 `--config` 里的 `strategy.builtin_*` 信号参数"）。形状与 Q61
同族，但更难看：`strategy backtest` 一侧的读法早就存在（`builtin_strategy_config_from_runtime`），
四条内置链却各自走 `BuiltinStrategyConfig::new` 的 **5/20/14/100** 写死默认 —— 不是"没人读"，而是
**同一件事有两个人读、只有一个读到**。本轮把它收成一处，并顺手把 paper/live worker 那条链也接上，
于是"五链同源"第一次成为可门禁的对象。

### 23.1 落地内容

1. **一处赋值**（`crates/qx-cli/src/strategy_binding.rs`）：`builtin_signal_source` 判定本轮到底用不用
   配置（任一 `builtin_*` 键在场即 `source=config`），`apply_builtin_signal_overrides` 把四项并进
   `BuiltinStrategyConfig`。二者只此一份，全仓没有第二处 `config.fast_window = …`。
2. **一处包装**（`single_strategy.rs` 的 `apply_configured_builtin_signal`）：读配置 → 判定来源 →
   覆盖 → **复检** → 返回来源判词。四条链各拿它换掉自己那套写死默认，打印各自的一行
   `[X · Signal] source=… fast_window=… slow_window=… period=… threshold_bps=…`
   （`[Strategy · Signal]` / `[Builtin · Signal]` / `[Depth · Signal]` / `[Multi · Signal]`），
   判词文案由唯一的 `builtin_signal_note` 生成，四条链不许各自拼串。
3. **复检排在印口径之前**：运行时配置的体检（`qx-runtime`）**只在两个窗口键都给齐时**才比较
   `fast < slow`，所以"只写 `builtin_fast_window: 25`、慢窗留默认 20"这类单边覆盖，配置层看不见，
   只有并进默认值之后才成立。这项复检必须在 `apply_configured_builtin_signal` 与
   `builtin_strategy_config_from_runtime` 内做，且必须早于 `[X · Signal]` 那行 —— 先宣告再报错等于
   往 stdout 写了一套并没有跑过的参数。报错文案点名配置路径与覆盖后的四项值：
   `内置策略参数非法：…；<路径> 的 strategy.builtin_* 覆盖后为 fast_window=25 slow_window=20
   period=14 threshold_bps=0，请检查 builtin_fast_window / builtin_slow_window / builtin_period / builtin_threshold_bps`。
4. **交易链路（paper/live）同读**：`invoke_builtin_strategy` 走的就是 `builtin_strategy_config_from_runtime`，
   本轮给它补了进程内用例 `builtin_worker_reads_the_declared_signal_parameters`
   （`crates/qx-cli/src/tests/strategy_worker_entries.rs`），把"配置在 worker 侧也生效"钉住，
   而不是只让回测链同源。
5. **命令面帮助改口**：四个键在 `backtest builtin` / `book` / `multi-builtin` 的说明里写明"在本链生效、
   并会印出生效口径"。

命令行实测（本轮 `/tmp/qx_q64_evidence.log`，`deploy/qianxing.ashare.bar-frame.example.json` 6 根 Bar）：

| 入口 | 参数来源 | fills | return_bps | result_hash |
| --- | --- | --- | --- | --- |
| `backtest builtin ema_cross` | 无 `--config`（`source=builtin-default` 5/20/14/100） | 0 | 0 | `216c9e646a571444` |
| `backtest builtin ema_cross` | 配置 2/3/9/50 | **1** | **2** | `40124915b7750f56` |
| `strategy backtest` 同一份配置 | 配置 2/3/9/50 | 1 | 2 | `4e6f4347b48b4f16` |
| `backtest builtin grid` | 阈值默认 100 | 2 | 1 | `5209a4f61d94d34a` |
| `backtest builtin grid` | 阈值 0 | 1 | 1 | `4238f3e3957593ef` |
| `backtest builtin grid` | 阈值 500 | 0 | 0 | `216c9e646a571444` |
| `backtest book`（L1） | 阈值 100 / 0 | 0 / **1** | 0 / **10** | `753b006dfd086225` / `38da5498c6ed2e01` |
| `backtest multi-builtin` | 阈值 0 / 10000 | 1 / 0 | -113 / 0 | — |

阈值三档给出三个不同 `result_hash`（2/1/0 笔成交）—— 这是"参数是活的"最直接的形状；单边 fast=25 时
三个入口一律 `exit=2` 且 stdout 里没有任何 `[X · Signal]` 行。

### 23.2 与计划的偏离与本轮踩到的坑

本轮跑了三轮门禁，前两轮各暴露一个真弱点，都在第三轮收掉。

- **第一轮 MQ64e 完全不咬（`TESTS_MQ64e_FAILED_LINES=0`）**：变异是"覆盖之后不再复检"，而行为用例全绿。
  根因有两层 —— 运行时体检只比两个都给齐的键；`BuiltinStrategy::new` 后面还会再验一次，于是非法值
  仍会失败，只是**失败在打印口径之后**，且报错不含"是哪份配置的哪四个值"。也就是说：原来的用例
  看着在验复检，实际验的是"总有人兜底"。修法是新用例 `single_sided_override_is_rechecked_before_the_provenance_line`
  （只给 `builtin_fast_window: 25`），同时断言 `exit=2`、报错含覆盖后的 `fast_window=25 slow_window=20`、
  **且 stdout 不见 `[Builtin · Signal]`**，并同样跑 `backtest book` 与 `strategy backtest` 两条链。
  第三轮该变异点亮 1 条用例（`TESTS_MQ64e_FAILED_LINES=2` 行 = 1 个 fail + 汇总行）。
- **第一轮 G6 / G8 各点亮两项（`MUT_STATE=unexpected`）**：判据互相重叠 —— "四条链各印一行"与
  "判词只有一处定义"共用了同一处计数，帮助文本两项同理。拆法是把"标记+调用次数"合成一项、把
  "文案定义唯一"独立成一项（新增 G6b），帮助两项各自独立（新增 G8b）。第三轮 G1…G11 全部单项红。
- **第一轮 G9 假绿（改名不红）**：钉住的是 `fn depth_chain_applies_the_declared_signal`，加后缀改名
  后子串仍在。判据改为钉 `fn NAME(`（带左括号）。这条与 §22.2 (b) 是同一个坑的第二次踩到 —— 凡按
  函数名取证，必须连括号一起钉。
- **第二轮 G2 双红（`MUT_STATE=unexpected`）**：新增的"策略链也要复检"项与"策略链确实问过读点"项
  正则起点相同。把前者锚到 `fn builtin_strategy_config_from_runtime\([^)]*\)[^{]*\{.*?config\.validate\(\)\?;`
  的函数体尾部形状，两项彻底解耦（第三轮 G10 单项红）。
- **示例配置的"假基线"陷阱**：`deploy/qianxing.runtime.builtin-strategy.example.json` 写的四项正好是
  5/20/14/100 —— 拿它当"配置值"做对比，用例在实现完全没读配置时也会通过。paper 用例因此在**克隆出的
  配置**上自设 2/3/9/50，并另设一档"四项全缺 → 必须回到 5/20/14/100"作为回落证据；这个坑写进了用例注释。
- 行数纪律：`single_strategy.rs` 收进包装函数后 484 行（`OVERSIZED = 500`），赋值与判词因此住在
  `strategy_binding.rs`；`strategy_host.rs` 801 行，仍在预算 812 内。静态门禁由 §22.3 记录的 186 项
  增至 **201 项**，新增的 15 项全在 `builtin_signal_check`；本轮内部三轮的增量是 199 → 201
  （"策略链侧也要复检"与"交易链路侧一条进程内用例"，各由 G10 / G11 取证）。

### 23.3 验收数字（本轮日志实测，`/tmp/qx_q64_gate3.log` 与 `/tmp/qx_q64_evidence.log`，2026-09-22）

| 步骤 | 实测 |
| --- | --- |
| STAGE1 `cargo check --workspace --all-targets` | `STAGE1_CHECK_EXIT=0` |
| STAGE2 `cargo fmt --all --check` | `STAGE2_FMT_EXIT=0` |
| STAGE3 `cargo clippy -p qx-cli --all-targets` | `STAGE3_CLIPPY_EXIT=0`、`CLIPPY_WARNING_LINES=0` |
| STAGE4 行数预算 | `single_strategy.rs` 484 / `strategy_host.rs` 801（预算 812）/ `strategy_binding.rs` 249 / `builtin_signal_from_config.rs` 442 |
| STAGE5 行为基线（Q64 七条 + Q61 五条 + 内置链单测） | `builtin_signal_from_config` `7 passed; 0 failed`；`ashare_builtin_backtest`（Q61 回归）`5 passed; 0 failed`；`--bins` `111 passed; 0 failed`；paper 用例单跑 `1 passed; 110 filtered out` |
| STAGE6 行为变异（每次还原后 `*_RESTORED_FAILED_LINES=0`） | MQ64a 红 3 行 = 子进程 2 条（`both_bar_chains_read_the_same_declared_signal`、`single_sided_override_is_rechecked_before_the_provenance_line`）+ worker 1 条；MQ64b 红 5 行 = 4 条；MQ64c 红 3 行 = 2 条；MQ64d 红 2 行 = 1 条；MQ64e 红 2 行 = 1 条；MQ64g 红 2 行 = 子进程 1 条 + worker 侧另红 1 条；MQ64f 红 4 行 = 3 条 |
| STAGE7 静态门禁基线与变异 | 基线 `ARCH_FAIL_LINES=0`、`PASS_LINES=201`；G1…G11 各 `MUT_STATE=red` 且 `WANT_HITS=1`；`MUT_STATE=red` 13 次、`green` 14 次、`unexpected` 0 次、`RESTORE_EXACT` 20 次（无 `RESTORE_DRIFT` 行） |
| STAGE8 全量测试（设/不设 `QX_PYTHON`） | 设：`WORKSPACE_WITH_QX_PYTHON_EXIT=0`、`OK_SUITES=76`、`SUM_with_qxpython_passed=697 failed=0`；不设：`WORKSPACE_EXIT=101`、`SUM_without_qxpython_passed=169 failed=2`（两条 Python worker 用例，本机必然失败项） |
| STAGE9 收口 | `PRISTINE_OK final`、`FINAL_ARCH_EXIT=0`（201 项）、`BUILD_final_EXIT=0`、`MUTATION_RESIDUE=` 空 |

本轮未使用任何外部服务或凭据，`sandbox_tested` 全部保持 `false`。

### 23.4 交给后续轮次的事实

1. **Q65：A 股段在 paper/live 仍然没有落点**（§22.4 第 2 条的口径不变，本轮未动）。可接的是费率
   （paper 提交路径已有 `execution_cost_binding_from_config` 这个费率挂钩点），接不了的是 T+1 ——
   paper 侧没有"今日买入"结算状态。下一轮要么把费率接上并对 T+1 当场拒配，要么整段拒；不能再停在
   "配了不认"。
2. **两条 Bar 链同源不等于同哈希**：同一份配置下 `fills`/`return_bps`/`max_drawdown_bps` 逐项相等，
   `result_hash` 仍分别是 `40124915b7750f56` 与 `4e6f4347b48b4f16`（`result_hash` 是 `event_log.digest()`）。
   用例因此比的是那三个业务量而非哈希。哈希里还进了哪些链上元数据、其中哪些是有意不同，本轮没拆 ——
   归 Q1b 的输入指纹绑定一起收。
3. **单边覆盖是配置校验的系统性盲区**：运行时体检只比"两个都给齐"的键，这一模式在两处以上参数对
   （未来若有 `builtin_*_window` 之外的成对键）都会复发。让体检看"合并后的终值"而不是"输入的键对"
   才是根治，Q3a/Q11 一并收。
4. **门禁锚点继续按函数名取证**：本轮把 15 项里 7 项钉成 `fn NAME(`。经验已两次验证 —— 按名取证必须
   连左括号钉死，否则改名类变异必然假绿。

## 24. Q65 收口记录：A 股段在五个提交入口当场拒，且拒在任何副作用之前（2026-09-22，门禁 `/tmp/qx_q65_gate.log`）

### 24.1 缺陷与取舍

**缺陷形状**：`strategy.ashare_rules_path` 在 Q61 之后有了四条回测链的落点（两条生效、两条拒），
但**提交侧仍无人读它**。`paper-worker` / `paper-submit-order` / `binance-worker` / `binance-submit-order`
/ `ccxt-worker` 都会 `read_runtime_config` 收下整段 A 股配置，然后按 T+0、任意股数、无涨跌停把订单送进
Venue —— 配置文件写着 A 股交易制度，产物与 stdout 上没有任何一处说这段被认了还是被丢了。这是 Q61/Q64
那一族"配置说假话"的另一半。

**为什么不是"把费率接上就算了"**：费率确实接得上（`execution_cost_binding_from_config` 就是现成挂钩点，
佣金/印花税/过户费都能装）。接不上的是制度本身：T+1 需要"今日买入"的结算状态，整手与涨跌停要按板块和
昨收逐单判定，而提交前的账户闸门只做规格、杠杆、名义额与可用保证金预检。只接费率的产物会印着"A 股佣金"，
订单照样当天可卖、照样能报 150 股 —— **比整段不认更容易骗人**。所以本轮口径是整段拒，并把缺的那一项
和能用的替代入口写进拒绝文案（`ASHARE_SUBMIT_GAP` → 文案点名 T+1 与 `strategy backtest`），而不是留一句
"不支持"。

**判据只有一条**：这条路径会不会把一笔**新订单**送进 Venue。`Execution` 与 `SpreadRecovery` 算在内
（V10 Q57 已确认补腿本身就是一笔新订单）；Strategy / MarketData / UserStream / Reconciler / Scheduler /
Api 不算 —— 它们本来就不承诺交易制度，把它们一起拒会让"研究用配置"连启动都做不到，那是用一条正确的规则
制造一个新的假阴性。

### 24.2 落地内容

- **闸门只有一处定义**：`crates/qx-cli/src/runtime_wiring.rs:481` 的
  `reject_ashare_rules_on_submit_path(config_path, worker: Option<&WorkerConfig>, entry)`，配
  `worker_submits_orders`（:469）与文案常量 `ASHARE_SUBMIT_GAP`（:464）。它不自己写文案，而是把 `--config`
  路径转交回测侧那唯一的 `reject_ashare_rules_config`（wiring 内该调用只出现 1 次）。
- **五个提交入口各问一次**：`worker_entry.rs:155`（`binance-worker`）、`worker_entry.rs:253`
  （`ccxt-worker`）、`venue_runtime/paper_worker.rs:100`（`paper-worker`，`paper-e2e` / `paper-check`
  也走这条）、`venue_runtime/binance_submit.rs:106`、`venue_runtime/paper_submit.rs:47`。后两个一次性提交
  入口没有 worker 上下文，`worker` 传 `None` 即按提交对待。
- **顺序就是危害边界**：`run_paper_submit_order` 的闸门是函数**第一条语句**，排在 `read_runtime_config`
  与命令文件读取之前；`binance-submit-order` 排在 worker 合法性校验之后、读命令之前；`ccxt-worker` 排在
  解析 CCXT 配置文件之前（再往后就要起 Python 进程了）。用例对这三处都断言"报错文案里不能出现读命令的
  那一句"，并断言被拒入口没留下 `data` 目录这种半截副作用。
- **帮助文本同步**：`cli_help.rs` 现在六处提到 `strategy.ashare_rules_path`，五条提交入口各自写明
  "配了即在任何副作用之前拒绝（V11 Q65）"，并写明行情/策略角色不受影响。
- **行为用例**：`crates/qx-cli/src/tests/ashare_submit_guard.rs`（198 行 / 5 条，挂载在 `tests/mod.rs`）。
  夹具是 paper 示例配置克隆 + 临时 `data_dir`，唯一差异是**有没有那三个 A 股键**；每条拒绝用例都配一条
  "同一夹具摘键"的基线，证明红的原因是键而不是夹具本身。第五条用例把 2 个提交角色与 6 个非提交角色的
  返回值逐一对着断言 —— 闸门范围的**两个方向**都要咬得住。

### 24.3 本轮踩到与钉住的坑

- **首轮脚本 STAGE2 把 `tools/check_architecture.py` 也喂给了 rustfmt**。改为按扩展名过滤（`RS_FILES`），
  否则格式检查红在夹具上而不是代码上。
- **G5b 的设想后果与实际判据不符**：原以为"摘掉 `mod ashare_submit_guard;` 挂载 → 用例数跌破地板"，
  实测不成立 —— `test_module_shape_check` 的地板是按**磁盘文件**里的 `^#\[test\]$` 计数，与挂载无关。
  真正的后果由 `module_mount_check` 兜住（未挂载即死源码），于是把这条变异的期望改成点亮挂载项，实测单项红。
  教训：**设计变异前先看判据到底数什么**，否则会得到一条"MUT_STATE=green 但其实门禁没漏"的假警报。
- **同理 G6**：帮助文本有六处提到该键，删一行不会跌破 `>= 3` 阈值。变异改成删掉全部含该键的行（6 行），
  才真的证明"帮助不再描述这条拒绝"会被抓到。
- **追加型变异要算行数余量**：`runtime_wiring.rs` 已 490 行，`OVERSIZED = 500` 只剩 10 行余量，探针写成
  单行函数体；多写几行就会连"行数预算"项一起红，`MUT_STATE` 变成 `unexpected`。
- **改名类变异第三次同坑**：本轮把 Q61 的两项也升级为钉 `fn NAME(`（G7 改名即红），§22.2(b)、§23.2 已各
  踩过一次。

### 24.4 验收数字（本轮日志实测，`/tmp/qx_q65_gate.log`，2026-09-22；单轮通过，无第二轮）

| 步骤 | 实测 |
| --- | --- |
| STAGE1 `cargo check --workspace --all-targets` | `STAGE1_CHECK_EXIT=0` |
| STAGE2 触及的 9 个 `.rs` 逐个 `rustfmt --check` | `STAGE2_FMT_EXIT=0` |
| STAGE3 `cargo clippy -p qx-cli --all-targets -- -D warnings` | `STAGE3_CLIPPY_EXIT=0` |
| STAGE4 行数 | `runtime_wiring.rs` 490 / `worker_entry.rs` 463 / `paper_worker.rs` 400 / `paper_submit.rs` 175 / `binance_submit.rs` 399 / `cli_help.rs` 186 / `ashare_submit_guard.rs` 198 / `tests/mod.rs` 264 / `ashare_builtin_backtest.rs` 311 / `check_architecture.py` 2816 |
| STAGE5 行为基线 | Q65 `5 passed; 0 failed`；Q64 回归 `7 passed; 0 failed`；Q61 回归 `5 passed; 0 failed`；`--bins`（设 `QX_PYTHON`）`116 passed; 0 failed` |
| STAGE6 七个行为变异（每个恰好红 1 条用例，`TESTS_MQ65*_FAILED_LINES=2` = 1 条 fail + 汇总行） | MQ65a/c/g → `paper_submit_entry_refuses_the_ashare_section_before_touching_anything`；MQ65b → `paper_worker_entry_refuses_to_start_an_execution_worker_with_ashare_rules`；MQ65d → `ccxt_worker_entry_refuses_the_ashare_section_before_touching_the_ccxt_config`；MQ65e（漏 `SpreadRecovery`）与 MQ65f（扩到所有角色）→ 同一条 `only_roles_that_submit_orders_meet_the_ashare_gate`；七次还原后 `*_RESTORED_FAILED_LINES=0` |
| STAGE7 静态门禁基线与变异 | 基线 `ARCH_FAIL_LINES=0`、`PASS_LINES=210`；G1/G1b/G2/G2b/G3/G4/G5/G5b/G6/G7 十项各 `MUT_STATE=red`、`WANT_HITS=1`、`PASS_LINES=209`；`MUT_STATE=unexpected` 0 次、`RESTORE_EXACT` 17 次、无 `RESTORE_DRIFT`、`CONCURRENT_TREE_CHANGE` 0 次 |
| STAGE8 全量测试（设/不设 `QX_PYTHON`） | 设：`WORKSPACE_WITH_QX_PYTHON_EXIT=0`、`SUM_with_qxpython_passed=702 failed=0`；不设：`WORKSPACE_EXIT=101`、`SUM_without_qxpython_passed=700 failed=2`（`rust_invokes_python_strategy_jsonl_worker_through_versioned_contract`、`rust_invokes_python_multi_intent_strategy_contract`，本机无 `QX_PYTHON` 时的必然失败项） |
| STAGE9 收口 | `PRISTINE_OK final`、`FINAL_ARCH_EXIT=0`（210 项）、`BUILD_final_EXIT=0`、`MUTATION_RESIDUE=` 空 |

本轮未使用任何外部服务或凭据（`binance-submit-order` 用例里放的凭据字段是**环境变量名**，闸门也确实排在
真正读环境变量之前），`sandbox_tested` 全部保持 `false`。

### 24.5 交给后续轮次的事实

1. **要让 paper 真的跑 A 股制度，缺的是结算状态而不是费率读取**。先给 paper 账户加"今日买入"量（T+1 可用量）
   与涨跌停锚，再把 `ASHARE_SUBMIT_GAP` 换成绑定；本轮明确把这条路径留在拒绝侧，`maturity/capabilities.yaml`
   里 `local_backtest` 与 `paper_execution` 的 Q65 条目记的就是这个口径。
2. **门禁项的判据口径要与变异设想对齐**：本轮两条变异（G5b、G6）的初始期望落空，原因都是"我以为这项数的
   是 A，其实它数的是 B"。写变异前先读那一项的实现，比读它的名更快。
3. **Q62 / Q63 / Q1b 口径不变**：`replay_hash ≡ result_hash` 的恒等声明、`instrument_spec_version` 的误标、
   以及两条 Bar 链同源不同哈希，仍按 §23.4 的分工排。

## 25. Q62 收口记录：重放必须是一件真会失败的事，产物不再声明恒等（2026-09-22，门禁 `/tmp/qx_q62_gate3.log`）

### 25.1 缺陷与口径

**缺陷形状**：`ReplayVerifier::rebuild_from(events)` 做的事情是"把传进来的这段事件切片再哈希一遍"，
而 `result_hash` 本来就是同一批事件的哈希 —— 于是 `replay_hash ≡ result_hash` 是**构造出来的恒等式**。
它写在回测摘要里，读起来像"这份结果被独立重算过一次"，实际上这条校验在任何输入下都不可能失败。
取证（本轮实测，非引用旧日志）：`deploy/data` 下**被 git 跟踪**的产物摘要里有 16 份仍带着 `replay_hash`，
逐份读进来比较，16/16 满足 `result_hash == replay_hash` —— 一份都不例外。恒等不是巧合，是构造。

**为什么这不只是"多写了一个没用的键"**：重放是这条链路上唯一的"事实源够不够重建结果"检查。恒等版本对
三类真实故障全部无感 —— ① 事实漏发（账户被就地改了却没发 `LedgerApplied`）、② 事件重复/乱序（同一时间
戳上因果序倒退）、③ 事实与账簿条数错开。这三类恰好是 Q56/Q57/Q40 那一族"入账侧动手脚"的落点。

**新口径**：重放只有一个内核，内核做的是**重新驱动**而不是重新哈希 ——
`sourcing.rs:325 pub fn replay(events) -> (EventLog, Ledger)` 逐条 `append_checked`（重复 seq、乱序、
缺因果字段当场报错）、把 `LedgerApplied` 事实重新 `apply_entry` 入账、最后对整本日志 `validate()`。
`:349 replay_facts` 与 `:359 rebuild_ledger` 都只是它的投影；`:368 verify(events, &Ledger)` 加上报告侧
判据：重放出的账簿条数必须等于本轮账簿条数，否则报 `replayed={}, run={}`。两条结论各自都可能失败，
这是它与 `rebuild_from` 的分别。

### 25.2 落地内容

- **闸门排在结果出口，不在下游补检查**：`backtest.rs:1049` 与 `orderbook_backtest.rs:742` 各有一处
  `ReplayVerifier::verify(report.event_log.events(), &report.ledger)?;`，即引擎的 `run()` 通不过重放就
  不返回报告；错误以 `?;` 向上抛，`.ok()` 吞掉是本轮明确钉死的一种退化形状（见 25.3）。
- **落盘侧再问一次，并且问在写文件之前**：`crates/qx-cli/src/backtests/artifacts.rs:107` 的 verify 排在
  首个 `write_backtest_artifact(`（:164）之前，另加一条 `replay.log_digest != input.result_hash` 即拒绝
  落盘。跑不过重放的结果没有资格往产物里写一个"看起来校验过"的数字。
- **摘要形状改成可核对的三个事实**：`"schema_version": 2`，`replay_hash` 键删除，代之以
  `"replay": { log_digest, events, ledger_entries, run_ledger_entries }`。读者可以自己数事件条数、
  自己比对两侧的账簿条数，而不是接受一个不可证的哈希。
- **命令面同步**：`config_commands.rs:299` 打印 `replay_log_digest=`，`:305` 另起一行打印
  `replay_events=` 与 `replay_ledger_entries=n/m`（重放出的/本轮的），旧的那行"三个哈希"不再是两个恒等哈希。
- **顺带挖出的真缺陷（深度链事实流本来就不可重放）**：订单簿链的 `EventLog` 此前没有任何一处做过规范序
  校验。接上引擎闸门后它当场报错 —— 迟到的成交回报（因果上来自更早一次提交，口径
  `match-previous-submit-current`）与账簿事实排在 `Priority::APPLY`，会让同一时间戳上的策略 `COMMAND`
  在因果序里倒退。修法是把它们挪到 `FEEDBACK` 槽（`orderbook_backtest.rs:434`、`:450`），初始入金排在
  观测起点 `TIMER`（`:401`）。**这条不是为了让测试变绿而改测试**：改的是引擎发出的事实序，改完才谈得上重放。
- **用例侧**：新增 `crates/qx-cli/src/tests/backtest_replay_gate.rs`（162 行 / 3 条，挂在 `tests/mod.rs`）
  —— 一条证明摘要写的就是它查过的三个事实（并断言 `replay_hash` 键已消失），两条负向：账簿凭空多一条
  没有事实支撑的条目、事实流同戳倒退，都拒绝落盘且**不落任何文件**。`backtest.rs:1927` 加
  `assert_replay_matches(&report)` helper（定义 + 两处调用），把原先两处"哈希相等"的自证换成逐条比对
  `replayed.entries() == report.ledger.entries()`（:2616）外加一条"凭空多记"的反向证据；
  `qx-xingban/tests/ashare_pit_asof.rs` 也改走同一个内核。

### 25.3 本轮踩到与钉住的坑

- **恒等指纹会误伤新用例**：门禁里"被证伪的口径不得复活"最初钉的是裸 `replay_hash`，于是我那条
  `assert!(summary.get("replay_hash").is_none())` 自己把这一项点亮了。指纹收窄到 `"replay_hash":`（带冒号，
  即"往产物里写这个键"）后才是它本来要抓的东西。
- **删行型变异不能连带摘掉判断依据**：`log.validate()?;` 在 `sourcing.rs` 出现两次（内核与 `EventLog`
  自身的写路径），整行 needle 不唯一；变异要连着下一行 `Ok((log, ledger))` 才只命中内核那一处。
- **三条变异证明"这条判据只有文本防守"**（如实记录，不当场补强）：MQ62e（内核不再整本 `validate()`）、
  MQ62h（入金从 `TIMER` 换回 `MARKET`）、GA62e（引擎出口 `.ok();` 吞掉失败）在 6 个夹具上**全绿** ——
  逐条 `append_checked` 已经覆盖了这三者能造成的后果，`verify` helper 又是直接调内核而不是走 `run()`。
  也就是说这三处靠行为用例证不出来，只能由门禁钉字符串（`)?;` 传播形状、槽位计数、内核体三步）。
  反面教训：**不要声称行为用例覆盖了它们**。
- **改名类变异第四次同坑**：GA62a 原本改名 `pub fn replay(`，一次点亮两项（唯一性 + 内核体，因为
  `function_body` 找不到定义就返回空串）。改成投影函数 `replay_facts` 改名后恰好一项。本轮首跑
  （`/tmp/qx_q62_gate2.log`）留下的就是这个 `MUT_STATE=unexpected`，修探针后重跑一轮才是 25.4 的数字。
- **中文 needle 要按目标文件的换行符归一**：`mutate` 助手统一做 `\n`→`\r\n`，否则 CRLF 文件上的多行 needle
  命中 0 次，表现为"变异没生效"而不是报错。

### 25.4 验收数字（本轮日志实测，`/tmp/qx_q62_gate3.log`，2026-09-22；`/tmp/qx_q62_gate2.log` 是修探针前的同一轮）

| 步骤 | 实测 |
| --- | --- |
| STAGE1 `cargo check --workspace --all-targets` | `STAGE1_CHECK_EXIT=0` |
| STAGE2 触及的 8 个 `.rs` 逐个 `rustfmt --check` | `STAGE2_FMT_EXIT=0` |
| STAGE3 `cargo clippy -p qx-core -p qx-xingban -p qx-cli --all-targets -- -D warnings` | `STAGE3_CLIPPY_EXIT=0` |
| STAGE4 行数 | `sourcing.rs` 567（新登记进棘轮）/ `backtest.rs` 3341 / `orderbook_backtest.rs` 1256 / `ashare_pit_asof.rs` 208 / `artifacts.rs` 199 / `config_commands.rs` 596（590→596）/ `backtest_replay_gate.rs` 162 / `tests/mod.rs` 265 / `check_architecture.py` 2945 |
| STAGE5 行为基线 | 内核 `replay` 3 条 + `verify_fails` 1 条、Bar 链 1 条、深度链 1 条、`ashare_pit_asof` 3 条、CLI 落盘 3 条，共 12 条全 `0 failed` |
| STAGE6 行为变异 | MQ62a（`verify` 条数判据写成自比）→ 3 条用例红，跨三个 crate：`verify_fails_when_the_run_ledger_carries_a_fact_the_log_does_not`、`runner_preserves_next_bar_causality_and_replay`、`artifacts_refuse_to_land_when_a_ledger_entry_has_no_fact_event`；MQ62b（落盘闸门换成 `replay_facts`）→ 后者单条红；MQ62d（迟到回报放回 `APPLY`）→ `order_book_backtest_is_causal_and_replays_fill` 红；MQ62g（schema 回退 1）→ `summary_publishes_the_replay_facts_it_actually_checked` 红；MQ62e / MQ62h 全绿（见 25.3）；四次还原后 `*_RESTORED` 全 `0 failed` |
| STAGE7 静态门禁基线与变异 | 基线 `ARCH_FAIL_LINES=0`、`PASS_LINES=221`；GA62a/GA62c/GA62e/GA62i/GA62j 五项各 `MUT_STATE=red`、`WANT_HITS=1`、`PASS_LINES=220`；本轮 11 次门禁探针合计 `MUT_STATE=unexpected` 0 次 |
| STAGE8 全量测试（设/不设 `QX_PYTHON`） | 设：`WORKSPACE_WITH_QX_PYTHON_EXIT=0`、`SUM_with_qxpython_passed=708 failed=0`；不设：`WORKSPACE_EXIT=101`、`SUM_without_qxpython_passed=706 failed=2`（`rust_invokes_python_strategy_jsonl_worker_through_versioned_contract`、`rust_invokes_python_multi_intent_strategy_contract`，本机不给解释器时的必然失败项） |
| STAGE9 收口 | `PRISTINE_OK final`、`FINAL_ARCH_EXIT=0`（221 项，210→221）、`BUILD_final_EXIT=0`、`MUTATION_RESIDUE=` 空；全程 `RESTORE_EXACT` 11 次、`RESTORE_DRIFT` 0 次、`CONCURRENT_TREE_CHANGE` 0 次 |
| 用例地板 | `CLI_TEST_FLOOR` 158 → 161（本轮新增 3 条落盘用例，地板按磁盘 `^#\[test\]$` 实测总数抬） |

**本轮同时把一条事实钉死为后续前提**：整套测试跑完后 `git status --porcelain deploy/data` 里被跟踪的
71 个产物**一个都没有被改写**（0 项 `M`），而目录下多出 12 个未跟踪的新产物 —— 也就是说"基准产物"这套
文件现在没有任何一处代码在验证它，重 bless 只是把新数字抄进文件。这条直接排进 Q1b。

### 25.5 交给后续轮次的事实

1. **`assert_replay_matches` 只覆盖"结果侧一致"，不覆盖"输入侧被换掉"**。输入指纹（`input_data_hash`）
   与结果哈希的绑定关系仍是 Q1b 的活：本轮把"事实能否重建账户"问清了，没有回答"这份事实是不是来自被声明
   的那份输入"。
2. **三条只能靠文本钉的判据**（内核整本 `validate`、入金 `TIMER` 槽、引擎出口 `)?;`）如果要它们变成
   行为可证伪的，需要先有一条"从 `run()` 入口走完整链"的用例来喂坏事实流 —— 现在的负向用例都是直接调
   内核。这是有意留下的口径，别把"全绿"当成"没防守"。
3. **产物摘要已经是 schema v2**，任何读 `replay_hash` 的外部脚本都要改读 `replay.log_digest`；本轮不改
   `deploy/` 下的示例产物内容（0 项被跟踪文件改动），只保证新跑的落盘走新形状。（后续更正：Q66 已把摘要升到
   schema v3 并新增 `input` 块，Q72 又抬到 v4 并加 `account` 块，见 §27、§34。）

## 26. Q63 收口记录：规格来源写"实际读成的形状"，不写"有没有传路径"（2026-09-22，门禁 `/tmp/qx_q63_gate2.log`）

清单上的 FN6。本轮不在 §6 排期内，属于"交易链路和回测链路还有哪些潜在问题，全部修复"这条实测指令的回测
链路第六批。

### 26.1 缺陷与口径

**缺陷形状**：`market_spec_with_margin` 交付给链上的第三个值（也就是产物上
`RunManifest.instrument_spec_version` 那一行）问的是"**有没有传 `--market-spec`**"：传了路径就印
`ccxt-market-spec-v1`，没传印 `default-instrument-spec-v1`。于是仓库自己生成的那份**产品规格**
`deploy/qianxing.binance.spot.spec.json` —— 本轮实测它的键里有 `base_currency`，而 A 股那份
`qianxing.ashare.spot.spec.json` 的键是 `base` —— 在产物里被写成 `ccxt-market-spec-v1`。两种形状、两个
解析分支，顶着一个标签。

**为什么这不只是文案错一行**：规格内容不进 `model_fingerprint`，`contract_size` 与三项定点精度也都不进
（Q54/Q55 那两轮把精度闸门接到命令面时确认过），所以这个字段是产物上**唯一**交代"这份规格按哪种形状读的"
的地方。而两种形状的字段口径不同：CCXT 形状读 `*_raw` 且衍生品的杠杆/保证金率必填，产品形状直接反序列化
`TradingInstrumentSpec`。把两种形状声明成同一种，等于声明"这两套记账口径是一回事"，而这正是读者唯一没法
自己核对的一行。

**可达性（本轮实测）**：`crates/qx-cli/src/init_project.rs:186` 的 `init_backtest_step` 给非 A 股 profile
印的首屏命令带的正是 `qianxing.binance.spot.spec.json`（`copied("qianxing.binance.spot.spec.json")`）——
新用户照抄第一条命令就会得到一份"产品形状、CCXT 标签"的产物。

**为什么跟踪产物里看不出来**：`deploy/data` 下带这个字段的 run.json 一共 21 份（本轮逐份读进来），18 份被
跟踪、3 份未跟踪。被跟踪的 3 份 `ccxt-market-spec-v1` 全部来自 A 股那条真 CCXT 形状规格，换口径前后标签
不变；其余 15 份是 `default-instrument-spec-v1`（根本没传规格）。**没有任何一份 blessed 产物走过产品形状**
—— 缺陷是真的，但在示例集里零足迹。这也解释了 STAGE8 的 `MODIFIED_DEPLOY=0`：不是"改得没影响"，是示例
从没跑到这一档。

### 26.2 落地内容

- **形状判定与标签同一个问题**：`crates/qx-cli/src/market_spec.rs:9` `market_value_is_product_spec`（唯一的
  判据：`base_currency` 存在）→ `:18` `market_spec_source_label` 向它问同一个问题，`PRODUCT_MARKET_SPEC_VERSION`
  / `CCXT_MARKET_SPEC_VERSION` 分居两档（`:26`/`:27`/`:28` 三档常量各一名）。`:48` `market_spec_from_value`
  的解析分支复用同一个谓词，所以"标签说哪种形状"与"实际按哪种形状解"不可能各答一份。
- **来源只在 loader 问一次**：`crates/qx-cli/src/backtests/mod.rs:92` 的 `market_spec_with_margin` 返回
  `MarketSpecLoad { spec, margin, source }`（结构体定义在 `market_spec.rs:34`），`:116` 一处
  `market_spec_source_label(&market)`；"没给规格"那一档由 loader 独占（`DEFAULT_INSTRUMENT_SPEC_VERSION`
  在生产代码里只有它和常量定义两处）。链上不得自带兜底标签。
- **写来源的两条链原样搬用**：`single_strategy.rs:25` 与 `:309` 解构、`:162` 以字段简写写进
  `RunManifestIdentity`；`depth.rs:68` 解构、`:182` 同样简写。不落摘要的那条内置链把同一个值**印**出来：
  `single_strategy.rs:431` `[Builtin · Execution] fill_model={} source={} spec_source={}`。
- **多腿链明确不写**：`multi_builtin.rs:77`/`:82` 解构时 `source: _`。它一条命令吃两份帧、两条腿各一个规格
  路径，产物上没有"这一份规格"可标 —— 与其编一个单一来源，不如不写（见 26.5 第 2 条）。
- **用例侧**：`crates/qx-cli/src/tests/market_spec_single_source.rs:189` 的 `published_spec_source` 从落盘摘要
  顺着 `run_manifest` 读回那一行；`:208`（策略链）与 `:253`（深度链）各走**真实入口**跑三档可达输入
  （产品形状 / CCXT 形状 / 不给规格），逐档断言标签，并额外断言"三种输入必须给出三种来源"
  （`published.len(), 3`）—— 两档标签混不出这一条。
- **门禁侧**：`tools/check_architecture.py:4414` 新增 `_identity_block`（按花括号配对截出
  `RunManifestIdentity { … }` 那一块），`:4435` `market_spec_source_check` 静态 8 处 `check(`、其中一条按两条
  链循环执行 → 运行时 9 项：谓词/标签各唯一且标签复用谓词、`base_currency` 判据只在谓词里出现一次、三档常量
  取值互不相同、来源标签只在 loader 单点被问、`DEFAULT_INSTRUMENT_SPEC_VERSION` 由 loader 独占、**两条链的
  identity 块内只可能是 loader 返回的那个变量**（块内必须有简写、且不得出现 `instrument_spec_version:`）、
  builtin 那行的占位符与实参一一对应、两条行为用例按 `fn NAME(` 取证。`CLI_TEST_FLOOR` 161 → 163。

### 26.3 本轮踩到与钉住的坑

- **第一轮那条判据根本不咬，因此第一轮日志整份作废**：项目符号判据写的是"生产代码里出现
  `instrument_spec_version,`"，而这个子串被**解构行** `source: instrument_spec_version,` 先满足了 —— 于是
  MQ63a/MQ63b（"链把来源改写成常量"这两个正是缺陷本体的变异）在静态侧 `ARCH_FAIL_LINES=0`，只有行为用例红。
  判据作用域从"整个文件"收到"identity 块内部"（`_identity_block`）之后，同一对变异各点亮恰好 1 条具名项。
  这是"门禁变异没咬住"的一类新形状：**子串判据会被同名变量的其它出现处满足**，钉字段名要钉到作用域里。
- **`/tmp` 只有 MSYS 侧认识**：本轮的干跑助手用 Windows Python 往 `/tmp/…txt` 写文件直接
  `FileNotFoundError`。Git Bash 只对 **argv 里的路径**做转换，脚本内部的字符串不会 —— 助手内的路径一律写
  `%TEMP%` 绝对路径。
- **GBK 控制台下打印 `✗` 会在中途抛异常**：一个打印门禁输出的助手在**变异生效后**崩在 `print`，等于留下半拉
  变异状态。两次都靠 `cp -p` 镜像还原 + `sha256sum -c` 逐字节核对兜回来；助手输出改成写文件不写控制台。
- **本机 venv 缺 `tzdata`，Python 侧基线一直在缩水（环境事实，不是代码事实）**：`python/pyproject.toml` 声明了
  `tzdata; sys_platform == "win32"`，venv 里没装 → `qianxing_ashare` 一导入就
  `ZoneInfoNotFoundError: Asia/Shanghai`，STAGE9 表现为 `Ran 30 tests … errors=1`，13 条 A 股用例被这一个错误
  挡在门外。装上 `tzdata==2026.4` 后同一命令是 `Ran 43 tests … OK (skipped=1)`、`PY_TESTS_EXIT=0`。**后续轮次
  的 Python 基线按 43 这条读**，别把"30"当成历史基线。
- **`MarketSpecLoad` 挪过一次家**：三返回值元组在 `-D warnings` 下触发 clippy `type_complexity`，改名成结构体
  后又把 `backtests/mod.rs` 的顶层条目顶到 9 个（门禁上限 8，防实现长回 `mod.rs`）。最终结构体住在
  `market_spec.rs`（`ccxt_margin_rule_from_market` 本来就在），loader 留在 `mod.rs`。

### 26.4 验收数字（本轮日志实测，`/tmp/qx_q63_gate2.log`，2026-09-22 17:51:55 → 17:54:01；第一轮日志因 26.3 首条作废）

| 步骤 | 实测 |
| --- | --- |
| STAGE1 `cargo check --workspace --all-targets` | `STAGE1_CHECK_EXIT=0` |
| STAGE2 行数棘轮 | `SNAPSHOT_EXIT=0`（37 个超 500 行文件）、`BUDGET_DIFF_EXIT=0`（本轮无需新登记）、`ARCH_AFTER_SNAPSHOT=0` |
| STAGE3 触及的 5 个 `.rs` 逐个 `rustfmt --check` | `STAGE3_FMT_EXIT=0` |
| STAGE4 `cargo clippy -p qx-cli --all-targets -- -D warnings` | `STAGE4_CLIPPY_EXIT=0`、`CLIPPY_WARNING_LINES=0` |
| 行数 | `market_spec.rs` 233 / `backtests/mod.rs` 220 / `single_strategy.rs` 489 / `depth.rs` 283 / `market_spec_single_source.rs` 305 |
| STAGE5 行为基线 | `market_spec_single_source` `7 passed; 0 failed`、`backtest_entries` `9 passed; 0 failed`、`backtest_fill_model` `5 passed; 0 failed`、`multi_leg_attribution` `10 passed; 0 failed` |
| STAGE6 行为变异 | MQ63a（策略链把来源写成常量）→ 静态 `ARCH_FAIL_LINES=1 / WANT_HITS=1` + 用例红 1 条（`each_market_spec_shape_publishes_its_own_source_label`，`market_spec_single_source.rs:232`）；MQ63b（深度链同一处）→ 静态 1 条 + 用例红 1 条（`the_depth_chain_publishes_the_shape_it_actually_read`，`:289`）；MQ63c（两档形状都答 CCXT = 缺陷本体）→ 静态 `ARCH_FAIL_LINES=0` **全绿**、用例红 2 条（见 26.5 第 1 条）；三次还原 `RESTORE_EXACT` 且 `*_RESTORED` 四组全 `0 failed` |
| STAGE7 静态门禁基线与变异 | 基线 `MUT_STATE=green`、`PASS_LINES=230`；G63a–G63g 七项各 `ARCH_FAIL_LINES=1`、`WANT_HITS=1`、`PASS_LINES=229`、`MUT_STATE=red`；七次 `RESTORE_EXACT`；本轮 10 次探针合计 `MUT_STATE=unexpected` 0 次 |
| STAGE8 全量测试（设/不设 `QX_PYTHON`） | 设：`WORKSPACE_WITH_QX_PYTHON_EXIT=0`、`SUM_with_qxpython_passed=710 failed=0`；不设：`WORKSPACE_EXIT=101`、`SUM_without_qxpython_passed=708 failed=2`（本机不给解释器的两条已知 Python worker 契约项，`WORKSPACE_FAILED_LINES=3`） |
| STAGE8 产物侧 | `MODIFIED_DEPLOY=0`、`UNTRACKED_DEPLOY=12` —— 与 26.1 末段一致：示例集没有一份走过产品形状，因此换口径不改动任何被跟踪产物 |
| STAGE9 Python 侧（装上声明过的 `tzdata` 之后） | `Ran 43 tests`、`OK (skipped=1)`、`PY_TESTS_EXIT=0` |
| STAGE10 收口 | `PRISTINE_OK final`、`FINAL_ARCH_EXIT=0`（**230 项**，221→230）、`BUILD_final_EXIT=0`、`MUTATION_RESIDUE=` 空；全程 `RESTORE_EXACT` 10 次、`RESTORE_DRIFT` 0 次、`CONCURRENT_TREE_CHANGE` 0 次 |
| 用例地板 | `CLI_TEST_FLOOR` 161 → 163（策略链与深度链各一条） |

### 26.5 交给后续轮次的事实

1. **"标签接错档"这一类静态不可证伪**：MQ63c（`PRODUCT` 分支返回 `CCXT` 常量）在 230 项里全绿 —— 只要三档
   各还有名字、标签还在复用谓词，接错档在文本层面完全合法，只有那两条行为用例问得出来。别把它记成"静态项
   覆盖了来源映射"。
2. **多腿链没有来源可写**（`source: _`）：要给它一个诚实的披露，得先把产物形状改成"每腿一行规格来源"，那是
   `RunManifest` 的 schema 变更，本轮不做。
3. **来源对了，指纹仍然不含规格内容**：换一份 `contract_size` 或精度不同的规格进同一条链，`model_fingerprint`
   与 `result_hash` 都不受影响 —— 这一行只交代"按哪种形状读的"，不交代"读到的是哪一份"。它与 Q1b 的输入指纹
   是同一族问题；本轮实测"没有任何 blessed 产物走过产品形状"这条也归 Q1b（示例集覆盖面）。

## 27. Q66 收口记录：产物声明"跑的是哪一份输入"，且这句话必须能被别人重算（2026-09-22，门禁 `/tmp/qx_q66_gate1.log`）

不在 §6 排期内，是 Q1b（可复现性绑定）的第一批：`RunManifest.data_fingerprint` 与摘要里的输入身份。

### 27.1 缺陷与口径

**缺陷形状（两处同一个问题）**：

1. `RunManifest.data_fingerprint` 在没有数据集绑定时回落到 `report.input_data_hash` —— 那是引擎对**自己手里
   那段切片**的自哈希（`qx-guanxing::bars_digest`：条数 + ts/OHLCV，连 instrument 都不含）。于是"输入身份"
   这一行答的是"引擎当时看见一串数字"，不是"这次跑的是哪份数据"。本轮实测：改前 `deploy/data` 里那份 OKX 示例
   run.json 的 `data_fingerprint` 是 `d1eae507fc0a5d80`，与同文件的 `input_event_hash` **一字不差** ——
   一个字段被另一个字段的自哈希冒充着。
2. 摘要（`*.summary.json`）里**根本没有输入身份**：只有那个 `input_data_hash`。而注册表侧其实早就有被复核过的
   身份（`DatasetManifest`：`dataset_id` / `version` / `fingerprint`，由 `JsonDatasetRegistry::verify` 逐字节
   校验过），策略链跑完把它**打印到 stdout 就丢掉**。读者手里那份摘要无法回答"这份产物的输入是不是你说的那份"。

**为什么这不是"多写几行日志"**：产物可复核性的唯一入口是 `qx report <summary.json>`。在 Q66 之前，把摘要对应的
输入文件换一份（同长度、同列名，数值不同），摘要与 `qx report` 都不会有任何反应 —— 声明与实况之间没有任何可
重算的绑定。这也正是 Q1b 立条的原因。

**口径**：声明必须是**读出来的**而不是传进来的。三条会落产物的链（策略链 / 内置链 / 深度链）从 `artifacts.rs`
的**唯一读点**取帧，同一个读点被 `qx report` 再读一遍来重算身份，因此"入口看见的形状"与"复核看见的形状"
不可能各答一份（`check_architecture.py` 里 `chain_parses == 0` 那条钉的就是这件事）。

### 27.2 落地内容

- **单一读点 + 身份推导**（`crates/qx-cli/src/backtests/artifacts.rs`，378 行）：
  `:28` `read_bar_frame_for_backtest` / `:66` `read_depth_frame_for_backtest` 是全仓唯一两处解帧；
  `:37` `barframe_dataset_identity` 把帧喂回 `qx-data` 的 `JsonBarFrameProvider`（`load_bars_with_manifest`）
  拿回**被注册表复核过**的 `DatasetManifest`，并逐列比对 Provider 列式与 BarFrame 列式（不一致即
  "qx-data Provider 与 BarFrame 列式输入不一致，拒绝开始回测"）；`:73` / `:88` 两档 provenance；
  `:102` `input_provenance_json` 是摘要里 `input` 块的唯一写法；`:175` / `:177` 两个版本号常量各一档
  （`barframe-json-v1` / `depth-frame-v1`）。
- **摘要 schema v3 落 `input` 块**（Q72 已抬到 v4，行号随之移到 `:301`）：那里起的 summary 字面量带 `"schema_version": 3` 与
  `"input": input_provenance_json(&input.input)`。`:14` `BacktestInputProvenance { kind, path, dataset_id,
  dataset_version, fingerprint }` 由 `BacktestArtifacts.input` 承载，策略链与深度链各在调用点填一次。
- **报告出口会拒绝**：`:117` `recompute_declared_backtest_input` 先整体读完声明块（`kind` 不认→"未知的回测输入
  种类"，缺键→"回测摘要的 input 块缺 {name}"），再按 `kind` 走同一个读点重算，`dataset_id` /
  `dataset_version` / `fingerprint` 三项逐一对比，任一不符即 "回测产物声明的输入与实况不符: …"。
  `crates/qx-cli/src/config_commands.rs:258` 以 `?` 传播，`:263` JSON 侧输出 `input_check`
  （`verified` / `not_declared`），`:325`/`:328` 文本侧分别印 `input_verified=<路径> …` 与
  "该摘要没有 input 块，输入身份未经核对"。
- **RunManifest 回落换成被复核过的身份**：`single_strategy.rs` 的
  `data_fingerprint` 在没有数据集绑定时取 `format!("{}:{}", input.kind, input.fingerprint)`，
  不再取引擎自哈希；`format!("{:016x}", report.input_data_hash)` 这一形状在全仓清零（门禁钉住 0 处）。
- **用例侧**：`crates/qx-cli/src/tests/backtest_input_provenance.rs`（286 行）6 条，全部走**真实入口** ——
  声明等于重算、跑完换内容（篡改首值）即拒、输入文件消失即拒、同一注册身份不容两种内容、深度链同形、
  旧 schema（无 `input` 块）只能说"未声明"而不是"已核对"。`backtest_replay_gate.rs` 同步把摘要形状钉到当时的版本——Q72 抬到 v4 之后它钉的是 4（`:110`）。
- **门禁侧**：`tools/check_architecture.py:1628` `input_provenance_check()`（`:4624` 挂载）9 项 ——
  `input` 块只由 `input_provenance_json` 写一次且 schema 为 3；三条链 `chain_parses == 0`；
  `barframe_dataset_identity` 同时具备 Provider 读取与列式一致性；recompute 的函数体形状（三个键名 +
  两条错误文本）；`run_report` 以 `?` 传播且区分未声明；回落口径不再是引擎自哈希且旧形状全仓 0 处；
  两档版本号互不相同、字面量只出现在 `artifacts.rs`；6 条用例按 `fn NAME(` 取证。
  另有 6 条 `BACKTEST_ENTRY_OWNERS` 归属项把上述六个函数钉在 `artifacts.rs`。静态门禁 230 → **245 项**，
  `CLI_TEST_FLOOR` 163 → 169。

### 27.3 本轮踩到与钉住的坑

- **STAGE8 先红了三条集成用例，红因不是断言而是本地产物**：`builtin_signal_from_config.rs:292`、
  `fast_backtest_manifest.rs:65` / `:96` 全部报 "同一回测回测摘要路径已存在不同内容"。逐份查过：撞名的 3 份
  summary 是本轮改动**之前**（16:40:49）写下的 v2 产物，且它们的路径里那段 16 位摘要**没变**（那三条链用的是
  数据集绑定，`data_fingerprint` 走 `dataset-bundle:…`，Q66 只换了无绑定时的回落），于是 v3 内容撞在 v2 路径上，
  被不可变产物闸门正确拒掉。把它们整体移到 `%TEMP%\qx_q66_stale_v2_artifacts\`（12 个文件，未跟踪）后重跑：
  `builtin_signal_from_config` `7 passed; 0 failed`、`fast_backtest_manifest` `5 passed; 0 failed`，整树
  `cargo test --workspace`（设 `QX_PYTHON`）从 `713 passed / 3 failed` 变 `716 passed / 0 failed`
  （`/tmp/qx_q66_workspace_rerun.log`）。**闸门没做错**，是本地示例集需要随 schema 重 bless —— 这条记为 27.5 第 1 条。
- **对照证据**：同一条链的 OKX 示例（无数据集绑定、走回落）本轮文件名**换了**：改前
  `…OKX-4bdd4398c79c6138.*`（`data_fingerprint` = 引擎自哈希 `d1eae507fc0a5d80`）→ 改后
  `…OKX-5c38b56746b08948.*`（`data_fingerprint` = `barframe:673fd995f8625b97`）。回落换成被复核过的身份
  真的改变了产物路径，而不仅是改了文案。
- **`MQ66b` 是本轮唯一"静态全绿、只有用例红"的行为变异**（把重算改成自比，声明与实况永不比较）：
  `ARCH_FAIL_LINES=0 / PASS_LINES=245` 全绿、用例红 2 条。与 Q63 的 MQ63c 同一族 —— "比较被写成恒等"在文本
  层面完全合法。别把它记成静态项覆盖了比较语义。
- **用例侧的隐藏前提**：`isolated_backtest_runtime` 总会写 `dataset_bundle_path`，而回落分支正是本轮的缺陷本体，
  因此助手 `runtime_without_bundle` 会把 runtime JSON 重写一次清掉该字段。没有这一步，6 条用例里最关键的
  "回落用被复核过的身份"根本不会被触发。

### 27.4 验收数字（`/tmp/qx_q66_gate1.log`，2026-09-22 18:41:59 → 18:45:08）

| 步骤 | 实测 |
| --- | --- |
| STAGE1 `cargo check --workspace --all-targets` | `STAGE1_CHECK_EXIT=0` |
| STAGE2 行数棘轮 | `SNAPSHOT_EXIT=0`（37 个超 500 行文件）、`BUDGET_DIFF_EXIT=0`、`ARCH_AFTER_SNAPSHOT=0`（245 项） |
| STAGE3 触及 Rust 文件 `rustfmt --check` | `STAGE3_FMT_EXIT=0` |
| STAGE4 `cargo clippy -p qx-cli --all-targets -- -D warnings` | `STAGE4_CLIPPY_EXIT=0`、`CLIPPY_WARNING_LINES=0` |
| STAGE5 行为基线 | `backtest_input_provenance` 6/0、`backtest_replay_gate` 3/0、`backtest_entries` 9/0、`backtest_risk_provenance` 1/0、`market_spec_single_source` 7/0、`depth_backtest` 3/0、`multi_leg_attribution` 10/0 |
| STAGE6 行为变异（5 个） | MQ66a（回落退回引擎自哈希 = 缺陷本体）→ 静态 1 条具名红 + 用例红 1；MQ66b（重算改成自比）→ 静态 `ARCH_FAIL_LINES=0` **全绿** + 用例红 2；MQ66c（报告吞掉复核失败）→ 静态 1 + 用例红 2；MQ66d（链上再解一遍帧）→ 静态 1 + 用例全绿（该项本就是静态项）；MQ66e（摘要不写 `input` 块）→ 静态 1 + 用例红 4 + `backtest_replay_gate` 红 1。五次还原均 `RESTORE_EXACT`，`*_RESTORED` 七组全部 `0 failed` |
| STAGE7 静态变异（4 个）+ 基线 | 基线 `MUT_STATE=green`、`PASS_LINES=245`；G66a（改用例名）、G66b（摘掉列式一致性判据）、G66c（两档共用版本号）、G66d（身份写点在链上再定义）各 `ARCH_FAIL_LINES=1 / WANT_HITS=1 / PASS_LINES=244 / MUT_STATE=red`；四次 `RESTORE_EXACT`。9 次探针 `MUT_STATE=unexpected` 0 次 |
| STAGE8 全量测试 | 设 `QX_PYTHON`：`SUM_with_qxpython_passed=713 failed=3`、`WORKSPACE_WITH_QX_PYTHON_EXIT=101`；不设：`SUM_without_qxpython_passed=711 failed=5`、`WORKSPACE_FAILED_LINES=8`（多出的 2 条是本机不给解释器的已知 Python worker 契约项）。三条失败见 27.3 首条，清理本地产物后复跑 `RERUN_EXIT=0`、`SUM_rerun_passed=716 failed=0`、`RERUN_FAILED_LINES=0`（`/tmp/qx_q66_workspace_rerun.log`） |
| STAGE8 产物侧 | `MODIFIED_DEPLOY=0`、`UNTRACKED_DEPLOY=16` —— 被跟踪的 16 份摘要全是 `schema_version: 1`（Q62 之前的世代），当前代码在那些路径上根本不再落盘，所以"没改被跟踪产物"只说明 bless 集早已够不着，不说明改动无影响 |
| STAGE9 Python 侧 | `Ran 43 tests`、`PY_TESTS_EXIT=0` |
| STAGE10 收口 | `PRISTINE_OK final`、`FINAL_ARCH_EXIT=0`（245 项，230→245）、`BUILD_final_EXIT=0`、`MUTATION_RESIDUE=` 空；全程 `CONCURRENT_TREE_CHANGE` 0 次、`RESTORE_DRIFT` 0 次 |
| 行数 | `artifacts.rs` 378 / `single_strategy.rs` 496（离 500 只剩 4 行）/ `strategy_backtest.rs` 207 / `depth.rs` 283 / `config_commands.rs` 622（已重新登记）/ 新用例文件 286 |

### 27.5 交给后续轮次的事实

1. **摘要换 schema 会让本地产物撞名，而路径里没有世代信息**：产物名只由 `run_id` 与 `manifest.digest()`
   决定，`digest()` 覆盖的是**输入与模型**，不含摘要自身的 `schema_version`。所以任何一次"只改摘要形状、
   不改输入"的变更，都会让同一条示例命令在原地撞上旧文件并报"已存在不同内容"（本轮实测 3 条）。出路有两条，
   需要一次真正的决策而不是每次手工搬文件：把摘要世代写进文件名，或给示例集一条"重 bless"命令。**这是 Q67 的
   正题**。
2. **被跟踪的示例产物早已不可达**：16 份被跟踪摘要停在 `schema_version: 1`，`input` 块 0 份、`account` 块也 0 份（Q72 之后当前代码写 v4），当前代码在这些
   路径上不落任何东西（本轮 `MODIFIED_DEPLOY=0` 即此意）。"示例集验证了什么"这个问题的答案目前是"什么都没验证"，
   与 Q62 记下的 `tracked_sample_artifacts_are_validated_by_nothing` 是同一条，且随每轮变宽。
3. **静态门禁证伪不了"比较写成恒等"**（MQ66b 全绿）。要让它可证伪，得让复核真的产出两个不同来源的值再比较，
   那是把 provenance 做成带来源标记的类型，本轮不做。
4. **内置链仍不落产物**：`run_builtin_backtest` / `run_ccxt_builtin_backtest` 一条摘要都不写，因此也就没有
   `input` 块可核对；它们只把规格来源印成 `spec_source=`（Q63）。

## 28. Q67 收口记录：账户快照不再把"没算过的钱"印成 0（2026-09-22，门禁 `/tmp/qx_q67_gate1.log` + `/tmp/qx_q67_gate2.log`）

不在 §6 排期内，是交易链路实测（#54）排到的第二颗：账户级读模型 TX2。§27.5 第 1 条那颗"产物重 bless"的决定
本轮没有做 —— Q67 这个号被交易链路占了，它仍挂在 Q1b（#53）名下。

### 28.1 缺陷与口径

**缺陷形状（改前全部取自 `HEAD`，即 `0e2ec20`）**：

1. `crates/qx-cli/src/api_service.rs:273` 写的是 `snapshot.available_raw = snapshot.equity_raw;`。等权的权益
   不是"可用资金"——那段钱已经压在持仓上，抄 equity 等于对外宣布它还能自由花掉。
2. 协议上八个汇总钱字段全是裸 `i128`（HEAD `qx-protocol/src/lib.rs:105-112`），`new()` 把其中七个直接写成 0
   （HEAD `:181` 一带）。于是"这一层根本没算"与"算出来正好是零"在类型、在 `state_hash`、在线格式上是同一份
   状态，谁也无法把前者改出来。
3. **七个字段里有六个在全仓没有任何写入点**。最干净的一条是 `frozen_raw`：`git grep` 在 `HEAD` 的 `crates/*`
   里只命中 `crates/qx-protocol/src/lib.rs` 一个文件（结构体 `:109`、`new()` `:181`、两处哈希 `:233`/`:323`、
   折算 `:306`）——生产者数量为零，每条对外发布的快照都在说"这个账户没有冻结资金"。`margin_raw` /
   `realized_pnl_raw` / `unrealized_pnl_raw` / `funding_raw` 同样只有协议侧的读法。
4. `fees_raw` 是这六条里唯一"旁边就摆着真数"的：同一条快照的 `fills[].fee_raw` 逐笔带着费用，账户级却恒印 0
   —— 一份 JSON 两个口径。
5. 端点侧把这种假零再放大一次：HEAD `qx-api/src/lib.rs:1459-1460` 是
   `snapshot.as_ref().map(|snapshot| snapshot.available_raw)`，"没有快照"才是 `null`，"有快照但没算"印 0。

**为什么这不是"补几个字段就行"**：读模型拿得到的是账簿与事实流。`available` 与 `fees` 确实算得出（下面 28.2
各自有一个来源），`margin`/`frozen`/`realized_pnl`/`unrealized_pnl`/`funding` 在这一层拿不到任何来源 —— 给
它们填 0 就是造数。所以修法必须同时做两件事：**算得出的按自己的来源算**，**算不出的在类型上说不清**。后者
不成立时，前者随时可以被后者冒充（本轮 `MQ67b`/`MQ67f` 各证一次）。

### 28.2 落地内容

- **协议侧**（`crates/qx-protocol/src/lib.rs`，874 行）：`equity_raw: i128` 保持恒算得出，其余七个改
  `Option<i128>`，`new()` 全为 `None`。`:220` `scalar_money_raw()` 是八个钱的**唯一取法**（`Some(self.equity_raw),`
  全仓 1 处，没有第二份手抄列表）；`:235` `write_scalar_money` 先写 `u64::from(value.is_some())` 再写
  `value.unwrap_or_default()`，于是 `None` 与 `Some(0)` 是两份状态；`:266`（`state_hash`）与 `:345`
  （`scalar_hash`）共用同一次写入；`:242` `scalar_json_values` 是稳定 JSON 八个槽位的唯一填法，`:247`
  `None => "null"`，`:437` 起的实参只能是 `scalars[0..7]`。
- **读模型**（`crates/qx-cli/src/api_service.rs`，370 行）：`:276` `available_raw` 取本条快照记账的那一本结算
  账簿现金 `cash_for(account_id, pipeline.settlement_currency())`；`:296-302` `fees_raw` 由**同一条快照**的
  逐笔 `fill.fee_raw` 以 `checked_add` 累加，合计溢出时不发布快照而是 `Err("账户 {account_id} 的成交费用合计
  溢出，拒绝发布费用口径错误的快照")`。
- **端点**（`crates/qx-api/src/lib.rs`，3160 行）：`:1459-1460` 改 `as_ref().and_then(...)`，未算直接是 `null`。
- **用例侧**：`crates/qx-cli/src/tests/api_snapshot_money_fields.rs`（253 行，4 条，全部走真实 paper 入口 +
  真实读模型）——结算账簿口径（并先断言 `equity > cash` 免得两边本就相等）、费用与逐笔成交同源（先断言单笔
  费用 > 0 免得合计与 0 撞车）、未算缺席（五个字段 `None` 且 `to_wire_json()`/`to_json()` 印 `null`）、费用合计
  溢出即拒。`crates/qx-protocol/tests/snapshot_single_source.rs`（439 行，7 条）新增一条
  `uncomputed_money_is_not_the_same_state_as_computed_zero`：对七个字段逐个跑"哈希不同 → seal/validate 仍自洽
  → 稳定 JSON `null` vs `0` → 稳定 JSON 与线格式往返都保住区分且落回同一份哈希 → `diff`+`apply` 两个方向都是
  一次真实改动"。`qx-api` 新增端点用例 `balances_endpoint_publishes_absent_money_as_null_not_zero`。
- **门禁侧**：`tools/check_architecture.py:1918` `snapshot_money_honesty_check()` 11 项（挂载在
  `two_leg_partition_check()` 与 `capabilities_check()` 之间）：Option 形状、取法唯一、两处哈希共用、存在性标记、
  槽位只由 `scalar_json_values` 填（含实参列表不得出现 `self.`）、"权益副本"禁抄（见 28.3 第 3 条）、available
  取结算现金、费用 checked 加总、**五个未算字段在任何产码里都不得出现赋值写法**（整文件扫描、排除 `src/tests`）、
  端点两处 `and_then`、六条用例按 `fn NAME(` 取证。静态门禁 245 → **256 项**，`CLI_TEST_FLOOR` 169 → **173**
  （`tools/check_architecture.py:384`），`maturity/line_budgets.yaml` 重登记 `qx-protocol/src/lib.rs` 856 → 874、
  `qx-api/src/lib.rs` 3137 → 3160。

### 28.3 本轮踩到与钉住的坑

- **三条用例首轮全红，红因在夹具而不是断言**：paper 模板里的 `instrument_spec_path` 相对 `data_dir`，用例把
  `data_dir` 换到临时目录后读不到规格（`os error 3`）。修法是 `paper_runtime()` 里把带规格路径的 worker 逐项
  绝对化（`api_snapshot_money_fields.rs:16-24`）。
- **溢出夹具是被算出来的，不是猜出来的**：第一版两笔 `i128::MAX/2 + 1` 的费用先撞在账簿上（`现金余额溢出`），
  根本走不到读模型。重算后取 `fees = [i128::MAX/2 + 2, i128::MAX/2]`、入金 `Money::from_i64(200)`，买 1 付
  100：账户现金精确落在 `i128::MIN`（可表示，故账簿侧不炸），只有读模型的合计会回绕。用例 `:243`
  `assert_eq!(pipeline.ledger().cash_for("main", "USDT"), i128::MIN)` 钉住"极值到了但没溢出"这件事；把它改成
  `wrapping_add` 即红（本轮 `MQ67c` 同族的反向验证在上一段做过）。
- **两处"声明了不可能失败的检查"是本轮自己写出来、又被本轮拆掉的**（Q62/FN5 同一族）：
  ① `available` 曾包一层 `(!settlement_currency.trim().is_empty()).then(...)`，而 `LiveEventPipeline::open`
  早已拒空结算币种（`crates/qx-runtime/src/pipeline.rs:570-571`），分支永不成立 → 删。
  ② "权益副本"禁抄项原本盯的是字面量 `snapshot.available_raw = snapshot.equity_raw` —— 字段改成 `Option<i128>`
  之后这条写法**根本编译不过**，等于禁了一条回不来的形态。本轮把禁抄列表换成仍可写出的四种形状（含
  `Some(snapshot.equity_raw)` 与两种 `unwrap_or(…)`），`MQ67b` 实测一次点亮 2 条具名项。
- **STAGE4 clippy 首轮红 3 条，全在本轮新增用例里**（`/tmp/qx_q67_gate1.log`：
  `needless_borrows_for_generic_args` ×2 + `err_expect` ×1，`STAGE4_CLIPPY[qx-cli]_EXIT=101`）。上一轮只跑了
  `cargo check`/`cargo test` 就动了代码，clippy 是这轮才补上的门槛。修复后 gate2 三个 crate 全 `=0`。
- **两个 API 细节**：`ProtocolError` 没有 `Display`，用例里的 panic 文案只能用 `{error:?}`；
  `SnapshotDiff::replacement` 是私有字段，"未算 ↔ 算出为零"这条改动只能用公共 `diff()` + `apply()` 当见证。

### 28.4 验收数字（`/tmp/qx_q67_gate2.log`，2026-09-22 19:46:49 → 19:50:32；首轮 `/tmp/qx_q67_gate1.log` 19:39:36 → 19:43:34）

| 步骤 | 实测 |
| --- | --- |
| STAGE1 `cargo check --workspace --all-targets` | 两轮 `STAGE1_CHECK_EXIT=0` |
| STAGE2 行数棘轮 | `SNAPSHOT_EXIT=0`（37 个超 500 行文件）、`BUDGET_DIFF_EXIT=0`（本轮已提前 bless，重快照无漂移）、`ARCH_AFTER_SNAPSHOT=0`（256 项） |
| STAGE3 触及 Rust 文件 `rustfmt --check` | `STAGE3_FMT_EXIT=0` |
| STAGE4 `cargo clippy --all-targets -- -D warnings` | gate1：qx-protocol/qx-api `=0`、**qx-cli `=101`（3 条）**；gate2（修复后）：qx-protocol / qx-api / qx-cli 三条全 `=0` |
| STAGE5 行为基线 | `qx-protocol` lib 4/0 + 集成 7/0、`qx-api` 24/0、`api_snapshot_money_fields` 4/0、邻居 `account_event_log_identity` 8/0 |
| STAGE6 行为变异（6 个） | MQ67a（摘掉存在性标记 = 缺陷本体）→ 静态 1 红 + 协议集成 1 红；MQ67b（available 抄 equity）→ 静态 **2** 红（禁抄项 + 算点项）+ CLI 用例 1 红；MQ67c（费用写回常量 0）→ 静态 1 红 + 费用用例红；MQ67d（`None => "0"`）→ 静态 1 红 + 协议 lib `stable_json…` 红 + CLI 未算用例红；MQ67e（端点 `margin` 兜 0）→ 静态 1 红 + `qx-api` 端点用例红；MQ67f（凭空 `margin_raw = Some(0)`）→ 静态 1 红 + CLI 未算用例红。六次 `RESTORE_EXACT`，`*_RESTORED` 全部 `0 failed` |
| STAGE7 静态变异（3 个）+ 基线 | 基线 `MUT_STATE=green`、`PASS_LINES=256`；G67a（改协议用例名 → 测试仍全绿、只有静态红）、G67b（槽位绕开 `scalar_json_values` 手抄一份 → 静态 1 红**并**带 2 条用例红）、G67c（新用例文件不挂进 `tests/mod.rs` → `module_mount_check` 红，正是"半落地拆分 cargo 看不见"那条）各 `ARCH_FAIL_LINES=1 / WANT_HITS=1 / OTHER_FAILS=0 / PASS_LINES=255` |
| STAGE8 全量测试 | 设 `QX_PYTHON`：`SUM_with_qxpython_passed=722 failed=0`、`WORKSPACE_WITH_QX_PYTHON_EXIT=0`；不设：`SUM_without_qxpython_passed=720 failed=2`（`e2e_and_python_contract` 的两条 Python worker 契约项，本机不给解释器，与 Q66 同因） |
| STAGE8 产物侧 | `MODIFIED_DEPLOY=0`、`UNTRACKED_DEPLOY=12` —— Q67 只改建模型侧读模型，不落任何产物 |
| STAGE9 Python 侧 | `Ran 43 tests`、`PY_TESTS_EXIT=0` |
| STAGE10 收口 | `PRISTINE_OK final`、`FINAL_ARCH_EXIT=0`（245 → **256 项**）、`BUILD_final_EXIT=0`、`MUTATION_RESIDUE=` 空；全程 `CONCURRENT_TREE_CHANGE` 0 次、`RESTORE_DRIFT` 0 次、`MUT_STATE=unexpected` 0 次、`PRISTINE_OK` 14 次、`RESTORE_EXACT` 9 次 |

### 28.5 交给后续轮次的事实

1. **同一份 JSON 里的持仓行还在犯同一个错**：`api_service.rs:340-351` 走 Ledger 回退时写死
   `unrealized_pnl_raw: 0, margin_raw: 0`，`PositionSnapshot` 上这两项在协议上也是裸 `i128`。账户级修完，
   持仓级未修 —— TX2 的下半截。
2. **CCXT 事实折算把"缺字段"读成"零/多头"**：`crates/qx-cli/src/ccxt_facts.rs:106-110` 缺 `side` 默认 `"long"`
   （空头因此可能被记成正数量）、`:131-140` 缺失或 `null` 的 `unrealized_pnl_raw`/`initial_margin_raw`/
   `maintenance_margin_raw` 一律 `Money::ZERO`、`:181-184` 缺 `timestamp_ms` 回落 0。同族缺陷，且它的上游是
   Python worker 的输出形状，改法要先定"worker 契约允许不允许省略这些键"。
3. ~~**CCXT 对账的两半不相交**~~：`ccxt_reconcile_worker.rs:249` / `:283` 两处 in-loop 发现只写进事实流
   （`apply_reconcile_required` 会把本地非终态订单推到 `OrderStatus::Unknown`，
   `crates/qx-runtime/src/pipeline.rs:1503-1513`），**不进** `:307` 报告的 `order_issues`、也**不参与** `:316`
   的健康判定 → 一轮里已经有单子"结果未知"，worker 仍标 `Ready`；反过来 `open_order_issues`（`:216-236`）只进
   报告与健康，不落事实流，下一张报告就把它冲掉。**已由 §31（Q69）关闭。**
4. ~~**`binance_reconcile.rs:154-156` 的三个 0 是"这条链没取"**~~：`position_snapshots_count` /
   `funding_rate_snapshots_count` / `cashflow_count` 恒 0，读者无法区分"账户确实没有持仓"与"这条链根本不报
   持仓"。`ReconcileReportSnapshot`（`qx-api/src/lib.rs:987-1013`）只有 `validate()` 管身份，不管这类覆盖度。
   **已由 §31（Q69）关闭。**
5. **§27.5 第 1 条的重 bless 决定仍未拍**（把摘要世代写进文件名，或给示例集一条重 bless 命令）。本轮
   `MODIFIED_DEPLOY=0` 再证一次：当前代码在那 16 份被跟踪示例路径上不落任何东西。

## 29. Q68 收口记录：持仓行的未算钱不再印成 0，缺方向不再靠猜（2026-09-22，门禁 `/tmp/qx_q68_gate1.log` + `/tmp/qx_q68_gate2.log`）

不在 §6 排期内，是 §28.5 第 1、2 两条的下一颗：交易链路 TX2 的下半截（持仓行）+ 实盘回报入口（TX2b）。

### 29.1 缺陷与口径

**缺陷形状（改前全部取自 `HEAD`，即 `0e2ec20`）**：

1. **内核持仓观察的三个钱字段没有"没报"这个状态**：`crates/qx-core/src/event.rs:182-184` 是裸
   `unrealized_pnl: Money` / `initial_margin: Money` / `maintenance_margin: Money`，`:451-453` 的摘要写入是
   `h.write_i128(position.unrealized_pnl.raw())` 这样的裸取值。于是"这个交易所不报维护保证金"与"报了零"在类型、
   在 `Event::digest`、在线格式上是同一份状态 —— 与 §28.1 账户级那个缺陷同一个形状，只是换了层。
2. **线格式行把它固化成两列 `i128`**：`crates/qx-protocol/src/wire.rs:31-32` 是
   `unrealized_pnl_raw: i128` / `margin_raw: i128`，`:44-45` 的 `Default` 直接给两个 `0`。任何用
   `..PositionSnapshot::default()` 起步的构造（`crates/qx-cli/src/ecosystem_smoke.rs:192`、
   `crates/qx-protocol/src/lib.rs:814` 的夹具）都自动"报了一个零"。
3. **读模型的 Ledger 回退行写死零**：`crates/qx-cli/src/api_service.rs:322-333` 在没有 venue 持仓观察时自己拼一行，
   末尾两列就是 `unrealized_pnl_raw: 0, margin_raw: 0`。Ledger 里没有"未实现盈亏"与"占用保证金"这两个量
   （要按冻结的 market spec 逐标的算，读侧没有规格来源），所以这两个 0 没有来源。§28.5 第 1 条。
4. **CCXT 回报入口用猜的定方向**：`crates/qx-cli/src/ccxt_facts.rs:106-110` 是
   `.get("side").and_then(as_str).unwrap_or("long").to_ascii_lowercase()`。它的上游
   `python/qianxing_ccxt/__init__.py:1240` 在交易所不给方向时印的是字面量 `"unknown"`，于是 HEAD 的实际行为是：
   数量走 `else` 分支按**正数**入账、`position_side` 记 `"unknown"` —— 同一份观察里数量与方向自相矛盾，而下行的
   符号判断（权益、保证金、风控）只读数量。缺 `side` 键更直接：连 `"unknown"` 都没有，静默按多头。§28.5 第 2 条。
5. **同一个函数里三项钱一律兜零**：`crates/qx-cli/src/ccxt_facts.rs:131-140` 的 `optional_money` 返回的是
   `Result<Money, String>`，缺键与 `null` 都 `return Ok(Money::ZERO)`。连接器侧
   （`python/qianxing_ccxt/__init__.py:1233-1250` 的 `optional_raw()`）本来就产出 `None`/省略键，所以这是
   **生产方说"没报"、消费方改口说"零"** 的同一条链。
6. **守卫建立在裸取值上**：`crates/qx-runtime/src/pipeline.rs:1649-1651` 是
   `position.unrealized_pnl.raw() == i128::MIN || position.initial_margin.raw() < 0 || …`。字段改成 `Option` 之后
   这三行必须"报了才判"，否则不报保证金的交易所会被这份守卫判成非法回报。

**为什么不能只是"补几个字段"**：持仓行比账户标量多两层 —— 它有一个跨语言线格式（`wire.rs`）和一个事实入口
（`ccxt_facts.rs`）。只改协议结构体而不改入口，入口会立刻把 `None` 又填成 `Some(0)`；只改入口而不改摘要写入，
落盘日志里"没报"与"报零"仍是同一份内容，回读时无法复原。所以本轮六个位置一起改，并且每一处都配一条"结果确实
变化"的用例（29.2）与一次反向变异（29.4）。

**口径**：钱必须显式缺席；**价格继续用 `0` 表达"没有"** —— 定点价格里 `0` 不是合法值，`0` 与真值不会撞车，而钱
的 `0` 是一个完全合法的回答。这条区分写进 `crates/qx-protocol/src/wire.rs:31-33` 的字段文档，并由门禁的一项
（要求 `pub mark_price_raw: i128,` 仍在位）钉住，免得下一轮把价格列也"顺手改成 Option"。

### 29.2 落地内容

- **内核**（`crates/qx-core/src/event.rs`，本轮 674 → 749 行）：`:187`/`:189`/`:191` 三个字段改 `Option<Money>` 并
  各带 `#[serde(default)]`（老日志缺键读成未报）；`:463-466` 摘要改成一次循环，逐字段先写
  `h.write_u64(u64::from(value.is_some()))` 再写 `value.map(Money::raw).unwrap_or_default()` —— 与 Q67 的
  `state_hash` 同一条纪律。
- **线格式**（`crates/qx-protocol/src/wire.rs`，198 行）：`:34-35` 两列 `Option<i128>`，`:46-47` `Default` 给
  `None`；`:104-105` 正向折算 `.map(Money::raw)`（不兜零）；`:124-126` 反向折算把线格式没有的那一列读回
  `maintenance_margin: None` 而不是 `Money::ZERO`。
- **协议哈希与 JSON**（`crates/qx-protocol/src/lib.rs`，856 → 885 行）：`:236` `write_optional_money()` 成为账户
  标量与持仓行**共用**的带标记写入助手（`:287-288` 行哈希复用它，全仓 1 处调用），`:248` `money_json()` 是
  `null` 的唯一写法，行的两个槽位只由它填。
- **读模型**（`crates/qx-cli/src/api_service.rs`，373 行）：`:350-354` 回退行两项改 `None`，并把"Ledger 里没有
  这两个量"写成注释留在原地。
- **CCXT 入口**（`crates/qx-cli/src/ccxt_facts.rs`，412 行）：`:110-122` 方向闸门只接受 `long`/`short`
  （大小写归一后），其余（含缺键与连接器的 `"unknown"`）返回带 symbol 与"读到什么"的错误；`:103`
  `if contracts == 0 { continue; }` 仍在闸门**之前**（平仓位通常不带方向，不能连带打死）；`:146-154`
  `optional_money` 返回 `Option<Money>`，缺键与 `is_null()` 都读成未报，三项钱共用它。
- **运行时守卫**（`crates/qx-runtime/src/pipeline.rs`，2349 → 2391 行）：`:1649-1657` 三条钱守卫改
  `is_some_and`，只判"报了的那部分"。
- **用例侧**（8 条新行为用例，全部走真实入口）：
  `crates/qx-cli/src/tests/ccxt_position_facts_honesty.rs`（146 行，3 条：方向 fail-closed 且大小写归一/
  平仓位先跳过/省略与 `null` 都读成未报而 `"0"` 字符串与数字 `0` 仍是报零）；
  `crates/qx-cli/src/tests/api_snapshot_money_fields.rs`（253 → 343 行，加 2 条行侧：Ledger 回退行两列缺席
  且 `to_wire_json()`/`to_json()` 都印 `null`、venue 报的行保留"报零 vs 没报"两态）；
  `event.rs` 的 `unreported_position_money_is_not_hashed_as_zero`（摘要两态 + 老日志去键读回 `None`）；
  `crates/qx-protocol/tests/snapshot_single_source.rs`（624 → 665 行）加 `absent_venue_money_stays_absent_through_the_wire_folding`、
  `uncomputed_position_money_is_not_the_same_state_as_computed_zero`（逐列跑哈希/seal/稳定 JSON/线格式往返/
  `diff`+`apply` 双向）、`default_position_row_reports_no_money`（钉构造入口）；
  `pipeline.rs` 的落盘回读用例加第二行（`ETH/USDT:USDT.OKX`：`unrealized_pnl: Some(Money::ZERO)` 与
  `initial_margin: None` 混在同一份快照里，回读后不得一起收敛成 0）。
- **门禁侧**：`tools/check_architecture.py:2125` `position_money_honesty_check()` 13 项（挂在
  `snapshot_money_honesty_check()` 与 `capabilities_check()` 之间）：内核 Option 形状与 `serde(default)`、
  摘要存在性标记唯一、线格式两列形状**且价格列仍按 0 哨兵**、折算各一处、共用哈希助手、槽位只由 `money_json`
  填、全仓逐行禁 `NAME: 0|Money::ZERO` 兜底、回退行两处 `None`、方向闸门（作用域收在
  `ccxt_position_facts` 函数区间，禁 `'unwrap_or("long"'`/`'=> "long"'`/`'"unknown" =>'`）、跳过点必须先于闸门、
  `optional_money` 单点读法、守卫的 `is_some_and` 序列、八处用例按 `fn NAME(` 取证。静态门禁 256 → **269 项**，
  `CLI_TEST_FLOOR` 173 → **178**（`tools/check_architecture.py:390`），`maturity/line_budgets.yaml` 重登记
  `qx-core/src/event.rs` 674 → 749、`qx-protocol/src/lib.rs` 856 → 885、`qx-runtime/src/pipeline.rs` 2349 → 2391。

### 29.3 本轮踩到与钉住的坑

- **第一轮日志里有三条"红得不完整"，全部在第二轮改掉**（`/tmp/qx_q68_gate1.log` 作废重跑）：
  ① `MQ68a` 摘掉 `#[serde(default)]` 后**测试全绿** —— serde 对 `Option<T>` 字段的缺键本来就补 `None`，这个属性
  今天不带任何行为差。它因此从"行为变异"改登记为**声明型**（第二轮的 `A68a`，仍跑五条链证明测试全绿），门禁注释
  里写明"只能由静态项钉、真实行为由内核那条摘要用例锁着"。这属于 Q62/FN5 与 §28.3 那条"声明了不可能失败的
  检查"同一族。
  ② `MQ68d`（`Default` 兜 0）当时没有任何用例失败 —— 折算结果被钉住了，**构造入口**没有。补
  `default_position_row_reports_no_money`（先断言默认行两列 `None`、折算回内核仍是 `None`，再断言默认行与
  "交易所报了零"的行不得共用一份 `state_hash`），第二轮它打红一条具名用例。
  ③ `MQ68h` 第一版 needle 写成 `.unwrap_or(Money::ZERO).raw() < 0` —— 与 `is_some_and(\|m\| m.raw() < 0)`
  **语义等价**（零不满足 `< 0`），不是一个可达缺陷。换成 `.map_or(true, …)`：把"没报"当成非法保证金，
  这才是不报保证金的交易所会真的撞上的形状。第二轮它同时打红 `qx-runtime` 与 `qx-cli` 各一条用例。
- **`G68d` 的期望名子串抄错，一次 `MUT_STATE=unexpected`**：门禁 hint 是"行字段退回 i128 或 Default 又兜了一个
  0"，脚本里写成"有字段退回…"，于是命中 0、其他红 1。变异本身点亮的是对的那一项（`ARCH_FAIL_LINES=1`），
  但按门禁纪律这条日志不能当证据用 → 改字符串重跑。
- **按路径取数的门禁项会把正确的代码判成违规**：新增的 CCXT 断言起初扫整个 `ccxt_facts.rs`，而
  `ccxt_balance_facts` 里的 `.unwrap_or(Money::ZERO)` 语义本来就是"这本账簿里没有这个币种" → 首轮门禁红。
  修法是把禁零与方向口径的作用域收到 `fn ccxt_position_facts` 到 `fn ccxt_funding_fact` 之间。
- **搬东西会撞旧锚点**：Q67 那条"存在性标记"项盯的是 `fn write_scalar_money` 函数体，本轮把标量与行共用的写入
  搬进 `write_optional_money` 后它立刻红 → 锚点跟着搬，并补一项"行哈希必须调用它"的计数检查。
- **`cargo fmt` 会重排被门禁按精确字符串取的代码**：格式化把 `normalize_positions` 的守卫折成多行，精确字符串
  项红 → 改成函数区间内的空白容忍正则，并要求命中的捕获序列恰好是 `["margin", "margin", "pnl"]`（顺序无关、
  数量必须对）。
- **两个 E0716 是同一种写法错**：`restored.snapshot().positions…` 与 `paper_snapshot(&config).positions.get(..)`
  都把临时值当长生命引用用 → 先 `let` 绑定再断言。
- **摘要改动的连带后果本轮实测过**：`Event::digest` 的形状变了，含 venue 持仓观察的既有日志会在 manifest 核对处
  被拒（`crates/qx-storage/src/lib.rs:307-309` 分段日志、`crates/qx-storage/src/sqlite.rs:2104` SQLite）。
  这是**有意的 fail-closed**：旧日志里"没报"与"报零"本来就是同一份内容，无法事后区分。本轮查过被跟踪的产物侧
  不含这类日志：`deploy/` 里 `AccountPositionSnapshot`/`account_positions`/`unrealized_pnl`/`"digest"` 四种取法
  各 **0** 命中，`MODIFIED_DEPLOY=0`。
- **两件有意不做的事**：① 不给"交易所没报保证金"fail-closed（部分交易所合法地不报，`map_or(true, …)` 那条变异
  要禁的正是这个）；② 不在读模型里替持仓行算浮亏 —— `load_worker_instrument_spec` 是单标的、要 `Order` 和配置
  路径，硬接会给读侧新增一条"取不到规格就静默降级"的路径。行 PnL 因此仍是缺席，交下一轮（29.5 第 1 条）。

### 29.4 验收数字（`/tmp/qx_q68_gate2.log`，2026-09-22 20:54:05 → 21:00:10；首轮 `/tmp/qx_q68_gate1.log` 20:35:30 → 20:42:21 因 29.3 前三条作废）

| 步骤 | 实测 |
| --- | --- |
| STAGE1 `cargo check --workspace --all-targets` | `STAGE1_CHECK_EXIT=0` |
| STAGE2 行数棘轮 | `SNAPSHOT_EXIT=0`（37 个超 500 行文件）、`BUDGET_DIFF_LINES=0`（本轮已提前 bless，重快照无漂移）、`ARCH_AFTER_SNAPSHOT=0`（269 项） |
| STAGE3 触及 Rust 文件 `rustfmt --check` | `STAGE3_FMT_EXIT=0` |
| STAGE4 `cargo clippy --all-targets -- -D warnings` | qx-core / qx-protocol / qx-runtime / qx-cli 四条全 `=0` |
| STAGE5 行为基线 | `qx-core` lib 58/0 + 18/0、`qx-protocol` lib 4/0 + 集成 10/0、`qx-runtime` 46/0 + 4/0 + 8/0、`ccxt_position_facts` 3/0、`api_snapshot_money_fields` 6/0 |
| STAGE6 行为变异（7 个） | MQ68b（摘摘要存在性标记）→ 静态 1 红 + 内核摘要用例红；MQ68c（折算兜零）→ 1 红 + `absent_venue_money…` 红；MQ68d（`Default` 兜零）→ 1 红 + `default_position_row_reports_no_money` 红；MQ68e（回退行报数，缺陷本体）→ 1 红 + 行缺席用例红；MQ68f（方向退回默认多头，缺陷本体）→ 1 红 + 方向用例红；MQ68g（维护保证金写死零）→ 1 红 + 钱字段用例红；MQ68h（未报当非法）→ 1 红 + `qx-runtime` 与 `qx-cli` 各一条用例红。七次全部 `ARCH_FAIL_LINES=1 / WANT_HITS=1 / OTHER_FAILS=0 / PASS_LINES=268`，七次 `RESTORE_EXACT` |
| STAGE7 声明型 + 静态变异（5 个）+ 基线 | 基线 `MUT_STATE=green`、`PASS_LINES=269`；A68a（摘 `#[serde(default)]`）→ 静态 1 红且**五条链测试全绿**（声明型，见 29.3 ①）；G68a（新用例文件不挂载）→ `未挂载即死源码` 红；G68b（改名 ccxt 用例）→ `缺 ccxt 用例` 红；G68c（槽位绕开 `money_json` 手抄）→ 静态 1 红**并**带 `stable_json_and_file_snapshot_store_are_recoverable_and_idempotent` 红；G68d（钱列退回 `i128`）→ 静态 1 红。各 `PASS_LINES=268`、`MUT_STATE=red`，5 次 `RESTORE_EXACT` |
| STAGE8 全量测试 | 设 `QX_PYTHON`：`SUM_with_qxpython_passed=731 failed=0`、`WORKSPACE_WITH_QX_PYTHON_EXIT=0`；不设：`SUM_without_qxpython_passed=729 failed=2`（`e2e_and_python_contract` 的两条 Python worker 契约项，本机不给解释器，与 Q66/Q67 同因）、`WORKSPACE_EXIT=101` |
| STAGE8 产物侧 | `MODIFIED_DEPLOY=0`、`UNTRACKED_DEPLOY=12` —— Q68 只改读侧与回报入口，不落任何产物 |
| STAGE9 Python 侧 | `Ran 43 tests`、`PY_TESTS_EXIT=0` |
| STAGE10 收口 | `FINAL_ARCH_EXIT=0`（256 → **269 项**）、`BUILD_final_EXIT=0`、`MUTATION_RESIDUE=` 空；全程 `CONCURRENT_TREE_CHANGE` 0 次、`RESTORE_DRIFT` 0 次、`MUT_STATE=unexpected` **0 次**、`PRISTINE_OK` 19 次、`RESTORE_EXACT` 12 次 |

### 29.5 交给后续轮次的事实

1. **持仓行的浮亏仍然没有生产者，只是不再撒谎**：本轮把 `api_service.rs` 的回退行改成诚实缺席，但"算得出"这一半
   要先把冻结的 market spec 送到读侧（`load_worker_instrument_spec` 是单标的、要 `Order` 与配置路径）。TX2 还剩
   这一截。
2. **账户级五个字段仍然没有任何生产者**（`unrealized_pnl_raw` / `margin_raw` / `frozen_raw` / `realized_pnl_raw` /
   `funding_raw`，Q67 §28.5 第 1 条原样挂着）——现在它们在类型上说不清，但"说不清"不等于"算得出来"。
3. **同一个 CCXT 文件里的资金费时间戳仍兜零**：`crates/qx-cli/src/ccxt_facts.rs:195-198` 缺 `timestamp_ms` 时
   `unwrap_or(0)`，而同一文件的现金流水把 0 当"没有 received_ts"处理。本轮只收了持仓侧的钱与方向。
4. ~~**CCXT 对账的两半不相交**（§28.5 第 3 条原样）~~：in-loop 发现只进事实流、`open_order_issues` 只进报告与健康，
   worker 在已有单子"结果未知"时仍标 `Ready`。下一轮 #69（交易 TX3）。**已由 §31（Q69）关闭。**
5. ~~**`binance_reconcile.rs:154-156` 的三个覆盖度计数恒 0**（§28.5 第 4 条原样）~~：读者无法区分"账户确实没有持仓"
   与"这条链根本不报持仓"。**已由 §31（Q69）关闭**；取数缺口本身改成 limitation 挂在 `capabilities.yaml`。
6. **含持仓观察的既有事件日志会被 manifest 核对拒掉**（29.3 最后一条）：本机无此类被跟踪日志，但**真实跑过
   paper/CCXT 的用户目录里会有**。升级路径（重放时带摘要世代标记、或给一条显式重建命令）尚未设计，与 §27.5
   第 1 条的重 bless 决定是同一类待拍板事项。

---

## 30. 文档收口轮（2026-09-23）：仓库里只留"今天仍然正确"的文档

用户这一轮的请求是四件事：提交最新代码、删除历史无效文档、更新接口使用文档、更新项目说明文档。
前两件在 §30.1–§30.3，后两件在 §30.4，全部数字来自本轮命令输出。

### 30.1 判据：不是"旧"，而是"里面的名字已经不存在"

删一份文档的门槛写成一条可核对的判据：**文档里的 crate 名或命令名在当前代码里已经不存在，
且它不承担任何"唯一副本"职责**。取证方式是 `Cargo.toml` 的 `workspace.members`（本轮实测 23 个）、
`qx-cli --help` 的入口集合（本轮实测 50 条），以及对每份文档跑一次全仓引用清点
（`grep -F <basename>` 分成"代码/门禁/配置"与"文档内部"两栏）。结果：

- **没有任何工具、CI 作业或代码注释按路径读 `docs/`**：`tools/` 与 `.github/` 里 `docs/` 零命中，
  因此删除的唯一后果是 markdown 链接，不会有静默的门禁断裂。这是本轮敢动手的前提。
- 被删的 23 份里有 14 份、合计 115 行点名 `qx-domain` / `qx-kernel` / `qx-application` / `qx-oms` /
  `qx-portfolio` / `qx-fenye` / `qx-viz` / `qx-server` / `qx-schema`，而这九个名字在当前 `Cargo.toml` 的
  23 个 workspace 成员里一个都没有（"分野"现在是 `qx-core` 的模块，不是 crate）。口径：
  `git -c core.quotepath=false show HEAD:<path>` 后逐份数命中行；115 是**上限**，其中既有把早已并入的
  crate 当现役写的模块表与路径（`crates/qx-application/src/lib.rs` 一类，今天照着文件路径找不到东西），
  也有"该 crate 已并入 `qx-execution`"这类当时正确的记述。`Qianxing-v0.0.1-Release.md` 宣称
  "first unified release"，而 workspace 版本是 `0.1.0`。
- 改前的 README 里 10 处 markdown 链接有 9 处指向这些文件（唯一活着的指向是收口指南），且 `:164` 把 V9 称为
  "现行重构基线"——即**README 已经在指向错误的基线**，而且指向的是一份已经不存在的文件。

### 30.2 删除清单与一处判断题

删除（`git rm`，历史可取回）：`docs/` 下 `QIANXING_CODE_AUDIT_V2`、`Qianxing-Multi-Asset-Quant-Infrastructure-V2`、
四份 `Qianxing-V5-*` 与 `Qianxing-v0.0.1-Release`、`牵星完整架构方案-V1`、`牵星最终架构方案-V2-Barter对齐版`、
`牵星架构拆分与扩展决策-V1`、`牵星终极改造计划-V1`、`牵星终极改造实施状态-V1`、
`自研量化框架架构审计与重构方案-V8`、`自研量化框架重构方案-V6/V7/V9/V10`、`工业级产品化实施路线图-V1`、
`工业级落地验收与差距清单-V1`；根目录 `Qianxing-Visualization-Architecture-V1`、`自研量化框架V5落地审计`、
`自研量化框架完整架构设计方案`、`自研量化框架重规划方案-V5`。

唯一一条需要判断题的是 `Qianxing-Visualization-Architecture-V1.md`：它的核心规则
（"可视化只能消费由 EventLog 派生的只读投影，不能成为第二个事实源"）确实是别处没有的表述，
但正文其余 15 节是未落地的 Web/桌面端设计，其 V1.1 修订自己列出了不存在的 `qx-viz`/`qx-server`/`/ws`。
取舍：**把那条不变量搬进 README 的"设计底线"第 8 条，整篇删除**。规则留下，幻想部分进历史。

保留的 7 份专题文档承担"唯一副本"职责：外部链路验收的五段阶梯与 `sandbox_tested` 翻转判据、
A 股提供方语义与代码归一化、CCXT worker 契约与凭据隔离、衍生品统一模型、多语言策略与共享内存传输契约、
易用性收口指南（`maturity/capabilities.yaml:163` 直接把它当证据路径）、以及 V11 本身。

### 30.3 保留文档里修掉的死引用

- 命令名：`builtin-backtest` / `ccxt-builtin-backtest` / `strategy-backtest` / `ccxt-backtest` 四条旧入口
  在 V9 Phase 2 已整体删除。本轮用当前 binary 直接验证：`qx-cli strategy-backtest` 与 `qx-cli builtin-backtest`
  都落到"未知命令"路径并打印 help，只有 `backtest builtin` / `backtest ccxt-builtin` / `strategy backtest` 在 help 的 50 条里。
- 文件不存在：收口指南 4 处写 `deploy/qianxing.runtime.production.json`，实际只有 `…production.example.json`。
- 名单漂移：收口指南一处列 10 个内置策略、同篇另一处说 17 个。按 `builtin-strategies` 实测改为 17
  （13 单标的 + 4 个只被 `multi-builtin` 接受的套利 kind），并**停止在文档里复述名单**，只指向命令。
- 指向已删文档的链接：CHANGELOG 3 处、V11 §13.1 第 4 条 1 处，改为"已删除，原文在 git 历史"的注记。
  历史条目本身不改写 —— 那是当时的记录，不是链接承诺。
- 收口指南 §6 原文说"旧入口继续保留：`strategy-backtest` 等价于 …"，这句今天是**错的**，改为已删除清单。

### 30.4 接口与项目说明文档的重建口径

`docs/工业化易用性收口指南-V1.md` 新增 §3.2/§3.3/§3.4（回测入口族与配置落点、产物与输入身份与重放结论、
读模型 `null` 与 `0`），README 新增"文档地图"与按实测改写的"当前状态"。写法上定了两条纪律：

1. **命令面只指向 help，不在文档里维护第二份命令表**。`qx-cli help` 由 clap 派生，
   `tools/check_architecture.py` 钉「clap 命令表 ≡ `cli.rs` 分支 ≡ help 印出的入口」，它是唯一不会漂移的副本；
   README 的"四类入口职责"与指南 §2 表都改为只标注**副作用边界**。
2. **数字必须当轮抓到才写进文档**。本轮实测：23 crate、50 条入口、17 个内置策略、52 份 `deploy/*.json`、
   门禁 269 项全绿、`qx-cli all` 退出码 0、`backtest builtin macd` 在示例 BarFrame 上 `bars=70 fills=1`、
   能力矩阵 18 块中 15 块 `implementation`+`code_tested` 双真而 `sandbox_tested`/`production_approved` 均为 0
   （证据 155 条、limitation 52 条）。旧的 README 长句把 60 多项能力串成一段、结尾才说"仍需接入"，
   属于无法核对的表述，整段替换。

产物字段口径实抓自本轮一次真实运行：`*.run.json` 16 键（`run_id`/`code_commit`/`config_hash`/
`data_fingerprint`/`input_components`/`clock_start`/`clock_end`/`global_seed`/`determinism_mode`/`result_hash`/
`strategy_version`/`instrument_spec_version`/`model_fingerprint`/`input_event_hash`/`output_event_hash`/
`runtime_version`），`*.summary.json` 有 `input`（`fingerprint` 与 `run.json` 的 `input_components.bars` 同值）、
`replay`（`events`/`ledger_entries`/`log_digest`/`run_ledger_entries`）、`matching_kernel`、`fill_model`、
`execution_costs`、`risk_rules`、`rejected_orders`/`rejection_reasons`，且**不再含 `replay_hash` 键**
（Q62 的结论已落进产物形状）。

### 30.5 交给后续轮次的事实（文档侧）

1. **README 与 docs 里的命令是 prose，没有门禁**。`check_architecture.py` 只覆盖 CLI 内部三件一致性，
   本轮的"README 提及 ≡ help 条目"是临时脚本比出来的。若要让这条纪律长期有效，需要把它做成第 270 项。
2. **收口指南与 `deploy/README.md` 仍有重叠段**：`storage.consistency` 三档在两边逐字重复、CCXT 凭据键在三处出现。
   合并方向二选一（指南做成唯一教程 / `deploy/README` 缩成配置键参考），未拍板。
3. **`maturity/capabilities.yaml:163` 把收口指南当证据路径**。这意味着改这份文档等于改证据；
   上一轮的"产物声明的输入能否重算"（Q66）不适用于文档，因此这条引用没有指纹保护，删改只需在 CHANGELOG 记账。
4. **可视化/前端能力的完整设计只存在 git 历史里**。若将来真做只读投影消费方，需要重写而非找回 ——
   那份稿子的技术栈假设（qx-viz/qx-server/`/ws`）已经不存在。

---

## 31. Q69 收口记录：CCXT 对账的两半发现同时进事实流、报告与健康（2026-09-23，门禁 `/tmp/qx_q69_gate1.log` + `/tmp/qx_q69_gate2.log`）

不在 §6 排期内，是 §28.5 第 3、4 条（在 §29.5 以第 4、5 条原样重挂）的下一颗：交易链路 TX3。

### 31.1 缺陷与口径

**缺陷形状（改前全部取自 `HEAD`，即 `b1fbd82` 的 `crates/qx-cli/src/venue_runtime/ccxt_reconcile_worker.rs`）**：

1. **一轮对账的两半发现写在两份互不相交的清单上**。远端一半在 `:204` 攒进 `open_order_issues`，只喂 `:308` 报告的
   `additional_order_issues` 和 `:316` 的健康判定；本地一半在 `:249`（非终态订单查不到远端订单号）与 `:283`
   （`sync_order` 报错）两处**直接**写事实流，既不进 `:308` 也不参与 `:316`。于是 HEAD 的真实行为是：这一轮已经把
   某张单子推到 `OrderStatus::Unknown`（`apply_reconcile_required`，`crates/qx-runtime/src/pipeline.rs:1503-1513`），
   落盘报告里却写着"本轮没有 order 问题"，worker 心跳仍是 `Ready`；反方向也一样 —— `open_order_issues` 不落事实流，
   下一张报告冲掉之后没有任何地方记得那笔远端孤单。
2. **三面各有自己的读点，等于三种互相矛盾的"本轮结论"**。事实流（`ReconcileRequired`）、报告
   （`ReconcileReportSnapshot.order_issues`）、健康（`ServiceStatus`）本该是同一轮对账的三份投影，HEAD 里它们是
   两两不相交的三份子集。
3. **覆盖度计数把"这条链没去取"印成"取到且为零"**。`binance_reconcile.rs:154-156` 三项恒 `0`；CCXT 侧
   `:296-300` 的 `position_snapshots_count` 从 pipeline 快照取并 `.unwrap_or(0)`，而同一个函数在 `:133` 起对
   不支持 `fetch_positions` 的现货交易所是直接 `continue` —— 能力缺失与"确实空仓"在报告里是同一个 `0`。
   资金费率与账单两项目前连这个取值都没有。
4. **事实条目必须有本地句柄，这是约束而不是疏忽**。`EventLogReconcilePort::require_reconcile` 收
   `client_order_id: u64`，关联 id 是 `<worker>:<tag>:<client_id>`，下游靠它对上一张真实订单。远端孤单挂单的
   `client_order_id` 是 `""` 或交易所自填的字符串（`"99999"`），落事实流就得给 OMS 编一个不存在的句柄。

**口径**：两半发现从**汇总点**起只有一份清单，三面都读它；"能不能落事实流"由"这条发现有没有可解析成 `u64` 的
`client_order_id`"决定，而不是由它属于哪一半决定 —— 所以同一轮"报告 5 条、事实流 3 条"是正确答案，不是又一处
不一致（用例把这两个数同时钉住）。覆盖度一律表达"取没取过"：没取 = `null`，取了且为空 = `0` —— 与 §28/§29 的
钱字段同一条纪律，只是这次落在对账报告上。

### 31.2 落地内容

- **汇总点**（`ccxt_reconcile_worker.rs:411-447`，本轮该文件 341 → 469 行，仍在未登记的 500 行门槛内）：新增纯函数 `ccxt_reconcile_round(remote_open_issues,
  local_order_issues) -> CcxtReconcileRound { order_issues, require_reconcile }`。`order_issues` 是两半按序拼接；
  `require_reconcile` 逐项用 `issue.get("client_order_id")?.as_u64()?` 过滤 —— 拿不到数字客户号就没有条目，
  事件标签由 `ccxt_reconcile_event_tag(kind)` 给出（`remote-terminal` / `remote-unmapped` / `remote-unknown` /
  `missing-remote` / `order-error` / `reconcile`）。
- **本地一半改成结构化 issue**（`:387` `ccxt_local_order_issue`）：原先两处 in-loop 的 `require_reconcile` 直接
  调用换成 `local_order_issues.push(...)`（`:260`、`:295`），两半从此同形。
- **三面单一读点**：`:305` 汇总一次；`:306-316` 事实流循环 `&round.require_reconcile`（写入点全文件只有一处，
  标签取 `fact.event_tag`）；`:328` 报告收 `&round.order_issues`；`ccxt_reconcile_service_status(&round, &balance_discrepancies)`（`:343`）；`:344-352` 的运行明细同时印
  `open_order_issues=` 与 `local_order_issues=` 两个数，不再只报一半。
- **健康结论抽成纯函数**（`ccxt_reconcile_service_status`，`:447-456`）：写在 worker 循环里时，"本地-only 的发现
  也要降级"这一条只能靠真实运行触发，用例够不着；抽出来之后它有了直接断言（`ccxt_reconcile_round.rs` 第二条）。
- **观测位**：`cashflows_observed`（`:80/88/111`，`fetch_ledger` 与资金费历史两条取数路径各算一次）、
  `positions_observed`（`:135/147`）、`funding_rates_observed`（`:169/177`）。报告里的三个计数改为
  `<flag>.then(...)`：没取 → `None`，取过 → 真实条数。
- **协议与 Binance**：`crates/qx-api/src/lib.rs:998-1006` 三个计数变 `Option<usize>` + `#[serde(default)]`
  （旧报告缺键读成 `None`，写 `0` 的读成 `Some(0)`）；`binance_reconcile.rs:157-159` 三项传 `None` 并注明该链
  只查余额与订单。
- **用例**：新增 `crates/qx-cli/src/tests/ccxt_reconcile_round.rs`（四类远端归并 + 一条本地-only，断言报告 5 条 /
  事实流 3 条、`remote-foreign` 那种字符串客户号不得造句柄、reason 非空）；
  `crates/qx-cli/src/tests/e2e_and_python_contract.rs` 的
  `reconcile_report_persists_structured_balance_discrepancy` 扩成"落盘 JSON 里 null 与 0 可区分"并加两条旧报告
  反序列化断言；`worker_observability.rs::ccxt_open_orders_report_unknown_and_unmapped_remote_risk` 把远端一半的
  四类口径继续单独咬住。
- **门禁**：`tools/check_architecture.py` 新增 `reconcile_round_honesty_check()`（10 项），钉住"汇总只一次"、
  三面读同一份、事实写入点唯一、`as_u64` 句柄判据、报告字段形态、Binance 报 `None`、三个观测位、用例在位。
  门禁总数 269 → 279 项。

### 31.3 本轮踩到与钉住的坑

- **"五连红"里有四条只是静态门禁红，行为用例全绿**（`/tmp/qx_q69_gate3.log`）。逐条看 TESTS 行：M1/M2/M3/M5 之下
  三组用例仍然 `1 passed`、`15 passed`、`1 passed` —— 只有 M4（句柄判据）真让 `ccxt_reconcile_round` 失败。报告
  与健康都是 worker 循环里的一个表达式，纯函数没抽出来的部分测不到。于是本轮补
  `ccxt_reconcile_service_status` 与第二条用例，并加缺陷本体的变异 M6（判定忽略发现清单）：**M6 是唯一一条让行为
  用例真失败的缺陷变异**，报告一半与事实流一半仍只由静态门禁钉住，这条差别如实记在 §31.4。
- **首轮日志（gate1）不是被变异弄红的，是被自家格式门槛拦下的**：`cargo clippy -D warnings` 在
  `funding_rates_observed.then(|| snapshot.funding_rates.len())` 上报 `unnecessary_lazy_evaluations` 硬错
  （`STAGE4_CLIPPY[qx-cli]_EXIT=101`），`rustfmt --check` 另有两处（新用例的长断言要折行、
  `mod ccxt_reconcile_round;` 要按字母序落到 `ccxt_position_facts_honesty` 之后）。gate1 因此只跑到 M2 就作废，
  三处修完的 gate2 才是本轮的验收日志。教训写在这里：**变异轮之前必须先把 fmt/clippy 跑绿**，否则变异段的红
  与格式的红混在同一份日志里。
- **两条门禁项最初共用一条标签，导致 M2 红得没名字**。原写法把"报告收两半"和"健康收两半"塞进同一个
  `check(...)`，M2（健康只看远端）变异时点名不清 → 拆成两条独立项，之后 M2 只触发
  "worker 健康判定读同一份 `round.order_issues`（本地-only 的发现也要降级）"。
- **`" ".join(worker.split())` 这种归一化对 rustfmt 折行不够**：`funding_rates_observed` 与 `.then(` 之间是换行加
  缩进，join 后仍留一个空格 → 改成去空白的 `tight = "".join(worker.split())`。
- **一条过度收紧的门禁子条件被撤**：`"unwrap_or(0)" not in report_region`。那个 `unwrap_or(0)` 发生在观测位之后，
  读的是"取到了，但这账户/venue 没有持仓行"，是正确写法，不是 §31.1 第 3 条的缺陷。
- **新用例先写在 `worker_observability.rs` 里越了门槛**（505 行 > 未登记的 500 行）→ 按既有约定另开专题模块，
  而不是给该文件登记预算。
- **共享树上的 `worker_observability.rs` 一度被脚本截成 0 字节**：补丁脚本用
  `os.popen('cd /p/... && git show ...')` 取 HEAD 内容，MSYS 风格路径在 cmd 下找不到路径、stdout 为空，脚本仍然
  执行了写文件。恢复手段是 `subprocess.run(['git','show','HEAD:<路径>'])` 拿回 blob 原字节直接写回，`cmp` 到
  逐字节一致。两条纪律：**不要从可能为空的捕获结果写文件**；跨语言取 git 内容用参数化 argv，不拼 shell 串。

### 31.4 验收数字（`/tmp/qx_q69_gate4.log`，2026-09-23 08:59:35 → 09:01:15；整树基线 `/tmp/qx_q69_ws5.log`）

| 项 | 实测 |
|---|---|
| STAGE1 整树编译 | `STAGE1_CHECK_EXIT=0` |
| STAGE2 行数棘轮 | `SNAPSHOT_EXIT=0`、`BUDGET_DIFF_LINES=0`（`maturity/line_budgets.yaml` 里 `crates/qx-api/src/lib.rs` 已在 STAGE2 之前从 3160 升到 3167，本轮快照不再变动） |
| STAGE2 门禁 | `架构不变量自检全部通过 ✓（279 项）`（改前 269 项；本轮 `check(` 调用 209 → 219） |
| STAGE3 fmt | `STAGE3_FMT_EXIT=0`（六个触及 Rust 文件逐个 `rustfmt --check`） |
| STAGE4 clippy `-D warnings` | `STAGE4_CLIPPY[qx-api]_EXIT=0`、`STAGE4_CLIPPY[qx-cli]_EXIT=0` |
| STAGE5 行为基线 | `ccxt_reconcile_round` `2 passed`、`worker_observability` `15 passed`、`reconcile_report` `1 passed`，均 `0 failed`（qx-cli binary 共 138 条用例） |
| STAGE6 六条变异 | M1 报告一半 / M2 健康调用点 / M3 事实流循环 / M4 句柄判据 / M5 Binance 恒 0 / M6 健康结论忽略发现 —— 全部 `MUT_STATE=red` 且 `OTHER_FAILS=0`（每条只点亮被测项），六次还原全 `RESTORE_EXACT` |
| 变异中被行为用例抓到的 | M4 与 M6：`ccxt_reconcile_round` `FAILED. 1 passed; 1 failed`。M1/M2/M3/M5 三组行为用例**全绿**，只有静态门禁红（差别记在 §31.3 与 §31.5 第 7 条） |
| STAGE7 整树（`QX_PYTHON` 指向 venv） | `WS5_EXIT=0`、76 条 `test result:` 行合计 `733 passed / 0 failed / 0 ignored` |
| 不设 `QX_PYTHON` 的整树 | gate4 内 `STAGE7_WS_EXIT=101`、`2 failed` —— 均为已知的本机无解释器两条 Python worker 契约项（`e2e_and_python_contract.rs:446`/`:490`），与 §29.4 同一口径 |
| 作废轮次 | `/tmp/qx_q69_gate1.log`（clippy 硬错 + fmt 两处）、`/tmp/qx_q69_gate2.log`（M2 needle 0 次命中）、`/tmp/qx_q69_gate3.log`（M1–M5 走完，暴露四条只有静态红） |


### 31.5 交给后续轮次的事实

1. **同一个 CCXT 文件里的资金费时间戳仍兜零**：`crates/qx-cli/src/ccxt_facts.rs` 缺 `timestamp_ms` 时
   `unwrap_or(0)`（§29.5 第 3 条原样，Q69 未触碰）。
2. **账户级五个钱字段仍无生产者**（`unrealized_pnl_raw` / `margin_raw` / `frozen_raw` / `realized_pnl_raw` /
   `funding_raw`，§29.5 第 2 条原样）。Q69 只把"报告里没取"变诚实，没有给这些字段接上来源。
3. **持仓行的浮亏仍算不出来**：要先把冻结的 market spec 送到读侧（§29.5 第 1 条原样）。
4. **远端孤单挂单依然只有报告 + 健康两面**，这是 §31.1 第 4 条的口径而非缺陷。若将来要求"报告里的一条发现能点回
   一张单据"，需要先给它一个可寻址对象（新的待办，不是回归）。
5. **Binance 对账链仍不查持仓/资金费/账单**：Q69 之后报告诚实写 `null`，取数器本身仍缺（记在
   `capabilities.yaml` 的 `binance_reconcile_still_fetches_only_balances_and_orders`）。
6. **含持仓观察的既有事件日志会被 manifest 核对拒掉**（§29.5 第 6 条原样）：升级路径仍未设计，与 §27.5 第 1 条
   的重 bless 决定同类。
7. **报告一半与事实流一半仍只有静态门禁钉着**（§31.3 第一条）：`additional_order_issues: &round.order_issues` 与
   `for fact in &round.require_reconcile` 都在 worker 循环体内，抽不出纯函数，M1/M3 两条变异下三组行为用例全绿。
   要拿到行为证明，需要一条"用假 CCXT client 跑完一整轮 `run_ccxt_reconcile_worker`，再从 SQLite 读回事实流与
   报告 JSON 互相对账"的集成用例 —— 那要先解决 worker 的 CCXT client 注入点，本轮未做。


## 32. Q70 收口记录：账户权益不再拿剩余现金冒充"算不出的权益"（2026-09-23，门禁 `/tmp/qx_q70_gate1.log`）

不在 §6 排期内，是 §28/§29 那条"缺席不等于零"纪律的最后一颗：交易链路 TX4。前两轮把账户的七个钱字段与
持仓行的两列推到 `Option`，权益是漏下的那一个。

### 32.1 缺陷与口径

**缺陷形状（改前全部取自 `HEAD`，即 `99c7051`）**：

1. **读模型把内核明说的"算不出"折成剩余现金**。`crates/qx-cli/src/api_service.rs:265-272`（改前）写的是
   `equity_for(...).unwrap_or_else(|| cash_for(...))`。而内核 `Ledger::equity_for`
   （`crates/qx-core/src/ledger/query.rs:156-204`）的 `None` 是有明确含义的：**任何一条持仓拿不到标记价**
   （`marks.get(instrument)?`）或估值溢出。于是只要账户里有一条没有行情事实的持仓（本轮用例就是这个形状：
   日志里只有成交、没有报价；对应实盘里只跑对账 worker 不跑行情 worker 的账户），读模型就替它宣布
   "权益 = 剩余现金"，等于宣称压在标的上的那一腿不值钱。
2. **协议层根本没有"没算"这个状态**。`crates/qx-protocol/src/lib.rs:106`（改前）是最后一个不可区分的钱标量
   `pub equity_raw: i128`，构造函数 `:181` 写 `equity_raw: 0`，取法 `:222` 写 `Some(self.equity_raw),`。
   所以"缺陷 1"不是某处笔误，而是这条链上唯一的表达形状。
3. **冒充出来的数在全链路自洽**。`available_raw`/`fees_raw`（Q67）与持仓行两列（Q68）都已带存在性标记，
   权益算不出时印出的仍是合法的、恰好等于现金的整数 —— 哈希、稳定 JSON、无损线格式、两个方向的 diff 全都
   一致，读侧无从发现。这也意味着"权益与可用资金相等"这一条在读侧不再是信息。

**口径**：权益与其余七个钱字段走同一条纪律 —— 它需要"现金账簿"**和**"每一条持仓的标记价"两半同时读得出才成立，
任一缺失即 `null`；价格的 `0` 哨兵（`average_price_raw`/`mark_price_raw`）不动，那是 §3.4 里另一套编码。

### 32.2 落地内容

- **协议**（`crates/qx-protocol/src/lib.rs` 885 → 888 行）：`pub equity_raw: Option<i128>,`（`:109`）、
  构造 `None`（`:184`）、取法 `self.equity_raw,` 直取（`:225`）、diff 的 `ScalarState.equity_raw` 同步（`:572`）。
  八个字段从此共用 Q67 那条 presence-tagged 哈希 / 稳定 JSON / 无损线格式 / 两个 diff 方向的通道，
  **本轮没有新增任何一份手抄字段清单**。
- **读模型**（`api_service.rs:268-272`）：直接发布 `equity_for` 的 `Option`，不再有 `unwrap_or_else`。
- **端点**（`crates/qx-api/src/lib.rs:1465`）：生产体**未改动** —— `.map(|snapshot| snapshot.equity_raw)`
  在字段变 `Option` 后仍把"没有快照"与"快照算不出"都印成 `null`、把 `Some(0)` 印成 `0`，所以它本来就不会
  错报；改的只用例（见下）。这条差别如实记着，因为它决定了 M3 的行为证明从哪来。
- **用例**：
  - 新增 `crates/qx-cli/src/tests/api_snapshot_money_fields.rs` 的
    `equity_without_a_mark_price_is_absent_rather_than_the_remaining_cash`（`:296-417`）—— 成对钉两件事：
    没有行情事实时权益 `None`、线格式与稳定 JSON 印 `null`、而 `available_raw` 照旧取得出现；补进一条 110
    的报价之后，同一个账户必须重新算得出 `equity = 现金 + 1×110`。缺席不是"永远不算"的挡箭牌。
  - 同文件 `available_is_the_settlement_cash_and_not_a_copy_of_equity`（`:79`）改成 Option-aware，
    仍要求"持仓未平时权益高于现金"且 available 不是 equity 的副本。
  - `settlement_currency_caliper.rs:149`/`:399` 两处按 `Some(...)` 比较。
  - 协议侧 `crates/qx-protocol/tests/snapshot_single_source.rs:342` 的 `OPTIONAL_MONEY_FIELDS` 从七个变**八个**
    （把 `equity_raw` 并进去），于是 Q67 那条遍历哈希 / seal / 两份线格式 / 两个 diff 方向的"缺席≠零"用例
    **自动**覆盖权益，不需要另写一条。
  - 端点用例 `balances_endpoint_publishes_absent_money_as_null_not_zero` 加第二段：默认快照（权益未算）在
    `/account/balances` 上必须印 `"equity_raw":null`。
- **门禁**（`tools/check_architecture.py` 3706 → 3734 行，280 项、`^    check(` 201 处，改前 279 项 / 200 处）：
  - 新增一项钉读模型（`:1876-1888`）：取 `snapshot.equity_raw =` 到分号这一整段语句，要求含 `equity_for(`、
    不含 `cash_for`、不含 `unwrap_or`。用"区域 + 子串"而不是整段字面量，是为了对 rustfmt 折行不敏感
    （§31.3 那个 needle 被折行作废一轮的坑不重演）。
  - "八个汇总钱字段在协议上全是 `Option<i128>`"项把 equity 并进类型清单（`:1804-1813`）。
  - 取法项从"数一行字面量"改成"盯函数体"（`:1814-1829`，见 §32.3 第 1 条）。
  - 端点项加 `"equity_raw": snapshot.as_ref().map(...)` 恰一次与 `equity_raw).unwrap_or` 缺席（`:1933` 附近）。
  - 读模型用例清单从四条称五条（新增的那条权益用例进 `SNAPSHOT_MONEY_CASES`）。
- **行数预算**：`crates/qx-protocol/src/lib.rs` 885 → 888、`crates/qx-api/src/lib.rs` 3167 → 3180，
  本轮 `--snapshot` 只动这两项（`BUDGET_DIFF_LINES=2`）。

### 32.3 本轮踩到与钉住的坑

- **我自己第一版写的取法门禁项挡不住本轮的缺陷变异**。原判据是
  `protocol.count("Some(self.equity_raw),") == 0` 且 `protocol.count("self.equity_raw,") == 1`，而
  `Some(self.equity_raw.unwrap_or(0)),` 两种写法都不命中 —— 也就是说 M2 会让行为用例红，却让静态项
  "红得没名字"。所以判据换成截 `fn scalar_money_raw` 的整个函数体，禁体内出现 `Some(` 与 `unwrap_or`，
  并要求八项逐个直取。这是"字面量计数"型门禁的通病，跟 §31.3 的折行坑同族：**判据要盯语义单元，不是盯一行文本**。
- **端点用例最初测不到权益**：`balances_endpoint_publishes_absent_money_as_null_not_zero` 里写的是
  `equity_raw = Some(500)`：把 `None` 折成 `0` 的变异动不了那三条断言，M3 的 panic 点
  `crates/qx-api/src/lib.rs:2545` 正是本轮新增的"默认快照 → null"那一段（日志里 M3 之下另外三组用例全绿）。
  也就是说，那个发布面原本对权益的缺席态是盲的。纪律：**给一个字段新增"缺席"状态时，逐个发布面检查是否真有一条用例让它缺席**。
- **M5（字段退回 `i128`）是本轮唯一必然编译不过的变异**：`STAGE6_M5_CHECK_EXIT=101`，三条 `error[E0308]`。
  它的证明形状本来就是"静态项 + 编译器"，不硬凑行为用例 —— 类型层的保证由类型系统管。
- **M6 那一次 qx-api 重链接被本机文件占用挡了一下**：日志里
  `TESTS M6 [qx-api --lib balances_endpoint] error: linking with 'link.exe' failed: exit code: 1104`
  （紧跟 M5 还原之后的重建窗口）。`M6_RESTORED` 同一格 `1 passed`、STAGE7 整树 `0 failed` ——
  记为本机 flake，不是信号。
- **Q67 那条"available 不得抄 equity"的禁抄项仍成立，且现在更需要**：字段变 `Option` 后
  `snapshot.available_raw = snapshot.equity_raw` 从"编译不过"变成"编译得过"，所以判据里连 `Some(…)` 包起来的
  形态一起禁（`tools/check_architecture.py:1985-1997` 的注释即为此写）。

### 32.4 验收数字（`/tmp/qx_q70_gate1.log`，2026-09-23 19:05:08 → 19:07:55）

| 项 | 实测 |
|---|---|
| STAGE1 整树编译 | `STAGE1_CHECK_EXIT=0` |
| STAGE2 行数棘轮 | `SNAPSHOT_EXIT=0`、`BUDGET_DIFF_LINES=2`（只有 §32.2 末列的两项）、`ARCH_AFTER_SNAPSHOT=0` |
| STAGE3 fmt | `STAGE3_FMT_EXIT=0`（七个触及的 Rust 文件逐个 `rustfmt --check`，其中两个是 CRLF 文件） |
| STAGE4 clippy `-D warnings` | `qx-protocol` / `qx-api` / `qx-cli` 三格 `_EXIT=0` |
| STAGE5 行为基线 | `snapshot_single_source` 10 passed、`api_snapshot_money` 7 passed（132 filtered，qx-cli binary 共 139 条）、`settlement_currency` 11 passed、`balances_endpoint` 1 passed，均 0 failed |
| STAGE6 六条变异 | M1 读模型兜底 / M2 取法折零 / M3 端点折零 / M4 共用的 null 编码器印 0 / M5 字段退回 `i128` / M6 读模型干脆不算权益 —— 六条全部 `MUT_STATE=red`、`ARCH_FAIL_LINES=1`、`OTHER_FAILS=0`（每条只点亮被测那一项），六次还原全 `RESTORE_EXACT` |
| 变异的行为证明 | M1：`equity_without_a_mark_price_…` FAILED（`api_snapshot_money_fields.rs:378`）。M2：协议 `uncomputed_money_is_not_the_same_state_as_computed_zero` FAILED（`snapshot_single_source.rs:401`）。M3：端点用例 FAILED（`crates/qx-api/src/lib.rs:2545`）。M4：协议 `:413`/`:557` 与 CLI `:384`/`:168` **四条同时红**（编码器共用，一处改坏处处红）。M6：新用例 `:378` **加** `available_is_the_settlement_cash_…`（`:101`）两条 —— 后一条钉的就是"有标记价时必须算得出"那半边。M5：只有静态项红，另附 `STAGE6_M5_CHECK_EXIT=101` |
| 与 §31.4 的对比 | Q69 六条变异里四条只有静态门禁红；本轮六条中五条（M1/M2/M3/M4/M6）都有行为用例同红，唯一例外是 M5 —— 它的性质就是类型层的事 |
| STAGE7 整树（`QX_PYTHON` 指向 venv） | `STAGE7_WS_EXIT=0`、76 条 `test result: ok`、合计 `734 passed / 0 failed` |
| STAGE8 复跑门禁 | `架构不变量自检全部通过 ✓（280 项）`、`STAGE8_ARCH_EXIT=0`、`ARCH_CHECK_CALL_SITES=201` |
| 作废轮次 | 无 —— `gate1` 即本轮验收日志（fmt/clippy 在变异段之前先跑绿，是 §31.3 那条教训的落地） |

### 32.5 交给后续轮次的事实

1. **账户级五个钱字段仍无生产者**（`unrealized_pnl_raw` / `margin_raw` / `frozen_raw` / `realized_pnl_raw` /
   `funding_raw`，§29.5 第 2 条原样）。Q70 只把权益这一颗推到诚实，没有给那五个接上来源。
2. **持仓行的浮盈仍算不出来**：要先把冻结的 market spec 送到读侧（§29.5 第 1 条原样）。权益现在能算，
   靠的是 EventLog 里的标记价，不是规格折算。
3. **同一个"退回现金"的表达式在风控侧是保守用法，不得顺手统一**：
   `crates/qx-cli/src/venue_runtime/worker_runtime.rs:189-191` 的 Paper 现货分支
   `equity_for(...).unwrap_or(cash_only)` 方向是"算不出时把权益往小里说"，闸门只会更紧，其函数注释
   （`:179`）已写明这一点。Q70 的红线只划在读模型侧（门禁项的作用域就是 `api_service.rs` 那一段）。
4. **三把权益尺子仍在**：读模型 `equity_for`（现货名义）、`paper_available_margin`（spec 尺子）、
   `crates/qx-cli/src/strategy_contract.rs:406-409` 的 `available_margin_raw`（也是 `equity_for`，且本来就把
   `Option` 原样交给策略上下文）。本轮未追策略上下文消费方如何区分"没算"与"零"，属下一颗。
5. **权益 `null` 时读侧看不出是哪条持仓缺标记价**：要给出诊断需要新的线格式形状（一条缺价标的清单），
   本轮刻意不加 —— 先把"不冒充"做对，再考虑"说清楚为什么算不出"。
6. **`AccountSnapshot` 没有 `#[serde(default)]`**：外部 JSON 必须带全八个标量键（Q67 起如此，Q70 把这个数
   从七改八）。老客户端发 `"equity_raw": 数字` 仍读成 `Some(数字)`，缺键则整份快照读失败。
7. **`equity_for_with_spec` 只估 spec 自己那条腿**：共享账户日志上另一条腿的持仓不进气（§29.5 原样）。


## 33. Q71 收口记录：多腿组合收益按两条腿的钱算，不再把两腿 bps 平均（2026-09-23，门禁 `/tmp/qx_q71_gate1.log`）

不在 §6 排期内，是回测链的第八颗（回测 FN8）。§28/§29/§32 那条纪律管的是"没算过的钱不许印成 0"，
这一颗管的是**加权**：一个印在终端上的收益数字，如果它是把两个各自分母不同的比率等权平均出来的，
那它既不代表组合、也不代表任何一条腿 —— 而且它看起来完全像一个合理的组合收益率。

### 33.1 缺陷与口径

**缺陷形状（改前取自本轮开始时的 `HEAD`，即 Q70 的 `4264760`；`99c7051` 上同一行同形）**：
`crates/qx-cli/src/backtests/multi_builtin.rs:274` 在
`[Multi-leg · Backtest]` 那一行里印的是

```rust
(i64::from(primary_report.return_bps) + i64::from(reference_report.return_bps)) / 2,
```

两个加数各自是"这条腿期末权益相对**这条腿自己的**本金"的比率，而两条腿的本金由 `multi_leg_leg_cash`
（`crates/qx-cli/src/backtests/leg_funding.rs:50-65`）按**本腿行情帧的最高价**分别定资 —— 本金天然不等，
把两个比率平均等于宣称两条腿投了一样多的钱。这不是理论问题：仓库自带的那对现货夹具就是极端不对称的。

**实测分叉**（`backtest multi-builtin pairs_arbitrage … --quantity 100 --funding-bps 0`，收口后的代码跑）：

| 事实 | 主腿 BTCUSDT.BINANCE | 对冲腿 ETHUSDT.BINANCE |
|---|---|---|
| 期初本金（1e-9 定点整数） | 12,606,301,000,000,000 | 600,301,000,000,000 |
| 期末权益 | 12,357,121,000,000,000 | 599,851,000,000,000 |
| 腿级 `return_bps` | -197 | -7 |

两腿本金差 **21.0 倍**。合计投 13,206,602（同一份定点数除以 1e9）、合计亏 249,630，
`-249630 × 10000 ÷ 13206602 = -189.019…` → **`combined_return_bps=-189`**；
而等权平均念出来的是 `(-197 + -7) / 2 = -102`。**这个组合的真实亏损被念轻了 87bp，接近一半。**
除法沿 Rust 整除的向零截断（与引擎其余 bps 数字同一口径）；拿 python `//` 复核的人会先得到 -190，
那不是本仓库的算法，用例里也是同一份 Rust 整除。

**口径**：组合收益只许有一个算法 —— `Σ(期末权益 − 期初本金) × 10000 ÷ Σ期初本金`。
合计本金 ≤ 0、`× 10000` 越界、结果超出 `i64` 三种情形一律 `Err` 而不是返回一个数：印 0 会把
"这个组合根本没法度量"伪装成"这单套利不赚不赔"，与 `multi_leg_leg_cash` 撤掉
"静默截断到 `i64::MAX`"（§4.18）是同一条纪律。

### 33.2 落地内容

- **实现**（`leg_funding.rs` 130 → **165 行**）：新增 `multi_leg_combined_return_bps`（`:161-185`），
  输入是 `[(&str, i128, i128); 2]`（标签、期初本金、期末权益）而不是两条 `BacktestReport` ——
  纯整数签名才能直接喂进单元用例去测溢出与零分母那两支。放在 `leg_funding.rs` 而不是
  `multi_leg.rs`：它和 `multi_leg_leg_cash` 是同一件事的两端（先按本腿行情定资，再按那份本金称收益），
  分住两处就等于"权重"这个口径又有了第二个答案。
- **调用点**（`multi_builtin.rs` 492 → **493 行**，净 +1）：`:268-273` 取两腿权益并调那一份实现，
  `:283` 把结果送进 `[Multi-leg · Backtest]`；产物侧 `accounts` 补两腿期末权益（`:450-451`，
  与 `:447-448` 的期初本金同侧，缺一侧就复算不出另一侧），`totals.combined_return_bps`（`:464`）。
- **一次被行数逼出来的搬家**：新代码落地时 `multi_builtin.rs` 会越过 Phase 4s 那条"回测主题模块逐个
  `< OVERSIZED(500)`"的兄弟模块线（`tools/check_architecture.py:711` 的 `cli_backtest_module_check()`，
  `:723` 处 `>= OVERSIZED` 即红；这条线登记进 `maturity/line_budgets.yaml` 也救不了，判据不看预算表）。
  越界不是靠放宽判据过去的：把 `:316` 那 11 行组级合计折叠搬进归因内核 `multi_leg.rs:452-470`
  （`multi_leg_group_totals`，返回同一个五元组，保证金仍取各组峰值而非求和），入口只留 1 行调用 ——
  反过来说，今天这 493 行若不搬就是 503 行（493 + 11 − 1），当场红。它合计的正是那个内核自己的产物，
  所以这次搬家两头都说得通。
  结果 `multi_leg.rs` 450 → 470，`maturity/line_budgets.yaml` **一个字节都没动**（`BUDGET_DIFF_LINES=0`）。
- **用例**：
  - 单元 `crates/qx-cli/src/tests/execution_and_multi_leg.rs:304-331`
    （`multi_leg_combined_return_weights_each_leg_by_its_own_capital`）：主腿本金是对冲腿 3 倍、两腿分别
    +100bp 与 +1000bp 时必须给 **325bp** 而不是等权的 550bp；本金相等时两种算法重合（同两组收益率
    给 550bp，排除"只是换了个说法"的解读）；亏 1000bp 的主腿压过持平的对冲腿时给 -750bp 而不是 -500bp；
    两腿本金全零时 `unwrap_err()` 且错误文案含"本金合计必须为正"。
  - 端到端 `crates/qx-cli/tests/multi_leg_attribution.rs:726-792`
    （`combined_return_pools_both_legs_by_capital_instead_of_averaging_bps`）：跑真实 CLI，从产物自己声明的
    `accounts/*_initial_cash` 与 `accounts/*_final_equity_raw` 复算钱口径，要求 stdout 的
    `combined_return_bps` 等于它、产物里的同一颗也等于它，并**另加一条 `!=` 两腿平均**的断言 ——
    否则这条用例在旧实现下也是绿的。
- **门禁**（`tools/check_architecture.py` 3734 → **3783 行**，280 → **283 项**，四缩进 `check(` 201 → 204）：
  `multi_leg_honesty_check()` 从十二条长到十五条，新增的三条各自抽掉就变红（`:3423` / `:3436` / `:3449`）：
  ① 调用点只能出现 `multi_leg_combined_return_bps([`，`i64::from(primary_report.return_bps)` 不得复活，
  实现体内必须留着"本金合计必须为正"的错误文案、且不得出现 `Ok(0)` 或 `unwrap_or`；
  ② 单元与端到端两条用例名齐备，且产物两腿期末权益两个键齐备；
  ③ 组级合计折叠只在 `multi_leg_group_totals` 一处（入口重新自己 loop 一遍即红）。
- **用例地板**：`CLI_TEST_FLOOR` 178 → **183**。这个数字本轮实际上落后两次 —— Q69/Q70 落地时新增的
  三条用例没有回写地板（磁盘 181 而门禁仍写着 178），加本轮两条一起按实测总数（`src/tests` 143 +
  `crates/qx-cli/tests` 40）抬平，并把这段滞后写进 `:383-392` 的注释里。

### 33.3 本轮踩到与钉住的坑

- **一条我自己写的判据是假防守，被预检变异当场抓到**。原判据含 `"funded_raw <= 0" in combined_body`，
  而 M2 的坏形状恰恰是**保留那个条件、把 `Err` 换成 `Ok(0)`** —— 条件还在，判据仍绿。换成钉错误文案
  （`"本金合计必须为正" in combined_body`）加两种折零形状缺席（`Ok(0)`、`unwrap_or`）。这是
  §28.3 那条"改类型之后禁抄字面量的项可能变成永不咬的空判据"的第二次踩法：**每写一条"某串文本必须
  在/不在"的判据，先问一句"我要防的那个坏形状写出来还带着它吗"**。
- **rustfmt 会按 76 字符把元组拆成 4 行**，本轮 4 行 × 2 = 8 行的增量正好把文件顶过 `< 500`。
  修法不是压注释而是先 `let primary_equity = primary_report.final_equity();` 再传（同 §29.3 那条
  `fn_call_width=60` 的坑）；顺带这两个绑定在产物侧复用，`accounts` 与调用点读的是同一个值。
- **产物字段的位置在收口过程中挪过一次**：两腿期末权益先写进 `totals`，后来移到 `accounts`
  —— "组合收益的两端"里期初那端本来就在 `accounts`，把期末单独放进 `totals`（那里全是跨腿合计）
  会让复算的人两头找。门禁判据按**键名**而不是按键所在块匹配，所以只有用例里那张表名改了一处。
- **M4 是本轮唯一"只有静态项红、行为用例全绿"的变异**，这是预期而不是洞：把那 11 行折叠搬回
  编排入口算的是同一份五元组，任何行为用例都分不出来；它要防的是"下一次改合计只改了其中一份"。
  与 §27 那条"只有文本防守"的结论同族，如实记着，没有为了配对好看去补一条只在变异下才红的假用例。
- **旧用例里的 `combined_return_bps != "0"` 断言没被本轮改动**：`crates/qx-cli/tests/builtin_signal_from_config.rs`
  那两条（近档要求非零、远档要求恰好为 0）在新口径下仍成立 —— 远档是 `fills=0` 的零成交，两条腿的
  期末权益都等于各自本金，按钱合起来同样是 0。
  这一条要在收口时逐个复核，因为"平均"与"加权"在零附近恰好重合，旧断言不构成新口径的证据。

### 33.4 验收数字（`/tmp/qx_q71_gate1.log`，2026-09-23 20:03:48 → 20:05:28）

| 项 | 实测 |
|---|---|
| STAGE1 整树编译 | `STAGE1_CHECK_EXIT=0` |
| STAGE2 行数棘轮 | `SNAPSHOT_EXIT=0`、`BUDGET_DIFF_LINES=0`（预算表与本轮开始前逐字节相同）、`ARCH_AFTER_SNAPSHOT=0` |
| STAGE3 fmt | `STAGE3_FMT_EXIT=0`（五个触及的 Rust 文件逐个 `rustfmt --check`） |
| STAGE4 clippy `-D warnings` | `STAGE4_CLIPPY[qx-cli]_EXIT=0`（本轮只改 qx-cli 与门禁脚本） |
| STAGE5 行为基线 | `multi_leg_attribution` 11 passed、`multi_leg_combined_return` 1 passed（139 filtered）、`builtin_signal_from_config` 7 passed，均 0 failed |
| STAGE6 四条变异 | 全部 `MUT_STATE=red`、`ARCH_FAIL_LINES=1`、`OTHER_FAILS=0`、`PASS_LINES=282`（每条只点亮被测那一项），四次还原全 `RESTORE_EXACT` |
| 变异对应的事实 | M1 调用点退回平均 → 静态项红 + 端到端用例红（`multi_leg_attribution.rs:774` 那条"stdout 必须等于钱口径"先炸；`:786` 另有一条 `!=` 两腿平均，旧实现下它也炸 —— 这一条才是"用例不是换了个说法"的证明）；M2 合计本金非正返回 `Ok(0)` → 静态项红 + 单元用例红（`execution_and_multi_leg.rs:326` 的 `unwrap_err()`）；M3 产物删掉两腿期末权益 → 覆盖项红 + 端到端用例红（`:756` 缺键 panic）；M4 入口重新自己折一遍合计 → 只有结构项红，行为用例全绿（预期，见 §33.3） |
| STAGE7 整树（`QX_PYTHON` 指向 venv） | `STAGE7_WS_EXIT=0`、`WS_OK_LINES=76`、合计 `WS_PASSED_TOTAL=736 / WS_FAILED_TOTAL=0`，与 Q70 的 76 / 734 同一批目标（本轮多出的 2 条正是新增的单元 + 端到端用例） |
| STAGE8 复跑门禁 | `架构不变量自检全部通过 ✓（283 项）`、`STAGE8_ARCH_EXIT=0`、`MODIFIED_DEPLOY=0`（跟踪产物零改写）、`PRISTINE_OK final` |
| 作废轮次 | 无 —— `gate1` 即本轮验收日志；needle 唯一性在开跑前用一次性预检脚本量过（八个 needle 全 `count=1`） |

### 33.5 交给后续轮次的事实

1. **`cost_bps` 仍会把越界念成 `i64::MAX`**：`multi_builtin.rs:379-383` 是
   `saturating_mul(10_000) / totals.1` 再 `unwrap_or(i64::MAX)`。这与本轮 M2 防的"折成 0"同型
   （一个是把算不出说成不赚不赔，一个是把算不出说成赚疯了），只是需要一个真能越界的夹具才谈得上；
   下一颗按 `multi_leg_combined_return_bps` 的同一形状改成报错。
2. **`BacktestReport::final_equity()` 的空曲线兜底是 0**：`crates/qx-xingban/src/backtest.rs:220-222`
   写 `*self.equity.last().unwrap_or(&0)`，而高保真那条链的 `final_equity()`
   （`orderbook_backtest.rs:197-199`）兜的是 `initial_equity_raw` —— 两把尺子。本轮之后组合收益直接
   吃这个值：目前不可达（Bar 内核在空行情帧上先报错），但它是"缺席折成零"在回测链上最后一个藏身处。
3. **多腿链不写 `summary.json`**，所以 `combined_return_bps` 只在 stdout 与归因产物里，单腿产物侧的
   同名指标仍来自引擎自己。这与 §30.5 那条"多腿口径只在产物"是同一条缺口。
4. **权重永远由定资规则决定**：本金取自 `multi_leg_leg_cash`（本腿最高价 + 同名义额 taker 费余量，
   下限 100,000），用户无法指定两腿资金分配。也就是说这条链念出的组合收益率天然绑着它自己的定资
   口径 —— 产物里那三行 `accounts` 是这件事的唯一证据，`--primary-cash` 一类的显式定资没做。
5. **组合保证金取各组峰值（`max`）而非求和，本轮没有为它设计变异**：判据只写在
   `multi_leg_group_totals` 的文档注释里（"同一账户上两条腿共享那份保证金，加总等于把它算两遍"）。
   仓库夹具里 `margin_peak_raw` 在现货用例恒为 0、衍生品用例只成 3 组，`max`↔`sum` 的互换会不会被
   现有用例抓住，本轮未测。

## 34. Q72 收口记录：回测压在多少钱上，从"三处字面量"变成一格可声明、四处可见的事实（2026-09-23，门禁 `/tmp/qx_q72_gate3.log`）

不在 §6 排期内，是回测链的第九颗（回测 FN9）。§28/§29/§32/§33 那条纪律管的是"没算过的钱不许印成 0"，
这一颗管的是**被假定的钱**：一个数字如果每条链都心里有数、嘴上不说，那它和"没算过"是同一类缺陷 ——
它决定收益率的分母、风控门看到的可用现金、以及引擎那句"买入成本（含预估手续费）超过账户可用现金"，
而使用者从头到尾没被问过一句，也没在哪个产物里读得到它。

### 34.1 缺陷与口径

**缺陷形状（本轮开始时的 `HEAD` = `99c7051`）**：`Money::from_i64(100_000)` 在生产代码里出现 **6 次** ——
`backtests/mod.rs:49`（装配里那个看不见的默认）、`backtests/depth.rs:80/81/144/157`（现金腿、可用保证金、
两个引擎配置）、`backtests/single_strategy.rs:58`（策略链自己的一份），另有 `ecosystem_smoke.rs:345-346`
自检的副本。三条链的形态各不相同：策略链自己写一个再**事后改写装配字段**（`single_strategy.rs:85`
`assembly.initial_cash = initial_cash;`，因为 `BarBacktestAssembly.initial_cash` 当时是 `pub(crate)`，
`mod.rs:14`），深度链绕过装配直接写进两个引擎配置，内置链（`backtest builtin`）**一格都没写**，
于是它吃的正是 `mod.rs:49` 那个从没被人读过的默认。摘要当时是 schema v3（`artifacts.rs:294`），
只有 `metrics.final_equity_raw`，期初那笔钱根本不在产物里。

**实测分叉**（同一份 `sma_cross`、同一份 `pairs-primary` 夹具、同一个 `--quantity 2`，
本轮 `/tmp/qx_q72_probe/probe.log`）：

| `strategy.initial_cash_raw` | `[Builtin · Account]` | `fills` | `return_bps` | `result_hash` | 拒单 |
|---|---|---|---|---|---|
| 没声明 | `initial_cash_raw=100000000000000 account_base_source=builtin-default` | 0 | 0 | `892f8478c281a3e5` | 1x 超过账户可用现金 + 1x 禁止卖空 |
| `200000000000000` | `initial_cash_raw=200000000000000 account_base_source=strategy-initial-cash` | 1 | -23 | `4faa4db9d340864d` | 只剩 1x 禁止卖空 |

改前这两行的差别全在第一列，而后两列长得分不出彼此：`fills=0 return_bps=0` 念出来像"这策略不赚不赔"，
真相是"这两单位没人给它算过钱"。本轮把差别的来源摆到台面上之后，同一道命令才第一次有了
"我跑在 100,000 上"这句话可听、可抄、可复算。

**口径**（与 Q0a/Q0c 的费用、Q1a 的撮合模型同构，一条也没放宽）：
① 默认本金是仓库里**唯一一处具名常数**，生产代码不得再抄字面量；② 它是装配的**必答构造参数**，
字段私有 ⇒ 新链漏答过不了编译，答完也不许事后改写；③ 每条单腿链必须在结果行之前把
"用了多少 + 从哪来"印进 stdout，落摘要的链还要写进 `account` 块；④ 来源分三种
（`builtin-default` / `strategy-initial-cash` / `multi-leg-funding-rule`），"没配"与"配了同一个数"必须分得开；
⑤ 非正声明**当场报错**而不是回落默认；⑥ 两条腿本金各按本腿行情定资的多腿链**拒绝**那一格单账户数字，
而不是收下再静默丢掉（与 §22 的 A 股段、§23 的延迟设置同一条纪律）。

### 34.2 落地内容

- **新落点 `crates/qx-cli/src/backtests/account_base.rs`（0 → 67 行）**：常数是这一处
  （`DEFAULT_BACKTEST_INITIAL_CASH: i64 = 100_000`，`:12`），三个来源标签 `:15-18`，
  `BacktestAccountBase{cash, source}` `:21-25`，读法 `backtest_initial_cash(Option<i128>)` `:31-48`，
  配置侧那一格的取数 `configured_initial_cash_raw` `:51-58`，stdout 与摘要共用的排版
  `backtest_account_base_note` `:61-67`。非正声明的报错文案点出键名、值与理由
  （"收益率拿它做分母、风控拿它当可用现金，零或负数账户上跑出来的数字没有任何含义"）。
- **装配侧改成必答题**（`backtests/mod.rs` 220 → **234 行**）：字段 `initial_cash: Money` 去掉
  `pub(crate)`（`:16`），`new` 多一个 `initial_cash: Money` 入参（`:48`），装配体内不再有默认值，
  `:74` 只是原样搬进 `into_config`；`single_strategy.rs:85` 那行事后改写随之删除。
- **三条链各自接上**：策略链 `single_strategy.rs:58-59` 读 `config.strategy.initial_cash_raw`，
  印 `[Strategy · Account]`（`:215-216`）；内置链 `:294` 读 `configured_initial_cash_raw(runtime_config_path)`
  并把本金作为第 3 个入参交给装配（`:301`），印 `[Builtin · Account]`（`:371-372`）—— 这条链不落摘要，
  stdout 是本金对使用者唯一的出口；深度链 `depth.rs:74-75` 读同一格，现金腿与可用保证金（`:84-85`）、
  两个引擎配置全部改用这一个值，印 `[Depth · Account]`（`:228-229`）。`ecosystem_smoke.rs:345-360`
  的自检读同一份常数，"仓库里唯一一处"因此真就只有一处。
- **多腿链当场拒**（`leg_funding.rs` 165 → **185 行**）：新增 `reject_configured_initial_cash`（`:73-86`），
  `multi_builtin.rs:44` 在 `configured_fill_model` 之后立刻问一次；错误文案把本链的定资规则
  （`2 × quantity × 本腿全帧最高价 + 同名义额手续费余量`，下限 `MULTI_LEG_ACCOUNT_CASH_FLOOR`）
  与"改用 quantity 表达规模"一起说清。产物侧 `:446` 的 `accounts.account_base_source` 写
  `multi-leg-funding-rule`。
- **摘要升到 v4**（`artifacts.rs` 378 → **389 行**）：`BacktestArtifactsInput` 多一个必填字段
  `account_base: BacktestAccountBase`（`:223`，所以新增一条落摘要的链必须答它），
  `schema_version` 3 → **4**（`:301`），新增 `account` 块两格（`:305-308`），定点整数按仓库惯例
  写成字符串。
- **配置面**（`crates/qx-runtime/src/runtime_config/strategy_schema.rs:129-137`）：
  `pub initial_cash_raw: Option<i128>`（`Money` 是 `i128` 定点数，所以没有 i64 天花板），
  带 `#[serde(default, skip_serializing_if = "Option::is_none")]` —— 省略时配置序列化字节必须逐字节不变，
  否则已 bless 的 `config_fingerprint` 会因一个没改变任何行为的字段集体失真（`RunManifest` 同理）。
- **一次被行数逼出来的搬家**：`single_strategy.rs` 加完两处读点与两行打印会越过 Phase 4s 那条
  "回测主题模块逐个 `< OVERSIZED(500)`" 的兄弟模块线（`tools/check_architecture.py` 的
  `cli_backtest_module_check()`，登记进预算表也救不了）。搬出去的是与本金无关、但同样"三条链共用一句
  措辞"的信号参数侧：`apply_configured_builtin_signal` 与 `builtin_signal_note` 移进
  `backtests/signal_binding.rs`（0 → **47 行**），`single_strategy.rs` 496 → **477 行**。
  这次搬家顺带暴露了一条按路径取数的旧判据（见 §34.3 第四条）。
- **用例 8 条（地板 183 → 191）**：
  - 单元 `crates/qx-cli/src/tests/backtest_account_base.rs`（61 行，3 条）：默认必须是**有名字的**常数
    且写法为 `initial_cash_raw=100000000000000 account_base_source=builtin-default`；声明值原样落地
    （含 `Money::from_raw(123_456_789)` 不被任何整数单位截断）而来源改口为 `strategy-initial-cash`；
    `0 / -1 / -1e9` 三种声明全部 `unwrap_err()`。
  - 命令行 `crates/qx-cli/tests/backtest_account_base.rs`（339 行，5 条，真实子进程）：
    内置链没声明也要把默认印出来；**同一份配置只差那一格本金 → 结果必须不同**（`fills` 0→1、
    `result_hash` 变），并断言 `account_base_source` 两种写法可区分；非正声明 exit 2；
    落摘要的两条链（`strategy backtest` / `backtest book --fill-tier l1`）产物里
    `account == {initial_cash_raw, source=strategy-initial-cash}` 且 `schema_version == 4`；
    多腿入口 exit 2 且归因产物写 `multi-leg-funding-rule`。
  - 既有两条跟着改口径：`src/tests/backtest_entries.rs` 的装配用例不再比对"装配自带的默认"，改成
    "装配必须原样带出调用方答的本金"；`src/tests/backtest_replay_gate.rs` 的摘要夹具多一个
    `account_fixture()`，schema 断言与 `summary["account"]` 一起钉。
- **门禁 283 → 293 项**（`tools/check_architecture.py` 3783 → **3964 行**）：新增
  `backtest_account_base_check()`（`:2322` 起，10 条），逐个抽掉即红 ——
  ① 默认常数是唯一一处具名定义，且**全 qx-cli 生产代码扫不到 `from_i64(100_000)` 字面量**
  （跳过 `tests` 目录与文件尾部 `#[cfg(test)]`）；② 三个来源标签各有常量，多腿链不得冒充
  `builtin-default`；③ 装配字段私有、无默认、事后不可回写；④ 三条单腿链各自读过那一格声明
  （`config.strategy.initial_cash_raw` 1 次 + `configured_initial_cash_raw(` Bar 1 次 / 深度 1 次）；
  ⑤ 三行 `[X · Account]` 各印一次且都经同一函数排版；⑥ 非正声明闸门在位；⑦ 摘要 `account` 两格与 v4；
  ⑧ schema 字段形态；⑨ 多腿链的拒绝**必须带 `?` 传播**；⑩ 八条用例名齐备。
  `CLI_TEST_FLOOR` 183 → **191**（磁盘 `^#[test]$` 实测 `src/tests` 146 + `crates/qx-cli/tests` 45，
  `DISK_TEST_TOTAL=191`；默认 `cargo test -p qx-cli` 跑 188 条，另有 3 条在 `#[cfg(feature = "nats")]` 后面 ——
  地板按磁盘口径计，换 feature 组合不得少用例）。
- **登记集同步**：`CLI_BACKTESTS_MODULES` 加 `account_base`、`signal_binding` 两颗
  （`mounted == 登记集` 那条判据会当场拒绝未登记的模块）；`BACKTEST_ENTRY_OWNERS` 未加条目 ——
  本金这条不是"入口"，它的唯一性由新写的 ①②④⑨ 四条钉。

### 34.3 本轮踩到与钉住的坑

- **门禁项数与地板都不再"顺手估"**：191 这个数是 `grep` 量出来的，而 `cargo test -p qx-cli` 报 188 ——
  相差 3 条是 `worker_observability.rs` 里 `#[cfg(feature = "nats")]` 的用例。第一次我按 cargo 的数
  写注释，改成按磁盘口径并把差值解释写进 `:391-396`。
- **默认值改口径要连既有用例的判据一起改**：`backtest_entries.rs` 那两条原本断言"装配自带 100,000 默认"，
  必答题化之后那句话变成了本轮要消灭的形状；只改实现不改用例会让用例反过来防守旧缺陷。
- **`MUT_STATE=unexpected` 是判据文案与 want 串对不上，不是判据没咬**。拆项时我把第 ⑥ 条改成
  "非正声明**当场**报错而不是回落默认"，脚本里的 `arch_state` 仍写旧半句，gate2 的 M2 于是记成
  "点亮了别的项"。✗ 行与两条行为用例（命令行 1 红 + 单元 1 红）其实都在，但"want 命中"这项证据
  只能重跑取回 —— 整轮作废，`gate3` 才是本轮验收日志。教训与 §28.3 同型：**改判据文案时，
  变异脚本里的 want 串必须同批改**，否则日志会自我否认。
- **gate1 的 M5 是一条真空判据，被预检变异当场抓到**：原判据只数 `multi_builtin.rs` 里
  `reject_configured_initial_cash(` 出现几次，而坏形状恰恰是**保留这个调用、把 `?` 换成 `let _ =`** ——
  调用点还在，判据仍绿（`ARCH_FAIL_LINES=0`、`MUT_STATE=green`），只有行为用例侧红了。
  改成"这个调用点必须带 `?` 传播"（正则 `reject_configured_initial_cash[(][^)]*[)][?];` 命中 1 处）后，
  同一颗变异点亮自己那一项。这是 §33.3 那条"每写一条'某串文本必须在/不在'的判据，先问一句
  我要防的坏形状写出来还带着它吗"的**第三次**踩法。
- **拆文件必须回头清点按路径取数的门禁**：`signal_binding.rs` 落地后 `builtin_signal_check()` 的两条
  Q64 判据立刻红（`内置链侧的包装定义一处 — 定义 0 处`、`覆盖之后必须重做参数体检`），因为它按
  `single_strategy.rs` 这个路径取文本。修法是 `BUILTIN_SIGNAL_WRAPPER_FILE` 指向新落点，并把
  "包装里含调用点"这半个判据改成"定义文件集合 == 那一个文件"（调用次数本来就由逐链的循环钉着）。
  与项目记忆 [[path-keyed-checks-vs-file-splits]] 同一条：**先拆完再跑门禁不算收口，跑完还要往里注入一次违规**。
- **一次自己写的用例 bug 差点变成假绿**：`runtime_with` 最初把两份配置（声明/未声明）都写成
  `runtime.case.json`，后一份悄悄盖掉前一份，于是"未声明基线"其实是声明过的 —— 断言
  `fills==0` 当场失败才暴露。改成文件名即用例名（`:90-92`）。这条形状与本轮那颗缺陷同源：
  **两个事实共用一个名字，后写的那个会顶掉前一个，而且没人报错**。
- **Windows 链接竞态污染了 gate1 的 M4 行为证据**（`failed to remove target\debug\qx-cli.exe`、
  `link.exe exit code 1104`，同一轮 M4/M4_RESTORED 各中一次）。`run_tests` 现在识别这两种输出、
  等 5s 重试一次；gate3 里 M4 与 M4_RESTORED 两次都是干净的 `5 passed / 3 passed`。
  与项目记忆 [[gate-round-workflow-conventions]] 里那条"偶发锁"同族，判据是**输出文本**而不是退出码。
- **`maturity/line_budgets.yaml` 一个字节都没动**（`BUDGET_DIFF_LINES=0`）：搬出去的两个文件都远在
  500 行门槛之下，预算表只登记越界文件；`multi_builtin.rs` 现在 **498/500**，下一颗往那条链里加东西
  的轮次必须先搬家（见 §34.5 第 4 条）。

### 34.4 验收数字（`/tmp/qx_q72_gate3.log`，2026-09-23 21:20:58 → 21:22:39）

| 项 | 实测 |
|---|---|
| STAGE1 整树编译 | `STAGE1_CHECK_EXIT=0` |
| STAGE2 行数棘轮 | `SNAPSHOT_EXIT=0`、`BUDGET_DIFF_LINES=0`（预算表与本轮开始前逐字节相同）、`ARCH_AFTER_SNAPSHOT=0` |
| STAGE3 fmt | `STAGE3_FMT_EXIT=0`（九个触及的 Rust 文件逐个 `rustfmt --check`） |
| STAGE4 clippy `-D warnings` | `STAGE4_CLIPPY[qx-cli]_EXIT=0`（本轮只改 qx-cli / qx-runtime 的 schema 与门禁脚本；整树 `cargo clippy --all-targets` 亦无 warning） |
| STAGE5 行为基线 | `--test backtest_account_base` 5 passed、`--bin qx-cli -- backtest_account_base` 3 passed（140 filtered），均 0 failed |
| STAGE6 五条变异 | 全部 `MUT_STATE=red`、`ARCH_FAIL_LINES=1`、`OTHER_FAILS=0`、`PASS_LINES=292`（每条只点亮被测那一项），五次还原全 `RESTORE_EXACT`，`PRISTINE_OK` 逐段通过 |
| 变异对应的事实 | M1 内置链不再读那一格（`configured_initial_cash_raw(..)` → `None`）→ ④ 红 + 两条命令行用例红（`a_declared_account_base_…` 的 `fills` 1→0、`a_non_positive_declaration_fails_closed` 的 exit 2→0）；M2 `if raw <= 0` → `if raw < 0` → ⑥ 红 + 命令行 1 红 + 单元 `non_positive_declared_cash_…` 红；M3 摘要删掉 `account` 块 → ⑦ 红 + `summary_chains_land_…` 红（`tests/backtest_account_base.rs:225`）；M4 装配字段改回 `pub(crate)` → 只有 ③ 红，行为用例全绿（预期，结构项，与 §33.3 的 M4 同族）；M5 多腿链 `?` → `let _ =` → ⑨ 红 + `the_multi_leg_entry_refuses_…` 红 |
| STAGE7 整树（`QX_PYTHON` 指向 venv） | `STAGE7_WS_EXIT=0`、`WS_OK_LINES=77`、合计 `WS_PASSED_TOTAL=744 / WS_FAILED_TOTAL=0`。与 Q71 的 76 / 736 相比：目标数 +1（新的集成测试文件），用例 +8（3 单元 + 5 命令行） |
| STAGE8 复跑门禁 | `架构不变量自检全部通过 ✓（293 项）`、`STAGE8_ARCH_EXIT=0`、`ARCH_ITEM_TOTAL=293`、`DISK_TEST_TOTAL=191`、`MODIFIED_DEPLOY=0`（跟踪产物零改写）、`PRISTINE_OK final` |
| 作废轮次 | `gate1`：M5 判据空转（见 §34.3 第五条）+ M4 撞链接竞态，两条证据不可用；`gate2`：M2 的 want 串未跟着改文案，记成 `unexpected`。**只有 gate3 是本轮验收依据** |
| 分叉实测 | `/tmp/qx_q72_probe/probe.log`（本轮，收口后的 binary）：见 §34.1 那张表 |

### 34.5 交给后续轮次的事实

1. **多腿链不写 `summary.json`，且不 `--root` 时两条腿的本金在 stdout 上无处可印**：本轮给它的是
   "拒绝 + 归因产物里的 `account_base_source`"。`[Multi-leg · Account]` 这一行**没做**，因为
   `multi_builtin.rs` 已经 498/500 行，再加 4 行就翻过那条兄弟模块线。想补齐得先搬家。
2. **`initial_cash_raw`（回测）与 `worker.paper_initial_cash_raw`（paper）是两格，且互不知情**：
   本轮把前者做成回测唯一的声明入口（文档注释里点名了后者），但没有任何判据要求"同一份配置里
   两格都写时必须是同一个数"。使用者完全可能回测按 A、paper 按 B，然后拿两边数字互相印证。
3. **16 份 bless 进仓库的摘要仍是 `schema_version: 1`**（`deploy/data/**/runs/*.summary.json`，
   `git ls-files` 计 16 份），现在落后两代（v3 的 `input` 块、v4 的 `account` 块）。本轮新写的判据
   和用例都只读活跑的产物，`tracked_sample_artifacts_are_validated_by_nothing` 那条事实没变（§27.5、任务 #53）。
4. **`multi_builtin.rs` 距离 500 行只剩 2 行**，且它同时是四条回测链里最长的一条。下一颗要往多腿链
   里加东西的轮次，必须先做一次搬家（`run_leg` 那一段是最自然的切点），否则又会像 Q71/Q72 这样
   被行数逼出一场与本轮主题无关的搬迁。
5. **`MULTI_LEG_ACCOUNT_CASH_FLOOR = 100_000` 与 `DEFAULT_BACKTEST_INITIAL_CASH = 100_000` 只是同数不同义**：
   前者是"每腿账户至少要有多少钱"的下限，后者是"没人声明时按多少记账"的默认。本轮把它们分在两个文件、
   两个名字，并在报错文案里各写各的身份；合并成一颗常数是后续轮次要**抵制**的顺手简化。

## 35. R 轮收口记录：连通性、单一真相源与读侧诚实（2026-09-23，R1–R18）

### 35.1 本轮判据与立案口径

发布判据是五条：主体流程全部联通、核心链路不存在断链、逻辑连贯不存在孤儿逻辑、不存在死循环、前后端全部贯通。
按这五条做三遍独立扫描（§36 记方法），立案 18 项：R1–R7、R9–R16、R18 当轮修掉并留常驻证明；R8、R17 只报不修，
理由与升级路径写在 §35.5。编号缺口不是漏记：R8 与 R17 立案后即判为"需要部署侧决策"，未动代码。

### 35.2 逐项：断在哪、落在哪、谁常驻咬着

| 编号 | 断在哪（生产后果） | 落点 | 常驻证明 |
|---|---|---|---|
| R1 | 五条回测链只有一部分问 `strategies[]` 的绑定，A 股段能从策略数组绕开"当场拒"闸门 | `backtests/config_declarations.rs` 的 `first_strategy_block_declaring`（同一个入口也被 R11 复用） | `tests/ashare_submit_guard.rs` |
| R2 | Binance 对账器重启后把自己发布的余额事实当成交易所回报，自比对自 → 永远"无差异" | `market_bridges.rs:19` `venue_balance_fact_identity`（身份只构造一次） | `venue_runtime/binance_reconcile.rs` + `tests/worker_observability.rs` |
| R3 | 用户流重连预算按累计次数计，进程跑够久就永久断开，退避时长同步单调上涨 | `crates/qx-adapter/src/binance.rs` 两个 run 循环改用 `consecutive`，`report.reconnects` 退回纯统计口径 | `crates/qx-adapter/tests/binance_stream_retry.rs` |
| R4 | CCXT 行情 worker 整轮所有标的取数失败仍报 Ready | `venue_runtime/ccxt_market_worker.rs` `ccxt_market_cycle_health`（抽成纯函数才有断言点） | `tests/worker_observability.rs` |
| R5 | `/ready` 把"一个 worker 指标都没有"读成依赖健康 | `readiness.rs` `worker_metrics_readiness` | `tests/worker_observability.rs` |
| R6 | doctor 的运行拓扑项只打印不判定，构建失败也记 pass | `config_commands.rs:532-537` 的 `Ok/Err` 两支各有结论 | `tests/cli_surface.rs:195` |
| R7/R10 | 账户快照 `reconcile` 两格结构性为 0：既没有来源，也没有"有没有来源"这一位 | `api_service.rs` `apply_reconcile_reports` + `protocol/wire.rs` 的 `Option` | `tests/api_snapshot_money_fields.rs` |
| R9 | help 与文档列了不存在的入口、漏了新入口，用户照抄就跑不通 | `cli_help.rs` + README §3.1/§3.2 | `tests/cli_surface.rs` |
| R11 | 深度链把配置里的 `strategy.fill_model` 静默丢掉：用户以为换了撮合，其实没有 | `backtests/depth.rs` 调 `fill_model.rs` 的 `reject_fill_model_config` | `tests/backtest_fill_model.rs` 的 `depth_chain_refuses_a_configured_fill_model`（含"零产物"负行） |
| R12 | 无键读模型端点各挑各的"默认账户"：账簿说 A、快照说 B | `api_service.rs` `default_account_event_log` 是唯一答案 | `tests/api_default_account_reads.rs` |
| R13 | API 投影拿标的后缀当账户 venue，paper 账户自己的订单事实被判成外来事实 | `qx-api/src/lib.rs:832` `validate_projection_event`：订单与账簿条目只核对账户身份 | 临时用例证明（去掉修复即红）；常驻缺口见 §35.5 第 7 条 |
| R14 | 稳定 JSON 把裸 `u64` 当对象键，产出的 `{77:{...}}` 连合法 JSON 都不是，三个读侧一起失败 | `qx-protocol/src/lib.rs` `json_table_entries` 委托 serde | `snapshot_single_source.rs` 的往返用例 |
| R15 | `positions` 表仍是手写 `format!`：少印 `instrument`，且 `PositionSnapshot` 每加一个字段就是写侧静默丢一处 | 同文件的 `json_position_entries` + `instrument_key`（读写共用一个键写法） | 同上（四张表逐个与 serde 那份比对） |
| R16 | BarFrame 文档不自述契约版本：Rust 自己写出的帧在自家读侧被降级成 legacy 宽松分支 | `qx-datastruct` 的 `BAR_FRAME_JSON_SCHEMA_VERSION` 印成文档第一格，`from_json` 对未来版本 fail closed | `crates/qx-datastruct/tests/frame_contract.rs` 三条 |
| R18 | 跨语言读侧用例吃的是手抄字典（四张键表全填 `{}`），写侧把键印成裸数字的那几周它一条都不会红 | `python/tests/fixtures/account-snapshot-v1.sample.json` = `to_json` 原样输出 | Rust `the_cross_language_sample_is_what_the_writer_emits` + Python `test_reader_consumes_the_document_the_writer_emits` |

### 35.3 本轮反复生效的三条口径

1. **一份事实只留一份编码**。R14/R15 是同一条判据的两次应用（写侧手抄 vs 读侧 serde），R16 是"文档必须声明
   它按哪一版被读"，R18 是"对面那条链必须吃这一份编码的产物，而不是近似形状"。四处的修法是同一个：让读侧认的
   那一份成为唯一被产出的那份，再用夹具或断言把两侧焊上。
2. **缺席要说不清**。R7/R10 之后，`reconcile` 两格与七个账户钱字段在类型、哈希、稳定 JSON 上都能区分"没算"与
   "算出零"。消费方不得把 `null` 折成 0 这条写进 §3.4。
3. **健康结论必须与"真的测过"绑定**。R4/R5/R6 三条同形：一个布尔在"没有观测"与"观测通过"之间被混用，于是
   监控面在链路断掉时仍然绿。修法是给每个结论找一个会红的判据来源。

### 35.4 证明方式与数字

- **变异证明**（每条修复都要跑"把缺陷放回去"的驱动，且要求 `restore=EXACT`）：R12–R14 批次 6/6 击杀；
  R15/R16 批次 4/4 击杀；R18 批次 4/4 击杀，另附反证 —— 三条新判据在 `HEAD` 的内容里一处都不成立
  （`load_account_snapshot(ACCOUNT_SNAPSHOT_SAMPLE`=0、夹具名在两侧各 0 次），故它们不是恒真式。
- **R18 踩到的是驱动自身的坑，两条都记下来**：
  1. 首轮四条变异全"击杀"，实为 `ModuleNotFoundError: No module named 'tests.test_bridge'` —— `python/tests`
     不是包，`-m unittest tests.test_bridge` 根本跑不到用例，`FAILED (errors=1)` 被当成击杀。修正：以
     `cwd=python/tests` + `-m unittest test_bridge.BridgeTest.<case>` 运行，并在驱动里先断言 baseline 绿、
     `ModuleNotFoundError not in stderr`。
  2. 同一轮把 Rust 金样用例误判成"没咬住"：`cargo test` 的失败摘要落在 stdout，驱动只看 stderr。修正后同一
     改动两侧同时红（`test result: FAILED`），双钉成立。
  3. 一次夹具改坏后脚本崩在 `print` 之前，留下被改坏的夹具；靠 `to_json` 金样用例反向确认还原逐字节正确
     （`restore: EXACT` 之后 Rust 用例仍绿）。纪律：**变异驱动必须用 `try/finally` 包住还原**。
- **门禁数字**：

| 项 | 改前（`99c7051`） | 改后（本轮实测） |
|---|---|---|
| 架构不变量 | 279 项 | 288 项（`check(` 调用 200 → 209） |
| 整树 `cargo test --workspace` | 733 passed（§31.4 口径） | 748 passed / 0 failed / 0 ignored |
| qx-cli 行为用例下限 `CLI_TEST_FLOOR` | 178 | 187 |
| Python `unittest` | 43 | 44（2 skipped，均为本机缺依赖） |
| 行数预算 `crates/qx-protocol/src/lib.rs` | 885 | 844 |
| 行数预算 `crates/qx-datastruct/src/lib.rs` | 767 | 702（用例搬进 `src/tests.rs`） |
| 行数预算 `crates/qx-adapter/src/binance.rs` | 2291 | 2217 |

### 35.5 交给下一轮：只报不修与证明缺口

1. **R8 托管运行没有收尾**：`RuntimeSupervisor::request_shutdown`（`crates/qx-runtime/src/supervision/supervisor.rs:86`，
   它调 `ShutdownToken::request`）的唯一调用者是 `supervision/tests.rs:45`；qx-cli 侧只构建监督器（`event_pipeline.rs:289/578`、`runtime_check.rs:8`、
   `strategy_contract.rs:707`、`config_commands.rs:532`），而 `git grep -E "ctrl_c|tokio::signal|SIGTERM" -- crates`
   零命中。后果：托管运行没有任何优雅退出路径，只能被强杀，worker 的收尾（订阅关闭、落盘）都不发生。要修需要先定
   部署侧决策：谁发停机信号、停机窗口多长、超时是否升级强杀。
2. **R17 日历组件指纹的两侧 canonical 字节各写一遍**（**T2 已收口，见 §38.2**）：Rust
   `crates/qx-cli/src/dataset_commands.rs:244` `dataset_component_file_fingerprint` 与 Python
   `python/qianxing_ashare/__init__.py:1448` `component_fingerprint`，对齐此前只靠后者的 docstring
   （"必须和 CLI 保持一致"），全仓没有一条用例比过这两个字节。当时另有一句判断是错的，在此更正：
   "日历文档没有版本声明"只看的是 `crates/qx-data/src/calendar.rs`（那是一个 16 行的 trait 与结构体，
   与日历文档无关），真正的闸门在 `crates/qx-xingban/src/ashare.rs:735-765` —— 版本上界、字段白名单
   与 `source` 非空都在那里，Python 侧 `from_json` 同形。
3. **C3 账单水位是一个跨币种/跨腿的标量**（`ccxt_reconcile_worker.rs:61-69/81/100-104`）：多币种账户里一条腿
   推进会把另一条腿的新账单划成旧数据。
4. **C4 Binance 行情流被对端关闭后不重连**：读侧拿到 `None` 就 `break`（`venue_runtime/binance_stream_worker.rs:67`），
   循环结束、`stream.close()`、worker 收工，没有重连缝；R3 修的是"重连预算不复位"，没修"这一条流不重连"。
5. **C6 实盘 worker 记的是帧自哈希，回测链记的是注册表核过的数据集身份**：`crates/qx-cli/src/workers.rs:164` 的
   `frame.digest()` 对 `backtests/artifacts.rs:37` 的 `barframe_dataset_identity` —— 同一份帧经 bundle 装载时两条链
   给出两种身份。
6. **C9 `valid_from/valid_to` 从不按交易时钟校验**（`crates/qx-core/src/trading.rs:83`）；`strategy.max_orders`
   只在 worker 侧生效（`crates/qx-cli/src/workers.rs:277`）；`PipelineMetricsSnapshot` 没有生产读者；账户快照是
   boot-only 而行情是流的。
7. **R13 只有一轮临时用例的证明**（T1 已收口，见 §38.2）：`validate_projection_event` 的订单/账簿两支
   当时没有常驻反例，需要一条"paper 账户交易符号带交易所后缀"的用例补上——那条现在住在
   `crates/qx-api/tests/account_projection_identity.rs`。
8. **结构债**：`crates/qx-execution/src/lib.rs` 1619 行未拆；Q1b 的输入指纹/重 bless 决定仍未拍板（§27.5）。


## 36. 三遍扫描的方法、一次失败的委派与发布判定（2026-09-23）

### 36.1 三遍各查什么

| 遍次 | 视角 | 手段 | 产出 |
|---|---|---|---|
| 第一遍 | 主链路是否贯通：配置 → 绑定 → 装配 → 提交/成交 → 记账 → 投影 → 读模型 → 产物 | 逐条链路从入口 grep 到落盘点，缺环即立案 | R1、R11、R12、R14、R15 |
| 第二遍 | 运行期与外部接口：worker 生命周期、重连、健康结论、跨语言边界 | 读 worker 循环体与健康判定的真值表，构造"全失败"输入看它报什么 | R2–R7、R9、R13、R16、R17、R18 |
| 第三遍 | 反方向核对：声明了没人读的东西、读了没人声明的东西、循环能否终止、两侧契约是否互拒 | 机械枚举 + 逐个 grep（见 §36.3） | 未发现需要改代码的缺陷；一处示例覆盖缺口记在 §36.3 第 3 条 |

前两遍的编号全部落在 §35.2；R8 在第二遍立案后判为"需部署侧决策"。

### 36.2 第三遍的一次失败委派（如实记）

第三遍先派了一个只读扫描代理，要求"每条候选必须给 file:line + 双向 grep 证明"。它交回 6 条候选，其中 4 条的
锚点在本仓根本不存在，逐条核实后全部丢弃，未据此改一行代码：

| 代理声称 | 实测 |
|---|---|
| `crates/qx-cli/src/report.rs:265` 把 `TradeRecord.fee` 写死 "0" | 全仓 `TradeRecord`/`FillRecord` 命中数为 0，且没有 `crates/qx-cli/src/report.rs` 这个文件 |
| `crates/qx-core/src/risk.rs:557` 注释仍推荐已删除的 `qx-fenye` | 没有该文件；`pub mod fenye;` 在 `crates/qx-core/src/lib.rs:16` 仍在，文档行与代码一致 |
| `research.dataset_manifest_path`、`spread.spread_store_path`、`risk.risk_state_store_path` 等路径字段"声明了读不到、用户配不上" | 这三个字段名在 `crates/qx-runtime/src/runtime_config/` 全文里都不存在；真实的 `*_path` 字段有 14 个，逐个查消费点与文档，全部对得上（见 §36.3 第 3 条） |
| 其余两条为"未验证"或"已核对无问题" | 不构成候选 |

教训写在这里，供后续轮次复用：**委派报告里的每一个 file:line 都要自己 grep 一遍才算证据**；代理给的"命中数、
行号、字段名"是它自己叙述里的数字，不是仓库的事实。

### 36.3 自跑的机械判据（可复算）

1. **配置面双向贯通（反向）**：`crates/qx-runtime/src/runtime_config/*.rs` 里 4 空格缩进的 `pub` 字段共 120 个，
   逐个在 `crates/` 其余位置（排除 schema 自身）找读取点，**0 个零引用字段**。正向（读取点→文档）已在上一轮做过：
   53 条读取点逐条落在 §3.2。
2. **循环终止性**：`qx-cli`/`qx-orchestrator`/`qx-scheduler`/`qx-adapter`/`qx-execution` 五处共 14 个
   `loop`/`while` 站点，逐处检查其后 90 行体内是否有显式出口（`break`/`return`/停止位/通道关闭/`Err` 支），
   **0 处无出口**。这条只是筛查，不等价于证明：两条长跑 worker 的终止语义在 R3（连续失败计数复位）、
   C4（收到 close 帧直接退出、没有重连缝）、R8（停止令牌无人触发）里分别单独核过。
3. **路径配置的可发现性**：`runtime_config` 声明 14 个 `*_path` 字段，逐个对照 `deploy/*.example.json` 与
   `docs/`：`ashare_calendar_path` 等 13 个既有示例又有文档，只有 `cost_rules_path` 没进任何示例文件（文档里有，
   `strategy.cost_rules_path`）—— 是示例覆盖缺口，不是断链。**0 个"配得上但没人读"或"读得到却没人知道能配"**。
4. **前后端契约版本互拒**：Python 侧 `python/qianxing_bridge/strategy.py:95`（请求）与 `:252`（应答）双向校验
   `schema_version`，Rust 侧 `crates/qx-strategy/src/lib.rs:252` 校验回包版本、`strategy_host.rs:540` 经
   `StrategyContractOutput::from_json_for` 落同一份校验。两侧都会拒不同版本，本遍无需改动。

### 36.4 发布判定

本轮结束时门禁全绿：`cargo fmt --all --check` 0、`cargo clippy --workspace --all-targets -- -D warnings` 0、
`cargo test --workspace` 748 passed / 0 failed、`validate_core.py` 与 `check_architecture.py` 288 项全过、
Python `unittest` 44 项（2 skip）。

特性矩阵同样复算过：`qx-cli` 在 `sqlite`、`postgres`、`postgres,nats` 三档下 clippy `-D warnings` 干净，
用例分别 145/145/148 条 binary 用例 + 9 组集成用例全绿、`0 failed`。

据此判定：**研究档与 paper 档达到发布条件**。实盘档仍差三件事，且都不是本轮能拍板的代码问题：

1. §35.5 第 1 条（R8 停机收尾）与第 4 条（C4 行情不重连）——实盘长跑的可用性和"看起来在跑但已断流"直接相关；
2. §35.5 第 3 条（C3 账单水位跨币种）——多币种账户的对账正确性；
3. 外部交易所侧仍未拿到任何沙盒记录：`maturity/capabilities.yaml` 里 `sandbox_tested` 仍全为 `false`，
   本轮所有新增证明都是本机行为用例与静态门禁，不含真实下单回报。


## 37. S 轮收口记录：控制面自身的连通性，以及"验证手段本身可信"（2026-09-23，S1–S7、S13）

### 37.1 判据

§36.1 第三遍的"反向核对"只扫了数据面（配置字段、循环体、跨语言契约版本），那句"未发现需要改代码的缺陷"
在本轮被推翻：同一套反向核对换成对控制面自身，一次就出五条。本轮问的七个问题：

1. **`serve` 起不起得来**——控制面找 API worker 的判据，和监督器注册 worker 的判据，是不是同一个；
2. **跑起来停不停得下**——每条生产循环有没有一个"非错误"的出口；
3. **读出来的是现状还是开机那一刻**——只读运维端点每次请求重读来源，还是装配时读一次就冻住；
4. **告诉用户的和实际接受的是不是同一份**——help 的取值表、错误文案、真正匹配的那个集合；
5. **对外端点有没有人知道**——路由集合与文档表逐一对齐，未列路径的兜底口径写进门禁；
6. **上面这些证明本身可信吗**——每条新用例、每条新门禁项都要在"把代码退回缺陷态"时当场变红。第 6 问
   额外揪出了 S6，也正是它让第 4 问的第一次变异验证假绿。
7. **§36 的反向核对换到"仓库外那一端"还成立吗**——本仓的"前端"除了 HTTP 读面与 Python 桥，还有 C++ 插件
   边界。它由头文件与 Rust `#[repr(C)]` 两份手抄镜像描述，两侧之间没有任何编译期耦合。这一问出 S13。

### 37.2 逐项：断在哪、落在哪、谁常驻咬着

| 编号 | 缺陷 | 断在哪 | 落点 | 常驻证明 |
|---|---|---|---|---|
| S1 | `serve` 启动即死 | `RuntimeSupervisor` 的 `HealthRegistry` 按 `worker.id` 建键，`strategy_contract.rs` 两处 `spawn_worker("api", …)` 写死字面量；worker id 是自由命名，拓扑校验只数"启用的 api role worker 恰好一个"，所以任何改名后的合法配置都会让进程在启动瞬间死於"未知 worker: api" | `configured_api_worker_id()` 按 **role** 解析（与拓扑校验同一判据），两处 spawn 都用它；解析不到就报错而不是回落 | `tests/runtime_api_worker_identity.rs` 2 条（改名的 worker 能注册、字面量不能；没有启用的 api worker 时报错）+ 门禁 3 项 |
| S2 | 两条循环永不自然结束 | `workers.rs` 的 scheduler 与 strategy 循环是生产里唯二不读 `context.should_stop()` 的循环，`once` 之外的唯一出口是 `?` 抛错 | 两处循环体首加停机轮询 | 门禁 1 项（`if context.should_stop() {` 恰为 2 处）。**没有行为用例**：见 §37.5 第 6 条，令牌本身无人翻 |
| S3 | 运维读端点念开机那份 | `/scheduler/runs`、`/account/ledger`、`/reconcile/reports` 读的是 `ApiState` 里启动时装好的三份读模型，worker 之后落的对账报告、追加的成交在 HTTP 侧永远看不见 | `qx-api` 新增 `ApiQueryModels` + `with_query_models_provider`，`query_models()` 每请求现读并回填 `ApiState`（让 trait 与 HTTP 不再两份口径）；`api_service.rs` 把 boot 装载与 provider 指向同一个 `load_api_query_models`。范围只到这三条：`/account/snapshot|orders|positions|balances` 读的是 boot 装好的账户投影（`snapshot_for_query` → `ApiState.projections`），扩它要另加重投影泵，登记在 §35.5 第 6 条末句 | `tests/api_query_models_live.rs` 2 条（boot 后落盘报告读得到、boot 后追加成交读得到）+ 门禁 2 项 |
| S5 | 可用值抄了三份 | `init --profile` 的接受集合在 match 里，help 的 `<…>` 表和"未知 profile"文案各抄一份，且都漏了 `builtin`——用户被告知的取值集合里恰好少了那个真正能用的取值 | `INIT_PROFILES` 一份，三处由它生成 | 门禁 2 项（三处相等 + 文案不另抄）+ 既有 `init_onboarding` 两条用例（本轮由变异证实它们真会红） |
| S6 | 过期 binary 让子进程用例假绿 | `tests/mod.rs::qx_cli_binary()` 的 `option_env!("CARGO_BIN_EXE_qx-cli")` 在本 crate 的 `--bin` 单元测试里取不到值，走的是回落分支；而 `assert_binary_fresh` 只装在上面那条分支。`cargo test --bin` 又不重链 `target/debug/qx-cli.exe`（实测：touch 源码后 binary mtime 不动），于是定向跑子进程用例时断言的是旧行为 | 回落分支补同一道守卫 | 正反双向：源码新于 binary → 三条 `init_onboarding` 用例当场 panic 并给出"请先 cargo build"；`cargo build -p qx-cli` 后复跑 15 条全绿。+ 门禁 1 项 |
| S7 | 对外端点没有文档面 | 17 条路由里只有 `/health`、`/ready` 在 `deploy/README.md` 有过语义描述，其余 15 条在全仓 markdown 中零命中——"代码里有、文档里没有"与"文档里有、代码 404"是同一类断链的两个方向 | `deploy/README.md` 端点表（语义、查询串、非 200 口径、限流与鉴权次序，逐条按 `handle_inner` 实读写成） | 门禁 2 项：表内 `METHOD 路径` 集合 == 路由集合（实测 17↔17，两侧差集皆空）；"未列路径一律 404"与代码兜底同在一处 |
| S13 | C ABI 两份手抄镜像零比对 | 插件边界由 `cpp/include/qianxing_strategy.h` 与 `crates/qx-strategy/src/c_api.rs` 的 `#[repr(C)]` 各抄一份描述，Rust 侧不 include 头文件、C++ 侧不编译 Rust。头文件里连一个 `static_assert`/`sizeof` 都没有（`cpp/` 全仓 `static_assert` 零命中）；Rust 侧唯一的布局自证是 `c_layout_matches_expected_pointer_free_sizes`（`:1009`），钉的是 `QxRaw128` 16 字节/对齐 8 和 `QX_C_STRATEGY_API_VERSION == crate::STRATEGY_API_VERSION`——两份 **Rust** 常量互相钉，与头文件无关。CI 的 `cpp-sdk` 作业只 build 三个 C++ 目标并跑共享内存校验器，从没用宿主加载过 C++ 插件，所以改乱任一侧的字段顺序/类型在 CI 里都是绿的 | `c_abi_header_check()`：两侧类型集合对称差、每个结构字段名与顺序逐位、每字段类型按映射表（定宽整数/指针/嵌套结构）、枚举判别值序列与版本常量共四条比对。当前实测两侧全等：10 类型（8 个结构共 64 个字段 + 2 个枚举共 6 个判别值 `[1,2,3,4]`/`[1,2]`）、版本 `1`↔`1` | 门禁 4 项（M8–M12 五条变异各咬住一项）。**没有新增行为用例**：见 §37.5 第 8 条 |

编号说明：S 号发给落了代码的 S1–S3、S5–S7 与 S13；本轮新立但未修的项在 §37.5 另起 S8–S12 续号，避免与 §35.5 的
C 号混用（那边的 C3/C6 是另外两件事）。S4 没有对应改动，故 S 号在 S3 与 S5 之间留空。

### 37.3 变异报告（每条都是"退回缺陷态 → 看谁红 → 按字节还原"）

| 变异 | 改了什么 | 结果 |
|---|---|---|
| M1a | `configured_api_worker_id` 的判据从 `role == Api` 换成 `id == "api"` | `renamed_api_worker_is_the_one_the_supervisor_accepts` 红（"启用的 api worker 解析得出 id: 运行时配置必须且只能启用一个 api worker"）。门禁不红——它咬的是调用点，见 M1b |
| M1b | 一处 spawn 退回字面量 `"api"` | 门禁红："字面量 api 回到 spawn 点"。行为用例不红——它们测的是解析函数，不是调用点，两侧各咬一半 |
| M2 | 拔掉 `.with_query_models_provider(…)` | `api_query_models_live` 两条全红（0 passed / 2 failed）；同一次变异下门禁 provider 项红 |
| M3 | 删掉 scheduler 循环的停机轮询 | 门禁红："任一回退：那两个循环的唯一出口又只剩 once 或抛错" |
| M4 | 从 `INIT_PROFILES` 删掉 `builtin`（数组长度同步改，保持可编译） | 门禁红（`help=[…builtin…] declared=[缺 builtin]`）；`init_onboarding` 两条红，报错文案就是"未知 init profile=builtin"。**第一次跑这条变异时是全绿**——见下一行 |
| M4 首跑（假绿） | 同上 | 全绿。原因不是代码正确，而是子进程用的是过期的 `target/debug/qx-cli.exe`。补上 S6 守卫、并按守卫提示先 build 再复跑，才有上一行的红。这条变异是本轮唯一一次"验证手段本身说谎"，它本身立案成 S6 |
| M5 | 回落分支的守卫删掉一条 | 门禁红 |
| M6 | 端点表删一行（`GET /control/audit`） | 门禁红："只在代码 [('GET', '/control/audit')]" |
| M7 | 把"未列出的路径一律 404"改写成别的说法 | 门禁红 |
| M8 | 给 Rust 侧 `QxOrderIntent` 末尾加一个 `pub pad: u8`（头文件不动） | 门禁红："字段名与顺序…QxOrderIntent: 头 […position_side] != Rust […position_side, 'pad']"。行数预算顺带红（1107 > 1106）——它咬的是副作用，不是镜像本身 |
| M9 | 把 `reduce_only` 与 `post_only` 在 Rust 侧调换顺序（字段集合不变） | 只有"字段名与顺序"红：`头 [… reduce_only, post_only, position_side] != Rust [… post_only, reduce_only, position_side]`。这条是 M8 测不出来的那一半：集合相同、布局不同 |
| M10 | Rust 侧 `QxMarketEvent.has_last` 的 `u8` 改成 `u64`（宽度变了、名字没变） | 只有"类型按映射表"红：`QxMarketEvent.has_last: 头 u8 != Rust 'u64'` |
| M11 | `QX_C_STRATEGY_API_VERSION` 改成 `2`（只有 Rust 侧动） | "枚举判别值序列与 ABI 版本常量"红，且报告里两侧枚举本来就相等（`[1,2,3,4]`/`[1,2]`），红的是版本那半 |
| M12 | Rust 侧多一个没人用的 `#[repr(C)] pub struct QxOrphan` | "同一组类型"红：`只在头文件 [] / 只在 Rust ['QxOrphan']`。缺方向（头文件多类型）由同一项的对称差另一半覆盖，无需再造一次 |

十三次变异的锚点全部命中 1 次才动手，还原后逐个校验 sha256 与打补丁前一致：`strategy_contract.rs`
`be8b1d16…`、`api_service.rs` `a40b9e3c…`、`workers.rs` `4a64a860…`、`init_project.rs` `3aeeebef…`、
`tests/mod.rs` `d136dce9…`、`deploy/README.md` `a249f389…`、`c_api.rs` `4612280f…`。仓库文件是 CRLF，任何用 `\n` 拼锚点的
脚本化改写都会 0 命中（本轮 M2 的第一版锚点就是这样 0 命中的），改字节 needle 前必须先探文件的 EOL。

### 37.4 本轮反复生效的五条口径

1. **绿色的子进程用例不等于绿色**：跨进程断言的前提是"被测 binary 就是当前源码"。这条前提在仓库里
   本来有守卫，但只装在走不到的分支上——写守卫的人和我这次都栽了（S6）。
2. **变异必须双向看**：只跑"用退回缺陷态看用例红不红"会漏掉调用点回退（M1b 只红军禁），只跑门禁会
   漏掉判据退化（M1a 只红用例）。所以 S1 这种"函数 + 调用点"的修复要各来一次变异。
3. **门禁的锚点不能撞上自己要防的字符串**：S1 的 doc comment 里写了 `spawn_worker("api", …)` 就把
   "字面量不许出现"这条门禁自打红了，needle 必须收紧到 `supervisor.spawn_worker("api"`。
4. **委派报告仍要自己 grep**：本轮再次出现"端点/字段/文件根本不存在"的候选（`/live` 端点、
   `is_backtest_artifact_path`、`CLI_TEST_FLOOR` 过期三条），全部经自跑 grep 驳回，未据此改代码。

5. **静态比对门禁要先证明解析器没在偷读**：S13 的比对器定稿前栽在三处，每处一开始都表现为"报错"而不是"没
   意见"——`typedef struct { … } qx_raw128;` 的 tag 写在闭括号后（正则要 `[\w ]*`，否则那个结构在头文件侧
   整个看不见）、vtable 的回调字段要用 `\(\*(\w+)\)` 单独取（否则只剩回调名、丢掉 `abi_version`）、把
   `*const c_char` 一侧去空格另一侧不去（实测误报 18 处"类型不等"）。三处都是**红得没有名字**：看起来像
   "两侧真的分叉了"，实际是比对器自己没读对。定稿版因此把未登记的 C 类型直接 `raise KeyError`（"先补映射，
   别让它绕过比对"），并用 M8–M12 五条变异证明"字段少一个、顺序换一下、宽度改一下、版本动一下、类型多一个"
   各当场红——比对器只有在能看见差异时才算在比对。

### 37.5 交给下一轮：只报不修（本轮复核过，证据仍在）

本轮新立未修的五项在此续 S 号（S8–S12）；第 6、7 条是 §35.5 已立过的原号 carry-over，本轮只复核证据；
第 8 条不另发 S 号，它说明 S13 为什么只有静态门禁、没有行为用例。

1. **S8 产物路径编码**：磁盘上 46 份 `*.summary.json` 里 40 份没有 `input` 块、6 份的 `input` 带绝对
   路径根（都在 `D:\aex_work…` 这一台机器上）；git 追踪的 16 份全部没有 `input` 块。要修得先定"产物
   身份是否含路径"这条契约，不是代码问题。
2. **S9 `ProjectionLineage` 两格恒空**：`crates/qx-protocol/src/lib.rs:77`、`:79` 的 `dataset_version` /
   `manifest_digest` 在两个构造点（`crates/qx-api/src/lib.rs:1349`、`:2153`）都只靠
   `..ProjectionLineage::default()` 填空串，只有 `source_digest` 有值。补齐要改信封字段，属线格式变更。
3. **S10 schema 两份、服务侧那份更松**（**T3 已收口，见 §38.2**）：`ACCOUNT_SNAPSHOT_JSON_SCHEMA`（737 字节，`GET
   /schema/account-snapshot-v1` 实际发的那份）与 `schemas/account-snapshot-v1.json`（1,081 字节）逐键
   比对：顶层 required/properties 与所有已声明字段值全等（0 处不等），差在 6 个键——文件侧多 `title`、
   顶层 `additionalProperties: true`，以及 `positions`/`orders`/`fills`/`transfers` 四张表的
   `additionalProperties: {type: object}`；两份都没有字段级 description。而 `schemas/*.json` 在代码里
   零消费者（只有文档引用它，跨语言用例钉的是 `python/tests/fixtures/` 那份夹具）。要合要先定谁是权威，
   且 `crates/qx-protocol/src/lib.rs` 正好停在 844 行预算上。
4. **S11 限流参数硬编码**：`ApiRateLimiter::new(100, 100)` 两处（`crates/qx-api/src/lib.rs:1086`、
   `:1102`），运行时配置的 `deny_unknown_fields` 让"加一个 api 限流键"必须先动 schema。
5. **S12 对账默认 worker id 分叉**（**T4 已收口，见 §38.2**）：`crates/qx-cli/src/cli.rs:650` 默认 `reconciler-main`，CCXT 那条
   链实际是 `ccxt-reconciler-main`（`tests/worker_observability.rs:426` 钉着）。现状是"带清晰报错地
   失败"，不是静默错账，故留下一轮与命名规约一起定。
6. **R8 停机信号无人触发**：S2 给了两条循环出口，令牌也确实 clone 进了每个 `WorkerContext`
   （`crates/qx-runtime/src/supervision/supervisor.rs:130`），但唯一能置位的
   `RuntimeSupervisor::request_shutdown`（`supervisor.rs:86`）生产侧零调用者，只有
   `supervision/tests.rs:45` 调它；`ShutdownToken::request` 与 `shutdown_token()` 在 crate 内也无人取用。
   仓库也不引信号依赖（无 `tokio::signal`/`ctrl_c`，零新依赖纪律）。谁发信号、窗口多长、超时是否升级
   强杀，是部署侧决策。
7. **`/health` 恒 200 与 `--json` 只覆盖部分子命令**：都在 §35.5 记过，本轮复核文档口径与代码一致，
   维持不修。
8. **S13 只有静态比对、没有"宿主真加载 C++ 插件"的行为用例**：`cargo test` 侧的 C ABI 用例
   （`c_abi_strategy_copies_plugin_decision_before_freeing_it`，`crates/qx-strategy/src/c_api.rs:1077`）
   是把一个 **Rust** 实现的 `FAKE_VTABLE` 当插件喂给宿主，验证的是宿主的编解码与"复制先于释放"；真正
   跨越"clang 编译出的 C++ 结构体布局 ↔ rustc 的 `#[repr(C)]`"那条边在 CI 里从未闭合——`cpp/CMakeLists.txt:12`
   确实无条件产出 SHARED 的 `qianxing_strategy_example`，`cpp-sdk` 作业也真的 build 出它，但只 run
   `qianxing_ring_smoke` 与 `qianxing_strategy_jsonl` 两个可执行目标，没有任何一步用
   `DynamicCAbiStrategy::load_verified` 去加载那个产物。补它要在 CI 里做跨工具链衔接：算出插件的
   SHA-256 填进 `DynamicCAbiLoadPolicy`、由 Rust 测试宿主加载 C++ 产物并跑一次 `on_event`，属于门禁能力
   扩展而非本轮的连通性缺陷；本轮先把"两份手抄必须逐字段相等"这条静态能咬住的咬住。

### 37.6 门禁数字与发布判定（覆盖 §36.4）

`cargo fmt --all -- --check` 0；`cargo clippy --workspace --all-targets -- -D warnings` 0；
`qx-cli` 特性矩阵四档（`sqlite` / `postgres` / `nats` / `postgres,nats`）clippy 均干净；
`cargo test --workspace` **752 passed / 0 failed**（§36.4 的 748 + S1/S3 各两条；S13 不改用例，只加门禁项，
故该数字不变）；
`tools/check_architecture.py` **302 项全过**（288 → 295 加控制面 7 项 → 296 加 S6 → 298 加 S7 两项 → 302
加 S13 四项）；
`tools/validate_core.py` 全过；Python `unittest` 44 项（2 skip）。
`CLI_TEST_FLOOR` 187 → 191（实测 src/tests 152 + crates/qx-cli/tests 39）。
行数预算：`qx-api/src/lib.rs` 3167 → 3224、`strategy_contract.rs` 822 → 840、`workers.rs` 718 → 728
三处抬升换 §37.2 的修复，同批快照里另有 `binance.rs` 2291 → 2217、`qx-datastruct` 767 → 702、
`qx-protocol` 885 → 844 三处下降。

据此判定：**研究档与 paper 档达到发布条件**，且这一轮的连通性核对已从数据面扩到控制面（起得来、停得下、
读的是现状、对外端点有文档面），并第一次扩到仓库外那一端（C++ 插件的两份 ABI 镜像现在逐字段互钉）。
实盘档仍差 §37.5 第 6 条与 §35.5 第 1、4 条那三件事，加上外部交易所
沙盒记录仍为零——`sandbox_tested` 全为 `false` 这件事不随本轮门禁数字改变。

## 38. T 轮收口记录：把"只报不修"里缺证据的那四项补齐（2026-09-23，T1–T4）

### 38.1 判据：本轮不改口径，只问"这句话现在有人咬着吗"

§35.5 与 §37.5 两份清单里的项，本轮先按"到底为什么不修"分了类：

- **需要产品或部署拍板**：R8 停机信号由谁发、S8 产物身份是否含路径、S11 限流参数进不进 schema、C3 账单水位的
  币种与腿维度、C4 流关闭要不要重连、C6 实盘/回测两套帧身份、C9 交易时钟与 boot-only；
- **需要改线格式**：S9 的 `ProjectionLineage` 两格恒空——本轮纪律里"事件 schema 与 `RunManifest` 字段不动"直接挡住；
- **需要 CI 能力扩展**：S13 的行为侧（宿主真去加载 clang 产出的 C++ 插件）。

剩下的四项既不用拍板、也不动口径，缺的只有一件事：**证据留不住**。T1–T4 就是这四项，判据一句话——
"下一轮任何人把它改回缺陷态，CI 会不会响"。开工前逐条自测的答案是"不会响"或"只响一半"，本轮把它们全部改成
"响，而且只有改错的那一侧响"。

### 38.2 逐项：断在哪、落在哪、谁常驻咬着

| 编号 | 缺陷 | 断在哪 | 落点 | 常驻证明 |
|---|---|---|---|---|
| T1（R13） | 已修的判定拿不出常驻反例 | R13 的修复本身在 HEAD：`crates/qx-api/src/lib.rs:851`、`:853` 把订单与账簿两支的账户 venue 从"拿标的后缀"改成 `None`，`:865-878` 才做身份比对。但 `git grep account_projection_checks_venue_less HEAD` 零命中——R 轮只跑了一轮临时验证。回退方向有两个且互相冲突：退回收紧 → paper 账户看不见自己的订单与账簿；放宽过头（连成交也不核对）→ 跨交易所串读回来 | 新增 `crates/qx-api/tests/account_projection_identity.rs`，**生产代码 0 行改动**（`git diff HEAD -- crates/qx-api/src/lib.rs` 为空）。住 `tests/` 而不是内联 `mod tests`：`qx-api/src/lib.rs` 正压在 3224 行预算上，常驻反例不该给被棘轮看管的读面加压 | 1 条用例三向钉住：本账户的无 venue 事实必须全投影（3 条事件 + 读模型里真出现 `"symbol":"BTCUSDT"`）、换账户必须拒（订单与账簿各一次）、带显式 venue 的成交仍必须守住交易所边界 |
| T2（R17） | 同一份日历文档，登记侧与复检侧一宽一严 | Python `AshareTradingCalendar.component_fingerprint`（`python/qianxing_ashare/__init__.py`）把摘要登记进 DatasetBundle，Rust `dataset_component_file_fingerprint`（`crates/qx-cli/src/dataset_commands.rs:244`）在回测启动前对同一份文件重算，两份 canonical 字节各写一遍，中间只有一句"必须和 CLI 保持一致"的 docstring。本轮实读到的分叉是具体的：旧格式日历（顶层没有 `sessions`）Python 的 `from_json` 与回测规则装载 `apply_calendar_json` 都读成"没有时段"，指纹侧却直接 `Err` → **Python 已经登记好的 bundle 会被 CLI 判成组件非法而拒启** | (a) 共用夹具：`python/tests/fixtures/calendar-component-v1.json` 与 `…-legacy.json` 各配一份 `.fingerprint`，夹具由 Python **写侧函数**产出、摘要只存在那一份文件里；(b) `dataset_commands.rs:270-279` 把宽度对齐（`None => &[]`），而 `sessions` 是非数组时仍然报错——fail-closed 的那一半不动；(c) 门禁 `calendar_fingerprint_caliper_check()`（`tools/check_architecture.py:2691`）4 项 | Rust 3 条（`src/tests/calendar_component_fingerprint.rs`：共摘要 + 只随规范字段动 + 夹具能被回测链装载）+ Python 2 条（`python/tests/test_ashare.py:28` 起的夹具常量与两条用例）+ 门禁 4 项 |
| T3（S10） | 对外契约有两份手抄，且其中一份没人读 | `ACCOUNT_SNAPSHOT_JSON_SCHEMA`（服务侧手抄 737 字节）与 `schemas/account-snapshot-v1.json`（文件手抄 1,081 字节）逐键比过：已声明字段全等，差 6 个键——**但两份都漏掉了写侧恒印的八个钱字段**。`schemas/*.json` 在代码里零消费者。另一半是读侧：`from_json` 只核对协议字符串与"顶层/表内两个版本号是否自相矛盾"，一份按别的版本自洽封存的文档会被当 v1 解出来，而对面 Python `load_account_snapshot` 对同一份产物直接抛错 | 常量改成 `include_str!("../../../schemas/account-snapshot-v1.json")`（`crates/qx-protocol/src/lib.rs:33-34`）——手抄在编译期没有退路，文件是权威；新增 `ACCOUNT_SNAPSHOT_SCHEMA_VERSION`（`:37`）并在 `from_json` 处设版本闸门（`:425-431`）；`schemas/account-snapshot-v1.json` 补齐八个钱字段（权益不可空、七个"读过才算得出"的可空）与 `schema_version: {"const": 1}`；Python 桥必填集合补 `equity_raw`（`python/qianxing_bridge/__init__.py:342` 的 `load_account_snapshot`，新增键在 `:355`）；门禁 `account_snapshot_schema_check()` 8 项 | 6 条契约用例（`crates/qx-protocol/tests/snapshot_schema_contract.rs`：发出的就是仓库那份文件、键集合覆盖写侧产物、可空口径、版本闸门、拒收自洽的别版本文档、header 逐格对得上）+ Python `test_bridge` 的必填/版本双向断言 + 门禁 8 项 |
| T4（S12） | 入口的默认 worker 名是字面量，与实际链分叉 | `crates/qx-cli/src/cli_args.rs:269` 是全仓**唯一**一个可选 `worker_id`（其余 worker 子命令都是必填位置参数，不存在"默认名"这一类分叉）。HEAD 里 `cli.rs:650` 给它的默认值是字面量 `reconciler-main`，而 CCXT 那条链的 reconciler 实际叫 `ccxt-reconciler-main`。两个方向都会撒谎：名字合法改动的拓扑上报"找不到 worker: reconciler-main"（把"没解析"说成"不存在"）；而恰好有个叫这名字、角色却是 execution 的 worker 时，会真跑一次下单执行 | `worker_entry.rs:95-121` 新增 `configured_reconcile_worker_id(config, entry)`，按 **role + Venue 绑定**解析（与 S1 同一判据），零个或多个候选都报错并列出候选要求点名；`:124-133` `run_binance_reconcile_once` 承接 `Option<&str>`；`cli.rs:650` 不再给字面量 | `src/tests/reconcile_worker_identity.rs` 5 条（改名的能解析、关掉的 fail-closed、歧义不挑第一个、paper 不是 Binance 候选、四份发布示例各自解析出自己的名字）。行数预算：`worker_entry.rs: 509` 新入册，`cli.rs` 675 → 674。CCXT 那一半在生产里没有第二个调用点：`reconcile` 命令面只服务 Binance（`cli.rs:648-664`），CCXT 的 reconciler 由 `plan_workers` 逐一点名后交给 `ccxt-worker`（`worker_entry.rs:291-298` 的 `worker_id` 是必填），那条链从来不存在"默认名"这一类分叉——测试里的 CCXT 断言钉的是判据本身跨 Venue 成立，不是第二条待接的链路 |

### 38.3 变异报告（33 颗：T1 4 + T2 9 + T3 16 + T4 4，每颗跑完按字节还原）

| 变异 | 改了什么 | 谁红、红在哪 |
|---|---|---|
| U1 | 订单支的账户 venue 退回"拿标的后缀"（R13 缺陷本体） | T1 那条用例红在 `account_projection_identity.rs:93`，消息就是 `expected=account-a/paper actual=account-a/BINANCE seq=0` |
| U2 | 账簿支同样退回 | 同一条用例红在 `:93`，`seq=1`（说明两支各自都被钉住，不是一处覆盖两处） |
| U3 | 放宽过头：成交也不再核对交易所 | 红在 `:138` `unwrap_err() on Ok(1)`——跨 venue 的成交被收了 |
| U4 | 只摘掉账户身份比对（条件第一项改 `false &&`），保留 venue 那一半 | 红在 `:113` `unwrap_err() on Ok(1)`——外来账户的订单被收了 |
| M1 | `sessions` 缺席又变成"组件非法"（退回修复前的宽度） | `calendar_component_fingerprints_match_the_shared_python_digest` 红在 `calendar_component_fingerprint.rs:77`，报错原文 `calendar 组件缺少 sessions` |
| M2 | canonical 三字段的先后顺序换了 | 上面那条 + `calendar_digest_follows_the_canonical_fields_not_the_document_layout` 两条红，:79"两侧各写一遍的那份格式串又漂了一个字节" |
| M3 | 任何形状的 `sessions` 都折算成 `&[]`（fail-open） | 同两条红 :79——v1 夹具的时段不再进摘要，跨侧共数当场断 |
| G1 | Rust 常驻反例改名 | 门禁"三条 Rust 与两条 Python 常驻反例都在"红，缺用例里点名那条 |
| G2 | Python 用例不再读旧格式夹具的摘要 | 门禁"夹具与摘要成对存在，两侧用例读的是同一对文件"红：`['calendar-component-legacy.fingerprint']` |
| G3 | 把摘要手抄进 `test_ashare.py` | 门禁"期望摘要只在夹具旁边那一份文件里"红：`出现抄本 ['python/tests/test_ashare.py']` |
| G4 | Python 侧字段白名单多一项 | 门禁"字段白名单与契约版本号在两侧逐项相等"红（`extra` 出现在 Python 侧） |
| G5 | Python 侧 `ASHARE_SCHEMA_VERSION = 2` 先 bump | 同一项红（版本 `1` vs `2`） |
| G6 | Rust 白名单声明的个数与内容不符（`[&str; 5]` → `[&str; 4]`） | 同一项红（"声明的个数也等于白名单长度"） |
| T3a | `from_json` 的版本闸门摘掉 | `reader_refuses_a_self_consistent_document_from_another_version` + `reader_rejects_versions_the_served_schema_does_not_declare` 两条红，:45"v2 的文档不能被当成 v1 收下" |
| T3b | 契约里删掉一个钱字段 | `schema_keeps_uncomputed_money_nullable` + `served_schema_declares_every_key_the_writer_emits` 红（:147 键不存在） |
| T3c | 未算的钱不再声明为可空 | `schema_keeps_uncomputed_money_nullable` 红，:135"`available_raw` 必须声明为可空类型（未算时写侧印 null）" |
| T3d | 契约声明的版本号改了 | `reader_rejects_versions_the_served_schema_does_not_declare` 红，:174"契约声明的版本号必须就是读侧支持的那一个" |
| T3e | header 必填少一列 | `schema_frame_matches_the_protocol_it_describes` 红，:280"header 的契约里没有 `state_hash`，而读模型每天都在印它" |
| T3f | 读侧支持的版本号先 bump | 与 T3d 同一条红——两侧只动一侧必然被这条钉住 |
| T3g | 第二份手抄回来（`include_str!` → `"{}"`） | 五条契约用例同时红（含 `served_schema_is_the_repository_file_verbatim`） |
| T3-G1 | `include_str!` 的路径换了写法 | 门禁"只有一份文本：常量按字节包含 schemas/ 那份"红 |
| T3-G1b | 生产 Rust 里出现 schema 字面量 | 同一项红，且抄本文件被点名 `['crates/qx-protocol/src/lib.rs']` |
| T3-G2 | `required` 里多出一格没声明的键 | 两项同时红：契约自洽（`required 越界 ['ghost']`）与"对面 Python 的读侧与契约同宽"（`必填差 ['ghost']`） |
| T3-G3 | `properties` 里多出一格写侧不产的键 | "契约声明的顶层键集合正好等于写侧产物印出的那些"红：`只在契约 ['ghost']` |
| T3-G4 | 未算的钱不再可空 | "七个…可空、权益不可空"红（可空列与夹具未算列同时错位） |
| T3-G5 | 契约的版本号改了 | "契约里的协议名与版本号，就是读侧 `from_json` 认的那一对"红：`读侧常量 1 / 契约 const 2` |
| T3-G6 | 路由不再发出这份常量 | "GET /schema/account-snapshot-v1 发出的就是这份常量，且客户端取契约不必带操作者凭据"红 |
| T3-G7 | Python 必填集合漂移 | "对面 Python 的快照读侧与契约同宽"红：`必填差 ['equity_raw', 'equity_raw_x']` |
| T3-G8 | 契约用例被改名 | "契约的六条判据各有常驻用例"红：`缺用例 ['schema_frame_matches_the_protocol_it_describes']` |
| V1 | 唯一候选也回落到字面量 `reconciler-main` | `renamed_binance_reconciler_is_the_one_reconcile_resolves` + `published_runtimes_resolve_their_own_reconciler` 红，:52"必须按 role + Venue 解析，而不是回落到字面量" |
| V2 | 候选不唯一时挑第一个 | `ambiguous_reconciler_candidates_are_not_resolved_by_choosing_one` 红，:106 `unwrap_err() on Ok("reconcile-btc")` |
| V3 | 关掉的 reconciler 也算候选 | `reconcile_lookup_fails_closed_without_an_enabled_reconciler` + `published_runtimes_resolve_their_own_reconciler` 红，:76（基线示例里那条被关掉的腿成了候选） |
| V4 | 解析时不看 Venue 绑定 | `paper_reconciler_is_not_a_candidate_for_the_binance_entry` + `published_runtimes_resolve_their_own_reconciler` 红，:124 |

33 颗的锚点都要求命中 1 次才动手，还原后逐个 sha256 与打补丁前一致：`crates/qx-api/src/lib.rs`
`db7ec9944aebd8300b8efb49dec0145b94df3726b8968cc0da13c07526566ae6`（即 HEAD，T1 不改生产码）、
`crates/qx-protocol/src/lib.rs` `253cc1c35bb3e2a6e8544a369a952807fc039df66c51f5e0be5b818788140d58`、
`schemas/account-snapshot-v1.json` `5900fbe1bdf55731059e4e747fe9636deab46a6f1a0700e29f36c75ca90c0a76`、
`python/qianxing_bridge/__init__.py` `995f74de0010860caa02828fbe65774fec28127525a67df9dc34f951cef89802`、
`crates/qx-protocol/tests/snapshot_schema_contract.rs` `0d4d86c274ef8a626b75c5e56b252b09050911b2ff92fc1e07a03983c4ef6df4`、
`crates/qx-cli/src/dataset_commands.rs`、`crates/qx-cli/src/worker_entry.rs`、`python/qianxing_ashare/__init__.py`、
`python/tests/test_ashare.py`、`crates/qx-xingban/src/ashare.rs` 各自还原一致。

### 38.4 本轮踩到的两件事（都是"验证手段自己说谎"那一类）

1. **变异电池的"红"必须是断言红**。T2 电池首跑把 M2 记成了通过——它报的是
   `error: linking with link.exe failed: exit code 1104`：同一时间另有一颗 cargo 在抢同一个 `target/`，
   链接失败被脚本的"编译失败也算被抓"吞成了绿。单独重跑那三颗 Rust 变异才拿到上表 M2 的真红。口径落进脚本：
   编译/链接失败一律记作"这一颗没做完"，不再记作通过（`red` 为空即报"没有用例变红"）。
2. **变异要改判据，不是改判据所在的语法结构**。U4 的第一版把 `if let Some((account_id, venue_id)) = identity {`
   整段换成 `if false {`，块体内引用的两个绑定一起没了（E0425），这颗变异本身不合法。改成只把条件第一项写成
   `false && …` 之后，红在 `:113`。同一类坑本轮还撞到一次：G5 的锚点 `b"\nASHARE_SCHEMA_VERSION = 1\n"` 在 CRLF
   工作副本里 0 命中（与 §37.3 末段是同一条教训），锚点一律不带换行。

### 38.5 交给下一轮：只报不修

§37.5 的第 1、2、4、6、7、8 条（S8 产物路径、S9 信封两格、S11 限流参数、R8 停机信号、`/health` 恒 200 与
`--json` 覆盖面、S13 行为侧）与 §35.5 的第 1、3、4、5、6、8 条（C3/C4/C6/C9、`qx-execution` 1619 行结构债、
Q1b 输入指纹）本轮全部未动，理由与证据照旧。本轮新立四条，都还不需要拍板、但一拍就得改口径：

1. **T2 的夹具模式只钉住了 `calendar` 一条分支，`corporate_actions` 那一半仍没有跨语言用例**：
   `dataset_component_file_fingerprint` 只有三个分支——`corporate_actions`（`serde_json::to_vec(数组)` 的
   sha256）、`calendar`（本轮钉住）、`_` 通用分支。Python 侧 `build_dataset_bundle_manifest` 给
   `corporate_actions` 登记的 `fingerprint` 是 `AshareActionManifest.source_hash`
   （`python/qianxing_ashare/__init__.py:832-847`：`json.dumps(sort_keys=True, separators=(",", ":"))` 的
   sha256）。两个式子**今天**应当相等（serde_json 的 `Map` 是 BTreeMap，也是"键有序 + 无空白"），但全仓没有
   一条用例比过这两个数，仓库里也没有任何示例把 `dataset_component_paths.corporate_actions` 绑上去
   （`deploy/data/qianxing-ashare/` 下只有 `bundles/`、`datasets.manifest.json`、`runs/`，没有组件文件）——
   也就是说这条边 today 靠"看起来一样"活着，与 R17 立案前是同一种状态。
   **顺带核实并排除的一条**：`bars` 组件在 Python 侧登记的是 16 位的 `frame.digest()`
   （`python/qianxing_ashare/__init__.py:822`），与 CLI 的 64 位 sha256 永远不相等，看着像同一类分叉；但
   `dataset_component_paths` 里 `kind == "bars"` 会被拓扑校验当场拒（`crates/qx-runtime/src/runtime_config/strategy_validation.rs:94-101`，
   bars 走 `bars_snapshot_path`），所以那条 16 位摘要根本遇不上 JSON 复检，不是缺陷。
2. **旧格式（顶层没有 `schema_version`）的日历文档，现在两侧都读成"没有时段"，可"谁该写 `sessions`"仍然没有声明**：
   `ASHARE_SCHEMA_VERSION` 闸的是新版文档，v0 的宽度靠 Rust 与 Python 各自同意。要收口得先定"v0 文档还在不在生产里"。
3. **T3 之后契约仍然没有字段级 description，顶层保留 `additionalProperties: true`**：契约说"允许别的键"，
   而 `from_json` 对未知顶层键的实际行为没有用例，也没拍板要不要 fail-closed。
4. **`$id` 指向的 `https://qianxing.dev/schema/...` 解析不到东西**：它是本仓自定义命名空间，没有发布物。真要接
   JSON Schema 校验器（ajv 之类）去验产物，就得引依赖，与"零新依赖"纪律冲突，故只做静态互钉。

### 38.6 门禁数字与发布判定（覆盖 §37.6）

`cargo fmt --all -- --check` 0；`cargo clippy --workspace --all-targets --all-features -- -D warnings` 0；
`cargo test --workspace` **80 suites / 767 passed / 0 failed**（§37.6 的 752 + T1 1 + T4 5 + T3 6 + T2 3）；
`tools/check_architecture.py` **314 项全过**（302 → +8 T3 → +4 T2）；`tools/validate_core.py` 全过；
Python `unittest` **46 项 OK（2 skip）**（44 + T2 的 2 条）。`CLI_TEST_FLOOR` 191 → **199**（实测 src/tests 160 +
`crates/qx-cli/tests` 39：T4 的 5 条与 T2 的 3 条都在 src/tests 里）。
行数预算：`worker_entry.rs: 509` 新入册、`cli.rs` 675 → 674，`qx-api/src/lib.rs` 与 `qx-protocol/src/lib.rs`
本轮**都是零改动/零增行**（前者生产码不动，后者 18 增 18 删后仍正好 844）。

据此判定：**研究档与 paper 档达到发布条件**。本轮没有新增一条链路，做的是把上一轮"只报不修"里那四项缺的证据
补齐：现在 R13、R17、S10、S12 这四件事各有 4/9/16/4 颗变异当场咬住，其中"跨语言两侧各写一遍"的两件
（T2 日历指纹、T3 快照契约）第一次做到了**任一侧单独漂移必红、两侧一起漂移则夹具与产物对不上**。
实盘档的差距不变：仍差 §38.5 承接的那几条（R8 停机、C3/C4/C6/C9）与外部交易所沙盒记录为零——
`sandbox_tested` 全为 `false` 这件事不随本轮门禁数字改变。

## 39. 合流轮：两条并行线在同一条纪律上各自收口，一半有牙齿一半没有（2026-09-23）

T 轮（§38）落完之后 `git fetch` 才看到 `origin/main` 上多了 Q70/Q71 两颗（4264760 / 4d78c8d），
把那颗合完、门禁回到全绿，远端又多出 Q72（72c347e），于是同一轮里合了两次。本节只记录合流的后果与
修掉的那一半，不做代码移植。编号说明：远端已占掉 §32（Q70）/ §33（Q71）/ §34（Q72），本仓那五节
整批落到 R=§35、三遍扫描=§36、S=§37、T=§38、合流轮=§39。

### 39.1 第一次合流当场露出的三件事

1. **契约与类型在同一天各改各的**（本轮唯一的生产缺陷，已修）：Q70 把协议侧 `equity_raw` 变成
   `Option<i128>`（缺标记价时报缺席，而不是拿剩余现金冒充权益），T3 刚把对外契约钉成"七格可空、
   权益不可空"。两侧各自跑完自己的门禁与用例都是绿的，合流之后契约对它自家读模型每天印出的
   `"equity_raw":null` 说了谎。缺陷与修复见 §39.2。
2. **两条线各自写了同一份 paper 夹具，合流时塌回一个**（本轮已收口，不是待办）：上游那份文件顶部的
   `paper_runtime`（`origin/main` 版 `:13`）与本仓 `crates/qx-cli/src/tests/mod.rs:192` 的
   `paper_runtime_config` 逐行比过——除函数名与 `pub(crate)` 之外完全相同（同样读
   `deploy/qianxing.runtime.paper-strategy.example.json`、同样把规格路径绝对化），`seed_paper_fill_with_fee`
   在 `:31` 与 `mod.rs:209` 也是同一对。两份都留着就是三个"同一份配置"的真相源，而它们一旦漂移，对面那几条
   用例读的就不是它自己验过的形状。合流取共享那一份、把上游新增用例的调用点改名
   （`api_snapshot_money_fields.rs:249`）；"行为不变"不是感觉，就是上面那句逐行相等。
3. **门禁自己的失败文案停在旧口径**（本轮修，一行）：M5 那颗变异把 `/account/balances` 的权益折成 `0`，
   门禁确实红了，报出来的却是「余额端点对 available/margin 又用了兜底写法」——那一项的条件里
   `"equity_raw": snapshot.as_ref().map(|snapshot| snapshot.equity_raw)` 是第三条，文案只点名两条。
   判据对、文案错，一样会把人往错的方向上带：现在点名三个取法。

### 39.2 契约那一格的修复：判据从"抄名单"换成"问写侧与类型"

`schemas/account-snapshot-v1.json` 的 `equity_raw` 改为 `["integer", "null"]`。但改这一格只是跟上事实，
真正防复发的是把判据换掉——三处都不再抄字段名单：

| 落点 | 判据 |
|---|---|
| `crates/qx-protocol/tests/snapshot_schema_contract.rs` `schema_keeps_uncomputed_money_nullable` | 由写侧自己交两份产物：一份"八项全没算"、一份"八项全算得出"。印出 `null` 的那一格必须在契约里可空、印出整数的那一格必须照样声明 integer，且那份"全部未算"必须能被 `from_json` 读回 `None`。旧写法是抄一份七个名字的名单再加一句"权益不可空" |
| `tools/check_architecture.py` `account_snapshot_schema_check` 第 4 项 | 契约里声明为可空的那些，必须正好等于 `AccountSnapshot` 中类型为 `Option<i128>` 的那些（门禁直接正则读协议源码）。名单与类型任何一侧单独漂都红 |
| `python/tests/test_bridge.py` `test_account_snapshot_schema_boundary` | 契约允许 null 的键，Python 读侧也必须收得下：喂一份 `"equity_raw": null` 的产物必须解得开。必填集合说的是**键必须在**，与值能不能算得出是两件事 |

### 39.3 变异报告（5 颗，每颗跑完按字节还原，sha256 复核一致）

下面每一句"红在哪"都是当轮电池日志的原文，不是事后按记忆复述；日志随 `target/` 走、`cargo clean` 即失，所以它只是本轮的取证，长期证据是表里点名的那些常驻用例与门禁项。

| 变异 | 改了什么 | 谁红、红在哪 |
|---|---|---|
| M1 | 契约的 `equity_raw` 退回 `{"type": "integer"}`（缺陷原状） | 门禁红 1 项：「钱字段的可空声明逐项等于协议类型 — 协议 Option [八格，含 `equity_raw`] / 契约可空 [七格，缺 `equity_raw`] / 缺类型声明 [] / 夹具未算 [七格]」；常驻用例红在 `crates/qx-protocol/tests/snapshot_schema_contract.rs:161`「equity_raw 必须声明为可空类型（写侧没算过时印 null）」，同文件另 5 条仍绿 |
| M2 | 协议侧 `pub equity_raw: Option<i128>` 退回裸 `i128`（Q70 整体回退） | 门禁红**两**项：上游 Q70 那项「八个汇总钱字段在协议上全是 `Option<i128>`…」与本轮换的新判据（契约八、类型七）。另有 `error[E0308]` 4 处，第一处在 `crates/qx-protocol/src/lib.rs:182` 的 `Self { equity_raw: None, .. }` 构造点——协议 crate 自家就编不过，比"测试编不过"更早一道。编译红不是断言红，故只记第二道 |
| M3 | 门禁第 4 项退回抄名单口径（名单写死那七个，外加"权益不可空"） | **对照颗，两颗一起看**：(a) 只上 M3、代码保持今天的修复态 → 门禁红，但它只报得出「契约可空 [八格]」，说不出为什么这格该可空；(b) M3 与 M1 同时上（缺陷原状 + 旧判据）→ **门禁 318 项全绿**，契约对自家写侧说谎一句都挡不住。单报"M3 红"或"M3 绿"都不成立，合起来才是"判据必须换成类型比对"的证据 |
| M4 | 读模型把算不出的权益折成剩余现金（`equity_for(...)` → `Some(cash_for(...))`，即 Q70 之前的写法） | 常驻用例红在 `crates/qx-cli/src/tests/api_snapshot_money_fields.rs:323`：「assertion `left == right` failed: 算不出的权益不能印成剩余现金 9900000000000 / left: `Some(9900000000000)` / right: `None`」；门禁同轮红：「权益只在现金与每一条持仓的标记价都读得出时发布；算不出即缺席，不退回纯现金（V11 Q70）」 |
| M5 | HTTP 端点把缺席的权益印成 `0`（`map(…equity_raw)` → `map(Some(…unwrap_or(0)))`） | 常驻用例红在 `crates/qx-api/src/lib.rs:2602`：「算不出标记价时权益必须印 null，而不是给账户兜一个 0: `{"available_raw":null,"cash_raw":{},"equity_raw":0,"margin_raw":null}`」；门禁那项也红，只是当时的文案没点到 equity——见 §39.1 第 3 条 |

五份文件还原后的 sha256 前 16 位与开局逐一对上：`api_service.rs ab5ecd99788d1e97`、
`qx-api/src/lib.rs 711562390cbdac42`、`account-snapshot-v1.json 84a10cacd3ee7c4e`、
`qx-protocol/src/lib.rs f7dc31185d67206b`、`check_architecture.py f79241e1b8110d82`。

### 39.4 本轮踩到：一句从上游清单继承来的话被当场证伪

- 本节初稿在 §39.1 写过第三件事："多腿产物仍只印两腿 `return_bps` 的等权平均，`combined_return_bps`
  不进产物，差 87bp"。回读代码：`crates/qx-cli/src/multi_leg.rs` 里根本没有 `multi_leg_report_json`
  这个函数；那颗组合收益就写在多腿归因产物的 `totals.combined_return_bps` 里
  （`crates/qx-cli/src/backtests/multi_builtin.rs:464`），而 Q71 的常驻用例
  `crates/qx-cli/tests/multi_leg_attribution.rs:726` 同时钉住 stdout 与产物两处，还显式断言它
  **不等于**两腿平均。那句话是上游修完之前的旧状态。本轮把它删掉而不是改窄。
  教训：**并行轮记的"只报不修"清单，合流时必须回读一遍现状**，不能当既有事实继承。
- **`--exact` 只给末段函数名等于 0 条测试**：M4 第一版因此拿到 `rc=0` 的"通过"，差点被记成"这颗没抓住"。
  过滤词要的是完整路径 `tests::api_snapshot_money_fields::equity_without_a_mark_price_…`，
  重跑才有上面的断言红。命中 0 条时 cargo 报 `ok`，这一点与 T 轮那条 `link.exe` 假红是同一类事故。

### 39.5 第二次合流（Q72）：冲突的五份文件里，代码零重叠，要判的全是口径

Q72 动的是 `crates/qx-cli/src/backtests/*` 与那颗新增的 `account_base.rs`，与本仓 T 轮改动不重叠——
`git diff` 里这五份冲突全是文档与门禁。四处需要判断，每一处都是"两侧各写各的现状"：

1. **上游那份收口指南还在描述 `--fill-tier` 有 `l2`/`l3` 两档订单簿内核**，而合流后的代码只认
   `l1`/`l2`（R11 的收口：`L2L3` 一次吃掉帧内全部深度，没有独立 `l3` 内核，实测口径在
   `crates/qx-cli/src/cli_help.rs:67-68`）。照抄上游那两行等于把一条已修的缺陷重新写成现状。
   取本仓那份，只把 Q72 新增的"多腿链当场拒收 `strategy.initial_cash_raw`"折进同一行的
   "明确无落点"格。
2. **README 的 blessed 摘要那格两侧都漂**：本仓写"停在 `schema_version: 1`（无 `input` 块）"，
   Q72 把摘要抬到 v4，于是缺的是 `input` 与 `account` 两块。实测过再落笔：被 git 跟踪的
   `*.summary.json` 共 16 份，`schema_version` 全为 1、两块全缺，而当前代码写 4
   （`crates/qx-cli/src/backtests/artifacts.rs:301`）。
3. **上游 §34.5 那份"只报不修"清单按 §39.4 的教训回读了一遍现状**，两条都属实才留在 README：
   `multi_builtin.rs` 实测 498 行、兄弟模块线 500 行；回测读 `strategy.initial_cash_raw`、paper 读
   `worker.paper_initial_cash_raw`，且回测侧的字段注释自己就写着"回测读不到它"
   （`crates/qx-runtime/src/runtime_config/strategy_schema.rs:133`）——两格互不知情。
4. **编号位移不能全交给正则**：机械把 §34–§38 抬一位时，顺手把本节开头那句范围描述
   （远端占 §32–§35）也抬成了 §36，而范围两端的编号里只有一端该动。回读手写改回，并把
   "两次合流各让一次位"这件事写在同一段里。

`CLI_TEST_FLOOR` 同场再抬一次：地板两侧各报过一个历史值（本仓 202、上游 191），合流后按磁盘
`^#\[test\]$` 重测才是当轮事实——`src/tests` 165 + `crates/qx-cli/tests` 45 = **210**。

### 39.6 第一次合流后的门禁数字（覆盖 §38.6 与 §37.6）

| 项 | T 轮（HEAD 实测） | 合流后（本轮实测） |
|---|---|---|
| 架构不变量 | 314 项全绿 | **318 项全绿**（+9 / −5 逐标签比过：上游新增 4 项——「权益只在…都读得出时发布」「多腿组合收益只有一处算法」「组合收益必须同时有加权/平均分叉的行为用例与可复算的产物两端」「组级合计只在 multi_leg_group_totals 一处折叠」，另有 4 项按 Q70/Q71 改名（用例条数 199→202、读模型用例四→五、标量取法七→八、协议类型七→八），本轮换判据 1 项对删掉那项"权益不可空"，一换一） |
| 整树 `cargo test --workspace` | 767 passed | **770 passed / 0 failed / 0 ignored**（+3 条 `#[test]` 全来自上游：`api_snapshot_money_fields.rs` 7→8、`execution_and_multi_leg.rs` 3→4、`multi_leg_attribution.rs` 10→11；本轮把 T3 那条用例改写得不增计数） |
| `CLI_TEST_FLOOR` | 199 | **202**（实测 `src/tests` 162 + `crates/qx-cli/tests` 40） |
| Python `unittest` | 46 | **46 OK（2 skip）**（只在一条既有用例里加了一条断言） |
| `cargo fmt` / `clippy -D warnings` | 0 / 0 | 0 / 0（`--workspace --all-targets`） |
| 行数预算 | 全过 | 全过（`--snapshot` 后：`qx-api/src/lib.rs` 3224→3237 吸收上游端点用例、`qx-protocol/src/lib.rs` 888→847、新入册 `api_snapshot_money_fields.rs: 518`） |

据此判定不变：研究档与 paper 档达到发布条件；实盘档仍差 §35.5 与 §37.5 那几件事，
`sandbox_tested` 全为 `false` 这件事不随门禁数字改变。

### 39.7 第二次合流后的门禁数字（覆盖 §39.6）

| 项 | 第一次合流后（§39.6） | 第二次合流后（本轮实测） |
|---|---|---|
| 架构不变量 | 318 项全绿 | **328 项全绿**。两侧标签逐条比过：新增 13 / 消失 3，其中 10 项是 Q72 自带的判据（默认本金只有一处具名常数、三种来源各有常量、装配处本金只能由构造参数给出、三条链共用那一处读法、三条链各印一行且同一排版函数、非正声明当场报错、摘要以 v4 落 `account` 块、`strategy.initial_cash_raw` 是 `Option<i128>` 且省略不落键、多腿链拒收单账户声明、Q72 用例在位），另 3 项是同一判据改名：用例地板「不少于 202」→「不少于 210」、「内置链侧的包装定义一处，且 `backtest builtin` 问过它」→「定义一处，三条链各问过它一次」、「摘要以 schema v3 落 `input` 块」→「以当前 schema 版本落 `input` 块」。HEAD 树单独取出来跑过一遍，318 与上面那行对得上 |
| 整树 `cargo test --workspace` | 770 passed / 0 failed | **778 passed / 0 failed / 0 ignored / 81 个测试壳**（+8 条全来自 Q72：三条本金读法的单元用例 + 五条命令行端到端）。另跑 `--all-features`：**782 passed / 0 failed / 4 ignored** |
| `CLI_TEST_FLOOR` | 202（162 + 40） | **210**（磁盘 `^#\[test\]$` 实测 `src/tests` 165 + `crates/qx-cli/tests` 45）。两侧各留过一个历史值（本仓 202、上游 191），合流轮按磁盘重测，不继承任何一侧的数 |
| Python `unittest` | 46 OK（2 skip） | **46 OK（2 skip）**，`tools/validate_core.py` 全过 |
| `cargo fmt` / `clippy -D warnings` | 0 / 0 | **0 / 0**：`--workspace --all-targets` 一次，`-p qx-cli --no-default-features` 的 `sqlite` / `postgres` / `nats` / `postgres,nats` 四个组合各一次 |
| 行数预算 | 全过（三处随合流改快照） | **全过，且 `--snapshot` 重跑后 0 改动**：Q72 的新文件全在 500 行线下，最长的 `multi_builtin.rs` 实测 498 行，仍未入册 |
| 命令面与示例配置 | 52 条入口行 / 41 个命令名 / `deploy/*.json` 顶层 52 份 | **同值重测通过**（本轮用 `target/debug/qx-cli.exe help` 数出 52 条 / 41 个，`deploy/*.json` 顶层 52 份），README 那三处实测口径不改 |

判定同 §39.6：研究档与 paper 档达到发布条件，实盘档的差距不因合流改变。本轮新暴露的一条是**文档面的**：
上游与本仓在"同一条链的口径"上各写了一份现状，合流时如果按"上游优先"整段取走，就会把 R11 已修的
`--fill-tier` 三档说法重新发布出去（§39.5 第 1 条）——并行轮合流时，冲突的文档段落要按**当前代码**判，
不能按来源判新旧。

### 39.8 文档行号引用的复位口径：结构性跟着代码走，取证性跟着 commit 走

两次合流各自把上游的文件抬长了若干行（`tools/check_architecture.py` 4480 → 4662、
`multi_builtin.rs` 493 → 498），`file:NNN` 这一层是最先烂掉的东西。本轮按两条判据处置：

- **结构性引用复位到当前代码**。判据是逐条读过去、确认那一行确实写着它所说的事，
  而不是按位移量整批抬。共复位 29 条（V11 22 / CHANGELOG 7）：门禁侧 8 处
  （`_identity_block` 4414、`market_spec_source_check` 4435、`input_provenance_check` 1628 与其
  挂载点 4624、`snapshot_money_honesty_check` 1918、`position_money_honesty_check` 2125、
  `cli_backtest_module_check` 711 与 `>= OVERSIZED` 那一行 723、`calendar_fingerprint_caliper_check` 2691）、
  Q71 段里 `multi_builtin.rs` 的 7 处落点与 `leg_funding.rs` 的 `161-185`、`market_spec_with_margin` 92
  与标签单点 116、禁抄 `Some(…)` 那段注释 1985-1997、`capabilities.yaml` 里收口指南的证据行 163、
  `strategy_schema.rs` 的 `fill_model` 162、`backtest_entries.rs` 的 CRLF 归一 373。
- **取证性引用不动**，并把它们钉在 commit 上。§34.1 与 §33 开头都写着"改前取自本轮开始时的
  `HEAD` = `99c7051` / `4264760`"；`multi_builtin.rs:274` 那句"此前印
  `(primary_report.return_bps + reference_report.return_bps) / 2`"在 `4264760` 里逐字复核成立，
  而同一行在合流后的树里是 `println!(`。这类行号复位了就是把历史改成假话。
- **本轮检出面的边界要说清**：判据是"上游本轮触及的文件 + 逐行内容比对"，它有一类结构性盲区——
  某条引用若在合流前就已漂动、且两侧同源（比对基线与工作树那一行内容相同），内容比对不会报。
  要覆盖那一类得按**符号**取证（抽出引用点名的函数/字段名，断言它落在被引行附近），
  启发式跑出的命中里绝大多数是路径名与窗口噪声，逐条判的成本高于收益。
  登记为开放项：**文档行号引用的全量符号级复检**，与 §30 那条"仓库只留今天仍然正确的文档"是同一条纪律的两端。
