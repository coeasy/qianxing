# 自研插件化量化交易与回测框架 · 完整架构设计方案（V4）

> **参考项目（仅借鉴架构与模块设计思路，不照搬代码）**：RQAlpha（RiceQuant）、NautilusTrader、vn.py、Hummingbot、CCXT/CCXT Pro、vectorbt、DeepSeek Harness（Cordis）
>
> **V4 本版重点**：★ 多交易所（Multi-Venue）★ 多数据源（Multi-Source）★ 回测引擎（Backtest Engine）★ 撮合/成本/延迟/保证金四模型细化 ★ 多策略完善
>
> **两条底线**：① 仅研究接口契约、状态机、职责划分与模块组织，**不复制任何第三方源代码**；② **热路径静态化、接缝插件化**——撮合/订单状态机/组合记账/事件排序走 trait 直调，只有低频接缝开放插件。
>
> **证据分级**：`A` = 官方明确陈述 · `B` = 官方代码/可读源码可复核 · `C` = 架构推断（自研建议）。营销口径数字一律标注谨慎，不作为容量承诺。

---

## 0. 一页纸结论

### 0.1 定位

一套面向**国内外多场景**、支持 **多交易所 / 多数据源 / 多账户 / 多策略 / 多品种 / 多交易终端 / 多语言策略** 的自研插件化量化交易与回测框架，开箱自带经典策略模板库。

### 0.2 九大核心判断

1. **统一模型先于引擎**——统一的是**交易语义**而非策略写法；instrument/order/fill/position/event 标准化，外部差异在适配层收敛。
2. **双前端、单语义核心**——向量化做大规模研究（速度），事件驱动做精确仿真与实盘（精度）；**research-to-live parity 来自语义一致，而非"同一份代码"的标签**。
3. **内核三层**：Kernel（不可替换）/ Domain Mods（业务语义可换）/ Infra Adapters（外部适配可换）。把市场规则写进 Kernel，插件化就只剩命名层面。
4. **插件向扩展点贡献，不修改内核**——manifest 声明 `provides`/`requires`/`replaces`；**禁止隐式"最后加载者胜"**。
5. **"插件一次、运行静态"**——核心与规则启动期装配完成，运行期只允许观测类热插拔与故障转移。
6. **多交易所的身份先于连接**——`CanonicalProduct`（研究表达）/ `InstrumentId`（执行与估值）/ `DataSourceId`（数据血缘）**三层分离，绝不相互覆盖**。
7. **多数据源的 raw → quality gate → normalized → feature 是不可绕过的管线**；同一 canonical 标的**固定主源**，备源只做回填与离群校验。
8. **回测真实性 = 数据可见性边界 × 因果事件顺序 × 可重放状态**，而非更复杂的滑点公式。**撮合模型首先要回答"我不知道什么"**。
9. **多策略 = 隔离 + 净额求解 + 显式优先级 + 配额治理 + 归因**，不是多线程。

### 0.3 七大参考项目的借鉴边界

| 项目 | 最强贡献 | 借鉴点 | **不引入** |
|---|---|---|---|
| **RQAlpha** | Environment DI + Mod Hook、组合—账户—持仓分层、`sys_*` 系统 Mod、bundle 数据契约 | 扩展点对称性、Mod 生命周期、系统能力 Mod 化、启动期 DI 替换、规则管道、两级数据结构 | 单策略进程模型；全局单例 Environment；隐式覆盖；其运行时（Other 许可，商用需授权） |
| **NautilusTrader** | Rust 核心、确定性时间、`InstrumentId=symbol.venue`、分离式 Provider/Data/Execution Client、FillModel 家族、Run manifest | 身份模型、适配层拆分、撮合/成本/延迟模型语义、可复现性契约 | 其 crate 运行时（LGPL）；把"5M rows/s"当容量承诺 |
| **Hummingbot** | Clock/TimeIterator、`OrderBookTracker`/`UserStreamTracker`/`ClientOrderTracker` 三态分离、`AsyncThrottler` | 连接器内部解耦、订单追踪到终态、限频模型、优雅降级 | Python/Cython 执行核心 |
| **CCXT / Pro** | 统一 REST + `watch*` 流式、`markets`/`markets_by_id` 双向映射、错误分层 | 符号映射、限频、错误分类 | 其 SDK 与数据结构；用其代替券商/柜台/DEX 语义 |
| **vn.py** | `BaseGateway` 最小契约、事件回调顺序、国内生态 | 网关同步边界、`on_contract→on_account→on_position→on_order→on_trade` 回放顺序 | 单进程全局对象模型 |
| **vectorbt** | 策略实例矩阵化、批量参数扫描 | 研究通道、向量化初筛 | 作实盘执行器；把限价单/PRO 特性当开源版能力 |
| **dsh / Cordis** | Service 接缝、Scope 隔离、Fiber 生命周期、Profile/Bundle/Patch | 作用域隔离、可撤销贡献、声明式装配（**仅装配层与低频接缝**） | 全链路 waterfall；运行期热替换语义 |

---

## 1. 总体架构

```
┌──────────────────────────────────────────────────────────────────┐
│ 0. 接入层  UI / CLI / REST API / Scheduler / 策略市场 / 配置中心  │
├──────────────────────────────────────────────────────────────────┤
│ 9. 可观测与审计  Logging · Metrics · Tracing · Audit · Alert      │
├──────────────────────────────────────────────────────────────────┤
│ 8. 适配层（插件接缝主力）                                        │
│    VenueAdapter × N（协议/会话/数据/订单/执行五层）              │
│    DataSourceAdapter × N · ChainAdapter · StorageAdapter          │
├──────────────────────────────────────────────────────────────────┤
│ 7. 多账户与组合层                                                │
│    AccountTree · PortfolioComposer · SignalMerger · TargetSolver  │
│    Allocator · AccountRouter · AccountCommandQueue · Ledger       │
├──────────────────────────────────────────────────────────────────┤
│ 6. 执行核心  RiskGate → OMS → MarginEngine → ExecAlgo             │
│    （静态 trait 直调；热路径不插件化）                           │
├──────────────────────────────────────────────────────────────────┤
│ 5. 回测引擎  TestClock · EventQueue(因果优先级) · DataReplay      │
│    SimulatedVenue · MatchingEngine · FillModel/Fee/Latency/Margin │
│    RunManifest · ReplayVerifier                                   │
├──────────────────────────────────────────────────────────────────┤
│ 4. 策略层  StrategyRuntime × N（Scope 隔离）· 多语言(WASM/Py/Rust)│
│    StrategyRegistry · 经典策略模板库 · Signal/Intent              │
├──────────────────────────────────────────────────────────────────┤
│ 3. 事件/消息总线  market · order · fill · risk · timer · ctrl     │
│    （确定性回放队列 + 流式实时双模式）                           │
├──────────────────────────────────────────────────────────────────┤
│ 2. 数据平面  Ingest → QualityGate → Normalize → BarAggregator     │
│    Catalog(Raw/Validated/Normalized/Feature) · FeatureStore       │
├──────────────────────────────────────────────────────────────────┤
│ 1. 领域模型  Identity(Venue/Market/Instrument) · Clock · Money    │
│    Order/Fill/Position/Balance/Margin · Event(溯源)               │
└──────────────────────────────────────────────────────────────────┘
      ↕                    ↕                    ↕
 研究通道（向量化）    事件驱动通道           国内执行边界
 vectorbt 风格         Simulator / Live       China Adapter
```

**依赖规则**：依赖向内、副作用向外。

---

## 2. 模块化插件体系

> 吸收 **RQAlpha Mod Hook** 与 **Cordis/dsh** 思想，重构为「扩展点贡献 + manifest 契约 + 五阶段启动 + Profile 装配 + 热插拔守门」。

### 2.1 内核三层：谁能被替换

```
Kernel（不可替换）      时钟 · 事件调度 · 订单标识 · 状态机 · 事件溯源 · 审计 · 一致性规则
Domain Mods（可替换）   资产/账户规则 · Matcher · FeeRule · MarginRule · RiskRule · 绩效 · 调度
Infra Adapters（可换）  DataSource · VenueAdapter · EventSource · PriceBoard · Persist · MQ
```

> **判据**：把市场规则写进 Kernel，每接入一种资产/柜台都要改核心，插件化就只剩命名层面。Kernel 只保留"与具体市场无关"的不变量。

### 2.2 扩展点与贡献

| 扩展点 | 职责 | 默认实现 | 典型贡献者 |
|---|---|---|---|
| `DataSource` | 行情、快照、日历、合约信息 | bundle 本地 | RQData / Tushare / Polygon / IBKR |
| `VenueAdapter` | 交易所/券商/DEX 接入 | SimulatedVenue | CTP / IBKR / Binance / Uniswap |
| `PriceBoard` | 当前快照、报单参考价 | 历史快照 | 实时行情网关 |
| `EventSource` | 按时间序生成驱动事件 | 历史回放 | 实盘 tick / 外部调度 |
| `Matcher` | 撮合仿真 | 下一 bar 开盘价 | L2 深度行走 / L1 概率 / VWAP |
| `FillModel` | 成交假设 | DefaultFillModel | 11 类模型（见 §6.3） |
| `FeeModel` | 佣金、税费、资金费 | 零成本 | A股 / 期货 / maker-taker |
| `LatencyModel` | 命令延迟 | 零延迟 | StaticLatencyModel |
| `MarginRule` | 保证金与强平 | 无保证金 | 期货 / 加密合约 |
| `RiskRule` | pre/post-trade 校验 | 空规则链 | 限仓 / 自成交 / 熔断 |
| `Position` | 持仓模型（类型注册） | — | Stock/Future/Option/Crypto/Bond |
| `StrategyLoader` | 加载策略及执行域 | 文件系统 | DB / WASM / 编译型 |
| `ExecutionAlgo` | 下单算法 | 市价单 | TWAP / POV / Iceberg / 止损 |
| `Indicator` | 指标算子 | 内置基础集 | TA-Lib / 因子库 |
| `PersistProvider` | `load/store` 状态 | 本地文件 | DB / 对象存储 |
| `AnalyticsSink` | 绩效与归因 | 内置 analyser | tearsheet / 外部 BI |

**贡献规则**：独占扩展点强制 `priority` + 显式 `replaces`；**禁止隐式"最后加载者胜"**。

### 2.3 插件 manifest

```yaml
id: venue.ctp
version: 1.4.2
kind: infra-adapter          # domain-mod | infra-adapter | rule-pack | strategy-template
provides:
  - point: VenueAdapter
    cardinality: exclusive
    priority: 100
requires:
  - point: DataSource
  - plugin: rule.cn-futures@>=1.0
replaces: [venue.simulation]
config_schema: ./schema/ctp.json
capabilities: [futures, options, marketdata, order]
healthcheck: { endpoint: internal, timeout_ms: 3000 }
shutdown: { timeout_ms: 10000, drain: true }
scope: [account.*, strategy.*]
signature: <cosign>
```

### 2.4 生命周期与五阶段启动

```
discovered → validated → resolved → registered → starting → running
                                                    ↓
                                    stopping(drain) → stopped → disposed
                         ↘ failed ↗        ↘ degraded（降级运行）
```

**五阶段启动**（解决配置覆盖退化为初始化竞态）：
`1 配置校验` → `2 基础设施连接` → `3 领域规则注册` → `4 账户与策略装载` → `5 运行开始`

### 2.5 装配：Profile / Bundle / Patch

```
空配置 [] → Bundles 展开 → Profile patch 层 → 环境 overlay
```
**"换一套 Profile = 换一个运行场景，零代码改动"**。

### 2.6 ★ 热插拔边界（严守）

| 类别 | 时机 | 运行期可换 | 理由 |
|---|---|---|---|
| Kernel（时钟/状态机/溯源） | 启动期 | ❌ | 换它等于换系统语义 |
| Domain Mods（Matcher/Fee/Margin/Risk） | 启动期 | ❌ | 运行中换会破坏已成交路径可复现性 |
| Infra Adapter | 启动期 | ⚠️ 仅故障转移（留痕） | 换源须可审计 |
| Observability / AnalyticsSink | 任意 | ✅ | 不影响交易语义 |
| Indicator / 策略模板（研究态） | 启动期 | ✅ 研究模式 | 不涉及真实资金 |
| **运行中策略实例** | 启动期 | ⚠️ 仅 pause/stop | 热替换会让持仓与状态失配 |

> **一句话：插件一次、运行静态。**

### 2.7 内置插件清单（对标 RQAlpha `sys_*`）

| 内置插件 | 对应 RQAlpha | 职责 | 可替换 |
|---|---|---|---|
| `sys.accounts` | sys_accounts | 账户/组合/持仓/现金账簿 | 部分 |
| `sys.simulation` | sys_simulation | 模拟撮合 + 回测事件源 | ✅ |
| `sys.transaction-cost` | sys_transaction_cost | 佣金/印花税/过户费/手续费/资金费 | ✅ |
| `sys.risk` | sys_risk | 事前/事后风控规则链 | ✅ |
| `sys.analyser` | sys_analyser | 绩效、归因、tearsheet | ✅ |
| `sys.scheduler` | sys_scheduler | 定时/周期任务 | ✅ |
| `sys.margin` | （期货模块内） | 保证金、强平、逐日盯市 | ✅ |
| `sys.reconcile` ★ | — | 五类对账 | ✅ |
| `sys.replay` ★ | — | 事件溯源回放与确定性校验 | ❌（Kernel 侧） |
| `sys.data-quality` ★ | — | 数据质量门与隔离 | ✅ |

---

## 3. 领域模型

### 3.1 ★ 身份模型（多交易所的地基）

> 借鉴 Nautilus `InstrumentId = symbol.venue`（`A`）与 CCXT `markets`/`markets_by_id` 双向映射（`A`），并针对多子账户与 DEX 强化（`C`）。

```
VenueId      = provider + instance_region + account_set
               （子账户必须成为 venue 的一部分，否则不同保证金模式/权限会被错误合并）
MarketId     = VenueId + raw_symbol
InstrumentId = canonical_asset + quote + instrument_type + expiry/settlement + VenueId
BalanceKey   = (currency, venue, account, settlement_network, asset_type)
```

**关键：CanonicalProduct 只用于研究表达，InstrumentId 才用于执行与估值。**

```
CanonicalProduct（如 BTC-USDT）  → 研究、策略表达、跨所比价
InstrumentId（如 BTCUSDT-PERP.BINANCE） → 估值、执行、回测
DataSourceId                      → 数据血缘
三者用映射表连接，绝不相互覆盖。
```

> "同一 BTCUSDT"只表示资产参照相同，**不代表市场可互换**——不同 CEX 的标的确认规则、稳定币、保证金、资金费、交割时间、订单簿单位均可能不同。

### 3.2 InstrumentSpec 与 MarketState 分离（`A`+`C`）

| 对象 | 内容 | 性质 |
|---|---|---|
| `InstrumentSpec` | 乘数、价格/数量精度、lot、tick、到期、结算资产、费率、资金费规则、原始定义 | **不可变 + 版本化** |
| `MarketState` | 活跃、停牌、只减仓、交割中、指数异常、可交易额度 | 可刷新 |

**生命周期**：`Unknown → Loading → Ready → Halted → Retired`
- `Loading`：元数据未完整，**禁止下单**
- `Ready`：允许交易
- `Halted`：保留估值，**拒绝新开仓**
- `Retired`：禁止新订单与回测引用

元数据刷新必须**幂等**，返回版本、刷新时间、失效原因；热门 symbol 热更新，全量 symbol 灰度校验，**禁止在交易时段大范围阻塞刷新**。

### 3.3 ★ 时间与事件模型（跨所对齐的地基）

**五时钟 + 偏移**（`A`+`C`）：

| 字段 | 含义 |
|---|---|
| `event_time` | venue 产生或声明的业务时间 |
| `exchange_send_time` | 交易所发出时间 |
| `receive_time` | 本地接入点接收时间 |
| `ingest_time` | 入管线时间 |
| `engine_time` | 进入引擎时间 |
| `source_seq` / `venue_clock_offset` | 序号与时钟偏移 |

- **回测按 `event_time` 推进**；实盘用 `event_time` 作 watermark，按 `engine_time` 触发
- 每 venue 运行时钟健康度检查（回退、间隔异常、与 NTP 偏离）→ 可熔断，**但不能反向改写 venue 原始 event_time**（否则破坏审计链）

**事件信封**：`seq` · `ts` · `type` · `data` · `source_seqs` · `causation_id` · `deterministic_seed`

### 3.4 核心类型

| 类型 | 关键字段 |
|---|---|
| `Money` / `Quantity` | 币种 + 精度 + scale，默认 **128-bit 定点** |
| `Order` | client_id, venue_id, intent_id, strategy_id, **account_id**, algo_id, status, legs[] |
| `Fill` | order_id, qty, price, fee, side, ts —— **不可变事实** |
| `Position` | (InstrumentId, account, position_side, multiplier, avg_entry, unrealized_model) |
| `Event` | append-only、frozen、可重放 |

**订单状态机**（区分"提交意图"与"venue 事实"）：
```
PendingSubmit → Submitted → Accepted → Working → PartiallyFilled → Filled
CancelPending → Cancelled | Rejected | Expired | Finalizing
```
`Submitted` 只是离开本地的意图，`Accepted` 才代表获得 venue 确认。幂等键 + sequence number 是 WS 丢包后恢复一致性的最低要求。

---

## 4. ★ 多交易所（Multi-Venue）

### 4.1 Adapter 五层拆分（`A`+`B`+`C`）

> 若把 HTTP 签名、WS 状态、订单簿和 OMS 混写，多 venue 并发后必然失控。

| 层级 | 职责 | 项目对应 | 接口模式 |
|---|---|---|---|
| **协议层** | 签名、序列化、schema、错误码映射 | Hummingbot `Auth`；CCXT `Exchange` | `build_request/sign/parse_error` |
| **会话层** | 连接、订阅、心跳、重连、限频 | CCXT Pro WS；Nautilus session 恢复 | `connect/disconnect/subscribe/heartbeat` |
| **数据层** | order book、quote、trade、ticker、funding | Hummingbot `OrderBookTracker` | `apply_snapshot/apply_delta/refresh` |
| **订单层** | client order、exchange order、fills、reconcile | Hummingbot `ClientOrderTracker` | `send/cancel/replace/on_order/on_trade` |
| **执行适配层** | 归一化请求、交易规则、账户映射 | Nautilus `ExecutionClient`；vn.py `BaseGateway` | `submit/modify/cancel/query` |

```text
VenueAdapter {
  Config · InstrumentRegistry
  DataTransport + MarketStateStore
  OrderTransport + OrderStateStore
  RateLimitPolicy · LifecycleManager
}
```

**Hummingbot 三态分离是本层的最大启发**（`A`）：`OrderBookTracker`（市场状态）/ `UserStreamTracker`（账户状态）/ `ClientOrderTracker`（订单状态）**必须解耦**。

**vn.py 的回放顺序值得固化为契约**（`A`）：`connect` 后依次回放
`on_contract → on_account → on_position → on_order → on_trade`
这消除了"账户未就绪即下单""订单状态先到、合约定义后到"的竞态。回调数据必须**不可变**（缓存引用前拷贝）。

### 4.2 符号映射表（双向、版本化、可审计）

```
standard_symbol ↔ (venue, raw_symbol, instrument_type, version)
```

除字符串外还必须保存：**生效起始时间、退市/下架时间、合约切换规则、标的与结算资产、原始 market JSON hash、数据主键 source_symbol**。

> 切换合约时，旧映射**不能覆盖或静默删除**，否则 historical feature、回测路径与真实成交将失去可解释性。

### 4.3 跨所路由：四阶段（准入 → 评分 → 执行 → 后校验）

> **第一约束是可交易、可结算、可风控；价格或深度只是第二层筛选。** 若余额、权限、合约状态尚未统一，直接找"最优价格"会把不可成交的机会错误计入收益。

```
Strategy → SignalBus → ExecutionPolicy(Router) → RiskGate → OrderEmulator/Algo → VenueAdapter
```

| 阶段 | 内容 |
|---|---|
| **1 准入过滤** | venue 是否 Ready；币种/合约是否允许；账户权限（交易/杠杆/借币）；余额与初始保证金；精度/最小/lot/最大名义额；费率与结算币种；延迟与故障预算；订单生命周期支持 |
| **2 效用评分** | 可用深度、有效价、预计手续费、资金费、滑点、持仓与对冲成本、库存偏斜、预计延迟、清算/违约风险 |
| **3 执行策略** | 市价单只取 top-of-book 安全片段；限价单考虑 post-only/reduce-only/IOC-FOK；大额单 TWAP/VWAP/IS；跨所 hedge 用净 delta + 最大 legs 约束 |
| **4 后校验** | 提交前再查 pre-trade risk 与 venue rate limit；成交后 fill reconciliation、持仓再平衡、跨所库存校正 |

`Router` 返回 `CandidateSet[venue, expected_cost, available_qty, constraints]` 并**附带完整证据**；`RiskGate` 可拒绝但**不得静默改写业务含义**。

**必须显式定义的四个问题**：
- reduce-only 是否允许越过策略限额
- 强制平仓是否允许越过止损
- 算法替换是否生成新 client order id
- 跨所 hedge 在首腿成交后，另一腿继续 / 取消 / 降级

> **自有订单簿视角**：Nautilus 允许配置 `manage_own_order_books`，由引擎维护 MBO/L3 工作单视图，使策略能从公共 book 扣除自身订单估计净可用流动性（`A`）。可借鉴，但**前提是 own-order 与时间优先级得到正确 reconcile**，否则净流动性估计会优于实际可得流动性。

### 4.4 每 Venue 独立资源控制器（限频）

每个 adapter 持有**独立令牌桶/滑动窗口**，按维度分类：REST weight、IP/account/ws-session、订阅数、订单频率、签名有效期、重试预算。

- Hummingbot `AsyncThrottler` 用 `RateLimit` + `LinkedLimitWeightPair`，支持 endpoint 限流、rate-limit pool、权重与引用计数，限流发生在**请求前**（`A`）
- CCXT 提供 `rateLimit` 默认值，错误层次从 `BaseError` 延伸至 `OperationFailed`/`ExchangeError`/`DDoSProtection`/`RequestTimeout`/`InvalidNonce`（`A`）
- 参考 Nautilus Derive adapter 的可泛化原则（`A`）：固定窗口限流、签名前等待、自动重连、指数退避+抖动、30s ping、60s 无入站帧判死、重连后重放已确认订阅并刷新账户/订单/成交/持仓；**状态变更请求不重放**；登录或订阅连续失败 3 次停止 session

**错误分类即路由输入，不只是日志标签**：
`read/recoverable` → 可重试 · `conflict/nonce` → 刷新状态 · `insufficient_balance/risk/syntax` → 停止重试 · `unknown/timeout` → 进入 reconcile

### 4.5 跨所状态聚合

**余额**（`C`，关键）：
```
(currency, venue, account, settlement_network, asset_type) → quantity
```
`asset_type` 区分 spot、margin collateral、perp 未实现盈亏、options、locked、funding、staking、on-chain native、on-chain LP。
> **CEX 的 USDT 不能直接与同名链上代币相加**，除非桥接状态、托管关系与可转移性已被证明。

**持仓**：`(InstrumentId, account, position_side, multiplier, avg_entry, unrealized_model)`，再按 settlement currency 折算。**跨所 inventory 只有在"可自由转移、可对冲、具有相同 cashflow 与风险因子"时才有经济意义。**

**组合状态树**：
```
Portfolio → AccountState(Venue, Account) → AssetBalance / Position / Exposure → RiskMetric
```

**跨所风险三类指标**：

| 类别 | 内容 |
|---|---|
| 市场风险 | delta、beta、vega、correlation、scenario PnL、liquidity |
| 执行风险 | 可成交深度、价差、滑点、延迟、失败率、对冲缺口 |
| 清算/资产风险 | margin ratio、collateral haircut、settlement、transferability、wallet confirmation、MEV |

> 若 margin 以不同稳定币计价、币种不可互换或转账受限，**名义净值为正也不代表具备再平衡能力**。跨所再平衡必须显式建模 `hedge_ratio + transfer_time + transfer_fee + settlement_risk`。

### 4.6 状态恢复七步（幂等重建）

```
1 恢复 schema/version     → 2 同步 market metadata → 3 重建 book 至一致 snapshot
→ 4 查询账户/持仓/开放订单 → 5 重放本地 WAL 未确认事件 → 6 fill/position reconciliation
→ 7 标记 Ready
```
任何"本地有订单、venue 无订单"或"venue 有成交、本地无记录"，**都应升级为待人工确认**，不能默认视为成功或失败。

### 4.7 DEX / 链上

`ChainVenue = chain + protocol + pool/router + network`，保存 `pool_address`、`token0/1`、`fee_tier`、`reserve_source`、`router`、`permit_policy`、`multicall`。

| 维度 | CEX | DEX/链上 | 统一建模 |
|---|---|---|---|
| 身份 | exchange/account/symbol | chain/contract/pool/wallet | `VenueId + MarketId` |
| 报价 | L2/L3 | reserves/curve/quote | `MarketState` 多态 |
| 执行 | create/cancel | submit tx/simulate/confirm/reorg | `OrderState + ChainLifecycle` |
| 成本 | fee、funding | gas、priority fee、slippage、MEV | `ExecutionCost` |
| 时间 | venue timestamp | block time、inclusion、confirmation | 多时钟 watermark |
| 最终性 | 通常即时 | 区块深度、重组 | `finality_depth` |

链上适配器拆分：`WalletProvider`（地址/nonce/签名）· `ChainReader`（RPC/log/reserves）· `Simulator`（quote/slippage/gas）· `Broadcaster`（mempool/RBF/nonce）· `ConfirmationTracker`（block depth/reorg）。

**交易提交只以 `tx_hash` 作为 intent；只有达到配置的确认深度才转 `Filled`。** 需处理 revert、dropped、replaced、reorged、wallet nonce gap。

> 跨域套利必须扣除：稳定币脱锚、CEX 提币冻结、DEX 滑点、gas、确认时间、MEV、可用流动性。**跨链资产在未证明桥接最终性前，不视为同一即时可转移资产。**

---

## 5. ★ 多数据源（Multi-Source）

### 5.1 四级存储

> 正确顺序是**先保留原始证据，再统一 schema，最后生成特征**。若实时与回测用不同解析器，即使底层数据相同，策略收益也可能来自格式差异而非真实 alpha。

| 区域 | 内容 | 索引 | 角色 |
|---|---|---|---|
| **Raw** | venue 原始 JSON、Parquet、HTTP headers、wallet trace | `(source, venue, channel, date)` + 内容寻址 hash | 唯一可审计真相源 |
| **Validated** | schema/gap/mono/bounds/business-rule 检查结果 | Arrow/Parquet 分区 | 数据工程产物 |
| **Normalized** | trade、book、bar、corporate action、calendar、status | `(instrument_id, event_time)` | **回测与实盘共同输入** |
| **Feature/Event** | factor、signal、split/dividend、ST、delisting、settlement | 不可变版本 + feature registry | 研究、估值、风险 |

**统一接口**（`DataSource` 能力集）：
```
list_universes() get_instruments() get_calendar() get_bars() get_ticks()
get_orderbook() get_corporate_actions() get_status() health()
```
配置**必须声明能力**而非无类型字符串：
`asset_class × bar_size × history_depth × latency_class × point_in_time × license × rate_limit`

查询先匹配 `preferred_source`，失败后按 `fallback_priority` 回填；每个查询携带 `DatasetVersion`，输出附带 `source_lineage`。

### 5.2 多源路由：固定主源原则

- canonical 主键**固定主源**
- 备源只参与**缺失回填、延迟告警、离群校验**
- 任一 bar 进入审计日志
- **切换主源需要数据版本变更**

> 同一 canonical symbol 的跨所数据**不能按行择低**——否则同一序列在不同日期切换来源，历史研究失效。

### 5.3 质量门（机器可读，非只写日志）

| 类别 | 规则 |
|---|---|
| 缺失 | scheduled 后检测 missing bar、hole、zero volume、心跳缺口 |
| 时间单调 | 同 channel 内 `event_time` 单调；允许迟到，**不得倒灌已关闭窗口** |
| 价格边界 | high ≥ low；close 在合理范围；异常零值 |
| 成交逻辑 | quantity/notional/VWAP 与 trade count 关系合理 |
| 交易状态 | tick 只能在 `Ready` 状态被策略消费 |
| 企业行为 | split、dividend、ex-date、listing/delisting 前后状态 |
| 合约生命周期 | activation、settlement、retirement 顺序正确 |
| 估值一致性 | 证券数量与持仓快照可勾稽 |

每条记录写入 `quality_report` 并触发 `warn / quarantine / fail`：
- **quarantine** 可审计但**不得进入实时信号**
- **fail** 必须**阻断回测或实盘启动**

### 5.4 复权与 Point-in-Time（正确性约束，非显示参数）

**raw price 始终与成交和结算对应；adjusted price 只用于可视化或特定因子。** 回测必须按 `ex-date` 前可得信息逐步生成 adjusted series，**不能先按当前最新全量因子反向复权**。

```
adjustment_event(date, type, ratio, currency, ex_date, announcement_date, effective_date)
价格表保留 raw_close；factor 表保存 cum_adj_factor；视图层查询时按需计算
```

**财务/另类因子按公告或生效时点入库**，五字段缺一不可：
`report_period` · `announcement_time` · `restatement_of` · `as_of` · `valid_to`

策略查询使用 **双时态模型**（`valid_time` + `transaction_time`）：资产负债表只能在其公告时间之后可见。

**复权断点识别**：每日 factor 差分、前后价复权误差、volume/close 异常、与 reference source 滚动差异。

### 5.5 Bar 主键与跨所时间对齐

不同源的 bar 边界必须先声明（如 A股集合竞价/连续竞价、加密 24h、`09:30` vs `09:31` 起算）：

```
bar_key = (bar_size, alignment_rule, trading_calendar, timezone, open_policy, close_policy)
```

标准化阶段再生成策略消费的 `AlignedBar`。**同一原始 source 同时保留原始 bar 与标准化 bar**；不同源对同一标准化 bar 的冲突**不直接覆盖**，而通过质量门进入 `consensus/divergence` 视图。

**Tick→Bar 聚合**必须在带 schema 的 `BarAggregator` 中完成，输出至少含：
`open/high/low/close/volume/vwap/buy_volume/trade_count/first_event_time/last_event_time/quality_flag`

同一标准化窗口内，book/trade/mark/funding 事件应在 **barrier 关闭后原子发布**。回测用严格 past-only event；实盘允许 watermark 迟到，**但不得将未来聚合结果回填已关闭窗口**。

---

## 6. ★ 回测引擎（Backtest Engine）

### 6.1 五层架构（`A`+`C`）

```
1 Clock        单调时间 + TimeEvent 回调
2 EventBus     投递不可变领域事件
3 DataReplay   按可见性规则发布行情
4 Strategy / Portfolio / Risk   产生命令
5 SimulatedVenue   撮合、账户结算、成交回报
```

> 对应 Nautilus 的 `BacktestNode`（编排）/ `BacktestEngine`（确定性循环）/ `SimulatedExchange`（场地状态）/ `OrderMatchingEngine`（撮合）（`A`）。

**重放条件**：同一 run-id、manifest、初始状态与事件流，必须再次产生**完全相同**的事件、订单、账户与指标结果。

### 6.2 ★ 因果优先级（同时间戳不是任意排序）

事件携带 `(event_time, domain_priority, sequence)`：

| 优先级 | 处理顺序 | 设计含义 |
|---|---|---|
| 0 | 时钟/Timer 到期 | 改变内部状态，但**不应偷看当前 bar 收盘** |
| 1 | 前一时刻提交且延迟已到期的回报/控制命令 | 按因果依赖处理 |
| 2 | 本时点市场数据到达 | 新行情成为策略可见输入 |
| 3 | 策略/组合/风控命令 | 使用本时点可见数据决策 |
| 4 | 仿真交易所接收、排队、撮合 | 完成订单簿状态转换 |
| 5 | Fill、订单状态、账户、组合更新 | **保证策略在下一事件才看到结果** |
| 9 | 结算、盘后风控、指标快照 | 防止盘中估值混入当日成交结果 |

> RQAlpha 把交易日拆为 `BEFORE_TRADING → BAR → AFTER_TRADING → SETTLEMENT`（`A`），正是"准备—执行—结算—审计"的因果阶段化。

**确定性不以牺牲真实依赖为代价**：定时器回调可生成新定时器，命令可能触发更多事件 → 引擎必须**每次 handler 后重新检查队列**，而非预先展开全部时间轴（`TestClock` 仅在 `advance_time` 时前进，`A`）。

### 6.3 ★ 撮合模型：先回答"我不知道什么"

> **L1、Bar、L2/L3 不能共用"成交即正确"的语义。**
> - L2/L3 有价格和数量 → 按价格—时间优先级沿真实深度行走，`prob_slippage` 不适用
> - L1 只有买卖一档 → 若声称完全真实，就隐含了缺失深度信息
> - Bar 只有 OHLCV → 最多重构有限路径，**不得从 K 线"读出"不存在的盘口**

**FillModel 清单与语义**（Nautilus 官方，`A`）：

| 模型 | 流动性行为 | 适用场景 |
|---|---|---|
| `DefaultFillModel` | 使用撮合引擎记录的盘口 | 未配置时的基线 |
| `BestPriceFillModel` | 最优买卖价提供无限数量 | 容量无限、抢流动性 |
| `OneTickSlippageFillModel` | 所有订单固定滑点一档 | 保守上限 / latency 近似 |
| `ProbabilisticFillModel` | 最优价与差一档间按概率选择 | L1 下成交不确定性 |
| `TwoTierFillModel` | 第一档 10 单位，其余差一档 | 分层深度占位 |
| `ThreeTierFillModel` | 50/30/20 分布在三档 | 中等容量订单 |
| `LimitOrderPartialFillModel` | 第一档最多 5 单位，其余差一档 | 限价排队与部分成交 |
| `SizeAwareFillModel` | 小单与大单不同分层 | 订单规模敏感 |
| `CompetitionAwareFillModel` | 按 `liquidity_factor` 暴露部分最优档容量 | 其他参与者竞争 |
| `VolumeSensitiveFillModel` | 最优价使用近期成交量的一部分 | 成交量敏感容量 |
| `MarketHoursFillModel` | 低流动性时扩为一档更差价差 | 交易时段敏感 |

> 默认模型 `prob_fill_on_limit=1.0`/`prob_slippage=0` 只是把"完全成交"**显式标记为基线假设**，不代表现实约束已被建模。
> `SizeAwareFillModel` 阈值、`CompetitionAwareFillModel` 的 `liquidity_factor`（默认 0.3）**都与具体工具量纲绑定，不可跨资产复用**。

**规则包 × 数据档位 两维插件**（`C`，关键设计）：
- **规则包**：描述订单簿与业务约束（涨跌停、停牌、交割、冰山、FOK/IOC、时间优先）
- **数据档位**：决定可见深度（L2/L3、L1、Bar）
- **只有两者匹配才启用精细撮合，否则自动降级为保守假设并写入审计**

| 现象 | 推荐模型 | 必须记录 | 反模式 |
|---|---|---|---|
| tick/L2/L3 市价单 | 真实深度按价格—时间行走 | 档位、数量、队列位置 | 用固定点差替代逐档消耗 |
| tick/L2/L3 限价单 | 价格—时间队列 + 新增可见量 | 排队时间、撤单、成交概率 | 假设挂单必然成交 |
| L1 限价单 | `prob_fill_on_limit` + 队列深度假设 | RNG 种子、历史命中率 | 用单一确定结果掩盖不确定性 |
| Bar 趋势单 | 开盘或收盘假设 + 容量约束 | 使用规则、成交比例、偏差 | **cheat-on-close/open** |
| 大额市价单 | 深度行走 + 参与率上限 | 冲击曲线、剩余量 | 全量按一个收盘价成交 |
| 低流动性/跨时段 | `MarketHoursFillModel` 或时段表 | 交易状态、价差参数 | 忽略停牌和休市 |
| 高频竞争 | `CompetitionAwareFillModel` + 延迟 | 对手容量、延迟分布 | 容量设为无限 |

**限价排队必须编码真实规则**：同价按时间优先；撤单只在后续事件生效；冰山、隐藏量、FOK/IOC、涨跌停、停牌、交割需单独状态机。

**模型参数必须校准**：记录数据档位、校准样本、有效期、适用资产类；回测至少跑**基线/乐观/悲观**三套，报告净收益对滑点与容量的弹性。无 L2/L3 数据时不使用 `VolumeSensitiveFillModel` 并声称已还原订单簿——更诚实的是输出**"可交易容量区间"**。

### 6.4 ★ 成本模型（FeeModel）

> **真实回测不能只扣比例手续费。**

```
Fee = commission(按张数/金额/阶梯)
    + exchange/liquidity_rebate
    + settlement_currency_conversion
    + funding/interest(按持仓时段累积)
    + regulatory/tax(按 jurisdiction/日期规则)
```

| 市场 | 必填字段 | 常见陷阱 |
|---|---|---|
| A股 | 佣金率、最低佣金、印花税（卖出）、过户费、交易单位 | T+1、涨跌停、停牌、除复权时点弄错 |
| 期货 | 按手手续费、开平、平今/隔日、交割月、保证金、交割规则 | 用"成交额费率"替代阶梯费；忽略交割月与逐日盯市 |
| 加密现货 | maker/taker、币对最小金额、网络费、充提费 | 忽略吃单/挂单角色；把返佣当确定收入 |
| 永续/交割 | 资金费、指数、维持保证金、强平价、保险/ADL | 仅用瞬时标记价估值；忽略资金费结算时点 |
| 期权 | 佣金、行权/交割、保证金、Greeks | 用股票式固定费率替代复杂度 |

### 6.5 ★ 延迟模型（LatencyModel）

**延迟必须影响命令的可见时间，而不能只扰动成交价格。**
命令打上 `submitted_at`，仿真交易所按 `command_arrival = submitted_at + network + gateway + venue` 推进。

> **明确禁止**：把"看到行情"到"交易所接收订单"的时间，用成交价随机偏移偷偷替代——看似增加滑点，实则**破坏时间因果**。

随机延迟/抖动/时钟漂移/排队置于"高级 latency bundle"，**必须绑定种子**。

### 6.6 ★ 账户与保证金

账户模型**由资产类别决定，不由策略自由读写余额**：
- 现货：可用/冻结币种余额
- 期货：逐日盯市、初始/维持保证金、未实现盈亏、强平阈值
- 保证金：借入资产、利息、抵押率、追加保证金

venue 配置含 `account_type`（CASH/MARGIN）与 `oms_type`（NETTING/HEDGING）（`A`）——**把账户规则交给 venue 仿真，而非让策略直接改全局现金**。

**强平状态机**：`Warning → PartialDeleverage → Liquidation`，记录触发标记价、抵押率、结算时间、优先级、剩余风险，**不能只在收益曲线上体现为一次大幅回撤**。

### 6.7 ★ 可复现性：Run Manifest

```yaml
run_id: bt-20260910-7f3c
code:    { repo, commit, image_digest, feature_flags }
config:  { strategy, allocator, risk, venue, models }
data:    { source, symbol, interval, inclusive_range, sha256, point_in_time }
clock:   { start, end, tz, resolution, seed }
random:  { global_seed, per_model_seeds }
runtime: { version, cpu_arch, determinism_mode }
inputs:  { command_log, data_order, replay_fingerprint }
outputs: { event_log, fill_log, portfolio_snapshots, metrics, result_hash }
```

**三重重放校验**：
1. 相同 manifest 两次运行，结果哈希完全一致
2. 修改任一输入后，受影响事件哈希发生预期变化
3. 事件日志可重新驱动纯函数组合估值，得到相同账户与指标

**确定性模式必须禁用**：OS 线程调度依赖、无序 dict、外部实时时钟、未冻结的第三方库。并行只能发生在**互不依赖**的策略/参数组/时间段之间，且每个工作单元保留独立可重放 `run_id`。

### 6.8 ★ 前视偏差：五条断言（架构层封禁）

每个 `DataView` 都有 `as_of(event_time)`：

- ❌ 禁止同时点先交易、后接收行情
- ❌ 禁止访问未来 K 线字段
- ❌ 禁止订单回报先于订单发送
- ❌ 禁止数据修订覆盖原始数据
- ❌ 禁止账户状态被策略绕过风控直接修改

**幸存者偏差防护**：使用当时可得的合约、指数成分与退市状态，而非截止日仍存续的"当前 universe"。训练/验证/测试基于**滚动时间切分**并在写入阶段固化。

### 6.9 指标契约

必须同时揭示**路径风险**与**成交敏感性**：
总收益、年化、波动率、Sharpe、Sortino、最大回撤及持续时间、Calmar、盈亏比、胜率、平均盈利/亏损、换手率、容量、回撤修复时间、alpha/beta、因子暴露、交易成本与滑点分解。

> 不同项目的费率、基准、交易约束与年化假设不天然一致 → **自研框架必须统一指标契约，保留参数与复权方式，不能只比较最终 Sharpe**。

### 6.10 向量化 ↔ 事件驱动的交接

- **vectorbt 定位**：策略实例=数组列，大规模参数/资产/周期扫描（`A`）。**限价单、杠杆等属 PRO 功能**，开源版不能假设（`A`）
- **"1,000,000 orders/70–100ms"** 类数字属营销/测试口径，**不得转化为 SLA**
- **Nautilus "5M rows/s"** 同样未公开硬件、行宽、是否含反序列化与策略逻辑 → **不能作为容量承诺**，必须自建基准（按行宽、列投影、缓存命中、反序列化、分发、回调分别计时）

**梯度工作流**：
```
vectorbt 向量化扫描（速度） → 候选配置 → 事件驱动精确模拟（精度） → 同一内核实盘（一致性）
```
统一契约 `(instrument, timestamp, signal, constraint)` 衔接。**强路径依赖策略（网格/DCA/跨所套利/做市）的向量化结果只能作候选或基线。**

---

## 7. 多账户同时交易

### 7.1 四类账户

| 类型 | 定义 | 示例 |
|---|---|---|
| **资产账户** | 按市场/资产类别的结算规则 | STOCK / FUTURES / OPTION / CRYPTO / BOND |
| **交易账户** | 券商、交易所或子账户的实际主体 | `ctp_main` / `ibkr_hk` / `binance_spot` |
| **策略账户** | 策略被允许的账户白名单 | 策略A → [ctp_main, ibkr_hk] |
| **组合账户** | 多账户逻辑聚合视图 | 跨市场总组合 |

`TradingAccount` = 最小交易与账簿单元：账户 ID、经纪商、市场、币种、权限、凭证引用、费率模板、风险限额。
> **账户 ID 必须进入每笔订单，策略不能直接指定券商。**

### 7.2 资金分配

- **静态**：按初始资金、最大日亏损、Gross/Net 限额划分
- **动态**：组合层基于风险预算、流动性、相关性、保证金算各账户目标 Delta/风险贡献 → `RebalancePlan` → 风控 → 子账户订单

> **账户之间不允许策略直接 `transfer cash`**，真实调拨是独立资金管理流程。

### 7.3 跨账户风控：双树

| 视图 | 类型 | 内容 | 行为 |
|---|---|---|---|
| **账户树** | 硬约束 | 单账户保证金、单市场限制、单标的暴露、币种 | **立即失败** |
| **组合树** | 软约束 | 总杠杆、净 Delta、相关性、VaR、策略间相关性、当日 PnL | **先尝试再平衡** |

账户层结果进**交易日志**，组合层进**分配日志**，不混写。

### 7.4 路由与并发

```
StrategySignal → IntentOrder → PortfolioAllocator → RiskGate → OrderRouter
              → BrokerOrder(account, broker_order_id, route_rule, fallback)
```

**并发控制（分层锁）**：每 `TradingAccount` 一个串行命令队列 · 组合层无锁快照+乐观分配 · 全局风险预算 RWLock · 行情与订单线程分离。
订单幂等：`client_order_id` + 幂等键；网关超时走"查询—对账—补单/撤单"。

**持续对账**：本地 `ProjectedBook` vs 券商 `ReportedBook`，每日关键点及每笔成交后比对现金/持仓/冻结/保证金/订单状态/成交量/费用。
> **"本地账簿 = 柜台账簿"不能作为默认假设**（CTP 类 Mod 确有账户恢复数字差异的已知问题，`A`）。

---

## 8. ★ 多策略同时运行

### 8.1 StrategyRuntime 隔离

每个策略拥有独立 ID、配置、参数、特征缓存、模型版本、**订单配额**、审计主题。
- 策略之间**默认不共享可变对象**
- 跨策略通信只经**不可变事件 / 组合指令 / 只读快照**
- 同进程 / 多进程 / 集群可在**不改策略代码**下切换

### 8.2 信号聚合（净额求解）

```
Signal(symbol, desired_exposure, confidence, horizon, constraints, strategy_id, version)
 → SignalMerger（去重、时间衰减、容量限制、优先级）
 → TargetPortfolio → Optimizer/Allocator → RebalancePlan → Per-Account Risk & Router
```

> **两策略对同一标的分别买 100 / 卖 60 → 净目标 40**，而非"最后写入者获胜"。只有组合层统一求解，才能看到全系统暴露与交易摩擦。

### 8.3 优先级显式建模

`priority` · `mode: NORMAL | OVERRIDE | HEDGE | EMERGENCY` · `time_to_live` · `min_fill_ratio`

紧急风控清算可抢占普通策略；同优先级用**确定性仲裁**（如策略 ID 字典序）；对冲信号可部分抵消。**优先级不能由消息顺序隐含。**

### 8.4 资源配额三类

| 资源 | 治理 |
|---|---|
| 计算 | 策略 worker 池隔离 |
| 行情 | Data Gateway 统一限流 |
| 交易 | 按账户/策略/市场/订单类型设速率、并发、API 用量、费率预算 |

> **"资源不足"与"风控拒绝"必须返回不同错误码**——前者需退避，后者不应继续提交。混为普通失败会让重试、告警、归因全部失真。

### 8.5 生命周期

```
registered → initialized → running ⇄ paused → stopping(drain) → stopped
                  │                    │
          warmup(历史/指标/持仓)   drain(停新信号, 等订单闭环)
```

### 8.6 归因链

每笔 Fill 关联：`strategy_id` · `signal_id` · `model_version` · `rule_version` · `allocation_id` · `account_id` · `parent_order_id`

每日绩效按**策略 / 组合 / 账户**三维计算并保留交叉项，**避免把组合层对冲收益错误归因于单策略**。

---

## 9. 策略层：多语言 + 经典模板库

### 9.1 多语言运行时

| 语言 | 目标 | 加载 | 适用 |
|---|---|---|---|
| Rust / C / C++ / Zig | wasm | Wasmtime | **热路径首选** |
| Go | wasm | Wasmtime | 中等性能 |
| C#/.NET | wasm | Wasmtime | 业务规则型 |
| Python | PyO3 原生 / wasm | 进程内 | 控制面、研究、快速迭代 |
| 原生 .so | FFI | 动态链接 | 逃生舱（庞大既有库） |

**Host Imports 白名单**：`marketdata` · `portfolio` · `orders` · `log`/`metrics` · `determinism`

**性能铁律**：跨边界**批处理**（`evaluate_batch(ticks[])` 每 bar 一次），绝不逐 tick 往返。强制 fuel 中断、NaN canonicalization、固定内存上限、禁 relaxed SIMD。数值统一 128-bit 定点。

**Scope 隔离**：每策略独立 WASM 实例 + Scope；共享唯一 `ExecutionEngine` + `TestClock`。

### 9.2 经典策略模板库

> 策略 = **指标/条件 + 目标仓位 + 风控 + 成交后调整**。规则来自配置，跨市场差异来自 adapter/spec。

| 场景 | 策略 | 指标/条件 | 执行要点 | 回测注意 |
|---|---|---|---|---|
| 币圈 | **跨交易所套利** | 统一报价+手续费+转账时延+滑点后净价差 | 先下敏感/不确定侧 → fill 后动态对冲另一侧 | 必须扣 transfer_time/fee/settlement_risk |
| 币圈 | **三角套利** | 同 clock tick 的 book 算循环因子 | `MultiLegIntent` 原子意图；按风险最高剩余腿管理敞口 | 三腿不可当三个独立信号 |
| 币圈 | **期现套利** | basis、预测资金费、借贷成本、退出成本 | 以对冲组合为对象；按乘数换算；监控 IM/RM | 资金费结算时点、标记价 |
| 币圈 | **资金费套利** | funding + hedge_cost + exec_cost + liq_risk + opportunity_cost | 净 delta≠0 即视为方向性敞口 | 忽略现货腿会低估风险 |
| A股 | **趋势突破**（海龟/Dual Thrust/布林） | N 日高低、上下轨、ATR 仓位、移动止损 | 规则**后置**：涨跌幅/T+1/集合竞价/停牌由 Capability 校验 | Bar 单禁用 cheat-on-close；注意涨跌停不可成交 |
| A股 | **多因子/轮动** | 因子暴露、市值/行业中性、PIT 财务 | 复权与 PIT 为正确性前提 | 幸存者偏差、财务 PIT |
| 通用 | **网格** | inventory、spread、tick size、单边控制 | 与 DCA/做市复用 scheduler+订单网格 | 强路径依赖，向量化只能作基线 |
| 通用 | **DCA** | 总风险、单笔风险、冷却、均价、退出 | 分批是表象，核心是总风险预算 | 同上 |
| 通用 | **做市** | inventory skew、hedge、cancel、latency | inventory limit + adverse selection | 需 L2/L3 + CompetitionAware 模型 |

**统一执行六段**：`数据/状态 → 特征/机会 → Signal → PreTradeRisk → Intent/执行 → 成交后管理`
**组合管理先于下单**：多资产/跨所套利/期权对冲须**先算目标组合，再算 delta 订单**。

---

## 10. 国内外场景

| 场景 | 适配路径 | 规则来源 | 数据重点 |
|---|---|---|---|
| 海外 CEX | VenueAdapter（REST/WS） | InstrumentSpec + VenueCapability | 24h、多 venue book、资金费 |
| 海外 DEX | ChainAdapter | ChainVenue + finality_depth | 链上确认、gas、MEV |
| 海外传统 | FIX/REST Adapter | 同上 | 许可、聚合粒度、跨时区 |
| A股 | China Adapter | Calendar + TradingRuleSet（T+1/涨跌停/集合竞价/停牌/复权） | 交易日历、企业行为、停牌 |
| 期货/期权 | China Adapter + 规则包 | 保证金/强平/换月/行权 | 夜盘边界、交割月、期权链 |
| 可转债/ETF | China Adapter | 品种专属规则 | 转股、溢价率 |

**国内数据重点**：交易日历、企业行为、柜台与账户边界、授权与延时。
**国外数据重点**：许可、source-of-truth、聚合粒度、跨时区、24h 可用性。

**供应商选型按能力而非品牌**：

| 范围 | 候选 | 适配建议 |
|---|---|---|
| A股/衍生品 | **RQData** | 十多年日/分钟/tick + 实时；全量财务 + **财务 PIT** |
| A股 | **Tushare Pro** | 社区/自建；**必须补齐停牌、退市、复权与企业行为审计** |
| A股/多资产 | **Wind** | 机构终端/宏观；关注终端许可与并发 |
| 期货/券商 | **CTP / 券商行情** | 交易与行情网关，**不等价完整研究数据**，需另接历史源 |
| 美股/期权/外汇/加密 | **Polygon** | 多资产 REST+WS；逐笔与期权成本需复核 |
| 美股/加密/期权 | **Alpaca** | 经纪账户直连；历史深度与许可需核验 |
| 全球多资产 | **IBKR TWS** | 跨市场 + **多账户订单分配**；注意登录/时钟/重连 |
| 加密 CEX | **Binance** | 含**测试网**；需处理限流、维护窗口 |
| 历史/宏观/另类 | **Nasdaq Data Link** | 基本面/宏观；**不替代低延迟行情** |
| 免费兜底 | Yahoo Finance | **仅离线研究，不进生产关键路径** |

---

## 11. 可观测、审计与运维

- **Logging**：结构化，带 trace_id / strategy_id / account_id / order_id / venue
- **Metrics**：fill latency、slippage、queue depth、reconcile lag、P&L、风险限额、配额使用、限频水位
- **Tracing**：策略→风控→OMS→Router→adapter→venue
- **Audit**：事件溯源 + 参数/数据指纹/规则版本化，任意时点重放
- **对账**：order / balance / position / funding / settlement 五类
- **偏差归因**：request qty / filled qty / avg price / slippage / fee / latency / venue / algo

---

## 12. 技术选型与许可

| 层面 | 选型 |
|---|---|
| 核心 runtime | Rust（性能 + 安全 + 无 GIL） |
| 控制面 | Python（PyO3） |
| 策略 runtime | WASM (Wasmtime) + Python + 原生 Rust + FFI 逃生舱 |
| 数值 | 128-bit 定点（`i128`/`u128`） |
| 存储 | 紧凑内存对象 + 可演进 schema + Arrow/Parquet 列式 |
| 总线 | 确定性队列（回测）+ async 流式（实盘） |
| 插件体系 | 扩展点贡献 + manifest 契约 + 五阶段启动 + Profile/Bundle/Patch；隔离借鉴 Cordis Scope（**仅低频接缝**） |

**许可红线**：
- **RQAlpha**：默认 Other，**商用需 RiceQuant 书面授权** → 只吸收架构与规则资产思路，**不引入运行时**
- **NautilusTrader**：LGPL-3.0，修改源码后分发须开源 → 仅架构借鉴，不链接不复制
- **vn.py**：MIT，最宽松
- **vectorbt**：Apache 2.0 + Commons Clause，禁止商业转售
- 数据供应商：RQData/Wind/Polygon/Alpaca 等按许可与订阅范围使用，**PIT 财务与实时行情通常分档计费**

> ⚠️ 本框架**不直接依赖**上述任何运行时，全部自研，故无传染性；若未来引入某 crate 或运行时，须重新评估。

---

## 13. 分阶段落地路线

| 阶段 | 内容 | 验收 |
|---|---|---|
| **0** | 统一领域契约：身份/订单/fill/position/account/event/clock schema + 事件溯源 + 精确重放 | 同一事件序列始终重建同一状态 |
| **1** | 插件骨架：扩展点注册中心 + manifest + 依赖图 + 五阶段启动 + Profile/Bundle/Patch + 状态机 | manifest 校验、冲突解决、`dump-config` 可审查 |
| **2** | 回测引擎：TestClock + 因果优先级队列 + DataReplay + SimulatedVenue + 基础 Matcher/Fee | 三重重放校验通过；五条前视断言生效 |
| **3** | 撮合/成本/延迟/保证金四模型细化 + 规则包×数据档位矩阵 | L2/L1/Bar 三档降级正确；三套参数弹性报告 |
| **4** | 多交易所：VenueAdapter 五层 + 符号映射 + 路由四阶段 + 限频 + 状态恢复七步 | 接入 2 个 CEX + 1 个国内柜台，合约测试通过 |
| **5** | 多数据源：四级存储 + 质量门 + 固定主源 + PIT 复权 + BarAggregator | 故障转移留痕；quarantine/fail 生效 |
| **6** | 多账户：TradingAccount/Ledger/Margin/Composer/Router/CommandQueue | 多账户隔离、跨账户路由、对账告警 |
| **7** | 多策略：Runtime 隔离 + SignalMerger + 优先级 + 配额 + 归因 + WASM 运行时 | 净额求解正确；归因三维可算 |
| **8** | 经典策略模板库 | 每个模板过三阶段（向量化→事件模拟→影子运行） |
| **9** | 生产可靠性：风控、五类对账、断路器、可观测、paper、故障注入、灰度、插件签名 | 从研究工具升级为可实盘系统 |

---

## 14. 风险清单与对策

| 风险 | 对策 |
|---|---|
| 热路径性能 | 静态 trait 直调；WASM 批处理；自建基准（不用营销数字） |
| 确定性破灭 | 固化时钟/顺序/种子/初态/版本；NaN canonicalization；fuel；禁 now/网络/随机 |
| **跨所身份混淆** | CanonicalProduct/InstrumentId/DataSourceId 三层分离；子账户并入 VenueId |
| **符号映射被覆盖** | 映射表版本化、不静默删除、保留生效/退市时间与 raw hash |
| **跨所再平衡误判** | 显式建模 transfer_time/fee/settlement_risk；不同网络资产不直接相加 |
| **DEX 最终性误判** | `finality_depth` + ConfirmationTracker；reorg 处理；tx_hash 仅作 intent |
| **撮合模型与数据档位不匹配** | 规则包×数据档位矩阵；不匹配自动降级并审计 |
| **前视偏差** | 五条断言 + `as_of(event_time)` + 双时态 PIT + 滚动切分 |
| **数据源污染策略层** | 四级存储 + 统一接口 + 质量门 + 固定主源 |
| 多账户串扰 | 账户 ID 贯穿全链；双树风控；分层锁；持续对账 |
| 多策略冲突 | 隔离 + 净额求解 + 显式优先级 + 三类配额 |
| **插件隐式覆盖** | 独占点强制 priority + 显式 replaces；禁止最后加载者胜 |
| **运行期热替换** | 严守 §2.6；写入回归测试 |
| **插件供应链** | cosign 签名 + 依赖锁 + scope 最小权限 + WASM 沙箱 + 装前审计 |
| 许可传染 | 仅借鉴架构不引入运行时；数据按订阅范围使用 |

---

## 15. 最终边界

> **框架负责通用执行语义；插件负责具体市场与协议；策略负责信号与目标仓位；风控负责限制动作；执行算法负责订单生命周期；组合层负责聚合与分配；账户层负责资产与结算边界；数据层负责可审计的真相；审计系统负责保留可验证事实。**

**九条原则**：
1. **领域模型优先于供应商接口**
2. **事件契约优先于共享内存**
3. **回测与实盘共享规则内核**
4. **多账户是交易与结算边界，不是资金字段**
5. **多策略是隔离、协调、归因、配额的治理**
6. **可替换性服务于确定性**
7. **插件只向扩展点贡献，不修改内核；插件一次、运行静态**
8. **身份三层分离（Canonical / Instrument / DataSource），绝不覆盖**
9. **撮合模型先回答"我不知道什么"，而非追求更复杂的滑点公式**

---

## 数据来源

1. NautilusTrader 官方文档 / GitHub · https://github.com/nautechsystems/nautilus_trader
2. NautilusTrader Concepts / Execution API（FillModel、FeeModel、LatencyModel）
3. Hummingbot 架构文档 · https://hummingbot.org/academy-content/hummingbot-architecture---part-1/
4. CCXT Manual · https://ccxt.readthedocs.io/en/latest/manual.html
5. RQAlpha 官方文档（基本概念 / 系统 Mod / DataSource）· https://rqalpha.readthedocs.io/
6. VeighNa (vn.py) 官方 · https://www.vnpy.com/
7. VectorBT · https://vectorbt.dev/
8. DeepSeek Harness · https://github.com/deepseek-ai/deepseek-harness
9. RQData / Tushare / Wind / Polygon / Alpaca / IBKR TWS / Binance / Nasdaq Data Link 官方文档

> 本方案仅参考上述项目公开架构资料，研究接口契约、状态机、职责划分与模块组织，**不复制任何第三方源代码**。市场规则、费率、数据覆盖以券商/交易所/供应商实时规则与订阅协议为准。营销口径数字不作为容量承诺，须以自建基准验证。
