name: Determinism Gate

on:
  pull_request:
  push:
    branches: [main]

jobs:
  determinism:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify deterministic replay
        run: |
          echo "Run identical event stream twice"
          echo "Compare resulting state hashes"
